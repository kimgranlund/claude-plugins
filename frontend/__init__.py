"""frontend — generated by harness_emit.py from .claude-plugin/plugin.json. Do not edit."""
from pathlib import Path

_HERE = Path(__file__).resolve().parent

def register(ctx):
    ctx.register_skill('apple-mobile-facts', _HERE / "skills" / 'apple-mobile-facts' / "SKILL.md", description='Answers Apple Human Interface Guidelines (HIG) questions — its own navigation, modality, and component vocabulary — from a cited corpus. Use for "tab bar vs navigation stack", "sheet vs modal vs popover — which does HIG say to use", "what sheet detent heights does HIG define", "alert vs action sheet for this confirmation", "grouped vs plain list", "swipe action conventions", "where should search live in this navigation hierarchy". Cited to specific HIG pages. ANSWERS HIG\'s semantics; does not n…')  # one per skill
    ctx.register_skill('break-down-flow', _HERE / "skills" / 'break-down-flow' / "SKILL.md", description='Decompose, evaluate, and design ONE cross-screen user flow — a journey declared as a state machine. Use when walking or grading a journey: "walk this flow", "trace what happens from X to Y", "is this journey right", "map the checkout/signup/payment flow", "where does this flow dead-end", "the user is stranded / has no way back", "design the flow for X" — OUTSIDE-IN (task → journey) crossed with INSIDE-OUT (transitions → whole), backed by a *.flow.json card whose mechanical gates run in scripts/…')  # one per skill
    ctx.register_skill('break-down-layout', _HERE / "skills" / 'break-down-layout' / "SKILL.md", description='Decompose/evaluate/design any UI layout — four archetypes on two crossing axes (space × behavior). Use when analyzing a screenshot/mockup, naming regions, grading a layout, or scaffolding a shell: "review/critique this layout", "wireframe a dashboard/marketing/mobile app", "which archetype fits", "looks clean but nothing does anything", "every action works but it\'s one giant stacked column". NOT: visual/color (color-*-facts/lettering-facts), CSS/component code, or copywriting; cross-screen jour…')  # one per skill
    ctx.register_skill('check-focus', _HERE / "skills" / 'check-focus' / "SKILL.md", description='Verify focus-ring recipes, hit-target minimums, focus order, and keyboard affordances — and prescribe compliant values where missing. Use when checking focus order/tab sequence, keyboard navigation and traps, modal/dialog focus trap and restore, Escape and arrow keys per role, focus management on route change, focus-ring tokens that clear 3:1 under every surface, "the focus ring is invisible in dark mode", or hit-area expansions for small interactive elements. NOT for general text/background co…')  # one per skill
    ctx.register_skill('check-safety', _HERE / "skills" / 'check-safety' / "SKILL.md", description='Reason about and verify blast radius, reversibility, and friction for every destructive or high-consequence action a UI exposes — the UI safety layer. Use for destructive verbs a product ships (delete/send/publish/revoke), "the delete button is silently disabled when you lack permission" — undo over confirm, type-to-confirm, bulk dry-run, audit-trail UX, recall windows, re-auth, two-person approval — when dialogs proliferate or recovery is missing. NOT for the whole-product sweep (check-whole-u…')  # one per skill
    ctx.register_skill('check-speed', _HERE / "skills" / 'check-speed' / "SKILL.md", description='Reason about and verify perceived latency. Use when auditing loading UX against a performance budget — the invariants an async surface must satisfy: latency feedback windows, skeleton vs spinner vs optimistic decisions, streaming UX, Core Web Vitals (CWV) and CLS budgets, layout shift/jank, image-dimension reservation, cancellation of long-running operations — "the page feels slow even though the API is fast", "the layout jumps around while images load". NOT for a spinner/skeleton-loader compon…')  # one per skill
    ctx.register_skill('check-translations', _HERE / "skills" / 'check-translations' / "SKILL.md", description='Reason about and verify the locale-shaped primitives a UI system must absorb. Use when auditing a UI for i18n — RTL/bidirectionality, logical-vs-physical CSS axes, script metrics (line-height — Arabic, CJK), locale-sensitive number/date/currency Intl formatting, pluralization, encoding/collation, text-expansion budgets (German expansion): RTL support, dir/lang on text surfaces, icon mirroring, hardcoded/untranslated strings. NOT for choosing/pairing type or script anatomy (lettering-facts); NOT…')  # one per skill
    ctx.register_skill('check-ui-change', _HERE / "skills" / 'check-ui-change' / "SKILL.md", description='Verify a UI change end-to-end against the running artifact before declaring it done — start the app, interact with the change directly, screenshot before/after, check the console, audit performance. Use when asked to verify, check, or confirm a UI change — a new control, a layout edit, a style change, "is this button working", "did that fix the layout" — or whenever about to report a frontend change complete. NOT for reasoning in the abstract with no live artifact (check-colors/check-focus/chec…')  # one per skill
    ctx.register_skill('check-whole-ui', _HERE / "skills" / 'check-whole-ui' / "SKILL.md", description='Audit a whole product UI — every screen, flow, and surface — for cross-cutting findings no single screen review catches: pattern drift, token misuse, repeated defect quadrants. Use for a pre-launch or post-redesign sweep: "audit this UI", "sweep for accessibility / i18n / latency / safety", "how consistent is this UI". Composes break-down-layout, break-down-flow, the five verifiers, and ui-pattern-facts/ui-genre-facts conformance. NOT for one screen (break-down-layout, layout-checker); one flow…')  # one per skill
    ctx.register_skill('css-system-facts', _HERE / "skills" / 'css-system-facts' / "SKILL.md", description='Answers CSS ARCHITECTURE facts — @layer vs load-order, three-tier tokens + theme packs, light-dark() theming, FRAME-vs-RHYTHM, @scope vs CSS Modules, CSS as decision logs + tested contracts. Use for "@layer or import order", "theme pack needs no fallback", "light-dark() vs [data-theme]", "FRAME or RHYTHM token", "@scope vs CSS Modules", "unit-test a CSS file", "per-tier control-sizing lookup vs scale multiplier", "[density] scope — gap vs height". NOT token DESIGN (design plugin: make-palette,…')  # one per skill
    ctx.register_skill('data-wiring-facts', _HERE / "skills" / 'data-wiring-facts' / "SKILL.md", description='Answers how data gets WIRED into a UI, from a cited corpus — 3-tier streaming, bridge protocol, no-DI substitutes, six-pattern menu + Wiring gates. Use for "should this element use data-stream-* or its own fetch", "is mixing an imperative fetch with a declarative stream a bug", "does this busy flag have a guaranteed reset path", "what\'s our pattern for passing this dependency down without a DI container", "which of the six wiring patterns fits this need", "what counts as a done Wiring Record ro…')  # one per skill
    ctx.register_skill('dom-layout-facts', _HERE / "skills" / 'dom-layout-facts' / "SKILL.md", description='Answers how to reason within HTML/CSS layout mechanics — box model, normal flow, block formatting contexts (BFC), stacking contexts, containing blocks, flex/grid algorithms — from a cited MDN/CSS-spec corpus. Use for "why did this margin collapse", "what triggers a new BFC", "why can\'t this z-index escape its parent", "why is this flex item overflowing despite flex-shrink", "auto-fill vs auto-fit", "why doesn\'t height: 100% work". ANSWERS the mechanism; does not derive a component\'s sizing law…')  # one per skill
    ctx.register_skill('feature-intake-rules', _HERE / "skills" / 'feature-intake-rules' / "SKILL.md", description='Per-shape intake schemas for a UI feature ticket — component/module, layout/shell, UX flow, cross-cutting UX — fields lifted from the owning build skill\'s own two-plane axes, each naming its build owner and DoD checker. Use when capturing/reviewing what a UI-feature TICKET should state before build: "what fields does a button component ticket need", "what should a dashboard-shell ticket capture", "is this UI ticket capture-complete". NOT building/grading the component/layout/flow itself (make-c…')  # one per skill
    ctx.register_skill('make-component', _HERE / "skills" / 'make-component' / "SKILL.md", description='Author a zero-dependency web component. Use for a custom element outside a framework: "build a web component/custom element", "make a checkbox/select/toolbar", "design this component\'s API/anatomy", "wrap native or form-associated?", "tiers feel mis-cut", "renders empty after a re-render", "icon-only button isn\'t square", "icon/caret size on the ramp". NOT: ui-* controls (agent-ui); shells (break-down-layout); naming a pattern (ui-pattern-facts); decomposition (break-down-problem); color ramps…')  # one per skill
    ctx.register_skill('motion-rules', _HERE / "skills" / 'motion-rules' / "SKILL.md", description='Answers UI motion questions from a cited corpus — durations, easing and springs, choreography, reduced-motion policy. Use when timing or shaping animation: "how long should this transition/animation be", "what easing curve", "spring vs ease-in-out", "stagger these list items", "this animation feels sluggish/janky/too bouncy", "make it snappier", "reduced motion / prefers-reduced-motion policy". Carries Nielsen\'s limits, the duration ladders, Apple\'s spring model with CSS realizations, stagger v…')  # one per skill
    ctx.register_skill('perf-audit', _HERE / "skills" / 'perf-audit' / "SKILL.md", description='Run a Lighthouse audit reproducibly and save the JSON. Use when the user asks to "run Lighthouse", "get a Lighthouse report for this URL", "measure the performance score of the deployed site", "audit desktop and mobile", or needs a saved before-report for a fix loop: pinned `npx lighthouse@13`, deployed URL over localhost, 3 runs with the median kept, one JSON per preset. NOT for reading the report or deciding what to fix first (perf-triage); NOT for perceived-latency judgment, skeleton vs spin…')  # one per skill
    ctx.register_skill('perf-fix-loop', _HERE / "skills" / 'perf-fix-loop' / "SKILL.md", description='The loop contract for fixing against a perf brief: one cause family per iteration, evidence recorded before the fix, re-audit of the changed surfaces plus every previous finding, an lh-diff gate against the DO NOT BREAK list, revert on regression, stop after two failed attempts on one family. Use when the user asks to "fix and re-audit", "work through the Lighthouse findings", "get the score green without breaking anything", "iterate on the perf brief". NOT for producing the brief (perf-triage)…')  # one per skill
    ctx.register_skill('perf-playbook', _HERE / "skills" / 'perf-playbook' / "SKILL.md", description='Fix one page fully, write the playbook, then fan the fixes out to every other page without fresh reports. Use when the user asks to "apply the perf fixes to all pages", "roll this out across the site", "same fixes on the other routes", "write down what we changed so it can be repeated": one entry per fix (audit id, cause, files, verify command, shared vs page-local), remaining pages handled in parallel with disjoint files, each ending with lh-diff. NOT for the first page\'s own fix loop (perf-fi…')  # one per skill
    ctx.register_skill('perf-sweep', _HERE / "skills" / 'perf-sweep' / "SKILL.md", description='System-wide performance sweep: route inventory, per-route scoreboard, preserve-not-regress budgets, cause-family clusters across routes, a canary set for a fast gate and a full nightly sweep. Use when the user asks to "audit the whole site", "Lighthouse every route", "build a perf scoreboard", "set performance budgets from the baseline", "which pages share this failure". NOT for one page\'s audit (perf-audit), brief (perf-triage), or fix loop (perf-fix-loop); NOT for a perceived-latency budget c…')  # one per skill
    ctx.register_skill('perf-triage', _HERE / "skills" / 'perf-triage' / "SKILL.md", description='Turn one or more Lighthouse JSON reports into a prioritised, size-bounded brief the agent can act on. Use when the user asks "what do I fix first", "read this Lighthouse report", "turn the report into a plan", "which audits matter", or hands over a report.json: header, a DO NOT BREAK list, failing audits ranked with a cause-family tag (transport, build, css-loading, runtime, images-fonts, a11y-name, a11y-structure, a11y-contrast, seo-meta, third-party), a fix order. NOT for running Lighthouse (…')  # one per skill
    ctx.register_skill('persistence-facts', _HERE / "skills" / 'persistence-facts' / "SKILL.md", description='Answers client-persistence FACTS from a cited field-report corpus — storage discipline tiers (schemaVersion/rename-map vs ad-hoc JSON.parse vs cache-buster keys; the rename-without-migration data-loss class), when a sync store next to an async storage seam is legitimate design vs. bypass drift, the persistence-specific bypass-inventory audit shape (grep by storage-key namespace, not just importing module), and URL-state sync (PHI exclusion, push vs. replace, multi-value encode/decode). Use for…')  # one per skill
    ctx.register_skill('reactivity-facts', _HERE / "skills" / 'reactivity-facts' / "SKILL.md", description='Answers how UI reactivity/state-propagation works, from a cited corpus — signal/computed/effect kernels, version-verified vs dirty-flag recompute, ownership-scope teardown, stale-response guards, below-element reactivity vs render-clock coexistence. Use for "why did this effect refire twice", "computed vs signal vs effect", "guard a stale async response", "per-part reactivity vs whole-component re-render", "ownership scope disposal", "rAF vs microtask clash", "parametric doc + derive-then-rende…')  # one per skill
    ctx.register_skill('size-and-shape-rules', _HERE / "skills" / 'size-and-shape-rules' / "SKILL.md", description='Answers sizing and spacing SCALE questions — the theory beneath any dimensional token system, from a cited corpus. Use for "why 4px/8px steps", "how do I build a spacing scale", "linear or geometric progression", "why the scale gets sparser at larger sizes", "how nested containers compose their padding", "what\'s the right base unit". Carries base-unit rationale, dense-then-sparse progression, cross-system comparison. ANSWERS the theory — does not derive one component\'s law (make-component) or b…')  # one per skill
    ctx.register_skill('state-model-rules', _HERE / "skills" / 'state-model-rules' / "SKILL.md", description='Judges app-tier state ARCHITECTURE from a cited corpus — stacked generations, shared-name pathology, built-but-unadopted layers, an un-pulled re-evaluation trigger, doctrine-vs-practice divergence, two-owner fields. Use for "why is our state a mix of implementations", "is this store actually used or just built", "did we ever revisit this decision", "two places both claim to own this field", "four generations of state pattern stacked in one app", "how do I audit our state architecture", "two fie…')  # one per skill
    ctx.register_skill('ui-genre-facts', _HERE / "skills" / 'ui-genre-facts' / "SKILL.md", description='Answers which patterns and conventions a product CATEGORY expects — the genre world model behind "is this the right pattern for THIS kind of product". Use for "what genre is this app", "what do users expect in a dashboard/collab tool/habit tracker/AI chat app", "which patterns does this category demand", "is this convention-violation acceptable here". Covers dashboards, collaboration, productivity, utilities, social media, AI-native apps, games, health, fintech, marketplaces, travel. ANSWERS ge…')  # one per skill
    ctx.register_skill('ui-pattern-facts', _HERE / "skills" / 'ui-pattern-facts' / "SKILL.md", description='Answers UI-pattern questions — macro (templates), micro (modules), container (card/panel/dialog/drawer anatomy). Use to name, choose, or explain a pattern: "what pattern is this", "name this screen type", "how should a table / form / wizard behave", "common layout for settings / a feed", "what does a card usually contain", "should this header stay sticky", "how should footer buttons lay out", "what states does a screen need", "when is a modal wrong". Answers/names only — apply: break-down-layou…')  # one per skill
    ctx.register_skill('web-component-facts', _HERE / "skills" / 'web-component-facts' / "SKILL.md", description='Answers Web Components platform facts — light-DOM lifecycle (upgrade races, SSR shim scars, NOOP_INTERNALS), stamping strategies + keyed reconcile (adopt-or-stamp, logicalChildren, the display:contents trap), traits as a host-agnostic primitive vs. mixin-flattened-this, ElementInternals form association + a11y, the attributes-as-API grammar, and per-control testing. Use for "why didn\'t attributeChangedCallback replay on upgrade", "static parts vs tagged template vs imperative", "is a trait a mi…')  # one per skill
