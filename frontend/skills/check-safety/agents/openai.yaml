interface:
  display_name: "Check Safety"
  short_description: "Reason about and verify blast radius, reversibility, and friction for every destructive or high-consequence action a UI exposes — the UI safety layer."
