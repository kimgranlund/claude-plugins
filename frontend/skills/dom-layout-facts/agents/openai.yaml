interface:
  display_name: "Dom Layout Facts"
  short_description: "Answers how to reason within HTML/CSS layout mechanics — box model, normal flow, block formatting contexts (BFC), stacking contexts, containing blocks, flex/grid algorithms — from a cited MDN/CSS-spec corpus."
