# frontend — design, critique, and verify UI structure

Sibling plugin to harness (the authoring toolchain) and docs (which authors functional
documents); frontend authors and reviews UI structure — layouts, flows, components, patterns — and
verifies it against the non-functional bars every surface owes (focus, i18n, perf, safety).
Merges what plan-plugin-split surfaced as two candidate clusters (ui-architecture and ui-verify)
into one plugin on explicit direction: 13 cross-mentions between them was the single
highest-coupling boundary found in the analysis.

## Map

| Artifact | Type | Invocation | What it carries |
|---|---|---|---|
| `skills/break-down-layout` | Procedural skill | both (`/break-down-layout`) | Two-axis (outside-in space x inside-out behavior) layout decomposition/grading method; `references/decomposition-method.md`, `references/walkthrough.md`; four archetypes consumed from `ui-pattern-facts` |
| `skills/break-down-flow` | Procedural skill | both (`/break-down-flow`) | Cross-screen flow decomposition (task->journey x transitions->whole); `scripts/flow-check.py` — reachability, dead-end, exit-truth, recovery gates; `references/one-time-pay.flow.json` |
| `skills/make-component` | Procedural skill | both (`/make-component`) | Compose x Realize two-axis component method; `references/` — api-policy, attributes-as-api, composition-patterns, decomposition-method, family-controls, family-overlays, geometry-system, platform-baseline; `scripts/` — component-contract-check.py, composition-check.py, geometry-check.py |
| `skills/ui-pattern-facts` | Declarative skill | model-only | Macro/micro/container UI pattern catalog (page templates, module catalog, container Header·Body·Footer anatomy, screen-state grammar); `references/` — macro-patterns, micro-patterns, container-patterns, state-patterns, sources, four archetype-*.md files consumed by `break-down-layout` |
| `skills/size-and-shape-rules` | Declarative skill | model-only | Sizing/spacing SCALE theory — base-unit rationale, the dense-then-sparse progression shape, composable-spacing/nesting rules, density scaling, cross-system comparison (Material/Tailwind/Carbon/Apple); cites `make-component`'s geometry-system.md and design' `material-shape-facts` as worked instances rather than restating them; `references/` — scale-theory, composable-spacing, sources |
| `skills/dom-layout-facts` | Declarative skill | model-only | HTML/CSS layout-mechanics world model — box model + margin collapse, normal flow, Block Formatting Context triggers, stacking-context creation/nesting, containing-block resolution by position value, flex/grid sizing algorithms and gotchas (min-width:auto, auto-fill/auto-fit); cited against MDN + the CSS specs; `references/` — box-model-and-flow, formatting-contexts, stacking-contexts, flex-and-grid-mechanics, sources |
| `skills/reactivity-facts` | Declarative skill | model-only | UI reactivity/state-propagation world model, field-report-grounded (gen-ui-kit, agent-ui, ultimate-tokens, adia-v2) — signal/computed/effect kernels + the convergent 100-wave write-loop guard, version-verified vs dirty-flag recompute, ownership-scope teardown, monotonic sequence-token stale-response guards (6+ instances/2 repos), below-element per-part reactivity vs rAF/microtask clock coexistence; `references/` — signal-kernels, tier-split, verification-vs-dirty-flag, ownership-and-teardown, sequence-tokens, below-element-reactivity, sources |
| `skills/state-model-rules` | Declarative skill | model-only | App-tier state ARCHITECTURE judgment, field-report-grounded (agent-ui, gen-ui-kit, adia-v2) — the four-generations pathology, a shared name masking two live facts (tagged-union-vs-same-fact), built-but-unadopted vs load-bearing layers, never-pulled re-evaluation triggers, doctrine-vs-practice divergence measured per layer, one-name-two-owners contract collisions, and the audit methods themselves; fences reactivity mechanism to `reactivity-facts`, persistence facts to `persistence-facts`; `references/INDEX.md` + `references/` — four-generations, two-facts-one-name, adoption-verdict, never-pulled-triggers, doctrine-vs-practice, one-name-two-owners, audit-technique, sources |
| `skills/persistence-facts` | Declarative skill | model-only | Client-persistence FACTS, field-report-grounded (ultimate-tokens, agent-ui, gen-ui-kit, adia-v2) — storage discipline tiers (fuzzed-roundtrip+schemaVersion/RENAME_MAPS vs ad-hoc JSON.parse vs cache-buster keys) and the rename-without-migration data-loss class, dual sync-vs-async persistence contracts by design, the persistence-specific bypass-inventory audit shape, URL-state sync (PHI exclusion, push-vs-replace, multi-value encode/decode); fences architecture judgment to `state-model-rules`, reactivity mechanism to `reactivity-facts`; `references/` — discipline-tiers, dual-contracts, persistence-audit-shape, url-state-sync, sources |
| `skills/data-wiring-facts` | Declarative skill | model-only | Data-WIRING FACTS, field-report-grounded (gen-ui-kit, ultimate-tokens, adia-v2, agent-ui) — the 3-tier declarative streaming stack (attribute-driven transport, refcounted shared signal, one-way bridge) + the double-ownership hazard, the postMessage bridge protocol (symmetric request/reply pairs, the every-busy-flag-needs-a-reset-path rule), the no-DI four-substitute taxonomy (typed handles, re-derive helpers, callback injection, documented soft globals) measured across four repos with zero context APIs, the six-pattern need→pattern wiring menu + Wiring Record gates; fences reactivity mechanism to `reactivity-facts`, architecture judgment to `state-model-rules`, persistence facts to `persistence-facts`; `references/` — streaming-stack, bridge-protocol, no-di-taxonomy, wiring-menu, sources |
| `skills/web-component-facts` | Declarative skill | model-only | Web Components PLATFORM facts, grounded in live source (gen-ui-kit, agent-ui) + one dated doctrine report (27 ratified rules) + one contrast finding (ultimate-tokens) — light-DOM base element lifecycle (upgrade races gh#284, SSR shim scars gh#285/NOOP_INTERNALS), stamping strategies + keyed reconcile (adopt-or-stamp ADR-0010, logicalChildren/logicalSlotted, the display:contents wrapper trap), traits as the convergent host-agnostic behavior primitive (gen-ui-kit's `defineTrait()` registry + agent-ui's plain host-function shape) vs. ultimate-tokens' mixin-flattened-`this` contrast, ElementInternals form association + the host-as-control a11y pattern, the attributes-as-API contract grammar (yaml SoT, no-shadowing, the boolean flip rule + literal-false parity, null-over-sentinels, reflect rules, `data-*` ownership tiers, the closed six-event vocabulary + SPEC-R5's callback-registration escape valve), and per-control testing (the file quintet, jsdom-vs-browser, architectural-boundary tests); fences the build procedure to `make-component`, reactivity kernel mechanism to `reactivity-facts`; `references/` — lifecycle-and-upgrade, stamping-and-reconcile, traits-primitive, form-and-a11y, attributes-as-api-grammar, control-testing, sources |
| `skills/apple-mobile-facts` | Declarative skill | model-only | Apple HIG world model — its own navigation-model guidance (tab bar vs nav stack, nav-bar/toolbar/search placement), modality/sheet-detent behavior, alert/action-sheet/sheet escalation, button-style vocabulary, list/swipe-action conventions; fences page-shell regions to `ui-pattern-facts`, animation timing to `motion-rules`, icon sizing to `icon-rules`; `references/` — navigation, modality-and-sheets, components, sources |
| `skills/ui-genre-facts` | Declarative skill | model-only | Product-genre world model — which patterns a category expects; `references/INDEX.md` + `references/genres/` — thirteen genre files (dashboards-analytics through travel) |
| `skills/motion-rules` | Declarative skill | model-only | Motion world model answered from a cited corpus — duration ladders + Nielsen limits, Apple-spring easing (house default) with CSS realizations, choreography/stagger/what-never-animates, reduced-motion policy + WCAG floor; `references/` — durations, easing, choreography, reduced-motion, sources |
| `skills/check-whole-ui` | Procedural skill | both (`/check-whole-ui`) | Whole-product UI sweep composing the other nine members over a set of screens/flows; `scripts/` — inventory-scan.py, audit-diff.py, ui-probe.mjs (live DOM probe) |
| `skills/check-ui-change` | Procedural skill | both | Drives a UI change against the running artifact (launch, interact, screenshot before/after, console check, perf trace) before it's reported done — the live-artifact half of what `check-focus`/`check-translations`/`check-speed`/`check-safety` reason about in the abstract |
| `skills/check-focus` | Procedural skill | both (`/check-focus`) | Focus-ring, keyboard affordance, and hit-target verification; `scripts/focus-check.py`; `assets/focus-ring/recipes.json`, `assets/keyboard/affordances.json`, `assets/offsets/per-surface.json`, `assets/targets/minimums.json` |
| `skills/check-translations` | Procedural skill | both (`/check-translations`) | RTL/bidi, locale Intl formatting, and text-expansion verification; `scripts/i18n-check.py`; `assets/bidi/isolation-points.json`, `assets/formatting/intl-surfaces.json`, `assets/locales/expansion-factors.json`, `assets/locales/script-metrics.json`, `assets/mirroring/icon-policies.json` |
| `skills/check-speed` | Procedural skill | both (`/check-speed`) | Perceived-latency and Core Web Vitals verification; `scripts/budget-check.py`; `assets/cancellation/contract.json`, `assets/cls/budget.json`, `assets/decisions/skeleton-vs-spinner.json`, `assets/optimistic/eligibility.json`, `assets/streaming/posture.json`, `assets/thresholds/perception.json` |
| `skills/check-safety` | Procedural skill | both (`/check-safety`) | Blast-radius, reversibility, and friction verification for destructive UI actions; `scripts/safety-check.py`; `assets/audit/event-schema.json`, `assets/blast-reversibility/matrix.json`, `assets/defaults/confirm-posture.json`, `assets/friction/recipes.json`, `assets/permissions/error-ux.json`, `assets/recall/windows.json` |
| `agents/layout-checker.md` | Subagent (fresh-context critic) | dispatched (Task tool, model `fable`) | Grades ONE layout against `break-down-layout`'s two-axis rubric; preloads `break-down-layout` only |
| `agents/flow-checker.md` | Subagent (fresh-context critic) | dispatched (Task tool, model `fable`) | Grades ONE cross-screen flow against `break-down-flow`'s two-axis rubric and its `flow-check.py` gate; preloads `break-down-flow` only |
| `agents/component-checker.md` | Subagent (adversarial critic) | dispatched (Task tool, model `fable`) | Grades ONE component/composition against `make-component`'s Compose x Realize method and its three checkers; preloads `make-component` only |

Cross-plugin seam (soft, by design): all three reviewer agents return through harness's
`write-handoff` block where harness is installed, and fall back inline
(Status/Summary/Files changed/Tests/checks run/Evidence/Risks/Open questions/Recommended next action)
when it is not — no hard preload crosses the plugin boundary.

Directories align with plugin names (ADR-0007).

v2.6.1 · 2026-08-20 · closes #821 (post-campaign routing tune): fixed the 6 tunable failures
from the wave-6 `/check-routing` proof (515/522) — css-system-facts t09/t10 (stolen 3-of-3 by
size-and-shape-rules), data-wiring-facts t12 ↔ state-model-rules t02 (symmetric pair-confusion),
reactivity-facts t10 (stolen 3-of-3 by state-model-rules), make-component n26 (leaked 3-of-3 to
design:make-variants); amf t08 confirmed out of scope (hung 3-way, not tunable). Each of the six
descriptions gained the exact disputed phrasing verbatim as a new trigger or NOT-fence, evals.json
updated same-change (new reciprocal no-trigger cases in size-and-shape-rules/state-model-rules,
notes dated); data-wiring-facts also discovered pre-existing over the 700-char diet (1016→689,
prose-only trim, every quoted trigger/NOT-target preserved). A first attempt at size-and-shape-rules'
new css-system-facts fence used generic wording and still leaked 2-of-3 on a scoped blind re-judge;
sharpened to echo the disputed phrasing verbatim, re-verified clean, plus a body Boundaries-bullet
gap the fresh-context skill-checker caught. Fresh-context skill-checker run on all six edited files
(PASS, 3 nits, none blocking) plus a second targeted pass on the sharpened fence (PASS). Scoped
blind re-judge: 3 unnamed routing-judge agents over the full frontend menu (22 skills + none),
covering the six previously-failed ids plus every touched suite's full case list (152 cases) —
all six now pass 2-of-3 (several 3-of-3), no new failures beyond four pre-existing judge-variance
no-trigger cases unrelated to this edit's vocabulary; a follow-up scoped 3-judge run confirmed the
sharpened fence. `.claude/ops/routing-report.json` refreshed via `write_routing_report.py` in the
same PR; skill-checker + `release_gate.py frontend` clean.

v2.6.0 · 2026-08-20 · closes #810 (frontend wave 6, the FINAL wave of the series): new knowledge skill `css-system-facts` — six axes grounded in LIVE SOURCE reads (gen-ui-kit `docs/ops/adr/adr-0002/0003/0038` — three-tier token layering, the two-block `@scope` contract, cascade-layer precedence; agent-ui's own, differently-numbered `.claude/docs/adr/0003/0007/0032/0038`, `tokens.css`, `dimensions.css`, `themes/orchid.css`, and `disclosure-css.test.ts` — see PR body): `@layer` cascade architecture vs. agent-ui's own load-order discipline (both live systems, neither a strawman for the other); the three-tier token taxonomy + generated theme packs relying on ordinary CSS custom-property inheritance for their parity gap (no fallback mechanism needed); `light-dark()` as the theming primitive, composing with (not substituting for) a theme-pack attribute selector; the FRAME-vs-RHYTHM geometry split — scale vs. density, agent-ui's own rejected multiplier-ladder history (ADR-0007/ADR-0032, superseded by its own ADR-0038's explicit per-tier lookup table) — the single most novel concept per the research pass; `@scope` (compiler-enforced) vs. CSS Modules (build-time class hashing) scoping strategies; CSS files as decision logs (inline ADR/ticket-citing comments) + CSS-as-tested-contract (`disclosure-css.test.ts`'s structural CSS unit tests: sectioning, token-hygiene regex audits, and a negative control proving the detector detects). `css` registered in naming.manifest ObjectVocab as a new canonical (anti-ambiguity checked by hand against the existing 127-token list — no collision; distinct from the existing generic `token`/`component` tokens). Both directions of the critical design-plugin fence held: `make-palette`/`pick-fonts`/`font-token-rules`/`make-design-system`/`token-builder` checked directly against this pack's trigger vocabulary (`@layer`, cascade, `@scope`, `light-dark()`, CSS Modules, decision-log/tested-contract) and carry none of it in their own descriptions, so this stays a SINGLE-plugin PR (no reciprocal fence, no `design` version bump — judgment call recorded in the PR body) rather than a two-plugin one. No INDEX.md (6 axes + sources, flat 7-file corpus, wave-1/3/4/5 precedent). Reciprocal fences added in `size-and-shape-rules` (description NOT-list + n09: CSS-specific FRAME/RHYTHM naming vs. general scale theory) and `web-component-facts` (description NOT-list + n11: CSS-file structural testing vs. the per-control test-file quintet); fresh-context skill-checker findings applied (see PR body); skill-checker + `release_gate.py frontend` clean (disclosed-warn classes named in PR body if any recur).
v2.5.0 · 2026-08-20 · closes #809 (frontend wave 5): new knowledge skill `web-component-facts` — six axes grounded in LIVE SOURCE reads (gen-ui-kit `core/{element,template,form}.js` + `traits/define.js`, agent-ui's own trait/test sources) plus the `04-doctrine-vs-practice.md` Part 1 doctrine ledger (27 ratified rules) and one same-day report-correction (ultimate-tokens' `mixinInto` collision-guard finding, verified stale against live `app.js` — see PR body): light-DOM lifecycle (upgrade races gh#284, SSR shim scars gh#285, NOOP_INTERNALS); stamping strategies + keyed reconcile (adopt-or-stamp ADR-0010/ADR-0036, logicalChildren/logicalSlotted, the display:contents trap); traits as the convergent host-agnostic primitive (gen-ui-kit's `defineTrait()` vs. agent-ui's plain host-function shape) contrasted with ultimate-tokens' mixin-flattened-`this` composition; ElementInternals form association + the host-as-control a11y pattern; the attributes-as-API grammar (yaml SoT, no-shadowing, boolean flip + literal-false parity, null-over-sentinels, reflect rules, `data-*` tiers, the closed event vocabulary + SPEC-R5's callback escape valve); per-control testing (the file quintet, jsdom-vs-browser, architectural-boundary tests). `web-component` registered in naming.manifest ObjectVocab as its own compound canonical, distinct from the existing `component` token (anti-ambiguity: platform-standard Custom Element primitive vs. the generic authored UI building-block object — same precedent as `data-wiring` coexisting with process_lex's bare `wiring`; checked by hand against the existing 126-token list, no collision); no INDEX.md (6 axes + sources, flat ≤7-file corpus, waves-1/3/4 precedent); reciprocal fences added in `make-component` (description NOT-list + n28) and `reactivity-facts` (Boundaries bullet + n16); fresh-context skill-checker caught both this pack's own description (1011->654 chars) and make-component's re-broken fence addition (798->686 chars) over the W8 700-char house budget — both re-dieted same-change, every quoted trigger/NOT-fence target preserved verbatim, prose-only cuts; skill-checker + `release_gate.py frontend` clean (disclosed-warn classes named in PR body if any recur).
v2.4.0 · 2026-08-20 · closes #808 (frontend wave 4): new knowledge skill `data-wiring-facts` — four field-report-grounded axes (the 3-tier declarative streaming stack incl. the double-ownership hazard, the postMessage bridge protocol incl. the every-busy-flag-needs-a-reset-path rule, the no-DI four-substitute taxonomy measured across four repos with zero context APIs, the six-pattern need→pattern wiring menu + Wiring Record gates), [verified]/[incident]-marked from gen-ui-kit `core/{data-stream,streams-bridge,transport}.js` + its `04-doctrine-vs-practice.md` Part 2, ultimate-tokens' `04-context-and-messaging.md`, adia-v2's `04-context-di-patterns.md`, and agent-ui's `framework-state-idioms.md` (see PR body); `data-wiring` registered in naming.manifest ObjectVocab (anti-ambiguity checked by hand against the existing 125-token list, unattended manifest-authoring confirm skipped per dispatch carve-out); no INDEX.md (4 axes + sources, flat ≤7-file corpus, wave-1 precedent); reciprocal fences added in `reactivity-facts`/`state-model-rules`/`persistence-facts`; skill-checker + `release_gate.py frontend` clean (disclosed-warn classes named in PR body if any recur).
v2.3.0 · 2026-08-20 · closes #807 (frontend wave 3): new knowledge skill `persistence-facts` — four field-report-grounded axes (client-persistence discipline tiers incl. schemaVersion/RENAME_MAPS migration + the rename-without-migration data-loss class, dual sync-vs-async persistence contracts by design, the persistence-specific bypass-inventory audit shape, URL-state sync incl. PHI exclusion + push-vs-replace), [verified]/[incident]-marked from ultimate-tokens/agent-ui/gen-ui-kit/adia-v2's 2026-08-20 corpora (see PR body); `persistence` registered in naming.manifest ObjectVocab (anti-ambiguity checked by hand, unattended manifest-authoring confirm skipped per dispatch carve-out — no collision against the existing 124-token list); no INDEX.md (4 axes + sources, flat ≤7-file corpus, wave-1 precedent); reciprocal fences added in `reactivity-facts`/`state-model-rules` (this pack's own persistence-audit-shape.md cites state-model-rules' audit-technique.md as the general bypass-inventory method rather than restating it); skill-checker + `release_gate.py frontend` clean (disclosed-warn classes named in PR body if any recur).
v2.2.0 · 2026-08-20 · closes #806 (frontend wave 2): new knowledge skill `state-model-rules` — seven field-report-grounded axes (four-generations pathology, two-facts-one-name, adoption verdicts, never-pulled triggers, doctrine-vs-practice divergence, one-name-two-owners, the audit methods), [verified]/[incident]-marked from agent-ui/gen-ui-kit/adia-v2's 2026-08-20 corpora (see PR body); `model` registered in naming.manifest ObjectVocab (anti-ambiguity clean, unattended manifest-authoring confirm skipped per dispatch carve-out); INDEX.md shipped (8 files, over wave 1's flat-corpus threshold); reciprocal fence in `reactivity-facts`; skill-checker + `release_gate.py frontend` clean (one disclosed G8 false-positive, see PR body).
v2.1.0 · 2026-08-20 · closes #805 (frontend wave 1): new knowledge skill `reactivity-facts` — six field-report-grounded axes (signal kernels, tier split, recompute strategy, ownership/teardown, sequence tokens, below-element reactivity), [verified]/[incident]-marked; `reactivity` registered in naming.manifest ObjectVocab (anti-ambiguity clean); reciprocal fences in `make-component`/`ui-pattern-facts`; no INDEX.md (flat ≤7-file corpus, see PR body); skill-checker + `release_gate.py frontend` clean.
v2.0.0 · 2026-08-20 · BREAKING — plugin renamed screens -> frontend (Kim's ruling 2026-08-20: one plugin absorbing the incoming front-end knowledge series — reactivity/data, app-tier state architecture, persistence/gateways, web-components, CSS/design-systems packs land here in follow-up waves). Dir, manifest name/displayName, marketplace entry, workspace topology row, settings enablement, CI plugin loop, and every live cross-plugin `screens:` handle repointed to `frontend:`; `authorkit/renames.json` maps screens->frontend so stale handles keep resolving via fix-old-names; `frontend` registered in naming.manifest ObjectVocab. History (ledgers, ADRs, ops reports, CHANGELOGs) deliberately untouched — ADR-0006's own rewrote-the-counterexamples lesson. Skill/agent member names unchanged; no behavior change.
v1.1.3 · 2026-08-19 · closes #779 (overhaul wave S0-B+W2): S0-B — break-down-flow/break-down-layout/make-component cite `feature-intake-rules` directly instead of restating its ticket-capture sentence. W2 diets (chars, every trigger phrase + NOT-fence preserved): ui-pattern-facts 1031→696, make-component 995→700, break-down-layout 928→685, break-down-flow 731→696. Evals updated in-suite; wording-checker pass; scoped blind re-judge.
v1.1.2 · 2026-08-19 · reciprocal no-trigger fence (n26) in make-component evals citing design:make-variants (rides PR #754, closes #750 slice).
v1.1.1 · 2026-08-19 · closes #720 (routing fence, sweep #676): `ui-genre-facts`' description fence rephrased from "NOT for which modules a page needs (ui-pattern-facts)" to "NOT 'what modules/sections does a dashboard page need' — ui-pattern-facts even when a genre word appears" (n10 leaked 3-0 to the genre word 'dashboard'). Wording-checker pass caught a body/description tension (the body's own "module inventory" phrase contradicted the new fence) and an em-dash run-on: body phrase trimmed, description clause turned into a parenthetical. Rebased onto #727's v1.1.0 (feature-intake-rules) — no file overlap. `harness:wording-checker` pass per plugin-authoring.md's semantic-edit invariant.
v1.1.0 · 2026-08-19 · gh#711 (lld-0024): new knowledge skill `feature-intake-rules` — per-shape UI-feature ticket intake schemas (component/module, layout/shell, UX flow, cross-cutting UX), fields lifted from the owning build skills' own axes, each naming its build owner + DoD checker; 4 fixture tickets. Named `feature-intake-rules`, not the ticket's `ui-feature-intake-rules` — `ui` isn't a registered ADR-0011 token (validated, zero errors). Reciprocal fences added to `make-component`/`break-down-layout`/`break-down-flow`. Lint/evals clean; `/check-routing screens` owed at PR-open.
v1.0.16 · 2026-08-18 · #650: reciprocal no-trigger eval case added to `break-down-layout` fencing design's new `artifact-styling-rules` pack (Artifact-page-specific styling vs general screen layout critique). Evals.json only, no SKILL.md edit. `release_gate.py screens` clean.
v1.0.15 · 2026-08-16 · ui-probe.mjs entry guard now compares realpaths, fixing the symlink-fragile string compare (closes #438, same class as #436); unitChecks() gains a symlinked-dir negative control, proven to fail pre-fix and pass post-fix
v1.0.14 · 2026-08-16 · checker-agent description diet, tail of #357/#364 (#367): component-checker's description drops the fresh-context/read-only boilerplate #364 trimmed elsewhere; moved to body. collide.py ↔wiring-checker 59.7→48.1. Critic clean. No evals owed.
v1.0.13 · 2026-08-16 · checker-agent description diet (#357): layout-checker/flow-checker descriptions drop the shared fresh-isolated-context / never-grades-own-work / applies-fix boilerplate (collide.py's top cross-plugin *-checker baseline, 6 agents, 103.9-158.4); doctrine moved to body. Critic pass found layout-checker's fix-owner clause lost in the trim — repaired same-round. Re-run: layout-checker↔doc-checker 136.7→62.4, ↔wiring-checker 135.6→64.0; flow-checker↔design-system-checker 115.4→101.1, rest below threshold. No evals.json owed. Siblings trimmed same PR.
v1.0.12 · 2026-08-16 · #348 footer sweep: 6 packs' stamped "Extending this pack" paragraph replaced with the one-line `Extension: governed by [[make-pack]]` citation per pack-writing-rules (harness 3.8.9) — mechanical, bodies otherwise untouched
v1.0.11 · 2026-08-15 · description trims (issue #340): break-down-layout 1140→838, make-component 1025→908 chars — both still over the W8 700 bar (WARN not FAIL), triggers/NOT-fences kept intact; wording-checker restored one dropped collision quote (size-and-shape-rules) before landing; blind /check-routing clean (22/22, 24/24 incl. a 3-of-3 vote)
v1.0.10 · 2026-08-16 · check-whole-ui trim + fence (issue #300, #281 follow-up): description 1040→699 chars (W8 ≤700), reciprocal NOT-fence added naming `layout-checker` (agent); collide.py --against check-whole-ui: 25→10 pairs, the 53.9 layout-checker collision gone. ui-pattern-facts re-verified first — absent from top-30 both runs, no fence needed. evals.json updated same-change; wording-checker CLEAN (minor only); blind /check-routing re-judge: 26/26 PASS (t12 voted 3-of-3) — no tuning owed
v1.0.9 · 2026-08-16 · checker retier (Kim's ruling): 3 *-checker agents move effort high→medium, model fable unchanged — review quality held at medium across the 2026-08-15/16 rounds while inherited-xhigh runs added cost, not findings
v1.0.8 · 2026-08-15 · `break-down-layout` gains the reciprocal NOT-fence against harness's `break-down-problem` (issue #282, closing the third collision-baseline twin per PR #278's recipe): fences on concreteness — abstract system/goal/technical decomposition with no concrete screen is out of scope here; existing eval n09 ("decompose this technical architecture") gains a dated owner-comment crediting break-down-problem rather than a new near-duplicate case; description was already over the W8 700-char budget pre-existing (pre-existing WARN, not introduced by this change)

v1.0.7 · assembled 2026-07-25 · 1.0.7: description diet (PR #92) — 11 SKILL.md descriptions + 3 agent descriptions trimmed to the resident-context budget (issues #79/#80 completion) · v1.0.6 · assembled 2026-07-25 · 1.0.6: retired the stale ADR-0006 transition-table section — dead since ADR-0007 (2026-07-21) renamed every plugin dir to its plugin name and retired the workspace CLAUDE.md alias table it pointed to; replaced with the one true line, directories align with plugin names (ADR-0007); no other prose changed · v1.0.5 · assembled 2026-07-21 · 1.0.5: ADR-0008 design-merge sweep — live pointers (make-design-system, design plugin) rewritten; pointer updates only · v1.0.4 · assembled 2026-07-21 · 1.0.4: ADR-0007 dir alignment — the plugin's directory renamed to its plain plugin name (was the frozen `ui 0.1.0`); version-suffixed, space-bearing paths retired estate-wide; pointer updates only · v1.0.3 · assembled 2026-07-21 · 1.0.3: ADR-0006 harness-rename sweep — live references rewritten (checker agents' write-handoff blocks, make-pack routes); pointer updates only · v1.0.2 · assembled 2026-07-21 · 1.0.2: ADR-0006 docs-rename sweep — live references rewritten; pointer updates only · v1.0.1 · assembled 2026-07-21 · 1.0.1: ADR-0006 teamwork-rename sweep — live references rewritten (checker agents' code-checker fences, pack references to the delivery seats); pointer updates only · v1.0.0 · assembled 2026-07-21 · 1.0.0: ADR-0006 rename PR 6/9 — the PLUGIN renames ui → screens and all fifteen members + the three reviewer agents take the simple paradigm (transition table above). MAJOR bump — names are APIs and this is breaking. Workspace sweep (129 files + a hand pass on the bare `ui` plugin token, ledger history + forging evidence excluded); baseline = the color/typography/design-kits-wave results for 11 suites + a fresh 78-case blind run for dom-layout-facts / break-down-flow / apple-mobile-facts / ui-genre-facts; post-rename re-measure in the campaign PR: 342/345 aggregate with two per-suite regressions healed same-change (ui-genre-facts' modules fence widened to carry 'what modules does a dashboard need'; size-and-shape-rules gained the card-nesting-pattern fence + reciprocal n08 after its new name stole ui-pattern-facts' t29 — first heal attempt displaced the load-bearing anatomy fence and was itself reverted by the re-judge, the fence restored). size-and-shape-rules n05 marked KNOWN FLIP-BY-JUDGE (make-component seam; never tune to chase) · v0.9.7 · assembled 2026-07-21 · 0.9.7: ADR-0006 design-kits-rename sweep — live references rewritten; pointer updates only · v0.9.6 · assembled 2026-07-21 · 0.9.6: ADR-0006 typography-rename sweep — live references to typography's old member handles rewritten (layout-decompose fence, suites); pointer updates only · v0.9.5 · assembled 2026-07-21 · 0.9.5: ADR-0006 color-rename sweep — every live reference to color's old member handles rewritten (description fences, eval suites, routing corpora, verify-mechanics, ui-probe.mjs's sibling script path); pointer updates only · v0.9.4 · assembled 2026-07-20 · 0.9.4: ui-change-verify's NOT-for fence completed — a `plugin-decompose`
gap analysis of the color-verify/ui-verify family (kept no-partition; hard preload edge on
color-verify's side, soft co-occurrence kill on ui's) found every sibling except ui-change-verify
already named color-verify as the peer it drives live-artifact work for (focus/i18n/perf/safety-verify
all fence it); ui-change-verify's own list was missing it. Added, no vocabulary or trigger change —
pure family-consistency completion, no eval-run owed (no shared vocabulary exists between the two
suites for this boundary to affect) · v0.9.3 · assembled 2026-07-19 · 0.9.3: knowledge-pack factory-route convention repointed from scribe's retired `knowledge-forge` to forge's `pack-forge` (workspace-wide rename campaign) — every ui-genres/ui-patterns/motion-design reference to its authoring factory updated; no functional/behavior change, a naming correction only · v0.9.2 · assembled 2026-07-16 · 0.9.2: component-forge description re-budgeted under the 1024 portability cap (the #8/#9 fence additions had pushed it to 1127 without the re-budget the tier ladder mandates) — triggers and fences preserved, filler cut; wave re-judge clean · v0.9.1 · assembled 2026-07-16 · 0.9.1: the wave-boundary /eval-run (Issues #8/#9's batched boundary edits) — ui-change-verify 17/17 clean (the new ui-audit fence routes n07 home); geometry-systems t10 (nested-radius phrase) was stolen by component-forge's geometry vocabulary → reciprocal fence added to component-forge (+ suite case n24), re-judge confirms t10 routes home; t09 (cross-family ramp sharing) flipped 1-of-2 rounds on unchanged wording — recorded as boundary-adjacent judge noise per the observability doctrine, not tuned · v0.9.0 · assembled 2026-07-16 · 0.9.0: verify-family mechanics (Issue #8) — ui-audit gains references/verify-mechanics.md, the species canon (rule-ID'd findings incl. per-verifier judgment slugs, the scope ladder, two-sided monotonicity, repair-scope/blast-radius scoring, the narrowest-control waiver ladder with react-doctor's anti-sycophancy clause, armed mode, symptom indexes, the measurement-never-asks fence); focus/i18n/perf/safety-verify each wire a Family mechanics block (declared slugs, symptom index, armed mode, rung-2 instruments named per card schema); ui-change-verify gains its output contract (scope header, blast-radius row, change.no-observable-diff/console-regression slugs) + the ui-audit description fence (eval n07; /eval-run owed at the wave boundary); five fresh-context audits' findings applied, incl. the audit-diff semantics honesty pass (partial re-audits never persisted as baselines) · v0.8.0 · assembled 2026-07-16 · 0.8.0: taste encoding (Issue #9) — motion-design gains references/house-locks.md (the ratified point per knob inside the cited envelope: press scale 0.96, functional bounce 0, user-dismissal exits ≈0.8× with the initiator-flip exception, stagger 20ms adopting the Carbon/M2 citation, hover onset ≤150ms; the frequency gate; the precedence seam with the reduced-motion floor outside it; house-ruling provenance class in sources.md); geometry-systems gains the nested-radius house lock in composable-spacing.md (outer = inner + gap, escape hatch >24px, degenerate clamp, ramp-snap clause) + the radius trigger phrase and eval case t10 (/eval-run owed at the wave boundary); ui-patterns' footer contract upgraded to lock grammar (exactly 1 prominent action; autosave and status-only exceptions); three fresh-context audits' findings applied incl. the blocking exit-asymmetry envelope contradiction · v0.7.0 · assembled 2026-07-16 · 0.7.0: taste elicitation (Issue #13) — layout-decompose gains references/taste-elicitation.md, the cross-skill canon for AskUserQuestion at genuine design forks (five rules: taste-forks-only inside the verified envelope, preview-backed options, the ask-then-lock loop, one batched round, plan-carrying options; plus artifact escalation and the measurement-never-asks fence), wired into DESIGN mode + the decomposition-method workflow + the Verify Target; component-forge gains the step-4 taste gate (code-snippet previews → a rulings entry on the contract card, schema noted in attributes-as-api.md); flow-decompose gains the DESIGN-mode gate (stage-graph previews → the card + a DESIGN.md line for area-wide rulings); all five fresh-context audits' fixes applied · v0.6.3 · assembled 2026-07-15 · 0.6.3: the verification re-snapshot (Issues #5+#6) — dom-block-flow's citations independently verified against live MDN/W3C pages (71 checked: 70 confirmed, 0 diverged, 1 disclosed as spec-mechanics-grounded); mobile-hig-patterns' second research wave found current HIG pages the first wave missed — sheet detents+grabber, alerts vs action sheets, grouped lists, and search placement PROMOTED [inferred]→[verified] with URLs+access dates; buttons grounded on the real Buttons page with the plain/tinted/filled vocabulary mismatch disclosed (HIG says Style/Role); swipe actions and nav-bar anatomy confirmed ABSENT from current HIG and kept [inferred] · v0.6.2 · assembled 2026-07-15 · 0.6.2: subfolder conformance (ruled 2026-07-15: the sanctioned skill-subfolder set is evals/references/scripts/assets — release_gate G2 now warns on any other): the four verifiers' 20 topical JSON dirs (focus-verify ×4, i18n-verify ×4, perf-verify ×6, safety-verify ×6) moved under assets/<topic>/, and layout-decompose/flow-decompose's examples/ folded into references/ — every path referrer repaired (SKILL bodies, checker docstrings/messages, README rows; per-skill CHANGELOGs left as-written, history preserved), all four checker selftests green post-move · v0.6.1 · assembled 2026-07-15 · 0.6.1: ruff sweep (ADR-0002's G11 style tier arriving): component-contract-check.py drops one dead F841 assignment (`bools`, computed and never read). No behavior change, selftest green · v0.6.0 · assembled 2026-07-15 · 0.6.0: three new knowledge skills + one grown axis, all blind-eval-proven. geometry-systems — sizing/spacing SCALE theory (base-unit rationale, the dense-then-sparse progression every major system converges on, composable-spacing/nesting, cross-system comparison), cites component-forge's geometry law and design-systems' material-design-geometry-tokens as worked instances rather than restating them. dom-block-flow — HTML/CSS layout-mechanics world model (box model + margin collapse, BFC triggers, stacking contexts, containing-block resolution, flex/grid gotchas), grounded against MDN + the CSS specs. mobile-hig-patterns — Apple HIG's own navigation/modality/component vocabulary, fenced against ui-patterns' shell regions, motion-design's spring values, and iconography's SF Symbols sizing. ui-patterns gains a fourth axis, container-patterns.md (card/panel/dialog/drawer Header·Body·Footer anatomy, sticky headers, scrollable bodies, footer/nesting conventions) — extended rather than forked into a new skill, since the existing pack already owned pattern-naming territory. Designed via a plugin-decompose-style overlap check before writing a line (found ui-patterns had zero container-anatomy coverage, so the ask routed to growth not a fifth colliding skill). Blind /eval-run over all five touched suites: geometry-systems 16/16, dom-block-flow 16/16, mobile-hig-patterns 15/15, ui-patterns 39/39 — one real authoring bug the judge itself caught and fixed same-change (n18 was hand-authored with the wrong prompt AND wrongly fenced as a no-trigger; both the overlay-choice and container-anatomy questions correctly live inside ui-patterns' own corpus, reclassified to trigger). component-forge gains one reciprocal no-trigger case (n23). G4 (mechanization pair, forge 1.25.0) sweeps 10 scripts here now, one skipped (ui-probe.mjs, dependency absent, disclosed not failed). Same-day correction: mobile-hig-patterns was first drafted directly against established knowledge, then a dispatched research wave against live HIG pages found it had overgeneralized in two places — corrected in place per the grounding discipline (amend, dated note, never silent rewrite): the sheet modality section was missing HIG's actual platform split (always-modal on macOS/tvOS/visionOS/watchOS; modal OR NONMODAL on iOS/iPadOS, per HIG's own Notes-app example) and is now fixed with the direct citation; tab-bar guidance is now cited verbatim from HIG's own "Tab bars" page (navigation-vs-action framing, directional not numeric tab-count guidance, the More-tab overflow mechanism); sheet detents, alerts-vs-action-sheets, button-style vocabulary, list-grouping, and swipe-action conventions were all downgraded from implied-HIG-citation to explicit [inferred]/ecosystem-convention after the wave found no current dedicated HIG page for any of them — disclosed as real corpus gaps in sources.md rather than papered over. Same-day gap closure: a full pattern-taxonomy sweep against the existing corpus (shells → macro templates → containers → micro modules → screen states → mobile-native vocabulary) found exactly one real hole — "settings modal" (a lightweight ≤10-item preferences dialog) was never named as distinct from the full-page settings template — closed as a variant inside macro-patterns.md's existing settings entry (anatomy, fit threshold, and the promotion-not-a-bigger-dialog failure mode), proven with a blind-judged trigger case (t31, 40 cases total). Consult-graph wiring: layout-decompose now routes to the three new packs at the right rubric levels (References table + a new match-at-pattern-altitude SelfAudit rule — A3/A5 spacing-rhythm findings cite geometry-systems, A1/A2 frame/scroll/stacking mechanisms cite dom-block-flow, mobile-app-archetype HIG semantics cite mobile-hig-patterns, container anatomy cites ui-patterns' container catalog; a grade that re-derives substrate inline is working below its altitude), and archetype-mobile-app.md gains the reciprocal regions-vs-semantics pointer — body-tier edits, no description changes, no eval churn per the edit-tier ladder · v0.5.1 · assembled 2026-07-14 · 0.5.1: displayName 'UI' added to the manifest — plugin naming hygiene ruled 2026-07-14: Title Case display names with UI/LLM acronyms uppercased (marketplace entries carry the same field; Claude Code ≥2.1.143, falls back to name) · v0.5.0 · assembled 2026-07-12 · 0.5.0: seat-ladder realignment (forge 1.22.0): component-reviewer and flow-reviewer opus→fable+high, layout-reviewer gains effort: high; README inventory table synced · v0.4.3 · assembled 2026-07-10 · 0.4.3: author attribution corrected to Kim G / NONOUN (was the Agentic Harness placeholder) · assembled 2026-07-09 · 0.4.2: estate-wide blind run (70-skill menu, first of its kind): ui suites 118/120 — component-forge's repo-seat fence and ui-genres' t09 tuning PROVEN, focus-verify's t12 watch CLEARED; new first-strike watches recorded (component-forge t10 'ramp' ↔ geometry-tokens, ui-genres t02 'onboarding flow' ↔ flow-decompose) · assembled 2026-07-09 · 0.4.1: first blind /eval-run over all 12 suites (285 cases): 281/285, motion-design 23/23 clean on debut; the four failures tuned same-change — component-forge's agent-ui repo-seat fence strengthened with both leaked phrasings verbatim (n06/n07), ui-genres gains 'does a marketplace / this kind of product need a [surface]' (t09 stolen by ui-patterns), ui-patterns fenced both ways (genre includes-or-needs; which-keys → focus-verify, remedying focus-verify's t12 steal from the thief's side); descriptions re-trimmed under the 1024 bar · assembled 2026-07-09 · 0.4.0: motion-design — new knowledge pack (durations, easing/springs, choreography, reduced-motion), distilled 2026-07-09 from four research waves (primary sources; Carbon values verified in carbon source after the FLOOR audit caught a fabricated token name), independently reviewed (skill-auditor + linguistics-reviewer, all findings applied); perf-verify gains the reciprocal motion fence (description + n13/n14) and a compressed NOT-list back under the 1024 bar; flash/seizure (WCAG 2.3.1) named as an unowned verifier gap in motion-design's boundaries · assembled 2026-07-09 · 0.3.4: hygiene pass — ui-audit under the 1024 bar; i18n-verify's fix-owner row repointed from phantom spec/prd-author to scribe's doc-forge · assembled 2026-07-09 · 0.3.3: actually lands the description trim — 0.3.2's ledger claimed under-1024 while 3 chars remained (committed without re-verifying; lint now clean, verified pre-commit) · assembled 2026-07-09 · 0.3.2: safety-verify description back under the 1024 portability bar · assembled 2026-07-09 · 0.3.1: safety-verify's silently-disabled trigger made verbatim (third distinct routing failure across three blind runs — stolen, stolen, dead) · assembled 2026-07-09 · 0.3.0: BREAKING (ADR-0001) — component-author→component-forge; component-reviewer's preload updated in the same change · assembled 2026-07-09 · 0.2.3: eval-run tuning — ui-genres/ui-patterns dashboard-modules fence closed both ways; safety-verify fenced optimistic-UI to perf-verify and gained 'actions silently disabled by missing permission' (second-strike confirmed); layout-decompose's 'what shell is this app using' case reassigned to ui-patterns (blind judges routed it there in two consecutive runs — they were right) · assembled 2026-07-09 · 0.2.2: agent fallback blocks 'Tests run'→'Tests/checks run' (harness-audit finding, estate-wide sweep) · assembled 2026-07-07 · 0.2.1: ui-change-verify's evals/evals.json added (G7 coverage gap closed) · 0.2.0: ui-change-verify — drives a UI change against the running artifact (launch/interact/screenshot/console/perf) instead of reasoning about it in the abstract, closing the gap the getting-started-with-loops guide's turn-based-loop example named · 0.1.0: initial: ported from ~/.claude/skills + ~/.claude/agents/design/{layout,flow,component}-reviewer as part of a plugin-decompose partition; merges the ui-architecture and ui-verify candidate clusters on explicit direction (13 cross-mentions between them was the single highest-coupling boundary found)
