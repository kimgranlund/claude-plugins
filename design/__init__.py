"""design — generated by harness_emit.py from .claude-plugin/plugin.json. Do not edit."""
from pathlib import Path

_HERE = Path(__file__).resolve().parent

def register(ctx):
    ctx.register_skill('artifact-styling-rules', _HERE / "skills" / 'artifact-styling-rules' / "SKILL.md", description='Standards for styling/authoring Claude Artifacts: runtime constraints (CSP inlining, 16MB cap, theme tri-state, body-ground, native mermaid); the token role-alias method (light-dark() build-time CSS, never a re-ramp); type/layout doctrine (system-ui body, mono interactive, width/spacing/radius scales); mermaid authoring + re-theming; shell/genre doctrine (narrative vs tabbed, hero-as-thesis). Use for "why do the colors look off", "why sharp corners", "diagrams look off-theme", "what font/type f…')  # one per skill
    ctx.register_skill('check-colors', _HERE / "skills" / 'check-colors' / "SKILL.md", description='Use for a palette/mapping — contrast, hue, evenness, CVD-safety — emits a ColorProof: a full WCAG/APCA sweep, or one pair VERDICT — "check the contrast ratio of this text on this background", "does it meet the accessibility contrast floor", "does this clear 3:1 once composited". NOT a raw ratio/Lc NUMBER ("calculate the WCAG contrast ratio between these two colors", "what\'s the actual Lc value" — color-contrast-facts); NOT a CVD sim ("simulate deuteranopia on this palette and tell me what chang…')  # one per skill
    ctx.register_skill('color-contrast-facts', _HERE / "skills" / 'color-contrast-facts' / "SKILL.md", description='Use for color-accessibility QUESTIONS, never generates — APCA/WCAG 2.2, luminance, CVD, low-vision — incl. an ad-hoc ratio/Lc NUMBER, not a verdict: "calculate the WCAG contrast ratio between these two colors", "what\'s the actual Lc value"; a CVD sim, no card: "simulate deuteranopia on this palette and tell me what changes"; or a CVD-safe pick: "give me a CVD-safe pair for error and success states". NOT a pass/fail VERDICT — "check the contrast ratio of this text on this background", "does it m…')  # one per skill
    ctx.register_skill('color-perception-facts', _HERE / "skills" / 'color-perception-facts' / "SKILL.md", description="Answers vision and color-appearance science — what the eye and brain do with light. Use for chroma vs saturation, lightness vs brightness, why two colors of equal saturation aren't equally vivid; cones, opponent process, afterimages, simultaneous contrast; metamerism — swatches matching under one light but not another; appearance models (Fairchild, CIECAM02); MacAdam ellipses; warm/cool as a perceptual axis, tetrachromacy, OLO. NOT space-conversion math or CSS (color-space-facts); NOT contrast…")  # one per skill
    ctx.register_skill('color-space-facts', _HERE / "skills" / 'color-space-facts' / "SKILL.md", description='Answers computational-color questions — spaces and math. Use for converting or comparing color spaces (OKLCH, OKLab, sRGB, HSL, CIELAB, CAM16/HCT, XYZ), gamut mapping + peak chroma, perceptually even gradients/ramps, ΔE color difference, white points, HDR tone mapping, CSS color syntax (color-mix, relative color, light-dark), extracting a palette from an image, quantization, picking a library. NOT for contrast/CVD/legibility (color-contrast-facts); NOT for vision mechanics (color-perception-fac…')  # one per skill
    ctx.register_skill('color-theory-facts', _HERE / "skills" / 'color-theory-facts' / "SKILL.md", description='Answers questions about color theory — harmony, meaning, and history: whether a color choice reads as intended, and why. Use for color harmony ("why do these colors clash", complementary /triadic/analogous schemes, 60-30-10 proportion), the color wheel and its history, what a color communicates (symbolism, mood, pale-muted-vivid character). NOT for vision science (color-perception-facts); NOT for color-space math (color-space-facts); NOT for contrast/APCA/WCAG/CVD (color-contrast-facts); NOT fo…')  # one per skill
    ctx.register_skill('design-md-rules', _HERE / "skills" / 'design-md-rules' / "SKILL.md", description='Ground-truth knowledge of the DESIGN.md spec format consumed by Claude Design and Claude Code. Use for reading, explaining, extending, or validating a DESIGN.md ("design spec", "design system file"): anatomy, frontmatter schema, --{prefix}-{family}-{slot} grammar, light/dark pairing, @dsCard cards, disclosure-over-correction; the substrate for creating a system from a corpus. NOT any DESIGN.md lint ask — "fails lint", "orphan warnings", @google/design.md — that vocabulary is Stitch\'s lint gate…')  # one per skill
    ctx.register_skill('figma-plugin-facts', _HERE / "skills" / 'figma-plugin-facts' / "SKILL.md", description='Answers Figma Plugin-API questions from practice — variables, styles, sandbox, interchange. Use for raw→semantic ALIASING ("alias variables", "removing a mode threw"), styles bound to variables ("setBoundVariableForPaint"), sandbox/VM failures ("parses in Node, fails in Figma"), token JSON shapes ("UI3 import", "aliasData"), and pluginData/clientStorage. ANSWERS, does not generate. NOT the nonoun color-tokens plugin\'s binder/apply cascade (maintaining-figma-plugins); NOT the DESIGN.md format (d…')  # one per skill
    ctx.register_skill('font-token-rules', _HERE / "skills" / 'font-token-rules' / "SKILL.md", description='Use for UI TYPE on the default `--type-*` grammar (not Material\'s `--md-sys-typescale-*`) — font/size/weight/leading/tracking for heading, body, label, button, caption, kicker ("what type token for this", "which voice/step for this text", "size this heading"). 15-voice scale: bind `--type-*`/`--font-*`, pick VOICE/STEP by job/rank, a font per family slot; never hardcode px/line-height/tracking/family. NOT Material\'s typescale (material-type-facts); NOT no-token typography (lettering-facts); NOT…')  # one per skill
    ctx.register_skill('icon-rules', _HERE / "skills" / 'icon-rules' / "SKILL.md", description='Use for icon-system questions from a cited corpus — construction, sizing, style, metaphor, accessibility, RTL: "what size/stroke weight", "filled vs outlined", "should this mirror in RTL", "is an icon-only button okay", "is this icon big enough to tap", "fill the Iconography section of a DESIGN.md". Carries the per-system table (Material/SF/Carbon/Atlassian/Fluent), Material Symbols axes, WCAG floor, mirror taxonomy. ANSWERS only. NOT an icon build (make-component); NOT verifying mirroring ship…')  # one per skill
    ctx.register_skill('lettering-facts', _HERE / "skills" / 'lettering-facts' / "SKILL.md", description='Use for typography/lettering questions — comparing, explaining, classifying, specifying type: "my headings jump when the webfont swaps in", "does OpenDyslexic actually help", "why does this font look boring/generic", "why does text-wrap: balance not work in my browser", CSS `text-wrap: balance`/`pretty` support — covers type anatomy, classification (Vox-ATypI, humanist/geometric/grotesque), metric-compatible fallback stacks, font personality, world scripts, OpenType features, variable-font axes…')  # one per skill
    ctx.register_skill('make-design-system', _HERE / "skills" / 'make-design-system' / "SKILL.md", description='The cross-platform hub for design-system files LLM agents consume. Use for strategy/context asks spanning MULTIPLE platforms, or none: "which design tool should get our design system", "port our design system to Claude, Stitch, and Make", "our exports drifted apart", "make our generation prompts better", "our design agent output is generic". NOT one export for one platform (make-dscard-kit/make-stitch-kit/make-figma-make-kit) or none (make-dscard-kit); NOT DESIGN.md format Q&A (design-md-rules)…')  # one per skill
    ctx.register_skill('make-dscard-kit', _HERE / "skills" / 'make-dscard-kit' / "SKILL.md", description='Author, evaluate, or regenerate a Claude Design / Claude Code export bundle — DESIGN.md + tokens.json + @dsCard previews — from a corpus (css, tokens, code, brand decks). Use for "create a Claude Design design-system bundle", "based on these css files create a design system for claude design and claude code", "fix the dark scheme in my Claude design bundle". Covers corpus census, Root Brand Architecture, token grammar, OKLCH, light-dark(), and contrast disclosure. NOT format Q&A (design-md- rul…')  # one per skill
    ctx.register_skill('make-figma-make-kit', _HERE / "skills" / 'make-figma-make-kit' / "SKILL.md", description='Author, evaluate, or regenerate a Figma Make design-system guidelines/ folder for any theme or brand. Use for "create Figma Make design system guidelines", "write Guidelines.md for Figma Make", "Make output ignores our design system — fix the guidelines", "check this guidelines folder before shipping". Covers the routed-folder shape (Guidelines.md + foundations/ + components/), the React+Tailwind+shadcn/ui flavor, the naming grammar — Make validates nothing, so this is the gate. NOT Claude Desi…')  # one per skill
    ctx.register_skill('make-figma-skill', _HERE / "skills" / 'make-figma-skill' / "SKILL.md", description='Author, convert, check, or regenerate a single-file Figma custom skill — the kind the Figma agent and Figma Make load from one .md. Use for "create a Figma skill from our X skill", "convert this skill into a Figma custom skill", "export our Factory skills as Figma skills", "the Figma skill lost the contrast thresholds — regenerate it", "check this figma skill before I publish". Converts an estate reference skill without losing resolution: references inlined whole, scripts transposed to checklis…')  # one per skill
    ctx.register_skill('make-palette', _HERE / "skills" / 'make-palette' / "SKILL.md", description='Design OKLCH color ramps and semantic role mappings from brand anchors. Use to build a ramp from a brand anchor, extend a palette to a full scale, map semantic roles, choose intent hues (danger/warning/success/info), or derive the dark scheme — ramp skeletons, lightness spacing, chroma arcs, hue stability, gamut-safe reduction, role assignment, verified by check-colors before finalize. NOT for verifying an existing palette (check-colors); NOT for color-space math (color-space-facts); NOT for ha…')  # one per skill
    ctx.register_skill('make-stitch-kit', _HERE / "skills" / 'make-stitch-kit' / "SKILL.md", description='Author, evaluate, or regenerate a Google Stitch DESIGN.md — YAML frontmatter tokens + 8 canonical sections. Use for "create a Google Stitch DESIGN.md", "lint my DESIGN.md", "port tokens to Stitch", "why all these orphan warnings". Covers the alpha frontmatter schema, section order, OKLCH values, -dark scheme siblings, the --{prefix}-{family}-{slot} naming grammar, and the lint gate (npx @google/design.md lint, zero errors). NOT Claude Design DESIGN.md format Q&A (design-md-rules); NOT Claude De…')  # one per skill
    ctx.register_skill('make-variants', _HERE / "skills" / 'make-variants' / "SKILL.md", description='Publish an Artifact of N variants along declared axes, grounded in the project\'s design system when one exists, each card voted up/down with a note, serializing a live variant-feedback/v1 JSON block pasted back to resume at the same URL. Use when asked to explore variants of a component, show variants of X, N takes on a layout, iterate on a design, or run a variant exploration — or paste a blob whose first key is "schema": "variant-feedback/v1" (resume). NOT for one finished design or a hand-ed…')  # one per skill
    ctx.register_skill('material-color-facts', _HERE / "skills" / 'material-color-facts' / "SKILL.md", description='Use when choosing the COLOR of any UI element — which color/role a button, text, card, border, or state should take ("which color should this button use", "what color for a disabled input border", "what token for this text background", "why is this the wrong color", "wire the theme colors", "add hover/disabled colors", "make it work in dark mode"). Consumption guide for the 59-role `--md-sys-color-*` layer extending Material 3 — the estate\'s default color grammar: pairing laws, per-surface role…')  # one per skill
    ctx.register_skill('material-motion-facts', _HERE / "skills" / 'material-motion-facts' / "SKILL.md", description='Use when ANIMATING any UI whose motion tokens use the Material `--md-sys-motion-*` naming, or when the M3 motion token table is the question ("what easing/duration token for this dialog", "which spring token — fast or slow, spatial or effects", "emphasized or standard easing here"). Carries the verified table — 10 easing cubic-beziers, the 16-step duration ladder, the M3 Expressive spring tokens — the pairing laws (entrances decelerate, exits accelerate), the platform-drift map. Never hardcode…')  # one per skill
    ctx.register_skill('material-shape-facts', _HERE / "skills" / 'material-shape-facts' / "SKILL.md", description='Use for SIZING/SPACING UI using Material `--md-sys-{size,radius,space,border,focus-ring}-*` tokens — control height/padding/radius, card inset, gaps, icon size, border/focus-ring width ("what size/spacing token", "the md-sys border width token", "the md-sys focus ring token", "how tall should this control be", "make the layout denser"). Consumption guide for M3\'s dimensional layer: control ramp, corner scale, space/inset/gap ladders, touch-target min. Text-SIZE is `--md-sys-size-{step}-font`. N…')  # one per skill
    ctx.register_skill('material-token-facts', _HERE / "skills" / 'material-token-facts' / "SKILL.md", description='Use for what a `--md-sys-{color,typescale,size,radius,space,inset,gap,border,focus-ring}-*` token MEANS — glossary across color/typography/geometry: "surface-lowest vs surface-lower", "-dim vs -low", "what\'s the kicker voice for", "difference between the scrim strengths" or between two token families. Meaning only; recipes/bind-check load material-{color,type,shape}-facts. NOT a raw hex/px/dp value or "which token" consumption ask — e.g. "the md-sys border width token", "the md-sys focus ring t…')  # one per skill
    ctx.register_skill('material-type-facts', _HERE / "skills" / 'material-type-facts' / "SKILL.md", description='Use for Material `--md-sys-typescale-*`/`--font-*` tokens — font/size/weight/leading/tracking for heading, body, label, menu item, table cell, button ("which voice/level", "why is this text the wrong size/weight") — on the fifteen-voice scale extending M3: pick VOICE (READ prose vs OPERATE chrome) and LEVEL — never hardcode px/line-height/tracking/family. NOT a control\'s text size (material-shape-facts\' `--md-sys-size-{step}-font`: "what font size for this button"); NOT other radius/spacing (ma…')  # one per skill
    ctx.register_skill('physical-color-facts', _HERE / "skills" / 'physical-color-facts' / "SKILL.md", description="Answers physical-color and color-naming questions — pigment, print, and cataloging standards. Use for paint mixing (Kubelka-Munk, why blue+yellow makes green while RGB blending makes gray/mud; Spectral.js, Mixbox), pigment history, print-vs-screen mismatch, ICC profiles, iridescence and thin-film color, Pointer's gamut of real surface colors, and color naming (ISCC-NBS, Munsell, Ridgway, hex→name). NOT space- conversion math (color-space-facts); NOT vision/appearance science, incl. metamerism (…")  # one per skill
    ctx.register_skill('pick-fonts', _HERE / "skills" / 'pick-fonts' / "SKILL.md", description='Design an opinionated 15-voice typography system from a brand brief: pick a font pairing ("pick a body and display pairing"), verify a chosen pairing\'s craft ("is this pairing metric-compatible"), decide which typeface fills each voice, or turn "modern and clean" into a specific point in design space — territory, rationale, coherence, craft verification; font-token-rules realizes it. NOT for color ramps (make-palette); NOT for realizing a decided system as tokens (font-token-rules); NOT for typ…')  # one per skill
    ctx.register_skill('token-feature-intake-rules', _HERE / "skills" / 'token-feature-intake-rules' / "SKILL.md", description='Intake schema for a token/palette/typography feature ticket — which roles/ramps, both-theme reach, and consumers on one plane; specific token values, contrast gates, and the interaction-state ladder on the other. Use when capturing/reviewing what a visual-token TICKET should state before build: "what should a new-accent-color ticket capture", "what fields does a typography-seed ticket need", "is this token ticket capture-complete". NOT for building/grading the palette or type system itself (mak…')  # one per skill
