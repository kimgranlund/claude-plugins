# design — color, typography, and design-system exports

One plugin for the visual-design layer: color science and OKLCH palettes, the 15-voice
typography system, Material Design's token grammar, and the export bundles a generative design
agent consumes (Claude Design/DESIGN.md, Figma Make, Google Stitch). Formed 2026-07-21 by
merging the `design-kits`, `color`, and `typography` plugins (ADR-0008) — the trio carried 48
cross-plugin mentions, the workspace's highest-coupling seam after the ui merge precedent.
Legacy per-plugin ledgers: `legacy/README-{design-kits,color,typography}.md`.

## Map

| Artifact | Type | Invocation | What it carries |
|---|---|---|---|
| `skills/make-palette` | Procedural | both | OKLCH ramps + semantic role mapping from brand anchors; every ramp verified by `check-colors` |
| `skills/check-colors` | Procedural | both | Palette/pair verification — contrast, hue stability, evenness, CVD-safety → ColorProof |
| `skills/color-contrast-facts` | Knowledge | model-only | APCA vs WCAG, luminance, CVD simulation and safe pairs |
| `skills/color-perception-facts` | Knowledge | model-only | Vision/appearance science — chroma vs saturation, opponent process, CIECAM02 |
| `skills/color-space-facts` | Knowledge | model-only | Space conversions, gamut mapping, gradients, CSS color, libraries (project files: `color-science-project-files/`) |
| `skills/color-theory-facts` | Knowledge | model-only | Harmony, meaning, history — schemes, 60-30-10, palette mood |
| `skills/physical-color-facts` | Knowledge | model-only | Pigment/print physical color — Kubelka-Munk, ICC, iridescence, color naming (was `color-material-facts`) |
| `skills/pick-fonts` | Procedural | both | Design a 15-voice typography system from a brand brief; craft verification |
| `skills/font-token-rules` | Knowledge | model-only | The `--type-*` token grammar — voice × step, leading, concrete font per slot |
| `skills/lettering-facts` | Knowledge | model-only | The cited type corpus — anatomy, classification, scripts, OpenType, text a11y |
| `skills/make-design-system` | Procedural | both | The export hub: full design-system bundle across platforms (was `make-design-kit`) |
| `skills/make-dscard-kit` | Procedural | both | Claude Design / Claude Code bundle (DESIGN.md + tokens.json + @dsCard previews) |
| `skills/make-figma-make-kit` | Procedural | both | Figma Make guidelines/ folder export |
| `skills/make-stitch-kit` | Procedural | both | Google Stitch DESIGN.md export |
| `skills/design-md-rules` | Knowledge | model-only | The DESIGN.md format contract (term-of-art name, ADR-0008) |
| `skills/icon-rules` | Knowledge | model-only | Icon sizing/mirroring/set discipline (Phosphor house default) |
| `skills/figma-plugin-facts` | Knowledge | model-only | Figma plugin API facts |
| `skills/material-color-facts` | Knowledge | model-only | Material `--md-sys-color-*` consumption guide |
| `skills/material-type-facts` | Knowledge | model-only | Material typescale consumption guide (15 voices) |
| `skills/material-shape-facts` | Knowledge | model-only | Material geometry — control ramp, radius, space ladder |
| `skills/material-motion-facts` | Knowledge | model-only | Material `--md-sys-motion-*` tokens |
| `skills/material-token-facts` | Knowledge | model-only | The `--md-sys-*` token glossary across the three domains |
| `skills/artifact-styling-rules` | Knowledge | model-only | Styling/authoring doctrine for Claude Artifacts — platform facts, token role-alias architecture, type/layout, mermaid, shells/genres (#650) |
| `agents/token-builder.md` | Agent | dispatched | Realizes palettes/dimensions as project token layers |
| `agents/font-choice-checker.md` | Agent | dispatched | Grades ONE typography system against pick-fonts' rubric |
| `agents/design-system-checker.md` | Agent | dispatched | Grades ONE export bundle against the owning sibling's rubric (was `design-kit-checker`) |

## ADR-0008 transition table (merged 2026-07-21)

Three plugins merged into `design`; three members renamed. Old handles remain greppable only in
ledgers, CHANGELOGs, ADRs, and the legacy READMEs.

| Old | New |
|---|---|
| `design-kits` / `color` / `typography` (plugins) | `design` |
| `make-design-kit` | `make-design-system` |
| `color-material-facts` | `physical-color-facts` |
| `design-kit-checker` (agent) | `design-system-checker` |

v1.3.2 · 2026-08-20 · design-md-rules exporter-alignment amendments (ultimate-tokens#471 review): grammar.md — disabled wash allows resolved-role-token (preferred) or 60%-alpha; -active scoped to interactive-fill families (never a per-family mandate); Stitch-compat `primary` alias documented; family names ruled role positions, not literals. anatomy.md — state-complete rule + frontmatter comment aligned; Iconography/Motion noted as legal always-on spine sections. Dated notes in place; no description change, no evals owed.
v1.3.1 · 2026-08-20 · the screens->frontend plugin rename (Kim's ruling 2026-08-20: one plugin absorbing the front-end knowledge series): live `screens:` handles and plugin-prose mentions repointed to `frontend:`/`frontend`; ledger history untouched.
v1.3.0 · 2026-08-20 · closes #792: the voice taxonomy re-snapshot, 11 -> 15 voices (TKT-0008, source: ultimate-tokens `docs/reference/typography/intended-use.md` + `src/engine/type.mjs`, current main). `pick-fonts/references/house-typescale.md` re-snapshotted whole (the fixed size table, per-breakpoint), documenting the `modeFactor`-derived breakpoint mechanism (Tablet x5/6, Mobile x2/3) replacing the old hand-authored breakpoint rows. `font-token-rules/SKILL.md`'s canonical voice table rewritten for all 15 voices; the box-voice set re-split — `label` is now explicitly STATIC (never `-line-single`), the three genuine box voices are `kicker`/`ui-control`/`ui-widget` (the two new dedicated interactive voices, each a full 6-step XS-2XL ramp). `code` retired as a voice name, folded into `body-mono` (same mono-role job, pegged to body's sizes). Cascaded through `references/{prose,interface,responsive,font-selection}.md` (interface.md's control/badge/tag table re-mapped off `label` onto `ui-control`/`ui-widget`) and `pick-fonts/SKILL.md` + its `references/{font-craft,rubric}.md`. The bundled `scripts/type-check.mjs` checker's own `VOICES` array and `BOX_VOICES` set were STALE (still 11 voices, still `{label, code, kicker}`) -- a real functional bug, not just prose: fixed, plus 3 new selftest fixtures (a box voice missing `-line-single` fails, `label` with none is NOT flagged, a compound name like `body-mono` parses under its own longer name, not as `body` + stray step). Root `README.md`, `.claude-plugin/plugin.json`, and both skills' `evals.json`/`routing-corpus.json` example prompts updated 11->15/code->body-mono for consistency. `type-check.mjs selftest` + `skill_lint.py` W2/W8 clean on all touched files.
v1.2.5 · 2026-08-19 · closes #778 (overhaul wave S0-B+W2, Gate-A-approved campaign row, 2026-08-19): S0-B centralizes the feature-intake citation across three carriers — `pick-fonts`, `make-palette`, and the `design-system-checker` agent now point at `token-feature-intake-rules` with a bare pointer clause instead of restating the ticket-capture sentence. W2 diets 9 skill descriptions + the `design-system-checker` agent to the ≤700-char resident-listing budget (issue #79 class): check-colors 1280→699 (HIGH risk — the #716 number-vs-verdict seam against color-contrast-facts, and the check-focus fence, both preserved verbatim), color-contrast-facts 1196→696 (same seam, serialized after check-colors), font-token-rules 909→696, icon-rules 882→700, make-design-system 841→698, material-type-facts 814→697 (HIGH risk — 3 prior diet regressions on this member's seams per the v1.0.4/v1.2.1/v1.2.2 history; the material-shape-facts touch-target/text-size fence held verbatim, a mid-word YAML-fold break introduced mid-edit caught and reflowed before landing), pick-fonts 723→667, artifact-styling-rules 720→698 (trim-only), lettering-facts 717→686 (trim-only), design-system-checker (agent) 822→687. Every touched description carries its own evals.json dated note. A fresh-context `wording-checker` pass over the whole 11-file slice caught two real grammar nits from the lint-driven "Use for" trigger-phrase patches (design-system-checker's clipped "or asked" → "or when asked"; lettering-facts' mis-scoped colon → an em-dash) and flagged two PRE-EXISTING ambiguities (material-type-facts' "button" appearing in both its owned voice list and its own fenced example — present since v1.0.x, not introduced here; make-design-system's "naming none" appearing on both the positive strategy-ask side and the negative single-export-default side — also pre-existing) that were left untouched rather than risk-edited on the strength of a wording nit alone. A scoped blind re-judge (full 25-skill menu, all 10 touched suites, 258 cases, UNNAMED judges) ran 255/258 clean first pass; 2 real regressions surfaced and healed in the same change — color-contrast-facts' t12 ("give me a CVD-safe pair for error and success states") was stolen by make-palette post-trim, fixed by adding it as an explicit CVD-safe-pick example while trimming lower-value text to hold budget (t12 was already an accepted-ambiguous seam per this suite's own note, so this is a tightening, not a new-regression repair) — a second self-review caught that the same trim pass had also dropped the literal word "low-vision", a real risk to this suite's own t07/t08/t09; restored it, trimming "space/gamut math"/"vision science" to "gamut"/"vision" (citations still disambiguate) to hold budget instead, re-verified 696 chars, lint clean; make-design-system's t08 ("make our generation prompts better") was a dead miss, fixed the same way, trimming the "add a platform profile" quote (t07) to make room — both re-verified clean via a fresh blind re-judge, including confirming t07 still routes correctly without its own quote. The third finding, icon-rules' n03 ("check the RTL mirroring on our shipped settings screen") reading as icon-rules instead of no-trigger, is a scoped-menu artifact of this design-only re-judge (screens' check-translations, icon-rules' own correctly-named NOT-clause target, wasn't on the 25-skill menu at all) — not a description defect, no edit made; flagged for a full design+screens `/check-routing --estate` re-check. `eval_check.py --coverage` clean; `skill_lint.py` W2/W8 clean on all 11 touched files.
v1.2.4 · 2026-08-19 · #760: reciprocal NOT-fence tightening for the sibling-pack routing failures #750's full run surfaced. `make-design-system` names all three single-platform siblings + keeps the no-platform-named→make-dscard-kit default + fences design-md-rules; `design-md-rules` fences Stitch's lint vocabulary (orphan warnings, @google/design.md) even with Stitch unnamed, and the hub; `make-stitch-kit` fence reworded; `figma-plugin-facts`↔`make-figma-make-kit` reciprocal fence (the ticket's unlocated 5th pair, resolved design-internal — no screens skill mentions Figma; screens unchanged, 1.1.3 slot not taken). Pairs 1 (color-space/perception/contrast) and 4 (material-type/font-token/material-shape) re-audited: already reciprocally fenced, no edit. Reciprocal negatives: design-md-rules n11, figma-plugin-facts n07, make-figma-make-kit n13. UNNAMED skill-checker first verdict NO-GO (over-broad Stitch fence; hub lost its default fence + t11 phrase) — all findings fixed; three touched descriptions back ≤700, hub 863→841. Blind re-judge over the 6 touched suites, 126 cases (UNNAMED judges, menu inlined): 122/126 first pass; 2-of-3 contested re-vote cleared t06/t10/stitch-n06 as single-judge noise; n11 (orphan warnings) genuinely failed 2-of-3 → one heal round (fence re-keyed to lint vocabulary: "fails lint"/"orphan warnings"/@google/design.md) → 3/3 pass. Zero new stolen/leaked cases. Full detail on #760.
v1.2.3 · 2026-08-19 · closes #750: new skill `make-variants` (minted as `explore-variants`,
renamed same-day per Kim's marshal-relayed naming ruling — see below) — N declared-axis design
variants published as ONE artifact with per-card thumbs-up/down+note voting, a live
`variant-feedback/v1` JSON block (schema, stable axis-derived ids, resume-mode read; null
unvoted ≠ downvoted), clipboard export (navigator.clipboard + execCommand fallback), same-path
republish, and a terminate-on-pick handoff to the named build skill (default
screens:make-component). Corrected the source intent record's own Gates section, which had
pre-filled P2–P5 as PASS before those artifacts existed — real dates/results substituted. P2:
evals.json, 22 cases (11 trigger incl. the JSON-paste resume trigger + 5 verbatim phrasings; 11
no-trigger fencing the design canvas skill/artifact-design/design-system-checker/screens:component-checker/screens:make-component/pick-fonts),
5 behavioral assertions, documented-delta baseline note. P4: prompt-wording-rules pass —
potency_lint first flagged NEVER/prohibitions over budget (9/10 vs 3/5), rewritten to exactly 3
hard gates. P5: skill_lint clean; fresh-context skill-checker PASS (0 blocking, 2 minor findings
fixed inline, 1 accepted with note); `/check-routing design` run full-plugin (25 suites,
single-judge pass, contested-voting round not run — named scope simplification): this skill
scored 22/22 clean and never won a case in any of the other 24 suites — the new menu entry
introduced zero routing contamination either direction. 19 pre-existing failures surfaced
elsewhere (color-space-facts↔color-perception-facts/color-contrast-facts,
make-dscard-kit↔make-design-system, make-stitch-kit↔design-md-rules/make-design-system,
material-type-facts↔font-token-rules/material-shape-facts, one figma-plugin-facts leak) predate
this ticket and are flagged for a follow-up, not fixed here. `screens:make-component`'s own
evals.json gained a reciprocal no-trigger case (n26) for this skill's flagship phrasing. **Naming
grammar and rename:** minted as `explore-variants`, which FAILED ADR-0011's naming grammar
(`explore` has no legal skill-grammar head, and the exemptions array is shrink-only) — held as a
named blocker for the marshal's call per Kim's own commissioning of the original name. The
write-gate marshal relayed Kim's interactive ruling (AskUserQuestion) to rename to
`make-variants` — the originally-proposed nearest structurally-valid alternative, reusing this
plugin's own `make-` head. Executed same-day: directory + all internal self-references renamed;
`variant`/`variants` registered in `naming.manifest.json`'s `object_vocab`, fetched fresh from
`origin/main` via the GitHub API (never a local checkout copy, per the marshal's explicit
anti-stale-copy instruction) and edited in this same change. Post-rename re-verification:
`authorkit validate.py --scope grammar` PASS on `make-variants`; `skill_lint.py`/`eval_check.py`
re-run clean; `version_claim_check.py` re-run against a freshly-fetched `origin/main`; a
single-judge spot re-check of the `make-variants` suite confirmed clean routing under the new
name (full 25-suite re-run not repeated — the description body is byte-identical, only the
frontmatter `name:` and path changed, scoped down per the marshal's own instruction).
v1.2.2 · 2026-08-19 · closes #723 (the material-shape-facts t08/t10 follow-up #717 flagged, not fixed there): shape-facts' own prefix enumeration named only `size`/`radius`/`space`, omitting `border`/`focus-ring` even though its prose already covered them — a pre-fix blind judge confirmed both cases ("the md-sys border width token", "the md-sys focus ring token") read as material-token-facts, whose prefix list named border/focus-ring prominently. Fix: shape-facts' prefix list now enumerates `--md-sys-{size,radius,space,border,focus-ring}-*` explicitly with both phrasings as worked examples; token-facts gained a reciprocal fence distinguishing a "which token" CONSUMPTION ask (shape-facts) from a "what does it mean" MEANING ask (its own glossary job), plus n09/n10 reciprocal negatives. Both descriptions re-dieted to the W8 <=700-char budget in the same change (folded in per the ticket's own scope) — token-facts' first diet pass regressed 3 cases (t02 stolen by material-type-facts, t05 dead, t08 stolen by material-shape-facts), caught by a full-suite blind re-judge and healed over two rounds (restoring the scrim-strengths example and glossary-scope framing fixed t05/t08; swapping in a typography-domain example fixed t02, which needed a third judge to clear a 2-of-3 majority after the first vote went against it). Final state: pre/post blind judge on t08/t10 confirms the fix (both now route to material-shape-facts, previously both read as material-token-facts); full-suite blind re-judge 20/20 on material-shape-facts and 18/18 on material-token-facts, zero new stolen/leaked/dead cases either direction. `eval_check.py --coverage` clean, skill_lint W2/W8 clean on both touched descriptions. A fresh-context wording-checker pass (mandatory for a semantic description edit) caught a real defect the routing judges' own tolerance had missed: the YAML `>-` fold's line breaks landed mid-identifier three times across the two files, so the rendered strings carried spurious spaces inside `focus-ring` (×2) and `material-shape-facts` (×1) even though the worked example phrasing elsewhere stayed correct — reflowed so no fold point falls inside a hyphenated token, verified byte-identical to the intended string via `yaml.safe_load`; whitespace-only, no routing re-verification owed.
v1.2.1 · 2026-08-19 · closes #716, #717, #719 (check-routing sweep #676 regressions, batched with #715/#718/#720 across the estate; rebased onto v1.2.0/#728 after a same-plugin PR race — mechanical rebase only, no content change from the original v1.1.6-titled entry below, retitled/renumbered to the actual next-free slot): three description-boundary fixes. #716 — color-contrast-facts vs check-colors: the boundary is now a raw NUMBER vs a pass/fail VERDICT (color-contrast-facts computes one ad-hoc contrast-ratio/Lc number or a one-off CVD simulation with no card; check-colors owns any verdict against a threshold, even for a single ad-hoc pair, plus the full card-gated sweep) — first-pass fix (raw ad-hoc vs full-card) over-corrected and stole check-colors' own t03/t05/t11 ("check the contrast ratio…", "does it meet the floor", "does this clear 3:1"); the number-vs-verdict framing re-verified clean via `/check-routing` with no new stolen/leaked rows either direction. #717 — three of four named cases fixed: material-type-facts now fences a control's own text size to material-shape-facts' `--md-sys-size-{step}-font` verbatim; material-token-facts' M3-vs-extension NOT-fence strengthened to a NEVER-answers claim naming the exact phrase; icon-rules' touch-target-DP fence sharpened and material-shape-facts gained the reciprocal claim (both blind-verified). design-md-rules t06 ("pairing law for on-colors") is NOT a new regression — its own evals note already RULED it no-trigger on 2026-07-09 after two consecutive blind runs; the #676 sweep re-flagged an already-settled call, left untouched. Also surfaced (out of scope for #717, not fixed here): material-shape-facts t10/t08 ("the md-sys border width token" / "the md-sys focus ring token") lose to material-token-facts, 2 pre-existing failures unrelated to any of the four named cases — flagged for a follow-up ticket. #719 — artifact-styling-rules gained the Artifact-scoped font/type trigger with a reciprocal fence + negative in font-token-rules; lettering-facts gained the CSS `text-wrap: balance`/`pretty` trigger; check-colors gained an explicit focus-ring NOT-fence pointing to screens' check-focus. Blind-judged via `/check-routing` (UNNAMED synchronous judges, menus inlined) across all nine touched suites; every one of the eight originally-named cases now routes correctly with zero new stolen/leaked rows introduced. `eval_check.py --coverage` clean.
v1.2.0 · 2026-08-19 · gh#711 (lld-0024): new knowledge skill `token-feature-intake-rules` — intake schema for token/palette/typography feature tickets (roles/ramps/theme-reach/consumers × token values/contrast gates/interaction-state ladder), fields lifted from `make-palette`/`pick-fonts`/`check-colors`; 1 fixture ticket. Named `token-feature-intake-rules`, not the ticket's `visual-feature-intake-rules` — `visual` isn't a registered ADR-0011 token (validated, zero errors). Reciprocal fences added to `make-palette`/`pick-fonts`. Lint/evals clean; `/check-routing design` owed at PR-open.
v1.1.5 · 2026-08-19 · closes #649 (docs' cross-plugin ride-along; docs' `css_build.py` gains the emit-side type-doctrine binding, lld-0013 v2 Resolution 10): `artifact_check.py`'s `CSS_BUILD_OUTPUT_FIXTURE` re-captured verbatim to include the new fixed default-binding block (body/reading -> system-ui, interactive -> the mono stack) `css_build.py` now always emits between its `[data-theme]` toggles and the mermaid re-theme block — byte-verified identical to a real `css_build.build()` run. Re-synced a SECOND time same-day after an independent code-checker review of the docs-side change caught a HIGH-severity bug in the block's own doc comment (a literal `/* override: ... */` example nested inside it, silently terminating the CSS comment early via the first close-sequence encountered and corrupting every doctrine rule below — CSS comments never nest); this fixture's first capture carried that same bug verbatim, so it re-syncs here too, unchanged in its own check logic. The new rules resolve on-doctrine by construction (literal `system-ui`/`ui-monospace` stacks, no `--font-*` indirection), so the fixture's expected zero-FAIL / one-WARN count is unchanged either capture; re-verified via `selftest` both times.
v1.1.4 · 2026-08-19 · closes #684 (marshal ruling, option 3 — resolve-then-check, refining #662, not reversing it): `artifact_check.py`'s `doctrine-font-stack` check now FOLLOWS `var(--font-*)` indirection — the token's own declaration on the page is resolved and the RESOLVED stack judged. Doctrine-resolving tokens still pass (locked: `GOOD_FIXTURE_FONT_VAR`, now declaring `--font-system-ui`/`--font-ui-monospace`); a token resolving off-doctrine warns citing the resolved value unless an override comment precedes the use; an undeclared token warns naming it. `CSS_BUILD_OUTPUT_FIXTURE` (verbatim css_build output) stays zero-FAIL — its mono token resolves on-doctrine (`ui-monospace` in-stack); the synthetic body rule's `var(--font-gt-america)` now carries exactly the one #684-ruled WARN (correct behavior per ruling — make-artifact's justification mechanism is #649's v2 build, noted there). Selftest gains the three resolve-then-check bite/suppress fixtures. Pure script fix.
v1.1.3 · 2026-08-18 · closes #681: `artifact_check.py`'s `br-in-mermaid-label` check missed the HTML-artifact-native `<pre class="mermaid">` embedding form (markdown-fence-only before). New `MERMAID_HTML_BLOCK_RE` scans both forms. Selftest gains a bite fixture + a reverse control for the HTML form; existing fence fixtures unchanged. Pure script fix.
v1.1.2 · 2026-08-18 · closes #662 (design half): `token-architecture.md` clarifies its `--c-{family}-{slot}` naming grammar as the DESIGN-SYSTEM SIDE's own token grammar, distinct from the artifact page's own 14-live-roles inventory (`--paper`/`--ink`/`--accent`/…) which `css_build.py` (docs plugin) now emits directly — the "prefix-adaptive mechanical find/replace" clause retired as a required regen step, kept only as a legacy pointer. `artifact_check.py`'s `doctrine-font-stack` check now treats any `font-family: var(--font-*)` reference as on-doctrine by construction (third checker-alignment note on #662 — 10 false warns on a fully conforming page, since token indirection IS the doctrine's own prescribed mechanism). New selftest fixtures: the font-var-indirection reverse control, and a cross-script regression fixture (`CSS_BUILD_OUTPUT_FIXTURE`, a dated literal copy of `css_build.py`'s own selftest output — scripts can't import across the plugin boundary) proving verbatim `css_build.py` output passes this checker clean. PR #661's `(?:c-)?paper` acceptance kept as a harmless legacy-page allowance, documented as redundant against fresh output. Ships alongside docs v1.19.8 in the same PR. `release_gate.py design` clean.
v1.1.1 · 2026-08-18 · #660: artifact_check.py false-positive fixes — literal-outside-root scans only CSS (`<style>`/`style=""`; whole text for a bare stylesheet), all `:root` excluded, so prose `#NNN` issue refs no longer report; missing-ground accepts `var(--c-paper)` (css_build.py's emission). Selftest: negative controls for both + a real-literal-in-style positive. Pure script fix.
v1.1.0 · 2026-08-18 · #650: new skill `artifact-styling-rules` — 5-axis pack for Claude Artifact visual doctrine, migrated from docs' `artifact-rules` (now procedure-only) per Kim's ruling. 4-source research wave, dated 2026-08-18. New `artifact_check.py` (6 grep-gates, selftest green) + `rubric.md` (R1-R8). Reciprocal fences: make-design-system, break-down-layout. Gate clean. Full resolutions: lld-0020.
v1.0.9 · 2026-08-17 · reciprocal fence (docs' issue #619): `make-design-system`'s description gains "NOT for consuming an already-authored system into a rendered artifact/report page (docs' make-artifact)" — docs' new `make-artifact`/`artifact-rules` pair CONSUMES this hub's exported design systems; this hub still owns authoring/grading them only. One reciprocal no-trigger eval case added (n13). Trimmed the now-redundant "Owns core+profiles architecture and doctrines" clause to hold the W8 700-char budget. skill_lint.py clean.
v1.0.8 · 2026-08-16 · checker-agent diet tail (#367): font-choice-checker's description already had no removable boilerplate (collide.py scores unchanged, e.g. ↔doc-checker 75.5). Body opener reworded for consistency with #364's siblings. Critic clean. No evals owed.
v1.0.7 · 2026-08-16 · checker-agent description diet (#357): design-system-checker's description drops the shared fresh-isolated-context / never-grades-own-work / gap-map boilerplate (collide.py's top cross-plugin *-checker baseline, 6 agents, 103.9-158.4); doctrine moved to body. Re-run: ↔doc-checker 158.4→78.3, ↔flow-checker 115.4→101.1, ↔code-checker 109.9→91.3, ↔wiring-checker below threshold. Batched critic pass (6 files) clean; no evals.json owed. Siblings in docs/screens/teamwork trimmed same PR.
v1.0.6 · 2026-08-16 · #348 footer sweep: 8 packs' stamped "Extending this pack" paragraph replaced with the one-line `Extension: governed by [[make-pack]]` citation per pack-writing-rules (harness 3.8.9) — mechanical, bodies otherwise untouched
v1.0.5 · 2026-08-16 · checker retier (Kim's ruling): 2 *-checker agents move effort high→medium, model fable unchanged — review quality held at medium across the 2026-08-15/16 rounds while inherited-xhigh runs added cost, not findings
v1.0.4 · assembled 2026-08-15 · 1.0.4: issue #263 — the #79-style description diet wave scoped to `design`. Baseline (2026-08-15): 22 skills, 17,248 chars, ~784 avg — the estate's fattest per-skill, 8 of them over the W8 700-char budget (material-token-facts, make-stitch-kit, material-type-facts, color-perception-facts, physical-color-facts, make-dscard-kit, icon-rules, figma-plugin-facts — all 1,028–1,102 chars). Trimmed all 8 to the budget; the three kit skills (make-stitch-kit/make-dscard-kit/make-figma-make-kit) stayed model-invocable per a same-day scope ruling — make-design-system dispatches them by name and `disable-model-invocation: true` would block that Skill-tool path (#134/#135 class) — trim only, never unlisted. Blind re-judge via `/check-routing design` across all 22 suites (524 cases, fresh shuffle each dispatch): 3 real regressions surfaced from the trim itself — figma-plugin-facts lost its "unit-test plugin code" trigger (dead miss on t11), material-token-facts lost "-dim vs -low" and "scrim strengths" (dead + stolen), material-type-facts lost "menu item" from its covered-element list (stolen by font-token-rules), color-perception-facts lost "warm/cool as a perceptual axis" (stolen by color-theory-facts) — each healed by restoring the cut phrase and trimming elsewhere to hold the budget, then reverified via a scoped re-judge; all four now route correctly. Remaining failures across the full sweep (color-contrast-facts, pick-fonts, make-design-system, icon-rules t13, color-space-facts skips) are pre-existing single-judge noise in untouched siblings, several already recorded as known-ambiguous in their own suite notes — not attributable to this wave. Plugin total: 17,310 (measured) → 14,331 chars, avg 787 → 651. `evals/evals.json` dated-note updated on all 8 touched suites. skill_lint W8/W2 clean on every touched description · v1.0.3 · assembled 2026-07-26 · 1.0.3: issue #106 — material-color-facts' post-diet dead-routing repaired. #94's blind re-measure found FIVE bare-phrasing triggers routing to none (18/23): the dieted description's leading clause — 'whose color tokens use the Material --md-sys-color-* naming' — read as a PRECONDITION, so a prompt naming a color job but not the grammar failed it, even though two of the dead prompts were quoted VERBATIM in the description. Fix is shape, not phrasings: the description now leads with the JOB unconditionally and demotes the grammar to the covers sentence ('the estate's default color grammar'); suite note dated. Blind re-judge over the six seam suites (127 cases, fresh shuffle, judges given the new menu): material-color-facts 23/23, make-palette 23/23, material-type-facts 20/20, material-token-facts 15/16 (one judge-marginal glossary flip), check-colors and material-shape-facts unchanged at their pre-existing #94 fails — no new leak introduced anywhere. W8 budget held · v1.0.2 · assembled 2026-07-25 · 1.0.2: description diet (PR #92) — 14 SKILL.md descriptions + 3 agent descriptions trimmed to the resident-context budget (issues #79/#80 completion) · v1.0.1 · assembled 2026-07-25 · 1.0.1: retired the stale ADR-0006 transition-table section in `legacy/README-design-kits.md` — dead since ADR-0007 (2026-07-21) renamed every plugin dir to its plugin name and retired the workspace CLAUDE.md alias table it pointed to; replaced with the one true line, directories align with plugin names (ADR-0007); the live `## ADR-0008 transition table` in this file's own footer is unrelated and untouched · v1.0.0 · assembled 2026-07-21 · 1.0.0: ADR-0008 — design-kits 1.0.6 + color 1.0.6 + typography 1.0.6 merge into `design` (coupling evidence: 48 intra-trio mentions; plan-plugin-split merge tests PASS). Three renames ride along (table above): the hub returns to the real term of art make-design-system (member-level term-of-art stutter exception, ADR-0008, extending ADR-0006 Decision 7), physical-color-facts frees "material" to mean only Material Design inside the plugin, design-system-checker follows its hub. Source ledgers preserved under legacy/. Baseline floors carried from the ADR-0006 measured runs (508/515 union); post-merge re-measure on the 22-skill union menu: 3 blind-judge rounds (515→488 raw, 524→507, 524→515 = 98.3% vs 98.6% baseline), ADR-0008 Decision 5 seams fenced — 22 ordered context splits, 8 thief-side description fences, 9 reciprocal no-trigger cases; material-token n07 recorded known-ambiguous (3/3 rounds); parity evidence on PR #73 ·
