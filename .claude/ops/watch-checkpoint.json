{
  "_comment": "Per-source discovery checkpoint for ops-issues (spec-ticketing-watch-triage.md REQ-003). A source that fails to reach does NOT advance its own entry; each source advances independently. Today's resolved backend is git-native (ADR-0002/ADR-0003) -- discovery is gh-only; no Linear entry exists until that adapter ships and an MCP connector is configured (spec-linear-adapter.md REQ-009).",
  "repo": "kimgranlund/claude-plugins",
  "sources": {
    "github": {
      "adapter": "git-native (gh CLI, no MCP/Linear adapter shipped yet)",
      "mechanism": "on-demand gh issue list / gh pr list (no polling interval or MCP subscription configured yet -- REQ-002's entry-mode choice has not been made for this repo)",
      "status": "reachable",
      "last_successful_check_at": "2026-08-10T23:50:32Z",
      "bootstrap_note": "First-ever firing -- no prior checkpoint existed. Treated as discovering everything currently open plus full closed history for classification/dedup context (16 issues, 20 PRs, gh issue/pr list --state all, 2026-07-18). The checkpoint below is the discovery high-water mark going forward: the next firing's window is 'changed since this timestamp', not 'since repo creation' again.",
      "last_firing_note": "2026-08-10T23:50:32Z firing (full unscoped chore-lead sweep dispatch, issue-sorter seat run in parallel with decision-watcher and repo-cleaner; baseline checkpoint 2026-08-10T06:23:59Z from the prior firing, already committed -- treated as the checkpoint, not re-derived). `gh issue list --search \"updated:>=2026-08-10T06:23:59Z\" --state all` returned 10 issues: #167 (open, already fully labeled task/size:small -- minted via capture-skill path directly, dedup-before-mint matches it to itself), #161/#160 (closed record-only confirmation tickets, already fully labeled task/size:small, no action), #157 (open, filed directly on GitHub by kimgranlund with ONLY a bare `bug` label -- no severity label; classified defect per Bug/Task/Feature axis, applied severity `major` this firing -- systematic, ~100% reproduction across 3+ sweeps, real recurring tax, but a workaround exists (root session catches and redirects), so major not blocker), #156 (open, filed directly on GitHub by kimgranlund with NO labels at all -- classified defect (a labeling/collision bug the filing author's own body already frames as 'not a security bug, a labeling/documentation gap' with a suggested doc-only fix), applied `bug` + severity `minor` this firing), #152/#150/#149/#139 (closed, updated only via closing since the last checkpoint, already fully labeled task/size:small from a prior firing, nothing outstanding), #151 (open, already fully labeled task/size:small, updated via a linked-PR comment only, nothing outstanding). For both #156 and #157, attempted the native-type second call (`gh issue edit <id> --type Bug`) per this agent's own create/resume contract -- both failed identically (`type \"Bug\" not found; available types:` -- this org's repo has no Issue Types schema configured at all), so both are left with the label alone; noted here as a skipped type, not re-run as a create (nothing was re-minted -- #156/#157 already existed, only labels changed). `gh pr list --search` (same window) returned 10 PRs, #158 through #170, ALL merged, ALL authored by kimgranlund -- code changes by the trusted author, not filed work items this seat tickets (same handling as every prior firing's merged-PR set). Every discovered item authored by kimgranlund (trusted, on friendlies.json) -- zero unknown-author items, nothing newly held; `needs-triage-approval` label search repo-wide returned 0 both before and after this firing -- no outstanding held items anywhere, none created. `gh issue list --state open` -> 4 open issues (#167, #157, #156, #151), every one now fully labeled (kind + size, or kind + severity) after this firing's two label additions -- nothing left unclassified. friendlies.json and held-items.md UNCHANGED this firing -- no new author, no new hold, so neither is reproduced as a payload block (only genuinely touched state ships as payload per this agent's own contract). Step 8 (REQ-013 MCP offer) gate not met -- `github_mcp_offer` already recorded as 'accepted' (overridden 2026-07-25) in friendlies.json's policy block -- skipped silently per the failure-branch contract, not-applicable-this-firing, not re-evaluated as if unresolved. No Linear MCP connector configured -- out of scope today, not an unreachable failure. gh CLI reachable throughout (gh auth status: logged in as kimgranlund, repo/workflow/read:org/gist scopes) -- source measured, not UNMEASURED.",
      "issues_checkpoint": "2026-08-10T23:50:32Z",
      "prs_checkpoint": "2026-08-10T23:50:32Z"
    }
  }
}
