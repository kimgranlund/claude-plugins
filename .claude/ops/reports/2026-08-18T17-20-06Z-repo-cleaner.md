# repo-cleaner sweep — 2026-08-18T17:20:06Z

Classification set changed materially from the prior firing (`2026-08-18T02-32-43Z-repo-cleaner.md`) — full report, not abbreviated. Consecutive-unchanged-firings counter: **0**.

**Executed (gated):**
- `git fetch --prune` — pruned 8 stale remote-tracking refs (611, 612, 613, 619, 622, 629, 634, docs/six-intent-drafts-622-627).
- `primary_checkout_check.py /Users/kimba/Projects/nonoun/plugins` → clean, on `main`.
- `gitignore_check.py` — primary: 2 standing WARNs (`dist/`, `harness-audit-*/`). This worktree: 4 standing WARNs (adds `.DS_Store`, `.claude/worktrees/`). No new findings.
- `campaign_close.py` — not invoked: `git ls-remote --heads origin` shows only `main` and `fix-647-attention-audit-dmi-drift` remain; every merged PR's remote branch is already gone.
- `sync_main.py` — not invoked: could not be, from this worktree-isolated session (sandbox refuses git ops targeting the primary checkout). Proposed instead.

**Healthy / active — no action:**
- `.claude/worktrees/fix-647` (branch `fix-647-attention-audit-dmi-drift`) — clean, in sync with remote, backs open PR #648 (closes #647). Live work.

**Proposed only — orphaned worktrees (merged, remote gone, no host reap script exists in this repo):**
- `.claude/worktrees/629-self-improvement-retrospective` (branch `629-self-improvement-retrospective`) — PR #645 MERGED 2026-08-18T14:51:15Z. Recommend `git worktree remove` + `git branch -d`.
- `.claude/worktrees/build-554` (branch `634-req-infix-prefix`) — PR #646 MERGED 2026-08-18T15:37:45Z. Recommend `git worktree remove` + `git branch -d`.

**Proposed only — orphaned local branches (merged, no worktree, remote gone):**
`608-dispatch-ticket-file-bug-claim-fix` (PR #610), `611-backlog-roadmap-releases-loop` (PR #621), `612-harvest-domain-knowledge` (PR #614), `613-harvest-project-context` (PR #615), `622-feedback-intake-door` (PR #641), `637-drain-queue-command` (PR #643) — recommend batch `git branch -d` for each.

**Proposed only — primary behind, dirty-state unmeasured:**
Primary `main` is `[origin/main: behind 30]` (normal drift, not itself a defect). Dirty-tree status could not be read from this worktree-isolated session. Recommend a primary-rooted session run `sync_main.py`.

**Stale-claim check (ADR-0005) — clean:** 5 open issues (#647, #617-fixture, #609, #605, #490), none carry an assignee. No claim, so no stale-claim finding.

**Closed-not-merged PRs (#437, #391) — no action:** branches already absent from remote, no gated close path exists for a non-merged PR.

**Note:** this session's initial gitStatus context (branch `fix-423-fleet-bootstrap-phase1`, dirty ledger files) did not match this worktree's actual state (`fix-647-attention-audit-dmi-drift`, clean) — flagged as stale context, not acted on.
