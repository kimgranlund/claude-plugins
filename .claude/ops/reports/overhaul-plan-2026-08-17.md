# Overhaul plan — #373 deep pass (docs, harness, teamwork) — 2026-08-17

Phase-2 plan generated by authorkit:overhaul-planning against the Phase-1 baselines in
`.claude/overhaul-run-2026-08-17.md` (measured 2026-08-17 on worktree @650a4e6; figures used
verbatim, not re-measured). Plan-only: nothing below is executed or minted; Gate A approval of
the seed list happens on the orchestrator's side.

## Steps

### Phase 0 — Measurements (from the run ledger, verbatim)

| Instrument | Available? | Finding summary |
|---|---|---|
| attention-audit (rent.py/collide.py) | yes | docs 8,846 routable / 2,366 agent (flat); harness 19,121 / 5,870 (flat); teamwork 7,396 routable (+4,113) / 8,998 agent (+3,472) — un-dieted #433 growth. Worst: product-leader.md 1,707 (headroom −966), mobilize-chores 1,136, leading-planning 1,109, build-leader 1,064, planning-leader 1,054, docs:check-stage 1,047, clean-repo 1,040, team-scaffolding 1,039, repo-cleaner 1,035, fleet-bootstrap 1,019. Collisions: 4-way leading-* cluster 172–247 (all fence_tight/over budget); watch-adrs↔watch-tickets 185; authorkit:repo-audit↔docs:check-stage 54.5 (coincidence-leaning) |
| bloat-audit (DEEP — all 81 flagged files individually read) | yes | Verdict: calibration miss, not busy-work. Judged-recoverable ≈9,000–11,000 chars = 13% of measure.py's 72,528 raw estimate. Real findings: docs intake shared-template Failure-branches re-narration ~6,000+; harness 8 agent NOT-for descriptions ~1,200–2,400; docs backend-seam ×3 ~460; teamwork SendMessage/no-nested-wait ×4 ~700–1,000. Species misfits: none. Emergent: measure.py phase-heavy/done-section regex false positives |
| naming-audit (validate.py --scope grammar) | yes | 0 grammar / 0 structural errors in all three plugins. Exemptions in scope: docs 17, harness 41, teamwork 10 (68 of 120). Stale: 2 (teamwork build-lead, team-lead — files renamed *-leader by #433, entries not retired). Systemic: 19 bare-name agent + 43 lexicon-gap skill exemptions share two root causes (RoleLex too narrow for the seat pattern; ProcessLex/ObjectVocab under-populated) |
| doctrine-audit (sweep.py) | yes | 2 new findings vs 08-16, root cause #433's *-lead→*-leader rename: D02 verbatim-line MAJOR (build-lead.md dead path → build-leader.md unchecked), D05 vocab-term minor (team-lead.md → team-leader.md); D04/D08 stale-path metadata not firing; D08 model-tier fact needs re-verify against team-leader.md. D01/03/06/07 clean |
| pattern-audit | n/a | absent — no pattern named in #373's charter |
| check-routing / surface_map | deferred | not re-run at plan time; runs per-wave as Validation (every Wave-2 seed edits descriptions, so it fires there) |

### Phase 1 — Per-member kill-switch

All members stay where they live (plan-plugin-split anti-matrix: no absence with job
evidence, no two-members-one-procedure surplus found in Phase 1's reads) and keep their
species (naming-audit: 0 grammar/structural errors; bloat DEEP pass: species misfits none).
Columns below therefore carry the varying answers: blast radius, merge/split, knowledge tier,
verdict.

| Member (cluster) | Where it lives | Species | Blast radius | Merge/Split candidate? | Knowledge tier | Verdict |
|---|---|---|---|---|---|---|
| `.claude/doctrine.manifest.json` D02/D05 paths + D04/D08 metadata | estate root, correct | config ledger | manifest only; sweep.py re-run; no invocation strings | NO — n/a | n/a (ledger) | MOVE (Wave 1: one-line path fixes + D08 fact re-verify) — evidence: doctrine sweep D02 major/D05 minor @650a4e6 |
| teamwork exemptions `build-lead`, `team-lead` | naming.manifest.json, correct | exemption entries | manifest only; ratchet 120→118; validate.py re-run | NO — n/a | n/a | MOVE (Wave 1: authorkit:exemption-retire) — evidence: naming-audit "stale: 2", files already renamed by #433 |
| teamwork leading-* 4-way description cluster (incl. leading-planning 1,109, build-leader 1,064, planning-leader 1,054, team-scaffolding 1,039) | teamwork, correct | skills/agents | descriptions = routing surface → evals.json same-change, reciprocal fences, /check-routing after | NO merge — the cluster shares boilerplate, not audience; centralize-boilerplate extraction, not a roll-up | PROCEDURE — keep-inline; overflow is description boilerplate, not body | MOVE (Wave 2: ONE centralize-boilerplate extraction fixing 4+ collision pairs, scores 172–247; all four over the 700-char budget) |
| docs `product-leader.md` description | docs, correct | agent | description edit → agent routing; evals + /check-routing | NO — reason: no sibling shares its audience | PROCEDURE — keep-inline | MOVE (Wave 2 diet) — evidence: 1,707 chars, headroom −966, worst rent-payer in scope |
| teamwork SendMessage/no-nested-wait paragraph ×4 agent twins | teamwork, correct | agents | agent bodies only (prompt-carrying → critic pass); no description/eval change | NO — duplication is a paragraph, not a member | KNOWLEDGE overflow — move-to-references (shared reference) — evidence: bloat duplicate ×4 verbatim, ~700–1,000 chars | MOVE (Wave 2) |
| docs file-bug / file-feature / file-task Failure-branches | docs, correct | skills (command-invoked) | shared intake template + 3 sibling bodies; critic pass; descriptions untouched → no eval churn | NO — siblings are deliberate kin of one template; fix the TEMPLATE | KNOWLEDGE overflow — move-to-references (template-level de-narration) — evidence: ~6,000+ chars re-narrated, the largest single recoverable item | MOVE (Wave 2: template first, then the three siblings inherit) |
| docs backend-seam paragraph ×3 | docs, correct | skills | 3 bodies → pointer to one home; critic pass | NO | KNOWLEDGE — move-to-references — evidence: ~460 chars verbatim ×3 | MOVE (Wave 2, rides with the template seed) |
| harness 8 agent descriptions' NOT-for content | harness, correct | agents | 8 descriptions + evals.json same-change + /check-routing; agent-writing-rules governs the split | NO | PROCEDURE — keep-inline (content moves description→body, stays in-member) | MOVE (Wave 2) — evidence: ~1,200–2,400 chars movable; overlaps attention's rent finding |
| harness watch-adrs ↔ watch-tickets | harness, correct | skills | 2 descriptions + reciprocal fences in both eval suites | NO merge — collision fits a reciprocal fence, audiences distinct | PROCEDURE — keep-inline | MOVE (Wave 2, folded into the harness description seed) — evidence: collision 185 |
| harness check-all-agents / check-all-skills mirror | harness, correct | skills | n/a | **NO — KILLED**: bloat DEEP read judged the mirror deliberate; "merge-test later" is a future nomination, evidence today doesn't clear plan-skill-merge's bar | PROCEDURE — keep-inline | NO MOVE |
| authorkit:repo-audit ↔ docs:check-stage collision (54.5) | — | — | — | **NO — KILLED**: collision coincidence-leaning per attention baseline; authorkit is out of #373 scope; each is independently over budget → check-stage's 1,047 may ride a future docs diet, not this campaign | — | NO MOVE |
| the remaining ~70 long-body flagged files (harness 45, teamwork 17, docs 13 minus the clusters above) | in place | mixed | — | NO | KNOWLEDGE/PROCEDURE — keep-inline | **NO MOVE — KILLED by evidence**: the DEEP per-file read (all 81 read) returned "calibration miss, not busy-work"; only ≈13% of the script's 72,528-char estimate is real. Dieting these would cut load-bearing prose |
| systemic exemption root causes (19 bare-name agent + 43 lexicon-gap) | manifest lexicons | design question | naming.manifest.json lexicon sections; every future validate.py run | NO — not a member move | n/a | NO MOVE as a wave — design-ticket grade (Wave 3 seed): lexicon amendment beats 62 case-by-case retires |
| authorkit measure.py regex false positives | authorkit scripts (out of scope for moves) | script defect | measure.py + its selftest fixture | NO | n/a | NO MOVE — emergent measurer defect, Wave 3 seed (incident → infrastructure) |

### Phase 2 — Waved ticket seeds (not yet minted)

Version slots: docs, harness, teamwork all free; each plugin bumps once per wave it lands in,
logged in its README ledger. Every Wave-2 seed is semantic: fresh-context critic pass +
evals.json same-change (where descriptions move) + `/check-routing` on the touched plugin.

#### Wave 0 — merge/split nominations
None. Both candidates (check-all-* mirror; repo-audit↔check-stage) killed at Phase 1 with
evidence cited above.

#### Wave 1 — mechanically clean (risk: LOW)
- [ ] **S1 doctrine-manifest-repair** — rows: `doctrine.manifest.json` D02 path
  `build-lead.md`→`build-leader.md`, D05 `team-lead.md`→`team-leader.md`, D04/D08 stale-path
  metadata refresh; re-verify D08's model-tier fact against `team-leader.md` (content check,
  not just path). Blocked-by: none — status: todo — done-when: `sweep.py --json` clean, D08
  fact confirmed or filed as its own finding.
- [ ] **S2 teamwork-exemption-retire** — rows: retire `build-lead` + `team-lead` entries via
  `authorkit:exemption-retire`; ratchet 120→118. Blocked-by: none (serial on
  `naming.manifest.json` with S8 — S8 is Wave 3, so no live conflict) — status: todo —
  done-when: `validate.py --scope grammar` green, exemption count 118.

#### Wave 2 — semantic (risk: MEDIUM — critic + evals + /check-routing per seed)
- [ ] **S3 teamwork-leading-boilerplate-centralize** — one extraction dieting the 4-way
  leading-* description cluster (fixes 4+ collision pairs, scores 172–247; all four over the
  700-char budget; ledger figures 1,109/1,064/1,054/1,039). Blocked-by: none — status: todo —
  done-when: all four ≤700 chars, collision scores below fence_tight, `/check-routing teamwork`
  clean, teamwork version bumped.
- [ ] **S4 teamwork-agent-twin-dedup** — extract the SendMessage/no-nested-wait paragraph
  (×4 verbatim, ~700–1,000 chars) to one shared reference; agent bodies point at it.
  Blocked-by: S3 (same plugin's agent files and version slot; keep the teamwork wave serial) —
  status: todo — done-when: single canonical copy, critic pass clean.
- [ ] **S5 docs-intake-template-diet** — fix the shared intake template's Failure-branches
  re-narration (~6,000+ chars) so file-bug/file-feature/file-task inherit the cut; fold in the
  backend-seam ×3 → pointer (~460). Knowledge tier: move-to-references. Blocked-by: none —
  status: todo — done-when: template + 3 siblings carry one canonical branch narration, critic
  pass clean, `/check-routing docs` clean (descriptions untouched, run as regression proof).
- [ ] **S6 docs-product-leader-diet** — product-leader.md description 1,707 → ≤700 target
  (headroom −966), per agent-writing-rules' description/body split. Blocked-by: S5 (same docs
  version slot; serial within the plugin) — status: todo — done-when: ≤ budget, evals updated
  same-change, `/check-routing docs` clean.
- [ ] **S7 harness-agent-description-diet** — move the 8 agent descriptions' NOT-for content
  to bodies per agent-writing-rules (~1,200–2,400 chars); fold in the watch-adrs↔watch-tickets
  reciprocal fence (collision 185). Blocked-by: none — status: todo — done-when: evals updated
  same-change, `/check-routing harness` clean, harness version bumped.

#### Wave 3 — design-grade (risk: DESIGN — needs a ruling, not a diff)
- [ ] **S8 lexicon-amendment-proposal** — one proposal via `authorkit:manifest-authoring`
  covering the two root causes behind 19 bare-name agent + 43 lexicon-gap exemptions (widen
  RoleLex for the seat pattern; populate ProcessLex/ObjectVocab), instead of 62 case-by-case
  retires. Blocked-by: S2 (shared `naming.manifest.json` ledger — never parallel) — status:
  todo — done-when: proposal doc exists and Kim rules on it; approved terms then ratchet
  exemptions in a follow-up.
- [ ] **S9 measure-py-false-positives** — fix bloat-audit `measure.py`'s phase-heavy /
  done-section regex false positives (the 87%-overestimate driver), with a selftest fixture
  encoding the miss (incident → infrastructure, same day the fix ships). Out of #373's
  three-plugin move scope; seed routes to authorkit via `docs:file-task`. Blocked-by: none —
  status: todo — done-when: selftest green with the new negative fixture, authorkit bumped.

#### Grandfathered (ADR-0011 D8 ratchet)
- The 66 non-stale in-scope exemptions (docs 17, harness 41−2 systemic overlaps vary,
  teamwork 10 minus S2's two) — retained; S8's lexicon amendment is the shrink path, no forced
  renames.

### Phase 3 — Execution contract (per ticket, once approved)

claim → worktree → change → supersession note / `renames.json` entry where applicable → gates
+ critics → PR → human merge → verified close (`campaign_close.py`). Serial through shared
ledgers (`naming.manifest.json`, `doctrine.manifest.json`) — two tickets never touch the same
ledger entry in parallel.

## Validation

Per wave, once its tickets land:
- [ ] `/check-routing` clean for every touched plugin (teamwork after S3/S4, docs after S5/S6, harness after S7)
- [ ] `release_gate.py` green per bumped plugin
- [ ] doctrine `sweep.py` clean after S1; `validate.py` exemption count 118 after S2
- [ ] Records amended with dated supersession notes (never rewritten); attention trend row appended at campaign close, not mid-build

## Rollback

- A landed edit (Wave 1/2): revert the PR's merge commit; history preserved.
- A ledger entry retired (S2): re-add verbatim from pre-change git history — a rollback
  restoring a wrongly-retired entry is a correction, not a forced re-grant.
- A supersession note: append a new dated note reversing it, never edit/delete.
- A minted seed abandoned mid-build: ADR-0005 claim release; flip the seed back to `todo`.

## The five respect invariants

1. Evidence can veto the plan.
2. History preserved, never replaced.
3. Consumers degrade gracefully.
4. The old design's intent is read before it is judged.
5. Nothing semantic rides hidden in a move.

## Next step

Generated only — nothing executed, no Issues minted. Gate A: the orchestrator presents this
seed list for approval; approved rows are minted through `docs:file-feature`/`docs:file-task`,
then built per wave. On completion flip to `status: complete` and archive per PLAN's
living-state contract.
