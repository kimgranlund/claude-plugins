# ops-repo hygiene sweep — 2026-07-21T04:45:15Z (interactive, on-demand firing)

Per `forge 1.14.0/agents/ops-repo.md`, citing `git-campaign-workflows` (worktree/merge/silent-
failure doctrine) and `github-issue-pr-primitives` (PR-lifecycle facts) for grounding, never
restating them here. `gh auth status`: logged in as `kimgranlund`, scopes `repo`/`workflow`
present — every source (worktrees, branches, PRs, issues) was reachable; nothing UNMEASURED this
firing.

The most recent file in `.claude/ops/reports/` by filename is `2026-07-18T17-33-02Z.md` — an
**`ops-issues`** intake-sweep report (issue/PR triage-and-routing), not a prior `ops-repo`
hygiene report. Its findings are a different shape entirely (checkpoint/friendlies/held-items
state, not worktree/branch/PR/stale-claim classification), so this firing cannot be "unchanged"
relative to it by construction — this is a full report, not an abbreviated one. (The last
actual `ops-repo`-shaped report is `2026-07-18T13-19-41Z.md`, for context only; several PRs have
since merged, so the finding set has changed regardless.)

## Inventory (Step 1)

- **Worktrees:** one — `/Users/kimba/Projects/nonoun/plugins` itself, checked out to
  `ops-family-seats`. No dangling/orphaned worktrees.
- **Local branches (3):** `main`, `ops-family-seats` (current), `worktree-session-close-skill`.
- **PRs:** `gh pr list --state all` — 0 open, 0 closed-not-merged, all historical PRs merged.
  Most recent merge: PR #59 (2026-07-20).
- **Open issues:** 1 — #58 (`scribe routing: 2 blind-judge failures...`), `assignees: []`, label
  `task`. ADR-0005's `claim` shape requires an assignee set; #58 carries none, so it is not a
  claim at all — no stale-claim check applies to it.
- **Live remote branches** (`git ls-remote --heads origin`, ground truth over local
  remote-tracking refs): before execution, `main` **and** `worktree-issue-44-adr-0004-dual-write`.

## Classification (Step 2)

| Finding | Classification | Evidence |
|---|---|---|
| PR #53 (`worktree-issue-44-adr-0004-dual-write`) remote branch | **merged-and-verified** | `gh pr view 53` independently read `state: MERGED`; the remote branch was still live on origin despite the merge — the exact stale-branch shape `campaign_close.py` exists to catch |
| Local branch `worktree-session-close-skill` | **orphaned** | tracks `origin/worktree-session-close-skill: gone` — its PR (#56) merged 2026-07-19 and its remote branch is already deleted (a prior campaign_close run); the *local* branch is the only thing left, and no script gates local-branch deletion |
| Local `main` — 1 unpushed commit (`d2ac486`, "orchestration-coordinator agent: effort high→xhigh") ahead of `origin/main` (`7676340`) | **orphaned artifact, ambiguous class** | no PR covers it, no worktree carries `main` as dirty (nothing to quarantine, nothing to `--ff-only` pull — `sync_main.py`'s gate doesn't fire on an *ahead* main); a raw `git push` isn't one of the two scripts this seat can invoke, so propose-only regardless |
| `ops-family-seats` (current branch, dirty: 3 modified agent files + 2 new agent files + 2 new skill dirs) | **healthy** | this is the live session's own in-progress campaign work (visibly mid-flight: new `ops-orchestrator`/`ops-planner` agent+skill scaffolding). It is not `main`, so `sync_main.py`'s gate is out of scope regardless of interactive-vs-scheduled — no action, not even a proposal |
| Open PRs | none found | 0 open, so no stale-open/orphaned-PR findings this firing |
| Issue #58 | **healthy** | open, unassigned — not an ADR-0005 claim, no staleness check applies |
| `.gitignore` | **healthy** | `gitignore_check.py` — clean, no stale rules, no uncovered generated dirs |

## Executed (Step 3 — the two narrow gates only)

1. **`campaign_close.py 53 --repo kimgranlund/claude-plugins`** — C1 verified PR #53 `MERGED`
   independently via `gh pr view`; C2 deleted the remote branch
   `worktree-issue-44-adr-0004-dual-write` and reverified it gone. Script output: `clean`.
   Independently re-verified after the fact with a fresh `git ls-remote --heads origin` (never
   trusting the script's own print alone, per the pack's core invariant) — only `refs/heads/main`
   remains on the remote. Confirmed.
2. **`sync_main.py`** — **not run.** No dirty `main` exists to quarantine: `main` isn't the
   checked-out branch anywhere in this repo right now, so there is no working tree in the `main`
   state for the script to act on. The one dirty branch found (`ops-family-seats`) is out of this
   script's scope by definition (it only ever targets `main`), independent of this being an
   interactive dispatch.

## Proposed only — no mutation (Step 4)

- **Delete local branch `worktree-session-close-skill`.** Safe: its PR (#56) merged 2026-07-19,
  the remote branch is already gone, and the local branch has no unmerged commits relative to
  `main`. Recommended command (for a human to run, not this agent): `git branch -d
  worktree-session-close-skill`. No script gates local-branch deletion today — this stays a plan.
- **Push local `main`** (`git push origin main`) to land the unpushed `d2ac486` commit
  ("orchestration-coordinator agent: effort high→xhigh, orchestration 0.7.8") that's currently
  ahead of `origin/main`. No PR exists for it and no script gates a raw push; propose-only.
- **No stale-claim findings.** The single open issue (#58) has no assignee, so ADR-0005's claim
  check doesn't apply — nothing to propose here this firing.
- **No stale-open or orphaned PRs, no dangling worktrees.** Both queried and clean.

## Boundaries respected

No local branch was deleted, no PR was merged/closed, no ticket claim was reclaimed or commented
on, no `.gitignore` rule was hand-edited, no push was made to `main`. The only mutation this
firing performed was the reverified remote-branch delete inside `campaign_close.py`'s own gated
scope for PR #53.

## Summary

1 merged-and-verified PR's stale remote branch found and closed via `campaign_close.py 53`
(independently reverified gone). 2 items proposed only: delete local branch
`worktree-session-close-skill` (orphaned, its campaign already closed), push local `main` (1
unpushed commit, no PR, no gate). 0 open PRs, 0 dangling worktrees, 0 stale-claims (the one open
issue is unassigned). `.gitignore` clean. `sync_main.py` not run — no dirty `main` exists this
firing.
