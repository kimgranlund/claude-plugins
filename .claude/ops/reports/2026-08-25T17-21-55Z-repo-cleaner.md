## repo-cleaner sweep — 2026-08-25T17:21:55Z

**Survey status:** MEASURED. `git fetch --prune` succeeded (pruned 1 stale ref: `origin/927-dispatch-envelope-refspec`, matching merged PR #928). `primary_checkout_check.py` → PASS (`main`, primary root, clean).

**Classification set changed from the prior firing** (`.claude/ops/reports/2026-08-24T03-52-33Z-repo-cleaner.md`) — full report, not abbreviated. Consecutive-unchanged-firings counter: **0**.

**What changed since prior firing:**
- New orphaned local branch: `927-dispatch-envelope-refspec` (merged via PR #928, remote already deleted; prior firing's `git branch -vv` showed only `main`).
- `main` now behind `origin/main` by 1 commit (was fully up to date last firing).
- `gitignore_check.py` went from 4-WARN to **3-WARN** — `__pycache__/` no longer flags (now matches something real in the tree).
- Open-issue set changed: #914/#913 closed since last firing; #932 new. #617/#609 persist. All zero assignees.

**Worktree/branch surface:** `git worktree list` shows only the primary checkout. `git branch -vv`: `main` (behind origin/main by 1, no local dirt on tracked files) and `927-dispatch-envelope-refspec` (orphaned — merged, remote gone). `git branch -r` / `git stash list` otherwise clean.

**Open PRs:** none (`gh pr list --state open` empty).

**Executed:** nothing.
- No merged-and-verified PR has a remaining remote branch to close (PR #928's remote branch was already gone before this firing).
- `sync_main.py` NOT run. `main` is genuinely behind by 1 (real ff-pull benefit, unlike prior firing), but the only dirty item `git status --porcelain` reports is the untracked `.claude/ops/sweep-in-flight.json` (`startedAt 2026-08-25T17:19:52Z`, `pid 73478`, `session plugins-28`) — no matching process via `ps -p 73478`, but the ~2-minute recency and `plugins-28` session-name convention match this firing's own dispatching session (same pattern the 2026-08-24 firing classified as live, not cruft). Running `sync_main.py` would stash-quarantine that file before pulling, pulling it out from under the live coordinator mid-sweep for a 1-commit ff-pull. Treated as ambiguous, not ordinary local dirt — propose only.

**Propose-only:**
- Orphaned local branch `927-dispatch-envelope-refspec` (merged via PR #928, remote already deleted). No host-repo reap script exists (`CLAUDE.md`/`README` name none) — recommend `git branch -d 927-dispatch-envelope-refspec` by hand.
- `main` behind origin by 1 — recommend plain `git pull --ff-only` once `.claude/ops/sweep-in-flight.json` (session plugins-28) is confirmed no longer live, rather than routing through `sync_main.py`'s stash step while it's active.
- Gitignore repair (`gitignore_check.py`, 3 WARN, all G1 stale-rule-matches-nothing): `dist/`, `harness-audit-*/`, `.name-map.md` — recommend reviewing whether each still applies; report only, never hand-edited (`.claude/rules/gitignore-repair.md`).
- Stale-claim: clean. 3 open issues (#932, #617, #609) — all zero assignees under this repo's own ADR-0005 ticket-claim protocol (`doing` label on #609 not treated as a claim absent a ruling naming it, same precedent as prior firing). No reclaim proposals.
- Off-main-primary: none — `primary_checkout_check.py` reads PASS.

**Healthy, no action:** open-PR surface, remote branch surface (already fully reaped by the fetch's own prune).

Proposed only, nothing else mutated — the standing exception (`git fetch --prune`) is the only ref state touched this firing.
