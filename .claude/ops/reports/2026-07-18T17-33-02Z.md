# ops-issues intake sweep — 2026-07-18T17:33:02Z (interactive, on-demand firing)

First-ever `ops-issues` firing for `kimgranlund/claude-plugins`. No prior state existed
(`.claude/ops/` held only `reports/` from the unrelated `ops-repo` seat) -- all three of this
seat's own state files (`watch-checkpoint.json`, `friendlies.json`, `held-items.md`) were
bootstrapped this firing, per `spec-ticketing-watch-triage.md` and `spec-linear-adapter.md`
(both read fresh at dispatch time, not restated from a prior summary). Backend resolved:
git-native (ADR-0002/ADR-0003) -- discovery is `gh`-only; no Linear adapter exists to poll.

Dispatched interactively (a maintainer-requested immediate check, not the scheduled cloud
routine) -- so REQ-006's clarifying-round allowance (step 6 of the agent's own procedure) was
available had ambiguity required it. It did not: every discovered item's shape was unambiguous
from its existing kind label plus body sections.

## Discovery (Step 1)

No checkpoint existed, so this firing treated the window as "everything discoverable now":
`gh issue list --state all` and `gh pr list --state all` against `kimgranlund/claude-plugins`,
both reachable (`gh auth status` verified logged in, scopes `repo`/`workflow` present).

- **Issues:** 16 total (all-time) -- **2 open** (#33, #34), 14 closed.
- **PRs:** 20 total (all-time) -- **0 open**, 20 merged.

Full closed history was pulled for classification/dedup *context* only (confirming no open item
duplicates a closed one, and grounding the trust-check evidence below) -- it was not re-routed;
closed items are settled, not live triage work. The two open issues are this firing's actual
discovered-and-actionable set.

Both `github` sub-checks (issues, PRs) succeeded -- no source was UNMEASURED this firing.

## Classification (Step 2) and dedup (Step 3)

| # | Title | Shape | Existing kind label | Payload contract present? |
|---|---|---|---|---|
| #34 | Build the concrete Linear adapter per spec-linear-adapter.md | feature idea | `feature`, `size:big` | Yes -- Summary/Acceptance/Links/Scope-Open/Findings, matching `feature`'s payload contract exactly |
| #33 | Verify ops-issues' hourly cloud routine actually authenticates against GitHub | generic task | `task` | Yes -- Summary/Acceptance/Links/Scope-Open/Findings, matching `issue`'s payload contract |

Both items are already **complete, correctly-shaped ticket records** -- both were filed by the
maintainer through the git-native backend's own capture path (evidenced by the exact section
shape and kind labels this workspace's own scribe trio mints), not blank external filings. Dedup
sweep against the full 16-issue history found no other open or closed record covering either --
each is its own singular record. Per REQ-010/the Failure-branches dedup rule ("an already-open
match resumes it, never mints a duplicate"), and because in this workspace's git-native backend
the Issue *is* the ticket record (ADR-0002), there was nothing further to mint for either: the
record these would route into already exists, in place, as itself.

No PR required routing (0 open) and no PR-vs-issue linkage gaps were found (every merged PR in
history that closed an issue did so via a proper `Closes #N` reference, already reflected in the
issue's own closed state).

## Trust check (Step 4) and routing (Steps 5-6)

`friendlies.json` was bootstrapped empty-then-seeded with exactly one entry: **`kimgranlund`**,
the repo owner (`gh repo view` owner.login), the ADR-0002/ADR-0003 ratifying maintainer (both
ADRs record "ratified: maintainer, in-session AskUserQuestion"), and the author of literally
**all 36** historical issues+PRs (16 + 20, zero exceptions) -- the strongest evidence available
for a bootstrap seed, per the agent's own judgment call the dispatch authorized.

Both #33 and #34 are authored by `kimgranlund` -> **trusted**. Routed per REQ-006/Step 4: already
resident as complete records, confirmed in place, no mint, no hold, no human approval gate
touched.

**Held: zero items.** `held-items.md` was bootstrapped with the ledger header/template and no
entries -- nothing in this firing's discovery set had an unrecognized author. Location:
`.claude/ops/held-items.md`.

## Boundaries respected

No source file was edited, no PR was merged, no issue/PR was closed, no label beyond
`needs-triage-approval` was ever a candidate (never applied -- nothing held), no held item was
approved or denied by this agent. No commit/push was made -- per this dispatch's instruction, the
three state-file changes below are left for the maintainer to commit from the worktree.

## Final state of the three checkpoint/allow-list/held files

- **`.claude/ops/watch-checkpoint.json`** -- new. One source, `github` (git-native/gh, no
  Linear adapter yet), `issues_checkpoint` and `prs_checkpoint` both set to this firing's
  timestamp (`2026-07-18T17:33:02Z`) as the discovery high-water mark for the next firing.
- **`.claude/ops/friendlies.json`** -- new. One entry: `kimgranlund` (repo owner/maintainer
  bootstrap seed).
- **`.claude/ops/held-items.md`** -- new. Empty ledger (header + entry template only).

## Summary

2 open issues discovered (0 open PRs), both feature/task-shaped per existing labels, both
authored by the bootstrap-trusted maintainer, both already complete records requiring no mint --
routed as resident, not created. Zero items held. Zero sources unreachable. All three state
files bootstrapped for the first time this firing.
