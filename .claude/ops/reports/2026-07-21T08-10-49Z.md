# ops-issues intake sweep — 2026-07-21T08:10:49Z (scheduled, unattended cloud-routine firing)

Per `forge 1.14.0/agents/ops-issues.md`, citing `spec-ticketing-watch-triage.md` (REQ-001 through
REQ-013) and `spec-linear-adapter.md`, both read fresh at dispatch, not restated here. Backend
resolved: git-native (ADR-0002/ADR-0003) — discovery is `gh`-only; no Linear adapter/MCP connector
is configured, so that source was skipped entirely (out of scope today, not an unreachable
failure).

**Operational note:** this sandbox has no `gh` binary on PATH (`gh: command not found`). Discovery
used the session's connected read-only GitHub MCP server tools (`list_issues`, `list_pull_requests`)
instead — the same read-only discover surface REQ-001–003 specify, just a different transport. No
write ever went through that server; the only writes this firing made are the three state files
below, per the agent's own Write-tool scope. The MCP connection also dropped and reconnected mid-run
(read-only tool calls only; no in-flight mutation was affected).

## Discovery (Step 1)

Window: everything changed since the prior checkpoint (`2026-07-18T17:33:02Z`) through this firing.
This is the **first checkpoint advance since the original bootstrap firing** (`2026-07-18T17:33:02Z`,
PR #37) — the routine fired hourly in between but never actually completed a Write, per Issue #33
(fixed by PR #42's `.claude/settings.json` permission allowlist, merged 2026-07-18T20:40:28Z). This
firing is the first to run since that fix landed.

- **Issues changed since checkpoint:** 8 — 1 open (#58), 7 closed (#44, #51, #48, #39, #40, #33,
  #34; all closed before or during this window, settled, no live triage action).
- **PRs changed since checkpoint:** 19 — 1 open/draft (#61), 18 merged (#37, #38, #41, #42, #43,
  #45, #46, #47, #49, #50, #52, #53, #54, #55, #56, #57, #59, #60).

Both `github` sub-checks (issues, PRs) succeeded — no source was UNMEASURED this firing.

## Classification (Step 2) and dedup (Step 3)

| # | Kind | Shape | Existing label | Status |
|---|---|---|---|---|
| #58 | issue | generic task (routing-eval findings + suggested follow-up) | `task` | open |
| #44, #48, #39, #40, #33 | issue | task | `task` | closed |
| #51 | issue | task (verification probe, self-noted for immediate closure) | none | closed |
| #34 | issue | feature idea | `feature`, `size:big` | closed |
| #37–#61 (PRs) | pr | code changes closing/realizing the above | n/a | 18 merged, 1 open draft |

#58 is already a **complete, correctly-shaped ticket record** — filed directly by the maintainer
through the git-native backend's own capture path, already labeled `task`. Dedup sweep against the
full discovered set (open + the closed history pulled for context) found no other record covering
it. Per the Failure-branches dedup rule ("an already-open match resumes it, never mints a
duplicate"), and because in this workspace's git-native backend the Issue *is* the ticket record
(ADR-0002), there was nothing further to mint: the record already exists, in place, as itself —
resumed, not created.

The 7 closed issues require no action (settled, not live triage work). The 19 PRs are code changes,
not ticket records to mint — this seat mints Issues, not PRs; PRs are discovery context and
trust-check evidence only.

## Trust check (Step 4) and routing (Steps 5–6)

Every discovered item (all 8 issues, all 19 PRs) is authored by `kimgranlund`, already on
`friendlies.json`'s allow-list since the bootstrap firing. **Held: zero items.** `held-items.md` is
unchanged this firing — nothing in the discovered set had an unrecognized author.

Step 6 (interactive clarifying round on ambiguous shape): not reached — every item's shape was
unambiguous from its existing label plus body/section shape, and this firing is unattended regardless
(no `AskUserQuestion` available), per the dispatch's own instruction and the agent's own unattended
gate.

## Step 7 (REQ-011 bootstrap roster) and Step 8 (REQ-013 GitHub MCP offer)

Step 7: not applicable — `friendlies.json` already exists with a confirmed roster from the
2026-07-18 bootstrap firing; not re-run.

Step 8: **not-applicable-this-firing.** Resolved backend is Option B (git-native/GitHub) and
`github_mcp_offer` is not yet recorded in `friendlies.json`'s `policy` block, which would normally
gate this step open — but this is an **unattended** firing, and the agent's own procedure states
unattended firings never reach step 8 at all (same gate as step 7's unattended skip: the offer is a
question only a dispatching human session can ask). Distinct from the "surfaced-but-pending" state,
which applies only when an *interactive* firing surfaces the offer and no dispatch yet carries the
human's choice — that has not happened yet either way. A future interactive dispatch is the
correct place to surface this offer.

## Boundaries respected

No source file was edited, no PR was merged/closed, no issue/PR was closed, no `needs-triage-approval`
label was applied (nothing held), no held item was approved or denied by this agent, no `.mcp.json`
was written (step 8 not reached). The only writes this firing made are the three state files plus
this report.

## Final state of the three checkpoint/allow-list/held files

- **`.claude/ops/watch-checkpoint.json`** — updated. `issues_checkpoint` and `prs_checkpoint` both
  advanced from `2026-07-18T17:33:02Z` to this firing's timestamp (`2026-07-21T08:10:49Z`).
- **`.claude/ops/friendlies.json`** — unchanged (no new author evidenced, nothing approved).
- **`.claude/ops/held-items.md`** — unchanged (nothing held this firing).

## Summary

8 issues + 19 PRs discovered since the last checkpoint (2026-07-18T17:33:02Z) — all authored by the
already-trusted maintainer. Zero items held, zero minted (the one open issue, #58, was already a
complete resident record). Zero sources unreachable (Linear out of scope today; `github` reachable
via MCP tools since `gh` itself is absent from this sandbox). Checkpoint advanced for the first time
since bootstrap. Step 8's GitHub MCP offer not reached (unattended firing).
