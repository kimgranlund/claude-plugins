## repo-cleaner sweep — 2026-08-21T21:39:51Z

**Survey status:** MEASURED. `git fetch --prune` succeeded (pruned 1 stale ref: `origin/fix-850-fleet-bootstrap-phase5-wall`, already merged/deleted upstream). `primary_checkout_check.py` → PASS (`main`, primary root, clean vs. `origin/main`).

**Classification set changed materially** from the prior firing (`.claude/ops/reports/2026-08-21T17-21-08Z-repo-cleaner.md`) — full report, not abbreviated. Consecutive-unchanged-firings counter: **0**.

**Executed:** nothing.
- No merged-and-verified PR has a remaining remote branch to close — `campaign_close.py` has no target this firing.
- `sync_main.py` NOT run. Primary `main` carries real dirt (`.claude/docs/brief/brief-nonoun-plugins.md`, `.claude/ops/fleet-roster.md`, `.claude/ops/sweep-in-flight.json`, `teamwork/.claude-plugin/plugin.json`, `teamwork/README.md`, `teamwork/skills/fleet-bootstrap/SKILL.md`), and `sweep-in-flight.json` self-identifies a live session (`"session": "plugins-marshal"`, `pid 35586`, `startedAt 2026-08-21T21:37:30.381Z` — ~2 min before this firing). `ps -p 35586` found no matching process, but recency + an in-progress version bump on `teamwork` (`2.28.23` → `2.28.24` mid-edit, `fleet-roster.md` gaining a duplicate `plugins-marshal (takeover)` row) make this ambiguous-at-best, never confidently cruft — propose-only, not quarantined. This is an interactive-shaped dispatch, but the live-session evidence overrides — running `sync_main.py` here would stash a peer's in-flight work, exactly the class of mistake this gate exists to avoid.

**Propose-only — reap candidates** (no host reap script found in this repo's `CLAUDE.md`/`README`/`package.json`):

Clean, safe to reap (merged+verified via `gh`, remote gone, worktree clean, no uncommitted changes):
- worktree `build-554` / branch `634-req-infix-prefix` (#646)
- worktree `fix-647` / branch `fix-647-attention-audit-dmi-drift` (#648)
- worktree `fix-660` / branch `fix-660-artifact-check-false-positives` (#661)
- worktree `fix-794-marketplace-drift` / branch `fix-794-marketplace-drift` (#795)

Local branches with no worktree, all merged/remote-gone:
`608-dispatch-ticket-file-bug-claim-fix` (#610), `611-backlog-roadmap-releases-loop` (#621), `612-harvest-domain-knowledge` (#614), `613-harvest-project-context` (#615), `622-feedback-intake-door` (#641), `637-drain-queue-command` (#643), `650-artifact-styling-rules` (#651), `657-scope-audience-frontmatter` (#664), `670-unnamed-checker-dispatch` (#689), `fix-667-build-feature-gh541-doctrine` (#668), `fix-850-fleet-bootstrap-phase5-wall` (#851, NEW this firing), `post-series-cleanups` (#823, NEW this firing).

**Resolved since prior firing** (no longer needing action): `worktree-council-role-agents-840` (#843) and `worktree-brand-design-wave2-s1-s2-s3` (#848) — both worktree and local branch already removed between firings; no trace remains.

**NOT safe to reap — live uncommitted dirt (unchanged from prior firing):**
- worktree `629-self-improvement-retrospective` (merged #645) — untracked `.claude/docs/lld/lld-0018-estate-maintenance-retrospective.md`.
- worktree `fix-656-rdd-revalidation-rotation` (merged #663) — `revalidation_checkpoint.py` + `watch-adrs/SKILL.md` modified uncommitted.
- worktree `fix-667`, actually on branch `fix-683-container-grammar-role-aliases` — this branch's HEAD (`83fade5`) is fully contained in `origin/main` already (fully-merged content, via PR #697 under a different branch name) but there is no PR under *this* branch's own name — orphaned, plus `docs/skills/make-artifact/scripts/css_build.py` modified uncommitted.
- worktree `fix-684` (merged #699) — 2 modified + 3 untracked `overhaul-run-*.md` files, actively in use.

**Healthy, no action:** `gh pr list --state open` returned zero open PRs this firing.

**Standing, no gated path:** 2 stashes unchanged (`"On main: sync_main quarantine"` x2).

**Other findings:** `gitignore_check.py` — primary reads 3-WARN (`dist/`, `harness-audit-*/`, `.name-map.md` — all stale-rule matches-nothing warnings, same as prior firing; `.name-map.md`'s prior worktree instance is gone along with the council-role-agents-840 worktree, so this is now purely the primary's own carry). All 8 remaining worktrees show a uniform 4-WARN set (`.DS_Store`, `dist/`, `harness-audit-*/`, `.claude/worktrees/`) — no FAIL anywhere. Stale-claim: clean — `gh issue list --state open` returned 7 open issues (#863, #862, #861, #849, #617, #609, #490), all zero assignees.

**Proposed only, nothing else mutated.**
