# issue-sorter intake sweep -- 2026-07-22T06:09:56Z (scheduled, unattended cloud-routine firing)

Per `harness/agents/issue-sorter.md` (the current name of what the dispatching prompt still cites as
`forge 1.14.0/agents/ops-issues.md` -- renamed during the forge->harness campaign, ratified in
ADR-0007). Verified independently before proceeding: `forge 1.14.0/agents/ops-issues.md` does not
exist anywhere in this checkout; `harness/skills/naming-rules/references/estate-rename-map.md` maps
the agent `ops-issues` -> `issue-sorter`, and `harness/agents/issue-sorter.md` self-identifies as
this seat's contract (same `.claude/ops/` state paths, same REQ-001-013 scope, same procedure).
Treated as the same seat continuing under its current name, consistent with every prior firing's own
notes on this stale-path mismatch. Backend resolved: git-native (ADR-0002/ADR-0003) -- discovery is
`gh`-only per the dispatching prompt; no Linear adapter/MCP connector is configured, so that source
was skipped entirely, per the dispatching prompt's own explicit instruction to skip it (not an
unreachable-source failure).

**Operational note:** this sandbox has no `gh` binary on PATH (same as every prior firing). Discovery
used the session's connected read-only GitHub MCP server tools (`list_issues`, `list_pull_requests`,
`pull_request_read`) instead.

## Discovery (Step 1)

Window: everything changed since the prior checkpoint (`2026-07-22T05:08:09Z`) through this firing.

- **Issues changed since checkpoint:** 1 -- #79 (state flipped OPEN -> CLOSED, `updated_at`
  `2026-07-22T05:39:52Z`), via `list_issues` with `since: 2026-07-22T05:08:09Z` (`totalCount: 1`,
  no pagination needed).
- **PRs changed since checkpoint:** 1 -- #89 ("Estate description diet: 58 skills, W8 ratchet, ops
  command renames (closes #79)"), via `list_pull_requests` (state `all`, sorted `updated desc`)
  filtered in-script to entries newer than the checkpoint. The list endpoint's own `merged` field
  read `false` despite a populated `merged_at` (`2026-07-22T05:39:51Z`) -- cross-checked with
  `pull_request_read get`, which confirms `"merged": true`. Treated as merged.

Both `github` sub-checks (issues, PRs) succeeded -- no source was UNMEASURED this firing.

## Classification (Step 2) and dedup (Step 3)

- **Issue #79:** already discovered, classified (`task`, context-budget: skill listing
  truncation), and trust-checked as a no-hold item in the `2026-07-22T00:09:39Z` firing (see that
  firing's report table). This firing's appearance is a **dedup match** -- a state-change echo
  (closed via merge of PR #89), not a new filing. Resumed; no duplicate record created; no new
  triage action taken.
- **PR #89:** a code change (the estate description-diet campaign across harness 2.0.9 / docs
  1.0.6 / teamwork 1.0.4 / llm 1.0.7, closing #79), not a ticket-record item this seat mints --
  this agent intakes and routes filings onto the ticketing backend, it does not track ordinary
  merged PRs as records in their own right. Noted for completeness only.

## Trust check (Step 4) and routing (Steps 5-6)

Both #79 and #89 are authored by `kimgranlund`, already on `friendlies.json`'s allow-list
(bootstrap seed, repo owner/maintainer) -- trust check passes for both; neither required a hold.
Nothing new to mint (dedup on #79, non-ticket on #89), so `friendlies.json` and `held-items.md` are
both unchanged this firing and are not part of this firing's commit.

## Bootstrap gates (Steps 7-8)

`friendlies.json` already exists (bootstrapped 2026-07-18) -- step 7 does not apply. Resolved
backend is git-native/GitHub (Option B), but `friendlies.json`'s `policy` block still does not carry
a `github_mcp_offer` decision -- this is an UNATTENDED firing, so per the procedure's own gate,
step 8 is skipped, same as every prior firing. The offer stays surfaced-but-pending for the next
INTERACTIVE firing.

## Outcome

Nothing new found, nothing minted, nothing held. Only `watch-checkpoint.json` changed (both source
checkpoints advanced to `2026-07-22T06:09:56Z`). `friendlies.json` and `held-items.md` are unchanged
and were not part of this firing's commit.
