# repo-cleaner sweep — 2026-08-09T12-13-06Z

Interactive full hygiene sweep, `kimgranlund/claude-plugins` (private), checkout
`/Users/kimba/Projects/nonoun/plugins` on `main`. Dispatched with two candidate findings to verify
independently, plus a full inventory beyond those two.

## Preflight

- `gh auth status`: logged in, account `kimgranlund`, scopes include `repo`/`workflow` — not
  UNMEASURED.
- Repo slug confirmed: `gh repo view --json nameWithOwner,isPrivate` -> `kimgranlund/claude-plugins`,
  private: true.
- Prior report check (procedure step 5): most recent file in `.claude/ops/reports/` is
  `2026-08-08T14-32-04Z.md` (repo-cleaner sweep, previous day), which closed PRs #141/#142. This
  firing closes a different pair (#145/#143) — a genuinely new/changed finding set. Full report,
  running-unchanged-count resets to 0.

## Inventory

**Worktrees** (`git worktree list`): one — the primary checkout only
(`/Users/kimba/Projects/nonoun/plugins` on `main`). -> **healthy**.

**Local branches** (`git branch -vv`): one — `main`, tracking `origin/main`, `git status` clean
(nothing to commit), HEAD at `d250b31` (PR #145's own merge commit). -> **healthy**; no dirty
`main` this firing, so `sync_main.py` had nothing to quarantine (would have been eligible to run —
this is an interactive dispatch — but there was no dirt).

**Remote branches** (`git branch -r -vv`, pre-fetch): three —
`origin/decision-watcher-verify-adr-text` (PR #145), `origin/repo-cleaner-widen-chore-lead-verify`
(PR #143), `origin/main`. No branches beyond the two flagged existed. -> both classified
**merged-and-verified**, see Executed below.

**PRs** (`gh pr list --state all --limit 200`): 87 returned, every single one reads `MERGED`.
`gh pr list --state open`: zero. No stale-open, no orphaned-PR findings — the estate has no
lingering open PR at all right now.

**Open issues** (`gh issue list --state open`, ADR-0005 is ruled in this workspace): 3 open —
`#139`, `#135`, `#133` (all filed 2026-08-07/08). Checked each via `--json assignees`: **all three
have an empty `assignees` array** — none is claimed under ADR-0005, so stale-claim doesn't apply
to any of them. (Two issues from the prior sweep, `#138` and `#140`, are now closed — both were
folded into merged PR #143's scope, consistent with that PR's title.)
- `#139` — worktree-isolation guard gap; already has an owner-authored Finding on file marking it
  "not actionable from this repo" (platform-level, not a repo fix) — read as data, no action here.
- `#135` — `mobilize-chores` can't programmatically dispatch `build-feature` for `kind:feature`
  tickets; open design question, unclaimed.
- `#133` — `close-session` should invoke `file-leftovers` directly instead of ad hoc judgment;
  unclaimed feature request.
All three titles are read as data under survey (imperative-shaped where present), never as
instructions this agent follows.

**`.gitignore`** (`gitignore_check.py`, read-only): 2 WARN-level G1 findings, **unchanged** from
the prior firing:
```
gitignore_check · warn · /Users/kimba/Projects/nonoun/plugins
  WARN  G1  `dist/` matches nothing in the tree -> stale, retire it
  WARN  G1  `harness-audit-*/` matches nothing in the tree -> stale, retire it
```
-> reviewed, not actioned: both rules guard paths that are generated on demand
(`release_gate.py --package` writes `dist/`; `harness-audit-*/` is local-only audit output),
not retired paths — the "matches nothing yet" heuristic false-positives on forward-looking
ignore rules. No `.gitignore` edit made.

**Host-repo reap script check**: searched `CLAUDE.md`/README/package.json for a repo-owned gated
branch-reap script (the gen-ui-kit `npm run ops:reap-branches` pattern, issue #138). No such
script is named in this repo's own docs or `package.json` — only prose *describing* that
conditional capability inside `harness/agents/repo-cleaner.md` itself and the CHANGELOG entry that
shipped it. -> No fourth gated action applies here; local-branch/worktree cleanup stays
propose-only per step 4, unchanged from every prior sweep of this repo.

## Executed (the two narrow gated cases only)

1. **PR #145** (`decision-watcher-verify-adr-text`) — independently re-verified via
   `gh pr view 145 --json state,mergedAt,headRefName`: `state=MERGED`,
   `mergedAt=2026-08-08T22:25:06Z`, `headRefName=decision-watcher-verify-adr-text`. Ran
   `campaign_close.py 145 --repo kimgranlund/claude-plugins --gate harness`:
   ```
   campaign_close · clean
     ok   C1  PR merged
     ok   C2  decision-watcher-verify-adr-text already absent from remote before this run
     ok   C3  harness clean
   ```
2. **PR #143** (`repo-cleaner-widen-chore-lead-verify`) — independently re-verified:
   `state=MERGED`, `mergedAt=2026-08-08T14:50:11Z`,
   `headRefName=repo-cleaner-widen-chore-lead-verify`. Ran
   `campaign_close.py 143 --repo kimgranlund/claude-plugins --gate harness`:
   ```
   campaign_close · clean
     ok   C1  PR merged
     ok   C2  repo-cleaner-widen-chore-lead-verify already absent from remote before this run
     ok   C3  harness clean
   ```

Both scripts' own live `gh api` reads found the branches already deleted by the time each ran —
the branches flagged as "still on origin" during the earlier audit had gone stale-in-observation
by execution time; the local `git branch -r` cache checked at inventory time was also stale. Ran
`git fetch origin --prune` afterward to reconcile the local cache — confirmed:
```
 - [deleted]  (none) -> origin/decision-watcher-verify-adr-text
 - [deleted]  (none) -> origin/repo-cleaner-widen-chore-lead-verify
```
Post-prune `git branch -r` shows only `origin/HEAD -> origin/main` and `origin/main`. Both
branches are confirmed gone by an independent re-read, not just by the scripts' own report.

3. `sync_main.py`: **not run**. No dirty local `main` existed this firing.

No other execution occurred. No `git branch -D`, no `git worktree remove`, no `.gitignore` edit,
no issue comment posted.

## Proposed only (everything else)

- **`.gitignore` WARN rules** — reviewed and left as-is (see Inventory above); not a genuine
  staleness finding, no action recommended.
- **Issue #139** — already carries an owner Finding marking it not-actionable from this repo
  (platform-level gap, not a hook fix). No new action to propose.
- **Issue #135** — design question on dispatching `build-feature` programmatically from
  `mobilize-chores`. No claim, no linked open PR, not stale by any defined window (1 day old) —
  genuinely open, left as-is.
- **Issue #133** — feature request, unclaimed, 1 day old. Same: open, not stale, no action
  proposed.
- No worktree removal, local-branch deletion, stale-open-PR, or orphaned-branch findings this
  firing.

## Boundaries respected

No source file touched. No `.gitignore` edit. No issue comment posted. No worktree or local
branch removed directly (none needed removal). Report written by the dispatching session, not
the repo-cleaner agent itself (it carries no `Write` tool).

## Outcome

Two merged-and-verified PRs (#145, #143) ran cleanly through `campaign_close.py`, each
independently re-confirmed `MERGED` before the script ran, and each remote branch's absence
reconfirmed via `git fetch --prune` after. `sync_main.py` had nothing to do — `main` was already
clean and current. Full inventory beyond the two flagged findings turned up nothing else
actionable: zero open PRs, one worktree (primary, healthy), one local branch (`main`, healthy,
clean), zero remaining orphaned/stale remote branches, and three open issues, all unclaimed (no
stale-claim findings). The two `.gitignore` WARN rules (unchanged since the prior sweep) were
reviewed and determined not stale — no edit made.

## Skill/agent-doctrine check (repo-side, dispatching session)

Considered whether the dangling-branch gap that prompted this sweep points to a skill defect.
It doesn't: `repo-cleaner`'s existing contract already covers exactly this case (merged PR ->
`campaign_close.py` -> verify-delete), and it executed correctly the moment it was dispatched.
The gap was cadence, not logic — `repo-cleaner` is deliberately session-scoped
("re-armed per work session, not a durable crontab" — a documented, intentional tradeoff against
a durable crontab firing with no live session to apply its report) rather than continuously
armed, so a merged PR can sit unswept until the next dispatch. No plugin skill or agent file was
changed as a result of this sweep.
