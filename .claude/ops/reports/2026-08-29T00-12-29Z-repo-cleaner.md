## repo-cleaner sweep — 2026-08-29T00:12:29Z

**Survey status:** MEASURED. `git fetch --prune` succeeded (pruned 18 stale refs — every one of them a now-merged PR's remote branch: `945-adr-checkpoint-migration`, `952-figma-skill-check-py`, `956-decision-watcher-narrates`, `957-check-routing-estate-never`, `958-harness-sweep-chores-step`, `959-check-routing-runner-idles`, `960-build-leader-seats-cannot`, `961-docs-file-bug-fix`, `963-verdict-must-clear-runtime`, `964-harness-campaign-close-py`, `973-dispatch-envelope-py-seed`, `974-docs-check-stage-lifecycle`, `977-fleet-bootstrap-phase-2`, `978-sweep-22-orphan-adrs`, `979-teamwork-dispatch-ticket-qb3`, `980-fleet-sendmessage-rejects-a`, `fix/954-write-gate-wake`, `fix/harness-checks-long-goal-arg` — all correspond to PRs #951/#953/#967/#971/#975/#976/#960(#970)/#969/#972/#981/#982/#983/#984/#988/#985/#986/#955/#966, all MERGED). `primary_checkout_check.py` → PASS (`main`, primary root, exit 0, clean).

**Classification set changed from the prior firing** (`.claude/ops/reports/2026-08-28T01-02-49Z-repo-cleaner.md`) — full report, not abbreviated. Consecutive-unchanged-firings counter: **0**.

**What changed since prior firing:**
- `main` advanced from that firing's HEAD to `798983eb` (many PRs merged in between; primary checkout stayed correctly on `main` throughout — `primary_checkout_check.py` still PASS).
- Open-issue set changed: #948/#945 closed since prior firing; #987 and #978 are new/now-open. #617/#609 persist. All four still carry zero assignees under ADR-0005's claim protocol (`.claude/docs/adr/0005-ticket-claim-protocol.md`) — no stale-claim findings, same precedent as every prior firing (the `doing` label on #609 is not itself a claim absent a ruling naming it).
- `gitignore_check.py` changed: now 2-WARN, not 3 — `dist/` dropped off the stale list (something under `dist/` now exists in the tree; `harness-audit-*/` and `.name-map.md` remain stale G1 matches-nothing).
- **New finding, not present in the prior firing:** a real linked git worktree, `.claude/worktrees/plugins-reviewer` (registered in `git worktree list`), detached HEAD at `878a3c29`, no branch, clean tree, no open PR references it — orphaned.
- **New finding, not present in the prior firing:** ten leftover scratch-clone directories under `.claude/worktrees/` — `claude-plugins-958/959/963/964/973/974/977/978/979/980` — each a full independent git clone (own `.git` dir, not a `git worktree`-linked entry, so `git worktree list` correctly doesn't show them; this is why they weren't caught by that command). Each sits clean, on its own now-merged branch, and every one of those branches' remote refs was just confirmed gone by this firing's own `fetch --prune` (all 10 are in the pruned-ref list above) — i.e. `campaign_close.py` already did its job on the remote side; only the local scratch clones are stale. Verified individually: `claude-plugins-978`'s branch (`978-sweep-22-orphan-adrs`) traces to merged PR #988, clean, `git ls-remote origin refs/heads/978-sweep-22-orphan-adrs` returns nothing — not live work-in-progress on the still-open tracking issue #978, despite the name coincidence.

**Worktree/branch surface (`git worktree list`):** primary (`main`, `798983eb`) + `plugins-reviewer` (detached, orphaned, see above). `git branch -vv`: only `main`, tracking `origin/main`, in sync. `git branch -r`: only `origin/main` — no other remote branches exist.

**Open PRs:** none (`gh pr list --state open` empty; `--state all --limit 50` returns 50/50 `MERGED`).

**Local tree state:** `git status` clean on all tracked files in the primary checkout. One untracked file, `.claude/ops/sweep-in-flight.json` (`startedAt 2026-08-29T00:09:56Z`, `pid 81159`, `session plugins-c4`) — recency and session-name convention match this firing's own dispatching session, same live-coordinator-state pattern every prior firing has classified this as, not cruft. `main` is already `+0/-0` against origin, so `sync_main.py` has no ff-pull to offer regardless of interactive/scheduled classification.

**Executed:** nothing.
- No merged-and-verified PR has a remaining remote branch to close — this firing's own `fetch --prune` found all 18 reapable refs already gone (remote-side cleanup already happened, by a prior `campaign_close.py` run or equivalent).
- No dirty tracked-file state on `main` to run `sync_main.py` against.
- No host-repo reap script is named in this workspace's own `CLAUDE.md`/`README` for local branch/worktree/scratch-clone reap — this repo (`nonoun/plugins`) ships no such script (unlike gen-ui-kit's `npm run ops:reap-branches`), so nothing here executes directly.

**Propose-only:**
- **Orphaned worktree:** `git worktree remove .claude/worktrees/plugins-reviewer` (detached HEAD `878a3c29`, no branch, no PR, clean — safe to remove; a human should also confirm no session still expects it).
- **Orphaned scratch clones (disk cruft, not git-worktree-registered):** `rm -rf` each of `.claude/worktrees/claude-plugins-{958,959,963,964,973,974,977,978,979,980}` — every one clean, every one's remote branch already reaped by this firing's own prune. No gated script covers this category in this repo, hence propose-only rather than direct execution.
- **Gitignore repair** (`gitignore_check.py`, 2 WARN, both G1): `harness-audit-*/`, `.name-map.md` — recommend reviewing whether each still applies; report only, never hand-edited (`.claude/rules/gitignore-repair.md`).
- **Stale-claim:** clean. 4 open issues (#987, #978, #617, #609) — all zero assignees under ADR-0005. No reclaim proposals.
- **Off-main-primary:** none — `primary_checkout_check.py` reads PASS.

**Healthy, no action:** primary checkout branch surface, remote-branch surface (fully reaped), open-PR surface.

Proposed only for the worktree/scratch-clone/gitignore findings above; nothing mutated this firing beyond the standing exception (`git fetch --prune`).
