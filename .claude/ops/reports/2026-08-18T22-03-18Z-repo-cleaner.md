# repo-cleaner sweep — 2026-08-18T22:03:18Z

Classification set changed materially from the prior firing (`2026-08-18T17-20-06Z-repo-cleaner.md`) — full report, not abbreviated. Consecutive-unchanged-firings counter: **0**.

**Executed (gated):**
- `git fetch --prune` — pruned 4 stale remote-tracking refs (`fix-647-attention-audit-dmi-drift`, `fix-660-artifact-check-false-positives`, `fix-667-build-feature-gh541-doctrine` deleted; `fix-658-owed-chain-spec-lock` new), fast-forwarded the `origin/main` tracking ref by 42 commits.
- `primary_checkout_check.py /Users/kimba/Projects/nonoun/plugins` → clean, on `main`.
- `gitignore_check.py` — primary: 2 standing WARNs (`dist/`, `harness-audit-*/`). This worktree: 4 standing WARNs (adds `.DS_Store`, `.claude/worktrees/`). No new findings.
- `campaign_close.py` — not invoked: `git ls-remote --heads origin` shows only `main` and `fix-658-owed-chain-spec-lock` remain; every merged PR's remote branch is already gone.
- `sync_main.py` — not invoked: could not be, from this worktree-isolated session (sandbox refuses git ops targeting the primary checkout). Proposed instead.

**Healthy / active — no action:**
- PR #678 (`fix-658-owed-chain-spec-lock`) OPEN — no local worktree/branch for it in this checkout; live elsewhere.
- Issue #658 claim (`kimgranlund`) — linked to the open PR above by branch-name convention, fresh. Not stale.
- Issue #662 claim (`kimgranlund`, branch `fix-662-token-naming-unprefix`) — claimed 2026-08-18T21:39:11Z, latest comment 2026-08-18T21:59:36Z; actively building elsewhere. Not stale.

**Proposed only — orphaned worktrees (merged, remote gone, no host reap script exists in this repo):**
- `.claude/worktrees/629-self-improvement-retrospective` (branch `629-self-improvement-retrospective`) — PR #645 MERGED. Recommend `git worktree remove` + `git branch -d`.
- `.claude/worktrees/build-554` (branch `634-req-infix-prefix`) — PR #646 MERGED. Recommend `git worktree remove` + `git branch -d`.
- `.claude/worktrees/fix-647` (branch `fix-647-attention-audit-dmi-drift`) — PR #648 MERGED 2026-08-18T17:17:52Z. Changed since prior firing (was live/healthy there). Recommend `git worktree remove` + `git branch -d`.
- `.claude/worktrees/fix-656-rdd-revalidation-rotation` (branch `fix-656-rdd-revalidation-rotation`) — PR #663 MERGED 2026-08-18T21:04:02Z. New this firing. Recommend `git worktree remove` + `git branch -d`.
- `.claude/worktrees/fix-660` (branch `fix-660-artifact-check-false-positives`) — PR #661 MERGED 2026-08-18T20:54:14Z. New this firing. Recommend `git worktree remove` + `git branch -d`.
- `.claude/worktrees/fix-667` (branch `fix-667-build-feature-gh541-doctrine`) — PR #668 MERGED 2026-08-18T21:35:38Z. New this firing. **This is the worktree this repo-cleaner seat is itself running in** — cannot be removed mid-session; flag for cleanup once this session exits.

**Proposed only — orphaned local branches (merged, no worktree, remote gone):**
`608-dispatch-ticket-file-bug-claim-fix` (PR #610), `611-backlog-roadmap-releases-loop` (PR #621), `612-harvest-domain-knowledge` (PR #614), `613-harvest-project-context` (PR #615), `622-feedback-intake-door` (PR #641), `637-drain-queue-command` (PR #643), `650-artifact-styling-rules` (PR #651, new this firing) — recommend batch `git branch -d` for each.

**Proposed only — primary behind, dirty-state unmeasured:**
Local `main` in the primary checkout reads `[origin/main: behind 42]` per `git branch -vv` (0 ahead — a clean fast-forward). `primary_checkout_check.py` confirms it's on `main` but does not check ahead/behind or working-tree dirt. Dirty-tree status could not be read from this worktree-isolated session (sandbox blocks `-C`/`cd` to the primary). Recommend a primary-rooted session run `sync_main.py`.

**Stale-claim check (ADR-0005) — clean:** 19 open issues surveyed; only #658 and #662 carry an assignee, both freshly active with linked/named branch work in progress. No stale-claim finding.

**Closed-not-merged PRs — no action:** no new closed-not-merged PRs surfaced this firing; the prior firing's #437/#391 finding stands unchanged.
