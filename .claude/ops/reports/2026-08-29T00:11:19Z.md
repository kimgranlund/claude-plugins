# issue-sorter firing — 2026-08-29T00:11:19Z

Repo: kimgranlund/claude-plugins. Backend: git-native/GitHub (ADR-0002/ADR-0003), resolved.
Prior checkpoint: 2026-08-28T01:02:22Z. Discovery window: `updated:>=2026-08-28T01:02:22Z
-label:backlog -label:roadmap`.

## Discovery
- Issues touched in-window: #945, #948, #952, #954, #956, #957, #958, #959, #960, #961, #962,
  #963, #964, #965, #968, #973, #974, #977, #978, #979, #980, #987 (22 total; #945 and #948
  predate this checkpoint but were touched again in-window, already actioned in the prior
  firing — no new action here).
- PRs touched in-window (context/dedup only, all merged): #951, #953, #955, #966, #967, #969,
  #970, #971, #972, #975, #976, #981, #982, #983, #984, #985, #986, #988.

## Classification / trust / mint
- All 22 issues authored by `kimgranlund` — matches `friendlies.json` `policy.confirmed_by`
  exactly, no `user-signal` mismatch on any of them.
- 21 of 22 already carried a correct kind label (`bug`+severity, `task`(+size), or
  `feature`+size) at discovery — 0 action needed, per the same convention the prior firing
  recorded.
- **#987** ("harness: adr_checkpoint.py periodically truncates a recorded sha256 to 57 hex
  chars (adr-0065 in gen-ui-kit)") was the one item filed with no labels at all. Classified:
  defect shape (a script write/merge-path bug, not a decision only an operator can make — not
  ruling-shaped). Dedup-before-mint: checked against #945 (checkpoint hash-BASIS widening
  migration, resolved via merged PR #951) and #956 (decision-watcher queue-narration bug) —
  distinct symptom (57-of-64-hex truncation on write/merge, not a formula-version or narration
  gap), no duplicate found. Severity: `major` (aligned with the sibling checkpoint-integrity
  bugs #945/#968/#980 already carrying that severity in this window — a truncated stored hash
  silently undermines ADR-checksum verification, not merely cosmetic). Body cites its own
  provenance plainly ("Moved from adiahealth/gen-ui-kit#2193 ... filed via the adia-marshals
  channel by gen-ui-kit-marshal") but does not itself ask for a confirm-before-prioritizing the
  way #866 did — read as an ordinary cross-repo-relay bug filing (a pattern already established
  in this repo's history), not queued to Kim's ruling queue. Labeled `bug` + `major` directly
  (`gh issue edit 987 --add-label bug --add-label major`); both labels already existed in the
  repo, no label-create fallback needed.
- No item held (0 unknown authors). No `friendlies.json` change.

## Steps 7/8
- Step 7 (REQ-011 roster bootstrap): not-applicable — `friendlies.json` already bootstrapped
  (2026-07-18).
- Step 8 (REQ-013 MCP offer): not-applicable — `github_mcp_offer` already recorded (`accepted`,
  override on record 2026-07-25) and `.mcp.json` already carries the matching
  `githubcopilot.com/mcp` entry. No re-offer.

## Checkpoint
- github source: reachable both `gh issue list` and `gh pr list` calls succeeded. Checkpoint
  advances to `2026-08-29T00:11:19Z` for both `issues_checkpoint` and `prs_checkpoint`.

## held-items.md
- Unchanged — no new holds this firing (all 22 items were `kimgranlund`-authored, already
  trusted).

## Done-when
Every item discovered this firing classified, trust-checked, and either already correctly
minted or (for #987) labeled directly via `gh issue edit`; the one reachable source's checkpoint
entry advanced; this report ships as the fenced block named above; `watch-checkpoint.json`'s
full content lands as its own payload below. `friendlies.json` and `held-items.md` are unchanged
this firing — no payload block for either (nothing to apply).
