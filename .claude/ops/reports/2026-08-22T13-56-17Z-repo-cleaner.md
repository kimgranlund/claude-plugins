## repo-cleaner sweep — 2026-08-22T13:56:17Z

**Survey status:** MEASURED. `git fetch --prune` succeeded (pruned 3 stale refs: `origin/build-leader-naming-persistence`, `origin/fleet-bootstrap-bind-team-inline`, `origin/sub-agent-skill-targets-naming` — all three merged PRs #867/#868/#869, remote branches already gone). `primary_checkout_check.py` → PASS (`main`, primary root, clean, up to date with `origin/main`).

**Classification set changed materially** from the prior firing (`.claude/ops/reports/2026-08-21T21-39-51Z-repo-cleaner.md`) — full report, not abbreviated. Consecutive-unchanged-firings counter: **0**.

**Resolved since prior firing:** PRs #867 (teamwork: build-leader dispatch naming), #868 (teamwork: /sub-agent skill targets), #869 (teamwork 2.28.26: fleet-bootstrap Phase 1 inlines bind-team) all merged and their remote branches already gone — `campaign_close.py` had no target this firing (nothing left to close). Primary `main`'s prior dirt (`brief-nonoun-plugins.md`, `fleet-roster.md`, `sweep-in-flight.json`, `plan.md`, `watch-checkpoint.json`, three `teamwork/` files) is gone — main is now clean and `up to date with 'origin/main'`, no ahead/behind.

**Executed:** nothing.
- No merged-and-verified PR has a remaining remote branch to close.
- `sync_main.py` NOT run, though this is an interactive dispatch. Main carries only one piece of untracked "dirt": `.claude/ops/sweep-in-flight.json`, self-identifying a live session (`"session": "plugins-marshal"`, `pid 14733`, `startedAt 2026-08-22T13:54:43.226Z` — ~45 seconds before this firing's check). `ps -p 14733` found no matching process, but the extreme recency of the timestamp keeps this ambiguous-at-best rather than confidently stale — same judgment call as the prior firing's dirt finding, propose-only rather than quarantined. Separately, main already reads `up to date with 'origin/main'` (no ahead/behind), so `sync_main.py`'s actual job (quarantine + ff-pull) has nothing to pull regardless — running it here would only risk stashing a possibly-live peer's in-flight marker for zero syncing benefit.

**Propose-only — reap candidates** (no host reap script found in this repo's `CLAUDE.md`/`README`; no `package.json` exists at the repo root):

Clean, safe to reap (merged+verified via `gh`, remote gone, worktree clean, no uncommitted changes) — unchanged from prior firing:
- worktree `build-554` / branch `634-req-infix-prefix` (#646)
- worktree `fix-647` / branch `fix-647-attention-audit-dmi-drift` (#648)
- worktree `fix-660` / branch `fix-660-artifact-check-false-positives` (#661)
- worktree `fix-794-marketplace-drift` / branch `fix-794-marketplace-drift` (#795)

Local branches with no worktree, all merged/remote-gone — unchanged from prior firing: `608-dispatch-ticket-file-bug-claim-fix` (#610), `611-backlog-roadmap-releases-loop` (#621), `612-harvest-domain-knowledge` (#614), `613-harvest-project-context` (#615), `622-feedback-intake-door` (#641), `637-drain-queue-command` (#643), `650-artifact-styling-rules` (#651), `657-scope-audience-frontmatter` (#664), `670-unnamed-checker-dispatch` (#689), `fix-667-build-feature-gh541-doctrine` (#668), `fix-850-fleet-bootstrap-phase5-wall` (#851), `post-series-cleanups` (#823).

**NOT safe to reap — live uncommitted dirt (unchanged from prior firing):**
- worktree `629-self-improvement-retrospective` (merged #645) — untracked `.claude/docs/lld/lld-0018-estate-maintenance-retrospective.md`.
- worktree `fix-656-rdd-revalidation-rotation` (merged #663) — `revalidation_checkpoint.py` + `watch-adrs/SKILL.md` modified uncommitted.
- worktree `fix-667`, actually on branch `fix-683-container-grammar-role-aliases` — HEAD (`83fade5`) fully contained in `origin/main` already (via PR #697 under a different branch name) but no PR under *this* branch's own name — orphaned, plus `docs/skills/make-artifact/scripts/css_build.py` modified uncommitted.
- worktree `fix-684` (merged #699) — 2 modified + 3 untracked `overhaul-run-*.md` files, actively in use.

**Healthy, no action:** `gh pr list --state open` returned zero open PRs this firing.

**Standing, no gated path:** 2 stashes unchanged (`"On main: sync_main quarantine"` x2).

**Other findings:** `gitignore_check.py` — primary reads 3-WARN (`dist/`, `harness-audit-*/`, `.name-map.md` — all stale-rule matches-nothing warnings, unchanged from prior firing). All 8 worktrees show a uniform 4-WARN set (`.DS_Store`, `dist/`, `harness-audit-*/`, `.claude/worktrees/`) — no FAIL anywhere, unchanged. Stale-claim: clean — `gh issue list --state open` returned 6 open issues (#866 NEW this firing, #849, #617, #609, #490), all zero assignees.

**Proposed only, nothing else mutated.**
