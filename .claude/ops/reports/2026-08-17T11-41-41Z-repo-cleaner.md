# repo-cleaner sweep — 2026-08-17T11-41-41Z

Full report (classification set changed from the prior firing — `2026-08-16T05-16-57Z` found one
worktree and zero open findings; this firing found 24 worktrees, ~14 scratch local branches, and
one genuinely mutable remote branch). Consecutive-unchanged-firings counter reset to **0**.

Dispatched as an UNATTENDED sweep — `sync_main.py` explicitly out of scope this firing regardless
of findings (interactive-only per contract). Current checkout is on `fix-423-fleet-bootstrap-phase1`
@ `fc3942c`, not `main`, with tracked-tree edits (`.claude/ops/fleet-roster.md`,
`.claude/settings.json`, `attention-trend.csv`, plus sibling ops-seat payload files
`adr-checkpoint.json`/`watch-checkpoint.json` and two untracked sibling reports from
`issue-sorter`/`decision-watcher` firing in the same coordinating-session worktree this sweep).
None of that is git-surface cruft — it's this session's own live in-progress state (this agent's
own worktree, not a stray campaign checkout) — not a finding, and outside `sync_main.py`'s
"dirt on main" trigger in any case (wrong branch).

**Inventory:**
- Worktrees (`git worktree list`): 24, all under `.claude/worktrees/`, one primary checkout
  (this repo root, `fix-423-fleet-bootstrap-phase1`).
- Local branches (`git branch -vv`): ~40 total — 24 with a worktree, ~14 plain scratch branches
  with none (`build-425-work`, `build-429-orchestrator-introduction`, `build-430-work`,
  `build-432-work`, `restack-425-work`, `restack-432-work`, `restack-437-work`,
  `worktree-build-426-seat-retirement`), plus `main` (behind `origin/main` by 31 — no worktree of
  its own in this checkout, healthy, just stale).
- PRs (`gh pr list --state all --limit 50`): the 50 most recent all read `MERGED` except one
  `CLOSED` (unmerged duplicate, #437, superseded by #439) and #441 (see Executed below). `gh pr
  list --state open` → **zero**, confirmed.
- Open issues (ADR-0005 ruled in this workspace): exactly 2 — `#475`, `#295`. Both carry empty
  `assignees` arrays. **No stale-claim findings** — nothing claimed right now to go stale.
- `.gitignore` (`gitignore_check.py`, read-only): one WARN — `harness-audit-*/` matches nothing
  in the tree (stale, on-demand-generated path). No edit proposed.
- Host-repo reap script: none named in this repo's own `CLAUDE.md`/README. Fourth gated action
  does not apply — all local worktree/branch cleanup below is propose-only regardless of merge
  status.

**Executed:**
- `git fetch --prune origin` (twice, before and after the one mutation below) — local
  remote-tracking-ref sync only. First prune removed 24 stale refs whose branches were already
  deleted on GitHub (23 of the 24 worktrees below correspond to PRs whose remote branch was
  *already* gone — no `campaign_close.py` action needed for those; someone/something upstream had
  already run the close). Second prune, post-mutation, confirms no new refs appeared and the one
  branch closed below is reverified gone.
- `python3 harness/scripts/campaign_close.py 441` — PR #441
  ("teamwork 2.16.7: rename orchestrator seat session-name to `{repo}-team-lead`") independently
  re-read as `MERGED`; its remote branch `fix-434-team-lead-session-name` (no local worktree or
  branch existed for it — pure remote artifact) was the only merged PR whose remote branch was
  still present. Script output:
  ```
  campaign_close · clean
    ok   C1  PR merged
    ok   C4  no open PR uses fix-434-team-lead-session-name as its base
    ok   C2  fix-434-team-lead-session-name deleted and reverified gone
  ```
- Nothing else executed. No dirty `main` (wrong branch entirely, and this is unattended), no host
  reap script to invoke.

**Proposed only — every local worktree/branch below (no gated mutation path exists for any of
them; each independently confirmed by diffing against `origin/main` after the PR that carried its
content merged):**

*Worktrees whose content is fully merged (zero unique commits vs. `origin/main`), remote branch
already deleted — safe `git worktree remove` + `git branch -D` candidates, for a human to run:*
`adr-0015-agent-suffix` (#444), `adr-0015-authorkit-grammar` (#447), `adr-0016-lead-head` (#453),
`build-426-seat-retirement`/branch `build-427-reviewer-wall-fix` (#435), `build-434-team-lead-
session-name`/branch `build-433-lead-seat-renames` (#442), `build-445-version-gate` (#446),
`build-449-450-mobilize-gate` (#454), `build-458-leading-desc-diet` (#468),
`build-460-dedupe-leader-para` (#470), `build-461-agent-desc-diet` (#471),
`build-463-product-leader-diet` (no own PR — content matches #467, 0 unique commits, duplicate
worktree of `docs-462-intake-template-diet`), `build-465-measure-hardening` (#469),
`docs-462-intake-template-diet` (#467), `fix-428-fleet-bootstrap-phase4` (#440), `idr-431` (#439;
its own #437 was the closed unmerged duplicate), `intent-layer-draft` (#424),
`issue-438-ui-probe-symlink` (#452), `issue-443-stacked-pr-rule` (#451),
`issue-466-remove-hooks` (#472), `issue-473-authorkit-frontmatter` (#476, merged 20 min before this
sweep started), `p6-attention-remeasure` (#474), `retire-456-exemptions` (#457),
`s1-455-doctrine-manifest` (#459).

*Notable/ambiguous, flagged distinctly rather than folded into the bulk list above:*
- **`issue-475-teamwork-diet`** — locked (`git worktree list` shows `locked`), yet 0 unique
  commits vs. `origin/main` (content fully merged already). Its matching issue **#475** is open,
  unassigned, updated 11:31Z (10 min before this sweep). A lock is normally deliberate — flagging
  rather than bulk-listing so a human checks whether the lock is stale or intentional before any
  removal, even though the commit evidence says it's safe.
- **`pr442-finalize`**/branch `pr442-finalize-work` — 5 "unique" commits vs. `origin/main`, but all
  five are the pre-squash granular history behind PR #442's already-merged squash commit (same
  `#433` content, different SHAs). Superseded, not genuine unmerged work — same disposition as the
  bulk list, called out only because the naive ahead-count looked non-zero.

*Plain scratch branches (no worktree) — all superseded duplicates/pre-squash lineage of already-
merged campaigns, safe `git branch -D` candidates:* `build-425-work`, `restack-425-work` (both
duplicate pre-merge lineage of the current branch's own already-landed PR #425 content),
`build-429-orchestrator-introduction`, `build-432-work`, `restack-432-work` (duplicate lineage of
merged #432), `build-430-work`, `worktree-build-426-seat-retirement` (duplicate lineage of merged
#430 — content-identical, different SHA), `restack-437-work` (duplicate lineage of merged #439).

None of the above executed — no host-repo reap script is named in this repo's own docs, so per
contract this stays a plan, not an action, regardless of how confidently each is classified
merged-and-superseded.

## Boundaries respected

No worktree removed, no local branch deleted, no source file touched, no `.gitignore` edit, no
issue/PR comment posted, no ticket claim reclaimed. The one mutation (`campaign_close.py 441`)
only deleted a remote branch already independently verified merged, and reverified the delete
landed. Report returned as payload only, applied by the dispatching session.
