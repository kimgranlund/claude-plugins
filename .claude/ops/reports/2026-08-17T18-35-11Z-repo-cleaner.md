# repo-cleaner sweep — 2026-08-17T18-35-11Z

Classification set changed from the prior firing (`2026-08-17T14-50-18Z-repo-cleaner.md`, which
ended mid-firing on a tool block). Consecutive-unchanged-firings counter: **0**.

**Tooling:** `gh` GraphQL threw intermittent transient HTTP 503s throughout, all retried
successfully — no inventory gap. Local `origin/*` remote-tracking refs were stale before this
firing (`git fetch --prune` cleared 17 dead refs and revealed local `main` was 1 commit behind,
previously misread as clean-and-current) — any prior firing that skipped a fetch/prune first was
working from stale inventory.

**Executed (gated):**
- `sync_main.py` — main was clean, 1 commit behind, ff-safe → synced to `28c6b697f`, reverified by
  SHA.
- `campaign_close.py 544 --gate teamwork --gate harness` — PR #544 independently verified MERGED;
  C1/C5/C4/C3 all ok; **C2 FAILED** — `worktree-build-522` still on remote after delete attempt +
  full retry window, reconfirmed via `git ls-remote`. Refusal reported as evidence, not overridden.
  Human/next-firing remediation: `gh api -X DELETE repos/kimgranlund/claude-plugins/git/refs/heads/worktree-build-522`,
  then re-run `campaign_close.py 544`.

**Inventory:**
- Worktrees: main (healthy) · build-520/build-541/build-548-docs (orphaned post-merge, remote gone
  or never existed, content is an ancestor of `origin/main` for build-541 — propose worktree
  remove + branch delete, no host reap script exists) · build-522 (merged PR #544, remote branch
  refused-delete per above, worktree left as-is) · build-523-w5 (branch `worktree-build-523-w5`,
  PR #550 OPEN, fresh — healthy).
- Bare local branches (no worktree): `build-525-authorkit-skill-as-command`, `worktree-build-520`,
  `worktree-build-548-docs` — all orphaned post-merge (PRs #545/#538/#546 MERGED, remotes gone) —
  propose `git branch -D`. `worktree-build-529` — the prior firing's "held evidence" branch
  (`c393fcd`), content is now an ancestor of `origin/main` — propose `git branch -D`; the actual
  evidence snapshot stays untouched at
  `.claude/ops/reports/unclaimed-desk-seat-sweep-2026-08-17.diff.md`.
- PRs: one OPEN (#550, fresh, healthy); rest of last 100 MERGED/historical-CLOSED — no stale-open
  PRs.
- `gitignore_check.py`: two standing G1 WARNs (`dist/`, `harness-audit-*/`) — unchanged, pre-ruled
  on-demand-generated/accepted (matches `2026-08-17-entry11-gitignore-claims.md`). No G2 FAIL.
- Stale-claim survey (ADR-0005): 11 open issues; one assignee claim (#525, fresh, ~40 min old);
  comment activity on #539/#543/#524/#490 all same-session, dated 2026-08-17 15:00-18:32Z. No
  stale claims found.

**Proposed only:**
