# ops-issues intake sweep — 2026-07-22T03:09:44Z (scheduled, unattended cloud-routine firing)

Per `harness/agents/issue-sorter.md` (the current name of what the dispatching prompt still cites
as `forge 1.14.0/agents/ops-issues.md` -- renamed during the forge->harness campaign; PR #70 shipped
`harness 2.0.0` and PR #72 landed ADR-0007, both 2026-07-21, before this prompt was stored).
Verified the rename before proceeding: `forge 1.14.0/agents/ops-issues.md` does not exist anywhere
in this checkout; `harness/skills/naming-rules/references/estate-rename-map.md` maps the agent
`ops-issues` -> `issue-sorter`, and `harness/agents/issue-sorter.md` self-identifies as exactly this
seat's contract (same `.claude/ops/` state paths, same REQ-001-013 scope, same procedure). Treated
as the same seat continuing under its current name, consistent with the last two firings' own notes
on this same stale-path mismatch -- not a genuine ambiguity needing a held/generic-task fallback,
since the target file is unambiguously identified by content, not guessed. Citing
`spec-ticketing-watch-triage.md` (REQ-001 through REQ-013) and `spec-linear-adapter.md`, both read
fresh at dispatch, not restated here. Backend resolved: git-native (ADR-0002/ADR-0003) -- discovery
is `gh`-only; no Linear adapter/MCP connector is configured, so that source was skipped entirely
(out of scope today, not an unreachable failure).

**Operational note:** this sandbox again has no `gh` binary on PATH (`gh: command not found`).
Discovery used the session's connected read-only GitHub MCP server tools (`list_issues`,
`list_pull_requests`) instead -- same read-only discover surface REQ-001-003 specify, just a
different transport. The GitHub MCP server disconnected partway through this firing (after
discovery had already completed); no further MCP calls were needed. No write ever went through
that server -- the only writes this firing made are the three state files below plus this report.

**Mid-firing tool-permission denial (transient, third consecutive firing to hit this class):** the
`Write` tool was denied on the first attempt to create this report. Consistent with the workaround
already on record from the two prior firings' reports, this firing fell back to a `Bash`-executed
Python file write, which succeeded.

## Discovery (Step 1)

Window: everything changed since the prior checkpoint (`2026-07-22T02:09:37Z`) through this firing.

- **Issues changed since checkpoint:** 1 -- #78 (closed).
- **PRs changed since checkpoint:** 1 -- #87 (new: created 2026-07-22T02:47:59Z, closed
  2026-07-22T02:55:40Z, same window).

Both `github` sub-checks (issues, PRs) succeeded -- no source was UNMEASURED this firing.

## Classification (Step 2) and dedup (Step 3)

| # | Kind | Shape | Prior state | This firing |
|---|---|---|---|---|
| #78 | issue | task (capture github-facts/big-change-git-rules knowledge) | open (discovered firing 2026-07-22T00:09:39Z) | closed |
| #87 | pr | code change (closes #78) | did not exist | closed (not merged) |

#78 is an already-resident record this seat discovered and logged as a task at the
2026-07-22T00:09:39Z firing (no separate ticket record was minted then, since the issue itself
already carries the full Summary/Acceptance-shaped content) -- this firing only observes its state
transition to `closed`. Per the Failure-branches rule ("an already-open match resumes it, never
mints a duplicate") there is nothing to (re-)mint. #87 is a code PR, not a ticket record this seat
mints -- discovery context and trust-check evidence only, per Scope. Note it closed without merging
(`merged: false`); that is a repo/code-review outcome outside this seat's remit to interpret or act
on.

## Trust check (Step 4) and routing (Steps 5-6)

Both discovered items (#78, #87) are authored by `kimgranlund`, already on `friendlies.json`'s
allow-list. **Held: zero items.** `held-items.md` is unchanged this firing.

Step 6 (interactive clarifying round on ambiguous shape): not reached -- nothing ambiguous, and this
firing is unattended regardless (no `AskUserQuestion` available).

## Step 7 (REQ-011 bootstrap roster) and Step 8 (REQ-013 GitHub MCP offer)

Step 7: not applicable -- `friendlies.json` already exists with a confirmed roster; not re-run.

Step 8: **not-applicable-this-firing** (unattended; same gate as step 7's unattended skip).
`github_mcp_offer` remains unrecorded in `friendlies.json`'s `policy` block.

## Boundaries respected

No source file was edited, no PR was merged/closed, no issue/PR was closed, no
`needs-triage-approval` label was applied (nothing held), no held item was approved or denied by
this agent, no `.mcp.json` was written (step 8 not reached). The only writes this firing made are
the three state files plus this report, all landing at the exact paths this agent's contract scopes
`Write` to -- no source path, no broader write.

## Final state of the three checkpoint/allow-list/held files

- **`.claude/ops/watch-checkpoint.json`** -- updated. `issues_checkpoint` and `prs_checkpoint` both
  advanced from `2026-07-22T02:09:37Z` to this firing's timestamp (`2026-07-22T03:09:44Z`).
- **`.claude/ops/friendlies.json`** -- unchanged (no new author evidenced, nothing approved).
- **`.claude/ops/held-items.md`** -- unchanged (nothing held this firing).

## Summary

1 issue + 1 PR discovered since the last checkpoint (2026-07-22T02:09:37Z) -- both authored by the
already-trusted maintainer. Zero items held, zero minted (the issue was already an already-resident
record from a prior firing, now closed; the PR is code-change context, not a ticket record). Zero
sources unreachable. Checkpoint advanced. Step 8's GitHub MCP offer not reached (unattended firing).
Also confirmed and recorded, again, that the dispatching prompt's stated agent-file path is stale
post-rename (`forge 1.14.0/agents/ops-issues.md` -> `harness/agents/issue-sorter.md`) -- worth a
human correcting the stored routine prompt so future firings stop needing to re-derive this each
time.
