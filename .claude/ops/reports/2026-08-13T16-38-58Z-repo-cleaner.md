# repo-cleaner sweep — 2026-08-13T16:38:58Z

Bounded chore-lead dispatch targeting kimgranlund/claude-plugins
(/Users/kimba/Projects/nonoun/plugins), focused on ADR-0011 naming-v2 chain
readiness. Checkout on `main` @ `38e60fc`, in sync with `origin/main`.
Working tree carries three PROTECTED untracked items — do not clean, revert,
stash, or quarantine.

## Preflight

- `gh` reachable and authenticated throughout — no UNMEASURED condition.
- Prior report: `2026-08-12T17-20-41Z-repo-cleaner.md` (clean tree, 106/106
  PRs, 3 new unassigned issues, same 2 gitignore WARNs). This firing adds a
  never-before-seen axis — a dirty tree carrying protected live work product
  — so this is NOT a same-classification-set firing. Full report;
  running-unchanged-count resets to 0.

## ADR-0011 chain focus

Three untracked items form one coordinated landing tied to open issue #196
("Land authorkit 0.1.x through the gate"), cross-linked from #197, which
states the ADR-0011 + spec docs ride in #196's own PR:

1. `.claude/docs/adr/0011-adopt-naming-convention-spec.md` (host-flagged
   protected — status: proposed).
2. `.claude/docs/spec/spec-naming-convention.md` (step-2 spec-doc
   deliverable).
3. `authorkit/` — new self-contained plugin (naming-governance tooling:
   naming-audit-agent, rename-execute/exemption-retire/naming-audit
   commands, naming-conventions/rename-planning/naming-audit/
   manifest-authoring skills) with its own scoped
   `authorkit/naming.manifest.json` (exemptions: []).

None touched, staged, or proposed for removal — propose-only per dispatch
posture, repo-wide.

Chain-readiness findings (report only, no edit made):

- Filename drift: ADR-0011's own Execution-order step 2 text names
  `.claude/docs/spec/naming-convention-spec.md`; the landed file is
  `spec-naming-convention.md`, matching this directory's existing
  `spec-{topic}` convention (`spec-linear-adapter.md`,
  `spec-ticketing-watch-triage.md`). Recommend correcting the ADR's prose to
  match the landed path, not renaming the file.
- The estate-wide `naming.manifest.json` seed (ADR-0011 step 2, "at the
  nonoun-plugins repo root") does not exist yet. `authorkit/naming.manifest.json`
  is a distinct, self-scoped manifest for the authorkit plugin alone and is
  not a substitute — this deliverable is still outstanding.
- `authorkit/`'s `.DS_Store` files are already covered by the repo's
  existing bare `.DS_Store` `.gitignore` rule — no hygiene action needed.

## Inventory

- Worktrees: one (primary checkout, `main`) — healthy.
- Local branches: one (`main`, tracking `origin/main`, no divergence) —
  healthy.
- Remote branches (`git ls-remote --heads origin`): exactly one, `main` —
  zero stray.
- PRs (`gh pr list --state all`, full sweep): 109 total, 109 MERGED. Zero
  open, zero closed-unmerged. No live branch on any merged PR remains
  (remote has only `main`) — no `campaign_close.py` invocation applicable.
- Open issues (ADR-0005 ruled — `.claude/docs/adr/0005-ticket-claim-protocol.md`):
  7 open (#189–#193, #196, #197), ALL with empty `assignees` — no ADR-0005
  claim exists on any → no stale-claim finding possible. Content/routing is
  issue-sorter's remit.
- `.gitignore` (`gitignore_check.py`, read-only): same two repeat WARNs as
  every prior firing —
  ```
  gitignore_check · warn · /Users/kimba/Projects/nonoun/plugins
    WARN  G1  `dist/` matches nothing in the tree -> stale, retire it
    WARN  G1  `harness-audit-*/` matches nothing in the tree -> stale, retire it
  ```
  Reviewed multiple times as legitimate on/off generated-output paths — no
  action proposed.
- Host-repo reap script: none (no `package.json`, no gated reap script named
  in this repo's own CLAUDE.md/README). No fourth gated action applies.
- `gh` auth: reachable and authenticated throughout.

## Executed

Nothing. No merged PR has an outstanding remote branch. `sync_main.py`
deliberately withheld despite interactive dispatch — the tree's "dirt" is
explicitly protected live work product (ADR-0011 chain, tied to issues
#196/#197), not cruft; running it would quarantine state those issues
depend on. No host-repo reap script exists to dry-run.

## Proposed only

- Leave ADR-0011, the spec doc, and `authorkit/` exactly as-is; land
  together via issue #196's PR per its own cross-link comments.
- Correct ADR-0011's Execution-order step 2 text to reference
  `.claude/docs/spec/spec-naming-convention.md` (matching what's landed and
  the directory's own convention).
- Author the estate-wide `naming.manifest.json` at the repo root per D10's
  seed list — currently missing; not substituted by authorkit's own file.
- `.gitignore` G1 WARNs (`dist/`, `harness-audit-*/`) — repeat, no action.
- Issues #189–#193 — open, unassigned, no claim, too new for any staleness
  window; content routes to issue-sorter.

## Out of scope, routed not performed

Duplicated instruction trees, stale corpus, and dead automation beyond this
agent's git-surface + ADR-0005-claim charter are `/clean-repo`'s remit.

## Boundaries respected

No source file touched. No `.gitignore` edit. No issue comment posted. No
branch or worktree removed. No script's gate overridden. `sync_main.py`
withheld per explicit protected-work-product constraint. Report returned as
payload only — not written directly by this seat (issue #125, ops-write
sandbox split); the dispatching session applies the write.
