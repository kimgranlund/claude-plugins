# issue-sorter firing report — 2026-08-30T00:06:54Z

Repo: kimgranlund/claude-plugins. Backend: git-native (ADR-0002/ADR-0003), discovery via `gh` only.

## Discovery window
`updated:>=2026-08-29T00:11:19Z -label:backlog -label:roadmap`, prior checkpoint `2026-08-29T00:11:19Z`.

Surfaced 8 touched issues (#609, #978, #987, #991, #993, #994, #995, #996) and 3 touched PRs
(#989, #990, #992) — discovery/context only.

## Classification / trust-check
Every issue authored by `kimgranlund` (== `friendlies.json` `policy.confirmed_by`) — no
`user-signal` tag needed on any item. Every issue already carried its correct kind label
(and severity/size where applicable) from a prior firing:

- #996 task, size:small
- #995 task, size:small
- #994 task, size:small
- #993 task, size:small
- #991 bug, minor
- #987 bug, major (actioned prior firing; PR #990 open against it)
- #978 task, size:big (PR #989 open against it)
- #609 bug, major, doing

0 items reclassified, 0 labels applied, 0 duplicates found, 0 items minted, 0 items held.

## friendlies.json / step 7
Already bootstrapped (2026-07-18). No unknown author encountered this firing — no growth.

## github_mcp_offer / step 8
Already recorded `accepted` (with a later same-day override on record) and `.mcp.json` already
carries the matching `mcpServers.github` entry — step 8 not applicable this firing (decision
already recorded), not re-offered.

## held-items.md
Unchanged — no items held this firing.

## Source reachability
`github` (gh CLI): reachable for the full firing. Checkpoint advanced.

## Outcome
0 items dropped, 0 auto-created for an unknown author, 0 checkpoint advanced-on-failure. Clean,
no-new-action firing.
