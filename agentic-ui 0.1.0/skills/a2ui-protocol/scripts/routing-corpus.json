{
  "skill": "a2ui-protocol",
  "_note": "Deep-review corpus for the a2ui-protocol knowledge pack. Positives: imperative / diagnostic / symptom phrasings of wire-protocol + renderer-mechanics asks across the six axes (lifecycle, bindings, lists, actions/input, functions/checks, errors/versions). Negatives drawn adversarially from the dangerous near-neighbors: a2ui-composer (compose a payload), a2ui-builder (build renderer/catalog source), a2ui-reviewer (grade), and the future sibling packs a2ui-catalog-design / a2ui-training-corpus / a2ui-conversational-agent; plus generic component/design neighbors.",
  "positives": [
    "why did this a2ui payload fail validation",
    "what does a binding look like on the wire",
    "how do dynamic lists render in a2ui",
    "what is the difference between an action and a function call",
    "which error code does an unknown component type produce",
    "how does updateDataModel propagate to the widgets",
    "why did only one widget update when I changed the data model",
    "what does createSurface do and what state does it set up",
    "how does actionResponse correlate back to my action",
    "what does wantResponse mean on an action",
    "why is @index erroring outside a list",
    "how does two-way input binding write back to the data model",
    "what does ${...} interpolation resolve to",
    "why did my callFunction for required get rejected",
    "what is callableFrom and what is its default",
    "VALIDATION_FAILED vs INVALID_FUNCTION_CALL — when do I get each",
    "which protocol versions does the renderer support",
    "what happens to a message with an unsupported version",
    "does a failed check send an error to the server",
    "why does a positional list have no per-item key",
    "what fields are in the action message",
    "how is a relative path resolved inside a list item"
  ],
  "negatives": [
    "compose an a2ui payload for a checkout form",
    "author the message stream for this surface",
    "the renderer won't paint this payload, fix my payload",
    "add a new catalog type for a date picker",
    "implement LLD-C6 in the renderer",
    "the validator rejects this message, fix the validator code",
    "what coverage policy should the catalog have",
    "should this control be catalogued or excluded",
    "grade this a2ui payload against its rubric",
    "score this catalog row",
    "judge this corpus record",
    "what is the corpus record schema",
    "how does corpus admission dedup and heal a record",
    "how does the live agent turn a click into an agent turn",
    "which provider and model does the live demo use",
    "build a ui-button web component",
    "what pattern is this screen using",
    "pick an accessible contrast ratio for this text"
  ]
}
