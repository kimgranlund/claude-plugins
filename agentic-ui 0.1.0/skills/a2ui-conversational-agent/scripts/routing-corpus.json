{
  "skill": "a2ui-conversational-agent",
  "_note": "Routing corpus of record (minted 2026-07-07; SSE-wire-contract axis + t20/n18 added 2026-07-13). Positives: imperative / diagnostic / symptom / indirect phrasings of live-agent asks across the seven axes (transport seam, turn/session, produce loop, provider seam + trust boundary, the Anthropic SSE wire contract, switcher/overlay, the ADR-0088 gap). Negatives drawn adversarially from the dangerous near-neighbors: a2ui-protocol and a2ui-catalog-design (sibling ANSWER packs — the wire shape / catalog line), a2ui-training-corpus (this pack CALLS retrieve(); it does not own retrieval), a2ui-composer / a2ui-builder (the MAKER agents), system-planner (ADR/SPEC authoring), plus knowledge-author.",
  "positives": [
    "how does the transport swap from the recorded backbone to a live model",
    "how does clicking a button in the canvas become a chat turn",
    "wire up a new LLM provider for the live agent",
    "add another model to the provider switcher",
    "where does the API key live and is it safe from the static build",
    "why does every interaction with the surface force a full agent turn",
    "why can't the agent tell me why it chose a Card instead of a Table",
    "what is the produce() generate-heal-validate self-correct loop",
    "how is the system prompt built for the live agent",
    "what does the in-chat provider and model switcher do",
    "explain the AgentTransport seam and the zero-edit swap",
    "the live agent says no API key found but I set one in .env",
    "how do intent turns differ from client turns",
    "what is validate-then-stream and why does nothing invalid ever render",
    "how would we add a note channel so the agent can reply in prose",
    "what is the dev-proxy trust boundary and the PAIR allowlist",
    "why is the VITE_ prefix a footgun for the live agent key",
    "how does the agent continue the conversation after a client message",
    "what is ADR-0088 proposing for conversational reasoning",
    "why is the live agent's text streaming in truncated or dropping fragments"
  ],
  "negatives": [
    "why did this A2UI payload fail validation",
    "what does a binding or a check look like on the wire",
    "how do dynamic lists render with a child template",
    "which wire error code does an unknown function raise",
    "how do I add a new component type to the catalog",
    "why is this control uncatalogued and what is our coverage policy",
    "how does the renderer resolve a catalog type to a ui-* widget",
    "what should this bindable prop be named in the catalog",
    "how is a corpus exemplar record scored and admitted",
    "how does retrieve() rank exemplars over the judged shard",
    "compose an A2UI payload for a settings form",
    "author the message stream for this generative UI surface",
    "implement the note channel in the produce loop source",
    "add the Anthropic SSE parsing to the provider adapter",
    "write ADR-0088 and mark it accepted",
    "revise the live-agent SPEC to add the note requirement",
    "add a new axis to this knowledge pack",
    "why did this A2UI JSONL message fail to parse on the wire"
  ]
}
