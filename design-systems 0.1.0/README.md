# design-kits — design-system export authoring + the Material Design token grammar

Sibling plugin to forge (which authors the harness) and docs (which authors documents);
design-kits authors, regenerates, and grades the export bundles a generative design agent
consumes — Claude Design/Claude Code, Figma Make, Google Stitch — and carries the Material Design
token grammar those exports can extend. Two decomposition candidates (design-system-export and
material-design-tokens) merge here on explicit direction: the token skills exist chiefly to be
cited from the exports, so one plugin boundary serves both.

## Map

| Artifact | Type | Invocation | What it carries |
|---|---|---|---|
| `skills/make-design-kit` | Procedural skill | both (model + user) | The cross-platform hub: core+profiles architecture, prose-over-tokens and terminal-value doctrines, `references/rubric.md` (H1–H7) and `references/shared-doctrines.md`; dispatches the three platform siblings and design-kit-checker |
| `skills/make-dscard-kit` | Procedural skill | both | Claude Design / Claude Code bundle authoring, from a corpus (css/tokens/codebase/brand deck) or a bare brief — corpus census, six-slot Root Brand Architecture, DESIGN.md spine + tokens.json + `@dsCard` previews, round-trip check; the Ultimate Tokens grammar, OKLCH frontmatter, `scripts/bundle_gates.py` (B1–B7, count-exact contrast disclosure), `evals/assertions.md` |
| `skills/design-md-rules` | Declarative skill | both | First-party ground truth for the DESIGN.md format (written by Claude Design itself): anatomy + the openness doctrine ("a SKILL for a brand" — the spine is a floor, not a ceiling), the six-slot Root Brand Architecture, the token grammar, `@dsCard` card syntax; the knowledge substrate dscard's method steps 1–2 and 5 lean on |
| `skills/make-figma-make-kit` | Procedural skill | both | Figma Make `guidelines/` folder authoring — Guidelines.md entry + routed `foundations/`/`components/` leaves; `scripts/make_guidelines_check.py` (D1–D11), the only gate since Make validates nothing natively |
| `skills/make-stitch-kit` | Procedural skill | both | Google Stitch single-file DESIGN.md authoring — YAML frontmatter tokens + 8 canonical sections; `scripts/prelint.py` (G1–G7, R1–R5) ahead of `npx @google/design.md lint` |
| `skills/figma-plugin-facts` | Declarative skill | both | Figma Plugin-API knowledge earned shipping the ultimate-tokens plugin — variables/aliasing, bound paint+text styles, sandbox/VM traps, interchange schemas, pluginData; every claim confidence-marked ([verified]+date / [inferred] / [drift-prone]); minted by the ultimate-tokens session (then named `nonoun-color-tokens`; commit 5f3ee90), absorbed into 0.4.0 |
| `skills/icon-rules` | Declarative skill | model-only | Icon-system world model answered from a cited corpus — per-system construction table (grid/stroke/corners), size ladders + WCAG target floors, style families + the no-mixing law + NN/g metaphor doctrine, icon a11y + RTL mirror/never-mirror taxonomy; `references/` — grid-and-construction, sizing-and-placement, style-and-metaphor, accessibility-and-rtl, sources |
| `skills/material-color-facts` | Declarative skill | model-only | Consumption guide for the `--md-sys-color-*` 59-role semantic layer extending Material 3 (53 nonoun + 6 ADIA state-extension roles) — pairing laws, state families, tonal variants, surface/scrim ladders |
| `skills/material-shape-facts` | Declarative skill | model-only | Consumption guide for `--md-sys-size-*` / `--md-sys-radius-*` / `--md-sys-space-*` — control ramp, corner scale, spacing/inset/gap ladders, the centering law |
| `skills/material-type-facts` | Declarative skill | model-only | Consumption guide for the `--md-sys-typescale-*` fifteen-voice type scale — thirteen voices at uniform SM/MD/LG levels plus `ui-control`/`ui-widget` on their own full XS…2XL ramp — voice x level selection, baked leading/tracking/paragraph rhythm, sibling weights, control-text composition into geometry |
| `skills/material-token-facts` | Declarative skill | model-only | Cross-domain glossary answering what each color/typography/geometry token MEANS and when to reach for it — one line per token or per graduated group (e.g. surface-lowest→highest); routes all binding/recipe asks to the three sibling consumption guides |
| `skills/material-motion-facts` | Declarative skill | model-only | Consumption guide for the `md.sys.motion.*` layer — the verified easing/duration token table, M3 Expressive spring tokens (spatial/effects x fast/default/slow), pairing laws, platform-drift map; binds to M3 spec defaults until a nonoun motion export exists (disclosed in provenance) |
| `agents/design-kit-checker` | Agent | spawned | Independent critic for ONE export corpus (a bundle, a DESIGN.md, a guidelines folder, or a cross-platform set) — generator≠critic; preloads `make-design-kit`; runs each owning sibling's checker plus the platform linter as the gate of record, then judges against the bound rubric |

Cross-plugin seams (soft, by design): `make-design-kit`'s validation loop and
`design-kit-checker`'s output contract mention `linguistic-techniques`, `linguistics-reviewer`,
and `handoff-compose` — artifacts that now live in the forge plugin — and each degrades to an
inline checklist or fallback report shape when forge isn't installed. No hard edges (preloads or
literal script paths) cross the plugin boundary; every same-plugin script/rubric reference among
the four `make-design-kit*` skills and the agent resolves via `${CLAUDE_PLUGIN_ROOT}`.

## ADR-0006 transition table (renamed 2026-07-21)

The PLUGIN renamed: `design-systems` → `design-kits` (new install identity; the directory
`design-systems 0.1.0/` keeps its frozen path — see the workspace CLAUDE.md alias table). Old
handles remain greppable only in ledgers, CHANGELOGs, ADRs, and attics.

| Old name | New name |
|---|---|
| `design-system-hub` | `make-design-kit` |
| `design-system-author-dscard` | `make-dscard-kit` |
| `design-system-author-figma-make` | `make-figma-make-kit` |
| `design-system-author-google-stitch` | `make-stitch-kit` |
| `design-md-format` | `design-md-rules` |
| `iconography` | `icon-rules` |
| `figma-plugin-api` | `figma-plugin-facts` |
| `material-design-color-tokens` | `material-color-facts` |
| `material-design-geometry-tokens` | `material-shape-facts` |
| `material-design-motion-tokens` | `material-motion-facts` |
| `material-design-typography-tokens` | `material-type-facts` |
| `material-design-token-semantics` | `material-token-facts` |
| `design-system-reviewer` (agent) | `design-kit-checker` |

v1.0.3 · assembled 2026-07-21 · 1.0.3: ADR-0006 docs-rename sweep — live references rewritten (make-doc fences, docs plugin mentions); pointer updates only · v1.0.2 · assembled 2026-07-21 · 1.0.2: ADR-0006 teamwork-rename sweep — design-kit-checker's code-checker fence repointed; pointer updates only · v1.0.1 · assembled 2026-07-21 · 1.0.1: ADR-0006 screens-rename sweep — live references rewritten (icon-rules + material-motion-facts fences now name the screens plugin, suites); pointer updates only · v1.0.0 · assembled 2026-07-21 · 1.0.0: ADR-0006 rename PR 5/9 — the PLUGIN renames design-systems → design-kits and all twelve members + the reviewer agent take the simple paradigm (transition table above). MAJOR bump — breaking. Workspace sweep (79 files, ledger history + forging evidence excluded; one historical suite-note corruption caught and restored with the true two-rename lineage); baseline = color/typography-wave results for 6 suites + a fresh 111-case run for the other 6; post-rename re-measure in the campaign PR · v0.10.3 · assembled 2026-07-21 · 0.10.3: ADR-0006 typography-rename sweep — live references rewritten (material-design-typography-tokens fence, platform-author references); pointer updates only · v0.10.2 · assembled 2026-07-21 · 0.10.2: ADR-0006 color-rename sweep — live references to color's old member handles rewritten (design-system-reviewer, platform-author fences and references, suites); pointer updates only · v0.10.1 · assembled 2026-07-19 · 0.10.1: knowledge-pack factory-route convention repointed from scribe's
retired `knowledge-forge` to forge's `pack-forge` (workspace-wide rename campaign) —
iconography/material-design-token-semantics' reference to its authoring factory updated; no
functional/behavior change, a naming correction only. · v0.10.0 · assembled 2026-07-17 · 0.10.0: material-design-geometry-tokens (+ material-design-token-semantics'
geometry.md) re-synced to the nonoun engine's TKT-0010 shape (2026-07-16) — `padding`/`edgePadding`
are RENAMED and reformulated into FOUR pads: `padding-narrow` = (height − icon)/2 (the centering
law, was `pad`) and `padding-wide` = (height − caret)/2 (was `pad-edge`, a flat height/2 — the new
law keys the bare/caret edge on the caret glyph and lands much tighter, e.g. `md` 14px → 7.5px),
plus `-compact` twins (`padding-narrow-compact` / `padding-wide-compact`) that also absorb the
control's own gap, for dense layouts; half-pixel results (7.5px, 5.5px) are EXACT by design, never
rounded. The control ramp grew from 9 to 11 fields per step. `gap` is no longer `font/2` — it's a
hand-CALIBRATED unit per step (`GAP_UNIT`), scaled by baseHeight and the density knob.
`scripts/dimension-check.mjs`'s FIELDS/GROUPS arrays updated to match (66 ramp tokens, was 54);
selftest passes and the checker was proven against a freshly-generated real export from the live
engine (`geomTokensCSS`), not just its synthetic fixture. Fixed in the same pass: controls.md's
stale `caret` recipe value (14px → 13px, matching the engine's own power law) and
material-design-token-semantics/geometry.md's `-font` row, which still described the retired
partial `label`-voice composition (sm/md/lg only) instead of the current `ui-control` voice riding
the full six-step ramp (TKT-0008, already corrected elsewhere in this plugin) · v0.9.0 · assembled 2026-07-16 · 0.9.0: material-design-typography-tokens (+ material-design-token-semantics' typography.md) re-synced to the nonoun engine's TKT-0008 shape (2026-07-16) — the type scale grew from thirteen voices to fifteen: `ui-control` and `ui-widget` are new, the ONLY voices riding the full six-level XS…2XL ramp (every other voice stays SM/MD/LG); they took over the BOX job (`-line-single`, 1.0×size paragraph rhythm) for interactive/widget chrome, and `label`/`body-mono`/`label-mono` became PROSE the same window (previously BOX — a control's own text is `ui-control` now, not `label`); `ui-control` also composes directly into `material-design-geometry-tokens`' control-box `-font` field at every one of geometry's six steps, retiring the old partial (SM/MD/LG-only) composition. `scripts/type-check.mjs` needed no code change — its dynamic parse already discovers whatever voices/levels/props are actually in a bound export and infers BOX-ness from `-line-single` presence, proven against a synthetic 15-voice fixture including the mixed 3-level/6-level ramp · v0.8.1 · assembled 2026-07-16 · 0.8.1: figma-plugin-api's user-invocable flipped to false (ruled 2026-07-16, harness-audit) — answers-only knowledge pack per the species table · v0.8.0 · assembled 2026-07-16 · 0.8.0: CLI/registry integration patterns (Issue #10) — shared-doctrines gains §7, the destructive-op ladder for export regeneration (evaluate → staged build → diff-and-present with carrier hand-edits named individually → apply-on-approval; receipts stay outside the ladder and regenerate every build); the hub's Method step 1 probes the project's real state and binds each discovered fact to its behavioral consequence; design-system-author-dscard's Regenerate cites the ladder; figma-plugin-api gains the tombstones + mutation-gated live re-verification discipline (named-hallucination fences as incidents bite; [drift-prone] rows gating mutations re-verify at consult time). Non-adoptions recorded in Issue #10: shadcn's honor-system verification, react-doctor's live-curled remote prompt · v0.7.5 · assembled 2026-07-15 · 0.7.5: subfolder conformance (ruled 2026-07-15: the sanctioned skill-subfolder set is evals/references/scripts/assets — release_gate G2 now warns on any other): design-system-author-dscard's examples/ (the mini-bundle cold-start template) folded into references/, three referrers repaired (SKILL row, gates.md, dialect.md) · v0.7.4 · assembled 2026-07-15 · 0.7.4: ruff sweep (ADR-0002's G11 style tier arriving): make_guidelines_check.py's oklab math renamed its E741-ambiguous `l/m/s` locals to `l3/m3/s3` — the fix wave's one incomplete rename was caught by this script's own selftest before landing (12/12 fixtures green). No behavior change · v0.7.3 · assembled 2026-07-14 · 0.7.3: the three Material `.mjs` bind/lint checkers (color's `token-check.mjs`, geometry's `dimension-check.mjs`, typography's `type-check.mjs`) retrofitted with `selftest` mode per forge's `script-authoring-standards` — the mechanization pair's flagged follow-up, closed same day. Each proves a full-role/token/voice fixture binds clean, one dropped item fails (naming it), a `var()`-wrapped consumer lints clean, and a raw hardcoded literal is caught — inversion + reverse controls both ways, subprocess-verified against the real script. `release_gate.py` G4 now sweeps 5 scripts here (was 2). No behavior change to the checkers themselves · v0.7.2 · assembled 2026-07-14 · 0.7.2: material-design-token-semantics proven live — its first evals/evals.json (16 cases; closes the plugin's one G7 warn) + full-plugin /eval-run (12 suites, 236 cases, blind judges). Round 1: 233/234 — the one real defect was the skill's own description planting a choosing-ask ('which scrim strength for a drawer') in a meaning-glossary's trigger list, grammatically identical to its own n01 no-trigger; ruled to consumption (color-tokens), description example replaced with 'what's the difference between the scrim strengths', reciprocal t12 added to color-tokens. Round 2 proved the scrim boundary both directions but flipped two round-1 passes (raw-value n04, pairing-law t11); t11's steal closed by adding 'pairing laws' to the glossary's ANSWERS-meaning fence, n04 self-restored. Round 3 final: 12/12 suites clean, semantics 16/16, color-tokens 23/23 · v0.7.1 · assembled 2026-07-14 · 0.7.1: displayName 'Design Systems' added to the manifest — plugin naming hygiene ruled 2026-07-14: Title Case display names with UI/LLM acronyms uppercased (marketplace entries carry the same field; Claude Code ≥2.1.143, falls back to name) · v0.7.1 · assembled 2026-07-14 · 0.7.1: the token family regenerated against the nonoun engine's now-current shape (13 typography voices + the fixed SM/MD/LG ramp + sibling weights, PR #279 onward) — material-design-typography-tokens rewritten in full (SKILL.md + all 4 references; the source engine, not any one bound export, is now the target — a project's own export must be re-bound and confirmed current before this doc is trusted against it); material-design-geometry-tokens' controls.md corrected for the renamed `label` voice and the narrowed type/geometry composition (only sm/md/lg now typed, xs/xl/2xl fall back to geometry's own law); material-design-color-tokens' role-table.json provenance path + role-count breakdown corrected (53 nonoun + 6 ADIA extension = 59, not a flat 59 from the engine). New: material-design-token-semantics — a cross-domain glossary answering what each color/typography/geometry token MEANS (one line per token or per graduated group, e.g. surface-lowest→highest), routing all binding/recipe work to the three sibling consumption guides; grounded directly in their own battle-tested prose plus the current engine source, no external research wave · v0.6.1 · assembled 2026-07-12 · 0.6.1: taxonomy drift disclosed, not forced — material-design-typography-tokens gains the dated drift note (the engine's 2026-07 voice rewrite, PR #279, postdates the ADIA export this skill is bound to; read-what's-in-the-file remains the rule; re-sync lands when the kit maintainer regenerates), design-md-format's preview-card voice grouping updated to the new names with the pre-2026-07 export disclosure · v0.6.0 · assembled 2026-07-12 · 0.6.0: seat-ladder realignment (forge 1.22.0): design-system-reviewer opus→fable+high · v0.5.6 · assembled 2026-07-10 · 0.5.6: the TEXT-RENDERING BASELINE joins the DESIGN.md Typography contract (design-md-format anatomy.md §3 + grammar.md Scales): the macOS smoothing pair, `text-rendering: optimizeLegibility`, `font-optical-sizing: auto`, `font-synthesis: none` (weights resolve from the real font, never synthesized), kerning + common ligatures, and the `code, pre, kbd { font-variant-ligatures: none }` exception — always included, a Typography section omitting the block is incomplete (standing user rule 2026-07-10; the ultimate-tokens exporters emit + gate the same block in DESIGN.md, the Make styles.css/typography.md, and every @dsCard preview) · assembled 2026-07-10 · 0.5.5: author attribution corrected to Kim G / NONOUN (was the Agentic Harness placeholder) · assembled 2026-07-09 · 0.5.4: figma-plugin-api amended — `setPluginData` is namespaced by the plugin's `id`, so an `id` rename ORPHANS every pre-rename key and no migration can be written (new section in `document-plugindata.md`, [verified] against ultimate-tokens PR #247 / ADR-014); source repo `nonoun-color-tokens` renamed to `ultimate-tokens` across all provenance lines and doctrine citations (historical changelog entries preserved under the old name) · 0.5.3: estate-wide blind run: 78/81 pre-reassignment — both motion packs and geometry 50/50 clean (all same-day tunings proven), cross-plugin fences resolve to true owners for the first time; two second-strike reassignments per the 0.2.3 precedent (design-md-format t06 'pairing law for on-colors' → material-design-color-tokens t11; iconography t13 '24dp touch target' → geometry t11); iconography t15 first-strike watch (→ focus-verify) · assembled 2026-07-09 · 0.5.2: material-design-motion-tokens re-synced by the kit maintainer — a nonoun motion export now exists (the nonoun color-tokens repo's PR #243: the tokens.json motion block + the DESIGN.md Motion rulebook, a deliberate subset of the M3 table — 7 easings, no springs, plus motion.animatable); bind step 1 + tokens.md amended with dated notes, the export repo's own suite is the drift gate; ledger + phantom-name hygiene folded in by the estate session (G10/G8) · assembled 2026-07-09 · 0.5.1: Phosphor Icons ruled the estate's default icon library (standing user preference, 2026-07-09) — encoded in iconography as a house default + deviation doctrine (overrides: --md-sys kits → Material Symbols, Apple-native → SF Symbols, existing family → keep), with verified facts from the phosphor-icons/homepage README (1,248+ icons, six weights incl. Fill/Duotone, 16×16 grid, MIT) in the corpus and a Phosphor row in the construction table; library-choice trigger + t16 added same-change · assembled 2026-07-09 · 0.5.0: iconography — the gap analysis' parked watch item built via the proven template (four research waves, cited corpus, independent skill-auditor + linguistics-reviewer, all findings applied pre-ship — including an audit-caught inverted forced-colors claim, corrected against live MDN with a dated ledger amendment, and the NN/g 5-second rule restored to designer-side); reciprocal fences closed in material-design-geometry-tokens (icon grid/style → iconography, n08) and design-md-format (Iconography-section CONTENT → iconography, n10); blind check over the three touched suites 60/64→tuned: geometry's t09/t10 losses (this wave's own trims) restored verbatim; iconography t13 and design-md-format t06 recorded as first-strike shared-territory steals, watch for second; geometry description re-trimmed under the 1024 bar · assembled 2026-07-09 · 0.4.0: two new packs. figma-plugin-api — minted by the nonoun-color-tokens session (commit 5f3ee90, handoff absorbed same day: folded into this bump rather than a separate patch — one wave, one trust decision; its per-skill evals/evals.json stays the single corpus of record, no estate-wide merge). material-design-motion-tokens — fourth member of the token family (verified md.sys.motion easing/duration table + M3 Expressive spring tokens with published stiffness/damping; pairing laws; platform-drift map), authored against the M3 spec with the no-export deviation disclosed in provenance (siblings bind to the ADIA export; no motion export exists yet — re-sync owner named); FLOOR audit's two provenance majors fixed pre-ship (material-web version corrected to v2.x, citation repaired to css/motion.css with both wild CSS shapes documented); reciprocal motion fences closed in all three sibling descriptions + suites (n11/n07/n08), descriptions re-trimmed under the 1024 bar; blind /eval-run over all 10 suites (191 cases): 190/191 — the one failure a leak of motion-tokens onto a general 'how long' ask whose true owner (motion-design) is cross-plugin and off-menu; fence tuned to carry the phrasing verbatim, suite annotated · assembled 2026-07-09 · 0.3.1: hygiene pass — google-stitch gains its first eval suite (closing the estate's last E6 coverage gap; first outing 153/153 with the family), material-design token descriptions rewritten under the 1024 bar, two ADR-0001 sweep artifacts repaired (hub's broken fence sentence, stitch's 'design-system-hub hub' stutter), phantom fences repointed · assembled 2026-07-09 · 0.3.0: BREAKING (ADR-0001) — design-system-author→design-system-hub (its own charter line: 'the cross-platform hub'); the three -author- platform siblings deliberately keep their names (recorded asymmetry); design-system-reviewer's preload updated · assembled 2026-07-09 · 0.2.3: eval-run tuning — design-md-format claims 'explain the frontmatter schema' verbatim; the hub's single-platform fence now states that one bundle from source material with no platform named defaults to dscard; post-tuning 140/141 · assembled 2026-07-09 · 0.2.2: design-system-reviewer fallback 'Tests run'→'Tests/checks run' (harness-audit finding, estate-wide sweep) · assembled 2026-07-08 · 0.2.1: hub CHANGELOG regains the kit-fidelity entry (PR #229) — it had been written only in the ~/.claude mirror, recovered at that tree's retirement · 0.2.0: absorbs Claude Design's contributed pair — design-md-format survives as the family's first-party format-knowledge seat (fences repointed, --md-sys-* and corpus-authoring eval routings corrected, tokens-in-companion-carrier ruling in anatomy.md); design-md-author merged into design-system-author-dscard per a synthesis verdict (total routing collision: same platform, same triggers, two methods) — dscard gains corpus census, the six-slot Root Brand Architecture step, the round-trip check, its first eval suite (also closing a standing G7 warn), and the inherited behavioral assertions; as-received originals preserved at commit 7ca2d96 · also folds in the previously-unbumped kit-fidelity change (nonoun-color-tokens PR #229): bundle_gates.py G1 reports count-exact DISCLOSED for receipt-disclosed sub-4.5:1 pairs — the disclose-not-enforce accessibility ruling, now ratified and consistent across dscard's SKILL.md, gates.md, and design-md-format's grammar.md · 0.1.0: initial: ported from ~/.claude/skills + ~/.claude/agents/design/design-system-reviewer as part of a plugin-decompose partition; merges the design-system-export and material-design-tokens candidate clusters on explicit direction
