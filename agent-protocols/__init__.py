"""agent-protocols — generated by harness_emit.py from .claude-plugin/plugin.json. Do not edit."""
from pathlib import Path

_HERE = Path(__file__).resolve().parent

def register(ctx):
    ctx.register_skill('a2a-protocol-facts', _HERE / "skills" / 'a2a-protocol-facts' / "SKILL.md", description='Explains the A2A (Agent2Agent) wire protocol + this estate\'s zero-dep implementation (@agent-ui/a2a, spec v0.3.0). Use for "what is an AgentCard", "Message vs Task vs Artifact", "which TaskState transitions are legal", "what goes in a TextPart/DataPart/FilePart", "which JSON-RPC method sends/polls", "how does SSE streaming work", "why does v1.0 break our wire". ANSWERS from a cited corpus; does not build. NOT for rendering payload content (a2ui-protocol-facts); NOT for building an agent (make-a…')  # one per skill
    ctx.register_skill('a2a-training-facts', _HERE / "skills" / 'a2a-training-facts' / "SKILL.md", description='Answers how the A2A concept/demo teaching corpus is structured, cited, and admitted (@agent-ui/a2a\'s corpus shards — the a2ui-training-facts sibling for the agent-to-agent layer). Use for "what does an A2A corpus record contain", "how do I add a concept record", "what makes a record admissible", "why was this record rejected", "how do citations work", "why is authoring order load-bearing". ANSWERS from the cited corpus; does not curate. NOT for the A2UI exemplar corpus (a2ui-training-facts); NO…')  # one per skill
    ctx.register_skill('a2ui-catalog-facts', _HERE / "skills" / 'a2ui-catalog-facts' / "SKILL.md", description='How an A2UI catalog is designed and extended in @agent-ui/a2ui — the row contract, factory/resolution, naming law, two-tier extensibility, security allowlist + conformance, coverage policy. Use for "add a component to the catalog", "why is this uncatalogued", "our coverage policy", "payload fails CATALOG", "name this bindable prop". ANSWERS from the cited corpus; does not build. NOT for the wire shape a component renders (a2ui-protocol-facts); NOT for corpus exemplars (a2ui-training-facts); NOT…')  # one per skill
    ctx.register_skill('a2ui-chat-agent-facts', _HERE / "skills" / 'a2ui-chat-agent-facts' / "SKILL.md", description='The A2UI LIVE-AGENT in @agent-ui/a2ui — a real LLM emitting A2UI. Use for "swap recorded → live transport", "how does a UI click become a chat turn", "wire a new provider / add a model", "the produce() self-correct loop", "what is validate-then-stream", "the dev-proxy / PAIR trust boundary", "why can\'t the agent explain its choice". Covers the AgentTransport seam, Turn/Session + reducer, the provider seam + dev-proxy, the Anthropic SSE contract, the conversational channel (ADR-0088). ANSWERS; d…')  # one per skill
    ctx.register_skill('a2ui-protocol-facts', _HERE / "skills" / 'a2ui-protocol-facts' / "SKILL.md", description='The A2UI wire protocol + this repo\'s zero-dep renderer (@agent-ui/a2ui). Use for "why did this payload fail validation", "what does a binding/action/check look like on the wire", "how do dynamic lists render", "action vs function call", "which error code and why". Covers the message lifecycle, the Binding union, data-model updates, dynamic-list templates, DynamicString interpolation, the callRendererFunction RPC, the error taxonomy, version pinning. ANSWERS from a cited corpus; does not build.…')  # one per skill
    ctx.register_skill('a2ui-training-facts', _HERE / "skills" / 'a2ui-training-facts' / "SKILL.md", description='The A2UI training-corpus subsystem in @agent-ui/a2ui — the curated store conditioning and measuring generation. Use for "how do I curate/admit a corpus record", "why was this record quarantined/rejected", "what error code does admission raise", "how does retrieval pick exemplars", "what does the healer fix". Covers the record schema, sub-corpora + no-leak invariant, dedup, the admission gate + healer, the judge adapter, retrieval + export. ANSWERS from a cited corpus; does not build. NOT for th…')  # one per skill
    ctx.register_skill('check-a2a-isolation', _HERE / "skills" / 'check-a2a-isolation' / "SKILL.md", description='Prove that two (or more) agents interact ONLY over the declared wire — no shared context, no prompt leakage, no side-channel. Use for "prove no cross-contamination", "are these agents really isolated", "verify the seats can\'t see each other\'s context", "make the isolation claim testable". Emits an isolation proof: per-seat canaries, wire-origin audit, closed message schemas, byte-complete boundary recording, negative controls. Generalizes to any multi-agent system claiming context separation. N…')  # one per skill
    ctx.register_skill('make-a2a-agent', _HERE / "skills" / 'make-a2a-agent' / "SKILL.md", description='Design and build an A2A-conformant agent — server or client side. Use for "make two agents talk", "build an A2A server", "write the AgentCard", "expose my agent over A2A", "wire the task lifecycle", "add SSE streaming to my agent", "my agent fails the other side\'s validation". Method: outside-in from the card\'s capabilities, inside-out from the Message/Task state machine, reconciled before code — conformance is a gate. NOT protocol facts (a2a-protocol-facts answers; this skill builds); NOT prov…')  # one per skill
