{
  "skill": "a2ui-training-facts",
  "_note": "Net-new mint 2026-07-07 (the 4-pack A2UI split — sibling of a2ui-protocol-facts / a2ui-catalog-facts / a2ui-chat-agent-facts). Positives span imperative / diagnostic / symptom / indirect phrasing across the pack's six axes (record schema, exemplar/eval split, canonicalization+dedup, admission+healing, judge/verdict adapter, retrieval+repair). Negatives drawn adversarially from the live neighbors: a2ui-protocol-facts (the wire shape a record's output conforms to), a2ui-catalog-facts (catalog rows + coverage the tier-1 check validates against), a2ui-chat-agent-facts (the live demo that CALLS retrieve()), a2ui-corpus-curate (the DOING of curation, not the world-model), the a2ui-builder / a2ui-reviewer agents (build source / judge a record).",
  "siblings_in_repo": [
    "a2ui-protocol-facts",
    "a2ui-catalog-facts",
    "a2ui-chat-agent-facts",
    "a2ui-corpus-curate"
  ],
  "positives": [
    "why was this corpus record quarantined and can it ever come back",
    "what does admission raise for a candidate that references a component not in the pinned catalog",
    "our judge grades records against target and some score garbage, is that a corpus bug",
    "why is qualityScore the minimum across dimensions instead of an average",
    "what exactly does the healer fix and what is it forbidden from touching",
    "explain the difference between the exemplar and eval sub-corpora and the no-leak invariant",
    "why does admission reject every eval record right now",
    "how does the corpus decide two exemplars are duplicates",
    "what fields must a corpus record carry and which are optional",
    "walk me through the admission pipeline stages in order",
    "how does retrieval pick the top-k exemplars for a prompt",
    "what happens to stored records when the catalog or protocol version changes",
    "why is description required but target optional in a corpus record",
    "what does status repaired mean versus valid versus quarantined",
    "why can't the judge just run inside the admit function",
    "what is the verdicts file and why must its rubricVersion match the rubric doc",
    "why did admission reject this exemplar with E_PIN instead of E_SCHEMA",
    "how do I get a below-bar record back into the corpus after it was quarantined",
    "is a multi-surface a2uiOutput allowed in a corpus exemplar",
    "why does canonicalization renumber ids but never rewrite json-pointer paths"
  ],
  "negatives": [
    "why did this A2UI payload fail validation at the renderer",
    "what does a data binding or action look like on the A2UI wire",
    "how do dynamic list templates render positionally",
    "how do I add a new component type to the default catalog",
    "what is our catalog coverage policy, whole-fleet or subset",
    "why did this payload fail CATALOG at render time",
    "how does the live demo swap from the recorded transport to a real LLM",
    "how does a UI click become an agent chat turn",
    "wire a new LLM provider into the live agent and add its model",
    "where does the live agent's API key live and is it safe",
    "admit this authored seed into the corpus for me",
    "back-score the phase-1 records and quarantine the ones below bar",
    "fix the bug in admit.ts where dedup registers too early",
    "judge this corpus record against the a2ui-corpus rubric and emit its verdict",
    "compose an A2UI payload that renders a settings form",
    "what should this bindable prop be named in the catalog row"
  ]
}
