# Changelog — orchestration-design

## 2026-07-03 — excellence-campaign batch 2 fixes
Deep-review fixes against `skills-audit/references/standard-of-excellence.md` v2 (ledger: `skills-audit/campaign/batch-2/orchestration-design.findings.jsonl`):
- **A3** (escalated — this skill teaches the description interface corpus-wide) `references/best-practices.md`'s Frontmatter section no longer teaches the pre-fence model: new "The fence is part of the interface" paragraph — descriptions PULL owned asks and REPEL neighbors' via description-altitude NOT-clauses; a body cross-reference is composition, not a fence; fences are reciprocal; the corpus-level law cited (standard-of-excellence §S2). Sits alongside the existing `skills:`-preload guidance.
- **S2** the description gains its own fences, in the canonical form the skill now teaches: NOT for a whole-corpus or team audit (skills-audit / agents-audit); NOT for a single agent definition (agent-author); NOT for when the next turn fires (loop-design — the reciprocal of the fence loop-design gained this batch).
- **M2** stem and house vocabulary added ("orchestration" itself now leads, "preload", "fan-out" triggers); routing corpus of record checked in (`scripts/routing-corpus.json`, 10 pos / 11 neg) — first pass exposed the fenceless description (precision 0.588, 7 grabs); after fencing F1 0.833, recall 1.000, every grab dispositioned (corpus-universal-token proxy artifacts against fenced territory).
- **N3** "review my frontmatter" scoped to "review my wiring/integration frontmatter" — single-definition frontmatter belongs to agent-author / skill-author.
- **S1** Improve and Update organs added (maker template); Update names both drift triggers — the build moves (re-verify fields, D4's verified-against-build anchor) and the capability set moves (re-review descriptions, preloads, fences).
- **A4** the no-checker choice is now argued per-gate: D2 is judgment (interface precision only shows against the sibling set); D4's YAML-validity checker is queued, not built — until it lands, D4 scores by inspection, skipped-not-passed where uninspected.
- **L** SKILL.md closes on the done/NOT-done predicate.
