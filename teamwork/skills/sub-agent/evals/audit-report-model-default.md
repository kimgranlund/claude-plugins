# Audit — sub-agent model-default amendment (2026-08-22, FLOOR)

Skill: /Users/kimba/Projects/nonoun/plugins/teamwork/skills/sub-agent/SKILL.md · Standards: skill-writing-rules · Lint: clean
Verdict: PASS

Scope: targeted amendment review — Phase 2a-skill `model: sonnet` default, Phase 2b cross-reference + override clause, description sentence + NOT-fence compression. FLOOR tier per dispatch.

| ID | Verdict | Severity | Evidence (file:line) | Fix |
|----|---------|----------|----------------------|-----|
| C1 (internal consistency: description ↔ 2a-skill ↔ 2b) | PASS with one gap | minor | SKILL.md:7-8 says override is "name a model in the task"; SKILL.md:150-153 (Phase 2b) additionally allows self-escalation when "its judgment load plainly earns more" — an override channel 2a-skill (SKILL.md:128-135) deliberately lacks and the description omits | Add one clause in the 2b paragraph marking the judgment-escalation channel as 2b-ONLY by design (a charter's load is unbounded; a registered skill run is bounded) — otherwise a future editor "harmonizes" by copying escalation into 2a-skill, reopening the fable leak the ruling closed. Description needs no change (command species; budget-capped) |
| C2 (effort claim vs agent-writing-rules) | PASS | — | SKILL.md:133-135 ("Effort cannot be set on a plain `Agent` dispatch … frontmatter or Workflow only") checked against harness agent-writing-rules SKILL.md:173-176 ("A plain Agent dispatch cannot vary `effort` — the frontmatter value is what it always gets"; Workflow takes both) — exact match. The session-inherit premise (SKILL.md:129-130) also matches agent-writing-rules:182-185 (ad-hoc `general-purpose` "rides the dispatching session's own model by default"). Dismissed as a finding on that cited read |
| C3 (NOT-fence weakening, /mobilize-chores) | PASS | — | Old vs new fence diffed (git diff HEAD): dropped text was rationale only ("which already names its own per-ticket build-leader dispatches", "mints none of its own"); the owner pointer (`/mobilize-chores`), the parseable NOT form, and the discriminator ("one ad-hoc target, not a queue") all survive at SKILL.md:13-14. Both skills are command species (`disable-model-invocation: true`) — the description never enters model routing, so the compression costs nothing mechanically. sub-agent's own reciprocal case n03 intact: evals/evals.json:19, comment still names the owner and the queue/ad-hoc discriminator |
| C4 (reciprocal suite on the mobilize-chores side) | UNVERIFIABLE — file absent | minor (pre-existing, not caused by this edit) | /Users/kimba/Projects/nonoun/plugins/teamwork/skills/mobilize-chores/evals/ contains audit reports + baseline/ only; no evals.json exists to fence-reciprocate (find over the dir returned no .json) | File the missing suite as its own chore against mobilize-chores; nothing in THIS change owed it (mobilize-chores' description was untouched), so it does not block this amendment |
| C5 (stale text contradicting the new default) | PASS | — | Grepped the body for fable/inherit/session-model residue: only SKILL.md:131 (the new paragraph itself). Phase 2a-agent (SKILL.md:107-114) says nothing about model — consistent, and the new paragraph explicitly carves it out (SKILL.md:131-133, "2a-agent is untouched"). Phase 3 relay bullets need no model line — "state the chosen model in the reply" is already a standing instruction at 135 and 153 |
| DM gate | PASS | — | Lint clean (mechanized R1-R3); no `context: fork`, no skill-level `model:` frontmatter, so no DM-R6 conflict surface. The sonnet pin is an Agent-tool param on a pin-less `general-purpose` target — the exact shape agent-writing-rules:173-174 sanctions |

Steelman record (checking-rules): C1 was drafted against the DESCRIPTION ("description omits the 2b escalation clause"); the rebuttal — command-species descriptions are menu docs under a 1024 budget, compression is legitimate — survives, so the finding was revised to target the un-annotated BODY asymmetry instead. A candidate finding on skill targets that themselves carry `context: fork` + `model:` was dropped: the wrap's sonnet governs only the wrapper agent, the inner fork's own `model:` legitimately wins there (F4), and the body claims nothing to the contrary.

Top 3: 1) C1 minor — annotate the 2b-only judgment-escalation channel so 2a-skill's stricter override rule reads as deliberate. 2) C4 minor — mobilize-chores has no evals.json at all; file separately. 3) None further — the amendment is internally consistent and the effort claim is verbatim-faithful to its source.

Reviewer: fresh-context skill-checker, FLOOR tier, 2026-08-22.
