# Floor-tier audit — sub-agent 2026-08-22 amendment (skill targets + explicit naming)

Skill: /Users/kimba/Projects/nonoun/plugins/teamwork/skills/sub-agent/SKILL.md · Standards: skill-writing-rules · Lint: clean
Verdict: **FAIL** (one blocking finding — the explicit-name-token grammar contradicts its own worked example)

Scope: fresh-context floor review of the 2026-08-22 amendment (Phase 2a-skill, explicit name
token, registered-target naming default flip) plus evals t09–t12 and the n03/suite-note text.
Dispatched by the maker's session; reviewed per `check-skill` + `checking-rules`.

| ID | Verdict | Severity | Evidence (file:line) | Fix |
|----|---------|----------|----------------------|-----|
| G1 name-token grammar | **FAIL** | **blocking** | SKILL.md:89-98 — the stated rule (`^[a-z][a-z0-9-]*$`, single bare kebab word, ≥1 more token follows) classifies the file's OWN contrast example the wrong way: in `/sub-agent build-leader ship ticket #240`, `ship` matches the regex AND is followed by more tokens, so the rule as written yields name=`ship`, task=`ticket #240` — the opposite of the outcome the same paragraph asserts. The explanatory clause "the next token `ship` isn't followed by nothing — it IS part of the task" (line 97) is a garbled double negative whose literal reading ("ship IS followed by something") is precisely the rule's name-condition. | Replace the positional heuristic with an explicit marker the grammar can key on unambiguously — e.g. `as <name>` (`/sub-agent {target} [as {name}] {task}`) or `name=<slug>` — and update description, argument-hint, the worked pair, and t10/t12 in the same change. No shape-based rule can separate a name slug from a kebab-shaped task-opening verb. |
| G2 worked-example walkthrough | (evidence for G1) | — | Hand-walked 5 strings against the literal rule: (A) `mobilize-chores plugins-chores-agent run the ops sweep` → name detected ✓ intended (SKILL.md:94-96). (B) `build-leader ship ticket #240` → name=`ship` ✗ contradicts SKILL.md:96-98 and eval t01. (C) constructed `build-leader fix-the-bug now` → name=`fix-the-bug`, task=`now` ✗ almost certainly wrong intent. (D) constructed `build-leader ship` (one word after target) → no follower token → task=`ship`, no name ✓ the rule handles the single-word edge correctly as task-not-name. (E) t12 `build-leader my-fix ship ticket #240` → name=`my-fix` ✓ intended — but structurally IDENTICAL to (B)/(C); the grammar cannot produce both t01's and t12's claimed outcomes. Steelman: read "kebab-case" as requiring ≥1 hyphen, excluding `ship` — the regex at :91 explicitly admits hyphenless words, and even under that reading (C) still misparses and (B) vs (E) stay indistinguishable. Finding survives. | — |
| G3 evals t09-t12 | PASS w/ caveat | major (rides G1) | evals.json:13-16 — the four cases cover exactly the claimed forms (skill target unnamed, skill+name, `plugin:skill`-qualified, agent+name). But t01 (:5) and t12 (:16) jointly encode a contrast the G1 grammar cannot deliver: identical `target bare-kebab-word more-words` shapes with opposite claimed parses. The suite is routing-only (trigger/no-trigger), so eval_check stays green while the comments claim parse outcomes the body can't produce. | Fixing G1 (explicit marker) makes t12's prompt `build-leader as my-fix ship ticket #240` and dissolves the collision; update t10 likewise. |
| G4 naming-default asymmetry | PASS | minor | SKILL.md:42-52 — the flip is deliberate and cited: Kim's quoted framing ("when you give it a name it stays"), a stated rationale (known target = bounded ask; free charter = substantial), a dated ruling, and the motivating case named. Phase 3 (:162-173) states each path's semantics so an accidental one-shot is at least visible at relay time. Residual risk, noted not blocked: the motivating case itself (mobilize-chores step 5) concluded ALWAYS-name for a known-target (`build-leader`) dispatch class, while this command's default for the same target is unnamed — a user typing `/sub-agent build-leader ship ticket #240` gets exactly the un-nudgeable shape the amendment cites as the hazard. Deliberate per the inline ruling; the user is live at the slash prompt and can add the name. | Optional hardening: one sentence in Phase 2a noting that a target whose own contract assumes resumability (a write-gate round trip, a long campaign) should be given a name — a nudge, not a default change. |
| G5 mobilize-chores citation | PASS w/ caveat | minor | SKILL.md:50-52's claim checked against PR #867's actual diff (`gh pr diff 867`): step 5 there dispatches `Agent(subagent_type: "teamwork:build-leader", name: "build-<ticket-id>")` and its NOT-done clause reads "a `build-leader` dispatch is fired UNNAMED … the write-gate accept-marker round trip has no seat left to nudge once an unnamed dispatch's one synchronous return has already been relayed" — the citation is accurate in substance, near-verbatim. Caveat: **PR #867 is OPEN, not merged** (`gh pr view 867` → `OPEN, mergedAt null`); the working-tree mobilize-chores/SKILL.md:230-231 still dispatches with no `name:`. The citation is forward-dated against an unmerged sibling. | Land #867 before (or with) this amendment's ship; if #867 dies, the motivating-case sentence dangles and needs a re-cite. |
| G6 stale old-behavior refs | PASS | — | Swept the untouched sections for leftover "Phase 2a is always unnamed" text: Phase 2b (:123-160) — its one Phase 2a reference (:158-160, "already unnamed by default") is correct under the NEW default; Phase 3 (:162-173) carries both registered-target branches (unnamed :164, NAMED :167); Failure branches (:175-201) — the nested-dispatch branches (:192-201) correctly split on named-vs-unnamed across BOTH paths; Done (:203-213) covers the name-token grammar and both relay shapes. Description (:3-14) and argument-hint (:17) both carry `[name]`. No stale reference found — dismissal check: every section read in full, cites above. | none |
| G7 n03 + suite note | PASS | — | evals.json:3 (suite note) accurately dates and scopes t09-t12; n03 (:19) — prompt has no slash form, owner comment `/mobilize-chores — batch find-and-confirm across a whole queue` still matches the description's fence (SKILL.md:12-14) verbatim. Not made stale by t09, whose prompt is the explicit slash form with a target token. | none |
| R7 contracts | PASS w/ nit | nit | Failure branch :182-183 "no task after the target name (and any explicit name token)" — under the grammar's own AND-condition, "target + name-token + nothing" is unreachable (a lone trailing token is always task, per case D), so the parenthetical describes a dead state. Harmless; becomes real if G1's fix moves to an explicit `as` marker (where `target as name` with no task IS reachable — keep the branch then). | rewrite the parenthetical alongside the G1 fix |
| R3/R4/R5/R6/R8 | PASS | — | Dials explicit (:15-16), command species matches slash-only surface and imperative content; register instantiates throughout (e.g. :105-109 spec-present); no restated substrate — fleet-rules/agent-writing-rules referenced by handle, not copied (:136-153); 213 lines, contracts well inside the head; numeric anchors where load-bearing (edit distance ~2 :83, max-1 redispatch :185). Skill-vs-agent shadowing (a name registered as both) resolves silently skill-first (:65-66) — deterministic and stated; nit only. | none |
| DM gate | PASS / N-A | — | In scope (dispatch mechanics throughout). Lint's R1-R3 line: clean. DM-R4/R5 N/A — no `context: fork` (frontmatter :15-23). DM-R6 PASS — no skill-level `model:` pin; Phase 2b's `model: sonnet` (:136) is a per-dispatch Agent parameter, not the frontmatter-precedence conflict F4 measured. | none |

Checking-rules compliance: every dismissal above cites its check (G6 full-section reads; G7
fence text compared verbatim; DM frontmatter read). Runtime-over-claim: the mobilize-chores
citation was verified against `gh pr diff 867` output, not the amendment's own prose. Steelman
recorded in G2 (hyphen-required reading drafted and rebutted).

Top 3:
1. **Blocking** — the name-token grammar misparses its own contrast example: `ship` in
   `build-leader ship ticket #240` satisfies every stated name-condition (regex match + a
   following token) yet the file asserts it is task. Fix by grammar, not prose: an explicit
   marker (`as <name>` or `name=<slug>`), then re-align description, argument-hint, the worked
   pair, and t10/t12.
2. **Major (same root)** — t01 vs t12 encode structurally identical argument shapes with
   opposite claimed parses; the routing suite stays green while asserting behavior the body
   cannot produce. Resolved by the same fix.
3. **Minor** — the motivating-case citation is accurate against PR #867's diff, but #867 is
   still OPEN; sequence the merges or the sentence cites a ruling that never landed.

Recommended next action: maker applies the G1 grammar fix (explicit marker) + the riding eval
edits in one change, then re-runs this check.
