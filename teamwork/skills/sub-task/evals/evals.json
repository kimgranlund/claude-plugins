{
  "skill": "sub-task",
  "note": "Trigger-routing suite. Command species (disable-model-invocation: true) — every positive case is the explicit slash form; expect=no-trigger: a near-miss owned elsewhere (owner in comment).",
  "cases": [
    { "id": "t01", "prompt": "/sub-task spin off a subtask about whether we should cache the API response — weigh it and report back.", "expect": "trigger" },
    { "id": "t02", "prompt": "/sub-task run this in a clean context: summarize the attached spec's open questions.", "expect": "trigger" },
    { "id": "t03", "prompt": "/sub-task spin off a side question to a fresh agent: what's the tradeoff between polling and webhooks here?", "expect": "trigger" },
    { "id": "t04", "prompt": "sub-task: analyze whether this migration script is safe to run twice.", "expect": "trigger" },
    { "id": "t05", "prompt": "/sub-task {instructions}", "expect": "trigger" },
    { "id": "n01", "prompt": "/sub-agent build-leader ship ticket #240 end to end, unattended.", "expect": "no-trigger", "comment": "owner: /sub-agent — a registered agent's own contract, not general-purpose arbitrary instructions" },
    { "id": "n02", "prompt": "/fork-agent code-checker review this diff, inline in a background fork of this session.", "expect": "no-trigger", "comment": "owner: /fork-agent — inherits this session's own context, the opposite of clean" },
    { "id": "n03", "prompt": "Make this session adopt the build-leader agent's own contract directly and hold it until I say stop.", "expect": "no-trigger", "comment": "owner: /bind-build — the host adopts the contract in place, no dispatch" },
    { "id": "n04", "prompt": "Use the Agent tool directly to dispatch a general-purpose subagent for this.", "expect": "no-trigger", "comment": "owner: raw Agent-tool use — not this command's own slash surface" }
  ]
}
