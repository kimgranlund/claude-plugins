{
  "skill": "bind-build",
  "note": "Trigger-routing suite. expect=trigger: the skill should fire on the prompt in a fresh session; expect=no-trigger: a near-miss owned elsewhere (owner in comment).",
  "cases": [
    { "id": "t01", "prompt": "/bind-build", "expect": "trigger" },
    { "id": "t02", "prompt": "Make this session a dedicated build seat for the repo — drive every ticket through the full record-first procedure.", "expect": "trigger" },
    { "id": "t03", "prompt": "Adopt the build-leader agent's own contract directly for this session and hold it until I say stop.", "expect": "trigger" },
    { "id": "t04", "prompt": "/bind-build ~/Projects/some-repo — I'll hand you ticket ids one at a time.", "expect": "trigger" },
    { "id": "t05", "prompt": "Turn this terminal into a standing build seat with the interactive clarify branches alive.", "expect": "trigger" },
    { "id": "n01", "prompt": "Build feature #240 for me right now.", "expect": "no-trigger", "comment": "owner: /build-feature — one forked build of a single ticket, not a standing seat" },
    { "id": "n02", "prompt": "Sweep the ops queue and mobilize whatever's buildable.", "expect": "no-trigger", "comment": "owner: teamwork:mobilize-chores — batch find-and-confirm, not a standing seat" },
    { "id": "n03", "prompt": "Set this session up as a generic coordination charter for the launch.", "expect": "no-trigger", "comment": "owner: teamwork:bind-team — a generic coordination charter, not the build-leader's own contract" },
    { "id": "n04", "prompt": "Decompose this system across both planes before we spec it.", "expect": "no-trigger", "comment": "owner: teamwork:bind-planning — a design/decomposition charter" },
    { "id": "n05", "prompt": "Dispatch the build-leader agent via the Agent tool for this one ticket.", "expect": "no-trigger", "comment": "owner: build-leader (Agent tool) — the dispatched sibling seat, not this session adopting the contract itself" }
  ]
}
