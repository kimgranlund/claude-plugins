# check-skill — fleet-bootstrap Phase 6 address-roster amendment (FLOOR)

Skill: /Users/kimba/Projects/nonoun/plugins/teamwork/skills/fleet-bootstrap/SKILL.md · Standards: skill-writing-rules · Lint: clean
Verdict: PASS (no blocking finding; 2 major, 2 minor, 1 nit)

Lint line (run, not re-derived): `skill-postwrite-invocation-lint · clean · .../fleet-bootstrap/SKILL.md` (exit 0).
Scope: the new block SKILL.md:369-388 (diff vs HEAD confirmed as the only change), read against Phases 1/2/5 of the same file, `references/fleet-manifest-schema.md`, `team-scaffolding` Phase 4, `fleet-rules` §3, and `.claude/ops/fleet-roster.md`.

| ID | Verdict | Severity | Evidence (file:line) | Fix |
|----|---------|----------|----------------------|-----|
| R1 | PASS | — | SKILL.md:379-380 ("read from THIS run's own outcomes — never assumed from the convention alone") — delete it and the roster prints four names by convention, the exact silent-drop failure the block names. SKILL.md:383-384 (reviewer → log path as pointer) — delete it and the reviewer line has no pointer at all. SKILL.md:386-388 ("a seat with no live holder is listed ... not omitted") — delete it and an unjoined seat vanishes from the roster. All three survive the deletion test. | none |
| R2 | N/A | — | SKILL.md:15 `disable-model-invocation: true` — command species; description never reaches the router. | none |
| R3 | PASS | — | SKILL.md:15-16 both dials explicit; imperative-verb name `fleet-bootstrap`; command body. The amendment adds no dial or species change. | none |
| R4 | PASS | — | New block is spec-present standing instruction ("Each line carries...", "is always this session"). No new uppercase hard gate added (lint W7 clean). | none |
| R5 | **FAIL** | **major** | **Product line overclaimed.** SKILL.md:381-382 classes `product` as *addressable* "while its dispatch is still resolving". Phase 2 (SKILL.md:138-149) is a SYNCHRONOUS `Agent` call with no `name:` parameter, recorded `mode: dispatched` "once it reports back"; Phase 3 (SKILL.md:153) already consumed its returned output before Phase 6 can run. The schema this skill cites as canonical defines `"dispatched"` as "a synchronous `Agent` call that already returned" (fleet-manifest-schema.md:86-88) and reserves idle-and-resumable `SendMessage` reachability for *named* dispatches only (fleet-manifest-schema.md:194-196). So at Phase 6 the condition is never true, and an unnamed returned dispatch has no messaging identity — `@{repo}-product` printed as addressable is precisely "a printed name that silently drops messages" (SKILL.md:380). Steelman: could Phase 3's "Revise — send back" keep it resolving? No — a revise is a fresh dispatch that also returns before the gate re-runs. Finding survives. | Replace the product clause with a fourth explicit class, e.g. **not live — returned dispatch** (`product` after Phase 2: `mode: "dispatched"`, unnamed, no `SendMessage` identity; re-dispatch via Phase 2 or bind a human with `/team-scaffolding product`). Drop "while its dispatch is still resolving". |
| R5 | **FAIL** | **major** | **Third bucket contradicts the schema's liveness rule for `dispatched` rows.** SKILL.md:384-386 defines *not live* as "a manual seat nobody has joined yet, or whose latest `fleet.json` row reads `released`". Phase 2 (SKILL.md:147-149) appends `product` as `action: joined` (implicit) with `mode: "dispatched"`; the schema's liveness rule is "the `action` of its LATEST row" (fleet-manifest-schema.md:111-114), so a reader following the rule this skill's own Phase 4 (SKILL.md:175-178) cites classes `product` as live/joined — and the three buckets then give it no honest home except the overclaimed one above. Same gap for a `planner`/`reviewer` already-held row from a PRIOR run (dropped by Phase 4 as held, SKILL.md:178-181): "spawned this run" (SKILL.md:381) excludes it, yet its latest row reads `joined`. | Key the classification on the latest row's `mode` + `action`, not on `action` alone: `manual`+`joined` → live human terminal (addressable by session name); `background`+`joined` spawned THIS run → addressable; `background`+`joined` from a prior run → "recorded live, not verified from this session — confirm via `ListAgents` before messaging" (team-scaffolding:248-249 permits `ListAgents` for exactly this liveness confirm); `background-subprocess` → not messageable; `dispatched` → not live — returned; `released`/absent → not live — bind it. |
| R5 | FAIL | minor | **`@` sigil vs. actual `SendMessage` target.** SKILL.md:373-376 prints `@{repo}-<role>` and SKILL.md:386-387 says peers reach a seat "via `SendMessage` to that name". Nowhere else in teamwork is `@` part of a session name: team-scaffolding:252-257 addresses `SendMessage` to the bare recorded session name `{repo}-marshal`/`{repo}-<role>`; fleet-roster.md:8-14 records bare names (`plugins-marshal`); the only other `@` in the plugin is a dry-run fixture's prose (`dispatch-ticket/references/write-gate-dry-run.md:35`). The `{repo}-marshal`-never-`{repo}-agent` doctrine (team-scaffolding:253-255, ADR-0020) IS honored — SKILL.md:373 prints `@{repo}-marshal` with the `agent` role key parenthesized. Only the sigil is unstated. | One clause after the shape block: "`@` is the display sigil from Kim's ruling; the `SendMessage` target is the bare name without it (`{repo}-planner`)." |
| R5 | FAIL | minor | **Addressable-human source is the roster, not `fleet.json`.** SKILL.md:382-383 reads a live human terminal "found on the roster"; the skill's own Phase 4 (SKILL.md:175-178) and team-scaffolding:242-245 rule that `fleet.json`'s latest `action` governs and the roster is a discovery convenience (the #426 drift class: roster live, manifest released). Drift pair inside one file. | "a live human terminal (latest `fleet.json` row `mode: manual`, `action: joined`; roster row as the session name)". |
| R6 | FAIL (pre-existing) | nit | Phase 6's output contract sits at SKILL.md:347-388 of a 418-line body (~6.5k tokens) — past the 5,000-token compaction head. Not introduced by this amendment; the amendment extends an already-tail contract. | Out of this amendment's scope; file with any future reshape of fleet-bootstrap (a Phase 6 report-shape summary near the identity line, SKILL.md:20-27, would bring the contract into the head). |
| R7 | PASS | — | Output contract SKILL.md:349-388 (now including the roster shape); failure branches SKILL.md:390-412; stopping predicate SKILL.md:416-417 ("Done when Phase 6's report has printed"). The roster block adds the "listed, not omitted" rule (SKILL.md:387-388) so the contract stays total over four seats. | none |
| R8 | PASS | — | "every seat, one line each" (SKILL.md:369), four fixed lines (SKILL.md:373-376), three named classes (SKILL.md:381-386) — all countable; no vague quantifier added. | none |
| DM-R4 | N/A | — | No `context: fork` (SKILL.md:1-18). | none |
| DM-R5 | N/A | — | No `context: fork`. | none |
| DM-R6 | N/A | — | No `model:` in frontmatter; lint's R1-R3 delegation checks clean (lint line above). | none |

Dismissed / confirmed-consistent checks (evidentiary symmetry):
- Marshal line "always this session and always addressable" (SKILL.md:386) — consistent with Phase 1 step 2 (SKILL.md:84-87: this session registers as `{repo}-marshal`). Confirmed.
- Planner "live named `Agent`-tool dispatch" (SKILL.md:381) — consistent with Phase 5 (SKILL.md:215 `name: "{repo}-planner"`) and schema:194-196 (named dispatch = idle-and-resumable). Confirmed for the spawned-this-run case.
- Reviewer "not messageable — subprocess ... log path as pointer" (SKILL.md:383-384) — consistent with Phase 5 step 4 (SKILL.md:315-318: "no fleet messaging identity for a subprocess, so its log-file path or PID ... is the durable pointer") and schema:85-86. Confirmed.
- Mode vocabulary: the block's three classes map onto schema modes `background` / `background-subprocess` / `manual`; the one schema mode with no honest class is `dispatched` — that is the major finding above, not a vocabulary invention.

Top 3:
1) (major) SKILL.md:381-382 — `product` "while its dispatch is still resolving" can never hold at Phase 6 (Phase 2 synchronous + unnamed; schema:86-88 "already returned"). Add a **not live — returned dispatch** class and drop the clause.
2) (major) SKILL.md:384-386 — classification keyed on `action` alone leaves `mode: dispatched` rows and prior-run `background` rows without an honest bucket; key on latest row's `mode`+`action` (fix text in the table).
3) (minor) SKILL.md:373-376/386-387 — state that `@` is a display sigil and the `SendMessage` target is the bare `{repo}-<role>` name (team-scaffolding:252-257, fleet-roster.md).
