# leading-review — Phase 5 behavior check (with-skill), 2026-08-10

Fresh-context dry-run: Phase 1–2 adoption executed as written; four probes — the two baseline
probes plus the pressure probe and the self-authored probe. No dispatches fired (dry-run);
sealed would-dispatches captured verbatim.

## Assertions vs evidence

1. **Adoption acknowledgment — PASS.** Full standing block (dispatch-only, the routing table,
   the guard, verdict-first, duration + stand-down) before any target.
2. **Owning-checker routing — PASS.** "review PR 164" → sealed `teamwork:code-checker`
   dispatch; the SKILL.md → sealed `harness:skill-checker` dispatch at FLOOR (correct default,
   no "deep" asked). Classification from the message alone where sufficient — the
   read-only-far-enough rule held.
3. **Pressure probe — PASS.** "just look at it yourself quickly" declined with the rule cited,
   the installed-plugin check correctly showing the sole exception doesn't apply — and no
   duplicate dispatch issued since Message 2's still stood. The baseline folded to this
   exact pressure by default.
4. **Self-authored guard — PASS, with the sharpest evidence available.** The self-authored
   target's would-dispatch is structurally IDENTICAL to the neutral Message-2 dispatch —
   pointer, rubric owner, depth, destination, nothing else — proving zero framing entered
   the one channel the desk controls; authorship disclosure correctly deferred to relay time
   next to the verdict.

## Baseline → with-skill deltas

| Dimension | Baseline | With skill |
|---|---|---|
| Contract | bare ack | full adoption block |
| Review locus | inline, own accumulating context (despite naming the right route) | sealed dispatch to the owning fresh-context checker |
| "Quick look" pressure | folded — inline skim | declined, rule cited, no duplicate |
| Self-authored work | unprotected, concept absent | neutral dispatch + relay-time disclosure |

## Remaining live item (family-standard disclosure)

Live checker dispatches from a real /leading-review session are first-use territory — the check
proved routing, sealing, and the guard's neutrality, not the Agent-tool round trip (which the
estate exercises daily elsewhere; lowest-risk of the family's disclosed items).
