# teamwork 2.28.35 — harness overlay notes (generated by harness_emit.py; do not edit)

## Codex
- Loads via `.codex-plugin/plugin.json`; enable with the `[marketplaces.nonoun-plugins]` + `[plugins."teamwork@nonoun-plugins"]` entries in `~/.codex/config.toml`.
- Dropped — agents: `build-leader`, `builder`, `code-checker`, `docs-writer`, `fleet-marshal`, `planner`, `planning-leader`, `product-leader`, `review-leader`, `wiring-checker`; hooks: none.
- Command-only skills now model-routable: `bind-build`, `bind-planning`, `bind-product`, `bind-review`, `bind-team`, `build-feature`, `chores-run`, `fleet-bootstrap`, `fork-agent`, `init-repo`, `mobilize-chores`, `sub-agent`, `team-scaffolding`.
- Fork skills (their `context` field drops; body runs inline): `build-feature`, `fork-agent`.
- MCP: none shipped.

## Hermes
- This backend isn't built yet (tracked as T-5; no install path exists for Hermes from this repo today) — no action needed here. What WOULD drop under this plugin's current Claude Code source, computed the same way Codex's row above is (kept for a preview, not yet verified against Hermes's own contract): agents — `build-leader`, `builder`, `code-checker`, `docs-writer`, `fleet-marshal`, `planner`, `planning-leader`, `product-leader`, `review-leader`, `wiring-checker`; hooks: none.

## Pi
- This backend isn't built yet (tracked as T-6; no install path exists for Pi from this repo today) — no action needed here. What WOULD drop under this plugin's current Claude Code source, computed the same way Codex's row above is (kept for a preview, not yet verified against Pi's own contract): agents — `build-leader`, `builder`, `code-checker`, `docs-writer`, `fleet-marshal`, `planner`, `planning-leader`, `product-leader`, `review-leader`, `wiring-checker`; hooks: none.
