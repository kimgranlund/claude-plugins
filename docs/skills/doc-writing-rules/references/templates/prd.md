---
doc-type: prd
id: prd-<slug>
status: draft           # draft | approved | superseded
version: 0.1.0
date: YYYY-MM-DD
owner: 
---
# PRD — <product outcome, not feature name>

## Problem
<!-- Head-first: the user pain in two sentences, then the evidence. -->

## Users
<!-- Who, specifically; primary vs secondary. -->

## Outcomes
<!-- Measurable outcomes with IDs (OUT-01...), not feature lists. Each phrased so it could fail. -->

## Non-goals
<!-- Load-bearing. What this deliberately does NOT address, and why. -->

## Agent verification
<!-- Outcome-level, not per-requirement (the SPEC this PRD earns owns that granularity): for
     each Outcome (OUT-##), how would a coding agent tell it was achieved without a human in the
     loop — the assert layer and harness this PRD implies exists or must be built. An Outcome
     with zero plausible agent-runnable check is a signal the outcome itself is unfalsifiable
     (J1), not just a testability gap. -->
