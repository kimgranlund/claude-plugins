"""docs — generated by harness_emit.py from .claude-plugin/plugin.json. Do not edit."""
from pathlib import Path

_HERE = Path(__file__).resolve().parent

def register(ctx):
    ctx.register_skill('agent-harness-rules', _HERE / "skills" / 'agent-harness-rules' / "SKILL.md", description="Standards for designing agent-native verification — HOW an agent tests a system it just built, not a human eyeballing a screenshot. Assert-layer choice, driver-agent harness design, assertion/fixture grammar, mapping this estate's instruments onto a SPEC's criteria, the Gen-UI case. Use when designing a test harness, picking an assert layer, judging HOW to make a criterion agent-runnable, or asking what instruments already exist before building a new harness. NOT whether a SPEC's Acceptance sec…")  # one per skill
    ctx.register_skill('artifact-rules', _HERE / "skills" / 'artifact-rules' / "SKILL.md", description="Procedure-side standards for artifact/report-page authoring: classifying a content source (report/retro vs handbook vs both), composing content from a project's own records tree (Intent/User-Story/Concept/System layers to chapters), the `css_build.py` script interface, and the provenance-footer/refresh procedure. Use for a standards question with no build ask. Visual/styling doctrine (tokens, type, mermaid, shells) lives in design's artifact-styling-rules — cited, not restated. Consulted by mak…")  # one per skill
    ctx.register_skill('bind-intake', _HERE / "skills" / 'bind-intake' / "SKILL.md", description="Makes this session a dedicated intake seat: it adopts the intake-leader agent's own contract directly — every raw report, idea, or chore sent here becomes a durable record via the file-bug/file-feature/file-task/file-leftovers procedures, applied inline with a live clarifying round — and holds that discipline until the session ends. Run /bind-intake [optional repo root]. NOT the dispatched sibling seat (intake-leader, Agent tool); NOT a one-off filing (/file-bug, /file-feature, /file-task direc…")  # one per skill
    ctx.register_skill('check-doc', _HERE / "skills" / 'check-doc' / "SKILL.md", description='Review an existing functional document (ADR, PRD, SPEC, LLD, PLAN, ROADMAP, TICKET, TASK): a mechanical doc_lint pass, then judgment — unfalsifiable requirements, missing non-goals, restated substrate, broken ID spine, class violations, verdict-first. Use it to review, audit, critique, or score a document, or when a doc "feels off". NOT for drafting one (make-doc); NOT the rules themselves (doc-writing-rules).')  # one per skill
    ctx.register_skill('check-stage', _HERE / "skills" / 'check-stage' / "SKILL.md", description='Read-only lifecycle-position report: which of the three nested loops (North star/Foundation/ Releases) a repo is currently emphasizing, the build turn\'s current stage (of seven), and the version triple — derived from a typed-record census (ADR/IDR/RDD counts, status distributions, orphan-ADR density, ROADMAP presence) plus narrated judgment calls, each labeled mechanized or judgment (never a bare verdict). Use for "what lifecycle stage is this project in", "are we pre- or post-Spec-lock", "whic…')  # one per skill
    ctx.register_skill('doc-writing-rules', _HERE / "skills" / 'doc-writing-rules' / "SKILL.md", description='Standards for authoring functional documents — ADR, PRD, SPEC, LLD, PLAN, ROADMAP, BRIEF, TICKET, TASK, IDR, RDD. Use when asking which document type fits, what sections/frontmatter a type requires, whether a document can be edited (why an accepted ADR, a locked IDR, or a locked RDD is append-only), how documents reference each other (the ID spine), how plans track status, or why doc_lint failed. NOT for drafting one (make-doc); NOT for reviewing one (check-doc); NOT the general "why do IDR/ADR…')  # one per skill
    ctx.register_skill('file-bug', _HERE / "skills" / 'file-bug' / "SKILL.md", description='Capture a user-reported bug as a durable record, then dispatch the investigation. Use when the user reports something broken, gives a repro or wrong output, flags a regression, or phrases it as "file a github issue for this crash" / "open an issue about this bug". /file-bug [raw report, or a TKT-/#issue/adapter id] resumes. NOT for a feature idea (file-feature); NOT for a chore/task (file-task); NOT for platform questions (github-facts).')  # one per skill
    ctx.register_skill('file-feature', _HERE / "skills" / 'file-feature' / "SKILL.md", description='Capture a feature idea as a durable record: a ticket, design docs — or, when it\'s a feature request that turns out to be "document/capture X as a reference corpus," it routes HERE FIRST: intake classifies, then hands to make-reference. Use when the user proposes a new capability or pitches an idea — "can we add a dark mode", "what if we supported CSV export". Intake only, never builds. /file-feature [idea, or a TKT-/#issue/adapter id] resumes. NOT for bugs (file-bug); NOT for chores (file-task)…')  # one per skill
    ctx.register_skill('file-leftovers', _HERE / "skills" / 'file-leftovers' / "SKILL.md", description='Sweep the current chat session for work mentioned but not advanced — bugs, feature ideas, feedback, chores, unanswered questions — and roll it into workable tickets: a candidate table, one clarification round, then each item minted through its owning intake skill. Use for "sweep this chat for leftover work", "ticket everything we didn\'t get to", "did we drop anything this session", "file the leftovers", "roll up what we mentioned but never did". NOT for decisions needing no ticket (find-open-qu…')  # one per skill
    ctx.register_skill('file-task', _HERE / "skills" / 'file-task' / "SKILL.md", description='Capture a work item that\'s neither bug- nor feature-shaped — a chore, follow-up, or debt — as a durable record, then resume by id, fold new detail, append dated Findings, or advance status. Use for "note this down as a follow-up", "log this technical debt", "track this for later", "file a task for X", or /file-task #NN / tkt-#### to resume. NOT for bug-shaped reports (file-bug); NOT for feature ideas (file-feature).')  # one per skill
    ctx.register_skill('harness-facts', _HERE / "skills" / 'harness-facts' / "SKILL.md", description='Harvest a project\'s context for a CODING AGENT\'S OWN HARNESS. Zones from prototypes, PRDs, brief/IDRs, ADRs, roadmaps, scored two-axis (Inside-Out x Outside-In, Inside-Out-higher); emits install-ready artifacts: a CLAUDE.md-grade entry-file section, `.claude/rules/` files, pack seed candidates, a dispatch-context digest. Use for "harvest this project\'s context for the harness", "produce CLAUDE.md sections and rules from what this codebase does". NOT docs:project-facts (human/business corpus, sh…')  # one per skill
    ctx.register_skill('html-to-markdown', _HERE / "skills" / 'html-to-markdown' / "SKILL.md", description='Convert HTML into markdown source. Use when bringing HTML into a markdown corpus, transcribing a rendered page or fragment back to markdown source, stripping presentational markup, or producing the markdown a renderer will consume. Also owns CONVERSION defects — stray divs/class attributes, literal asterisks turning into emphasis, or wrong headings after converting — even at render time. NOT for rendering markdown onto the page/DOM or extending the doc renderer with bold/links/italics (markdown…')  # one per skill
    ctx.register_skill('make-artifact', _HERE / "skills" / 'make-artifact' / "SKILL.md", description="Build/rebuild a rendered Artifact/report page — consume a design system (DESIGN.md/tokens.json), or synthesize one via design if absent, plus a report, handbook, or records tree (composed into chapters) into one page: light/dark CSS, house shell, themed mermaid, provenance footer. Use for 'turn this into an artifact page', 'rebuild the Estate Handbook', 'render with our design system', 'compose a handbook from our records'. Runs via /make-artifact [design-system path] [content source]. NOT the…")  # one per skill
    ctx.register_skill('make-doc', _HERE / "skills" / 'make-doc' / "SKILL.md", description='Author a functional document — ADR, PRD, SPEC, LLD, PLAN, ROADMAP, TICKET, TASK, IDR, or RDD — through gated phases: route, capture intent, draft, validate. Use it to write, draft, author, record, or spec out a document — "write the PRD for this feature", "mint the founding IDR for this project", "lock in the RDD for this release". Runs via /make-doc [type and intent]. NOT for the rules themselves (doc-writing-rules); NOT for reviewing (check-doc); NOT for feature intake (file-feature); NOT for…')  # one per skill
    ctx.register_skill('make-llms-txt', _HERE / "skills" / 'make-llms-txt' / "SKILL.md", description='Author or review an llms.txt (and llms-full.txt) — the agent-facing map of a documentation corpus — to the standard shape, scoring it against the bundled rubric. Use whenever the user mentions llms.txt, an AI-facing docs index, agent discovery of docs ("agents can\'t discover where our documentation lives"), "make our docs agent-readable / discoverable", "generate an llms.txt", "review our llms.txt", or an llms.txt that is a list of concepts with no links. NOT for the docs themselves (make-refer…')  # one per skill
    ctx.register_skill('make-reference', _HERE / "skills" / 'make-reference' / "SKILL.md", description='Author or review a referential knowledge document (a skill references/ file, an @-imported or Project Knowledge doc) against the bundled rubric. Use for "write a reference doc", "structure my project knowledge", "name and organize the knowledge files", "is this retrievable", "audit our docs for drift". NOT for a knowledge pack / corpus+index (harness\'s make-pack), nor an incoming feature request that turns out knowledge-shaped (file-feature intakes it first, then hands off here); NOT for whethe…')  # one per skill
    ctx.register_skill('make-rubric', _HERE / "skills" / 'make-rubric' / "SKILL.md", description='Author, evaluate, improve, or update a rubric for consistent judgments. Use when building, scoring, fixing, or re-syncing a scoring rubric, eval criteria, checklist, or /goal completion condition — "write a rubric", reviewers scoring the same artifact differently, vague anchors two readers won\'t converge, "which dimensions should gate promotion", or "re-anchor this rubric — the artifact standard changed". NOT for the skill/agent carrying the rubric (make-skill / make-agent), nor the /goal loop…')  # one per skill
    ctx.register_skill('make-vision-memo', _HERE / "skills" / 'make-vision-memo' / "SKILL.md", description='Use when the user asks to author or improve a vision memo — argues how to think about a problem, not what to build. Triggers: "write a vision memo/manifesto/strategic brief/position paper/think piece", "make the case for X", "reframe this decision", "crystallize this into a doc", "synthesize competing positions", or "tighten this memo\'s argument". NOT a PRD/SPEC/LLD/ADR (make-doc); NOT reviewing a finished memo (check-doc); NOT marketing, tutorial, or balanced-survey copy.')  # one per skill
    ctx.register_skill('markdown-to-markup', _HERE / "skills" / 'markdown-to-markup' / "SKILL.md", description='Render markdown source into safe DOM markup, or extend the renderer with a new inline/block form. Use for "render this markdown", "the backticks/stars show up literally", "add bold/links/italics to the doc renderer", or "is this markdown rendering safe from injection, it uses innerHTML" — inline/block grammar built via textContent, never innerHTML. NOT for converting HTML into markdown source, or defects a CONVERTER produced — stray divs, converted text re-rendering wrong (html-to-markdown). NO…')  # one per skill
    ctx.register_skill('product-lifecycle-rules', _HERE / "skills" / 'product-lifecycle-rules' / "SKILL.md", description='General doctrine for how products get built: three nested loops (North star -> Foundation -> Releases), the seven-stage build loop (Kickoff..Spec lock..Verify..Retro; Spec lock the only hard gate), the IDR/ADR/RDD alignment doc grammar, knowledge-base maturation, habits, named anti-patterns. Portable -- orients ANY project. Use for "what\'s an IDR/ADR/RDD for", "why Spec lock matters", "how a knowledge base matures", "the relearn rate", "admission test, ADR vs IDR". NOT which doc type/sections/f…')  # one per skill
    ctx.register_skill('project-facts', _HERE / "skills" / 'project-facts' / "SKILL.md", description='Harvest a project\'s DOMAIN KNOWLEDGE — business logic, architectures, unique-IP structures for a human/business reader. Discovers topic zones from prototypes, PRDs, brief/IDRs, ADRs, roadmaps; scores each zone two-axis (Outside-In x Inside-Out, weighted Outside-In-higher). Use for "extract domain knowledge from this project", "write up the business logic as a reference doc", "what this codebase does, for a non-engineer". NOT for a plain reference doc with no discovery/scoring (docs:make-referen…')  # one per skill
    ctx.register_skill('research-methods', _HERE / "skills" / 'research-methods' / "SKILL.md", description='Use for a measured investigation of a scorable system — six methods: autoresearch, hill-climb, ablation, sweep, bisect, adversarial. Triggers: "improve the score", "find the root cause of this regression", "which parts actually matter", "what\'s the best value for X", "tune these parameters", "what breaks this", "it used to work, now it doesn\'t", "stress-test this before we ship". NOT lookup (web search); NOT artifact review (*-checker agents); NOT authoring/building (*-forge/builder); NOT judgi…')  # one per skill
    ctx.register_skill('research-rules', _HERE / "skills" / 'research-rules' / "SKILL.md", description="The deliverable schema and rubric research-leader's typed findings are authored to and graded against — six required fields per finding, a four-axis rubric (knowledge, actionable, grounding, novelty-vs-known). Use when the user asks to dispatch research-leader, author or review one of its deliverable files, or score a research deliverable with doc-checker. NOT general rubric authoring (make-rubric); NOT a fixed-scorer investigation loop (research-methods/experiment-runner); NOT a gather-only /m…")  # one per skill
    ctx.register_skill('tidy-docs', _HERE / "skills" / 'tidy-docs' / "SKILL.md", description="Align an existing repo's scattered documents (rfcs/, NOTES.md, near-miss dirs) to the canonical docs/ layout: inventory, classify, propose one migration plan, execute on approval with git mv (history preserved), frontmatter, and link repair. Run /tidy-docs [optional subtree]. One approval gate; migration only — content is never rewritten. NOT authoring a document (make-doc); NOT reviewing content quality (check-doc); NOT whole-harness drift (clean-repo).")  # one per skill
