# docs — functional documents as interfaces

Sibling plugin to harness (the authoring toolchain; docs authors what flows through it).
Doctrine source of record: the corpus's Vol 3 (data plane). Designed by the make-plugin method;
shipped through the harness release gate.

| Artifact | Type | Invocation | What it carries |
|---|---|---|---|
| `skills/doc-writing-rules` | Declarative skill | model-only | Mutability classes, universal practices, the type contract table; `references/templates/` — ten authoring contracts (adr, prd, spec, lld, plan, roadmap, ticket, task, idr, rdd) |
| `skills/agent-harness-rules` | Knowledge pack | model-only | HOW to design agent-native verification (issue #542, `prd-agent-testability.md`): 5 axes — assert-layer choice, driver agent harness design, assertion/fixture grammar, mapping this estate's own instruments onto a SPEC's criteria, the Gen-UI grounding case. Answers the question `doc-writing-rules`' `## Agent verification` section and `check-doc`'s J7 require be answered; flat 5-file corpus, no INDEX.md per the 2026-07-09 ruling; named `agent-harness-rules` rather than the PRD's own working title (`agent-testability-rules`) — G12's naming-grammar gate (ADR-0011) has no live-confirm path to register a brand-new lexicon token unattended, so the name was chosen from already-registered vocabulary (`agent`, `harness`) instead |
| `skills/product-lifecycle-rules` | Knowledge pack | model-only | Realizes `product-lifecycle-bible.md` v1.1.0 (issue #320) as portable, this-repo-agnostic doctrine: the three nested loops, the seven-stage build loop, IDR/ADR/RDD alignment doc grammar, knowledge-base maturation, habits, anti-patterns. Now this workspace's canonical operating source for that doctrine (CLAUDE.md's sources-flow-outward invariant); `.claude/docs/spec/product-lifecycle-bible.md` is a dated snapshot pointing here. Fenced from `doc-writing-rules` (which type/sections/frontmatter here), `project-docs` (what this repo decided), and `check-stage` (this repo's live lifecycle-position reading — this pack is doctrine only) |
| `skills/check-stage` | Procedural (read-only report) | both (`/check-stage`) | Live lifecycle-position report (issue #336, `prd-lifecycle-stage-awareness.md`): which of the three loops is currently emphasized, the build-turn stage, and the version triple — a bundled census script (`lifecycle_census.py`) reads `.claude/docs/{adr,idr,rdd}/*.md` typed-record counts/status distributions/orphan-ADR density plus ROADMAP presence (mechanized), crossed with narrated judgment calls (POC-boundary crossed, loop emphasis, bug-vs-requirement-gap, Retro lessons landed) — every signal labeled mechanized or judgment, never a bare verdict. Modeled on and fenced against harness's `check-state` (work-state, not lifecycle-position); report-only v1, no ambient/write surface (deferred) |
| `skills/make-doc` | Procedural | both (`/make-doc`) | Type routing -> intent -> template draft -> language pass -> doc_lint clean |
| `skills/check-doc` | Procedural | both (`/check-doc`) | Mechanical pass first, then J1-J6 judgment; verdict-first report |
| `skills/file-bug` | Procedural (orchestrator) | both (`/file-bug`) | Capture -> classify -> record -> dispatch -> write-back: a bug-shaped TICKET (`kind: bug`) minted before any fork/agent starts, closing the loss window raw `/fork bug-name ...` left open |
| `skills/file-feature` | Procedural skill | both (`/file-feature`) | Feature intake, the file-bug mirror: find-intent → three-surface dedup → size (materiality floor) + shape (work → `kind: feature` ticket ± earned docs; knowledge → make-reference / harness's make-pack) → lint-clean record placed into existing ROADMAP/PLAN; never builds — `/build-feature` (orchestration) is the momentum half |
| `skills/file-task` | Procedural skill | both (`/file-task`) | The generic third sibling: any work item that is neither bug- nor feature-shaped (chore, follow-up, research item, debt) — shape-gate → dedup → `kind: task` record on the ruled backend (`task` label + optional size), plus the full resume surface: fold detail, dated Findings, status verbs (done/doing/wontfix) with the Findings-first close |
| `skills/file-leftovers` | Procedural skill | both (`/file-leftovers`) | The fourth sibling, batch intake from the CONVERSATION: sweeps the session for work mentioned but not advanced, presents one evidence-quoted candidate table (no verbatim quote → no candidate), one batched clarification round, then mints each approved row through its owning file-* sibling — minting is authorized only by per-run table approval, so a headless run delivers the table and mints nothing |
| `skills/bind-intake` | Skill-as-command | user-only (`/bind-intake`) | Makes THIS session the standing intake seat: adopts `agents/intake-leader`'s contract directly (the `/bind-team` ↔ `fleet-marshal` host-adoption pattern, docs' first instance) — every raw seed becomes a record via the four file-* procedures applied inline, with the one thing the agent structurally lacks: a live AskUserQuestion clarifying round. Intake-only held by stated discipline; ends when the session ends or the human stands the seat down. ADR-0020/#525 (closes #523): renamed from `lead-intake`, already skill-as-command shaped (no wrapper conversion needed) |
| `skills/tidy-docs` | Command skill | user-only (`/tidy-docs`) | Migrate an existing repo's scattered docs to the canonical directory-per-type map: three-surface inventory (canonical dirs, near-miss locations, loose files/README extractions) → ONE batched plan approval → git mv + minimal frontmatter (doc-type/id/status) + basename-first link repair + doc_lint → project-docs index installed; prose never rewritten, never commits |
| `skills/research-methods` | Declarative skill | model-only | Six measured-investigation methods (autoresearch, ablation, bisect, adversarial, hill-climb, sweep); the Phase −1/0/1/2 spine + investigation rubric; the `experiment-runner` agent runs one method in isolation |
| `skills/make-rubric` | Procedural | both (`/make-rubric`) | Create · evaluate · improve · update a rubric against the bundled rubric-for-rubrics |
| `skills/project-facts` | Procedural | both (`/project-facts`) | Harvest a project's domain-knowledge corpus (ticket #612): discovers topic zones per project (never a fixed taxonomy) from prototypes/PRDs/brief-IDRs/ADRs/roadmaps, scores every zone two-axis (Outside-In x Inside-Out, this corpus weighted Outside-In-higher) against the shared `references/harvest-core.md` core — the same core the `harness-facts` sibling (#613, below) cites, never duplicates. Named `project-facts` rather than the ticket's own `harvest-domain-knowledge` lean: same G12 unattended-naming-gate constraint as `agent-harness-rules` above (`lld-0008` records the full resolution + rejected alternatives) |
| `skills/harness-facts` | Procedural | both (`/harness-facts`) | Harvest the same project-context substance as `project-facts`, reframed for a coding agent's own harness (ticket #613, 2nd of the serial #612→#613 chain): shares `project-facts/references/harvest-core.md`'s definitions + two-axis rubric, weighted Inside-Out-higher (mirror of the sibling's Outside-In-higher); emits ready-to-install artifacts — a CLAUDE.md-grade entry-file section, `.claude/rules/` path-scoped files, knowledge-pack seed candidates, a dispatch-context digest — plus a run manifest, staged for the invoker to install, never installed in place. Eval is a WITH-vs-WITHOUT delta (not the sibling's single-WITH design): the same agent-task prompts answered with and without the emitted artifacts, scored against this workspace's own known-answer records. Named `harness-facts` rather than the ticket's own `harvest-project-context` lean: same G12 unattended-naming-gate constraint as `project-facts`/`agent-harness-rules` — chose already-registered vocabulary (`harness` + `facts`) instead, explicitly rejecting a `project-`-prefixed name (near-duplicate risk) and a `*-rules` tail (this is a capability you run, not a doctrine pack you consult) (`lld-0009` records the full resolution + rejected alternatives) |
| `skills/markdown-to-markup` | Procedural | both (`/markdown-to-markup`) | Markdown source -> safe rendered markup (DOM); inline + block grammar, parsed via `textContent`, never `innerHTML` |
| `skills/html-to-markdown` | Procedural | both (`/html-to-markdown`) | HTML -> markdown source; semantic element map (headings, strong/em, code, links, lists), drops presentational markup |
| `skills/make-artifact` | Procedural | both (`/make-artifact`) | Build or rebuild a rendered Artifact/report page (issue #619): a design system's DESIGN.md/tokens.json plus report or handbook content into a single-file page — bundled `scripts/css_build.py` mechanizes the token->CSS mapping (light/dark `light-dark()` custom properties, mandatory font fallbacks, spacing/radii/type scales, selftest-proven), the `artifact-rules` sibling carries the shell/mermaid/refresh standards it invokes |
| `skills/artifact-rules` | Knowledge pack | model-only | Standards for artifact/report-page authoring (issue #619, `lld-0013`): design-system consumption (both tokens.json/DESIGN.md input shapes), the house shell doctrine (narrative single-scroll for reports/retros, tabbed chapters for handbooks, mechanism-diagram-over-chip-wall), mermaid house style (single-line node labels, token-driven `!important` SVG re-theme, width-preserving tab hiding), and the artifact refresh procedure — flat 4-file `references/` corpus, no INDEX.md per the 2026-07-09 ruling |
| `skills/make-reference` | Procedural | both (`/make-reference`) | Author or review a referential knowledge doc (a skill's `references/` file, an @-imported doc, a Project Knowledge file) against its bundled rubric |
| `skills/make-llms-txt` | Procedural | both (`/make-llms-txt`) | Author or review an `llms.txt` (and `llms-full.txt`) to the standard shape |
| `skills/make-vision-memo` | Procedural | both (`/make-vision-memo`) | Author, or evaluate and improve, a vision memo (manifesto · reframe · case-for · synthesis); `doc-checker` grades the draft |
| `agents/experiment-runner` | Agent | dispatched | Runs ONE systematic investigation of a scorable system to a measured conclusion in its own context; preloads `research-methods` |
| `agents/intake-leader` | Agent | dispatched | The standing intake seat: mints durable records from raw seeds by applying the preloaded `file-bug`/`file-feature`/`file-task`/`file-leftovers` procedures inline — capture through record, then stop. Intake ONLY, structurally: its tool wall omits Agent and Skill, so it cannot dispatch builds or investigations (a bug record reports its resume command instead). Twin of `/bind-intake`, the host-adoption command; lives HERE, not teamwork's bind family, because preloads cannot cross plugin boundaries |
| `agents/doc-checker` | Agent | dispatched | Fresh-context critic for one rubric-bearing document (PRD/SPEC/LLD/ADR/PLAN/ROADMAP/TICKET/TASK plus reference doc/llms.txt/vision memo/rubric/handoff block/decomposition manifest/DESIGN.md); preloads `check-doc` + `doc-writing-rules` — closes the gap left by `check-doc` having no fresh-context agent pairing |
| `scripts/doc_lint.py` | Script | CLI + selftest + gate G10 (PostToolUse hook retired 2026-08-17, #466 — remove-all-hooks directive) | T1-T10: frontmatter/type/status/sections/ID-spine, orphan-ADR/RDD-citation/IDR-provenance/SPEC-agent-verification, and (`--spine` mode, T10) cross-file duplicate `id:` detection across `.claude/docs`'s adr/idr/lld/rdd families (closes #633); the accepted-ADR append-only rule (T4) is now enforced only at gate time, not on every write |

Folded in from the personal skill corpus per a `plan-plugin-split` partition decision: `measure`
(research-methods, make-rubric, the `experiment-runner` agent), `markdown-render` (markdown-to-markup,
html-to-markdown), and `knowledge-docs` (make-reference, make-llms-txt, make-vision-memo —
knowledge-pack authoring, originally `knowledge-forge` in this group, retired 2026-07-19 in favor
of harness's `make-pack`, the estate-wide factory-route name) — all small enough to fold into docs
rather than ship as standalone plugins.
Both ported agents soft-mention harness's `write-handoff` in body prose (not a preload — forge is a
sibling plugin) and fall back to a plain Status/Summary/Files changed/Tests/checks run/Evidence/Risks/Open
questions/Recommended next action block where harness isn't installed.

Rejected members (the design ledger): per-type skills x8 (cross-type asks are the majority — the
types form a pipeline; 8x listing budget for entangled "write the X" vocabulary); a separate
plan/roadmap family (same tests); a ninth BUG type (bugs are TICKET-shaped work items —
`kind: bug` plus five extra sections, no new template or validator needed); general rubric/report
types beyond bugs (Vol 3 covers them — flagged as the next wave, not built without demand).

Cross-plugin seams (soft, by design): docs uses the harness plugin's cross-cutting layer —
find-intent, break-down-problem, prompt-wording-rules, thinking-depth-rules — when installed, and
degrades to inline judgment when not. No hard edges cross the boundary.

Directories align with plugin names (ADR-0007).

PENDING (no version bump yet — coordinator bumps once the full #433 wave lands) · 2026-08-16 ·
issue #433 wave 3: the agent-file leg the v1.11.0 line below deferred now lands —
`docs/agents/product-leader-agent.md` moved OUT to `teamwork/agents/product-leader.md` (drops the
`-agent` suffix in the same move), sitting beside the `leading-product` skill it already paired
with. `product-lifecycle-rules`/`check-stage`/`doc-writing-rules` stay canonical here in `docs`;
the moved agent now reaches them only as soft cross-plugin named mentions (never a `skills:`
frontmatter preload — the hard plugin-boundary rule), with an explicit failure branch when `docs`
isn't installed. This agent file is non-conforming against the CURRENTLY-LIVE naming grammar
(ADR-0011 REQ-002's `-agent` suffix rule) until the naming-ADR Kim has ruled will supersede that
rule lands — intentional, tracked with its 4 teamwork siblings in the same wave.
v1.21.11 · 2026-08-23 · closes #891 (LLD-0025 W3): new plugin bytes — `harness_emit.py` (harness) now also derives a Pi overlay (`package.json` with `"pi": {"skills": ["./skills"]}`, plus `"prompts": ["./prompts"]` and one `prompts/<name>.md` template per command-only skill — R-5 amendment, #888: the human-only guard is preserved natively, a Pi prompt template expands only from a human keystroke) from this plugin's own manifest/SKILL.md frontmatter, alongside the Codex and Hermes overlays; `HARNESS-NOTES.md` gains a real Pi section, gated fresh by release_gate.py's widened G15 (`codex,hermes,pi`) — a cached install must refresh to receive the overlay. PATCH: overlay bytes only, no behavior change to this plugin's own skills.
v1.21.10 · 2026-08-23 · closes #890 (LLD-0025 W2): new plugin bytes — `harness_emit.py` (harness) now also derives a Hermes overlay (`plugin.yaml`, `__init__.py`'s `register(ctx)` — one `ctx.register_skill()` line per skill, nothing else registered this wave, `hermes-mcp.yaml` where `.mcp.json` exists) from this plugin's own manifest/SKILL.md frontmatter, alongside the Codex overlay; `HARNESS-NOTES.md` gains a real Hermes section, gated fresh by release_gate.py's widened G15 (`codex,hermes`) — a cached install must refresh to receive the overlay. PATCH: overlay bytes only, no behavior change to this plugin's own skills.
v1.21.9 · 2026-08-23 · closes #886 (LLD-0025 W1): new plugin bytes — `harness_emit.py` (harness) derives a Codex overlay (`.codex-plugin/plugin.json`, per-skill `agents/openai.yaml`, `HARNESS-NOTES.md`) from this plugin's own manifest/SKILL.md frontmatter, gated fresh by release_gate.py's new G15; a cached install must refresh to receive the overlay. PATCH: overlay bytes only, no behavior change to this plugin's own skills.
v1.21.8 · 2026-08-23 · closes #880: `file-bug`'s Phase 1 resume path could silently no-op on a pre-populated-Findings record — a fork could satisfy a branch's routing arrow (Phase 6/closed-stop/Phase 5) without emitting anything, and Phase 6's read-back presupposed a same-turn Phase 5 dispatch so a direct resume from Phase 1 could skip it. Two wording fixes per the wording-checker's root-caused findings: (1) every Phase 1 branch now ends only after running its routed phase's own body to completion and, as the last thing that phase does, emitting a mandatory typed status line (`Resumed: <id> · route:<phase6|phase5|closed-stop> · status:<value read from the record's own state field, not inferred from comment text>`), closing the closed-state-stop's bare-report ambiguity too; (2) Phase 6's opening now names both entry paths (post-dispatch return, or direct resume from Phase 1) as owing the same read-back; (3) Done-when gained a resume-scoped addendum so pre-existing record state never alone satisfies it. No description edit, evals unchanged. Two fresh-context `harness:wording-checker` passes — first FAILED on two residual gaps, both fixed, second PASS (report: `docs/skills/file-bug/evals/audit-report-880.md`). `release_gate.py docs`: CLEAN. Rebased onto #883's v1.21.7 (unrelated file, `check-stage`), re-slotted 1.21.7 -> 1.21.8 per plugins-marshal's VALUE-race heads-up.
v1.21.7 · 2026-08-23 · closes #882: `check-stage`'s `lifecycle_census.py` `roadmap_state()` probed only a root-level `ROADMAP.md`, so any repo on the `.claude/docs/` docs-root override (#514 — this workspace) got `roadmap.present: false` against a real, active `roadmap/roadmap-<scope>.md`. Now probes root `ROADMAP.md` first, then `<docs-root>/roadmap/*.md` gated on `doc-type: roadmap`, and reports the resolved `path`. Selftest gained the #882 pair (typed roadmap under `.claude/docs/` read; a non-roadmap `.md` in that folder ignored). SKILL.md step-1 wording updated to match. Verification tier: floor-tier in-loop — pure script change under its own selftest; the SKILL.md edit is a factual one-clause restatement of what the collector reads, no trigger/procedure change. `release_gate.py docs`: see PR.
v1.21.6 · 2026-08-22 · agent model retier (Kim's estate-wide ruling, live session): experiment-runner → sonnet+xhigh; doc-checker → sonnet+high.
v1.21.5 · 2026-08-20 · closes #803 (fix-old-names debt): one stale live pointer in `file-feature/SKILL.md` — a `dispatch-feature` rename-lineage mention already read correctly ("renamed from `dispatch-feature` per ADR-0010") but was still flagged by `authorkit/scripts/fix_old_names.py`, since replacing it would have broken the sentence's own meaning (self-referential "renamed from dispatch-ticket"); frozen in place with `<!-- fix-old-names: keep -->` rather than rewritten. Verification tier: floor-tier in-loop (no fresh-context `harness:skill-checker` dispatched) — the edit is an inert comment annotation, no change to the skill's trigger surface, procedure, or any routing-visible text. `release_gate.py docs`: CLEAN.
v1.21.4 · 2026-08-20 · closes #804: the `file-feature`/`make-reference` knowledge-shaped intake seam — t07 ("this 'feature' request is really asking us to document our color contrast standards as a reference corpus") confirmed stolen 3-of-3 by `make-reference` in the post-rename scoped re-judge (#802 follow-up; the seam predates that rename). Tuned rather than accepted-ambiguous: `file-feature`'s description now leads with the knowledge-shape claim explicitly, keeping the discriminating "feature request" framing inside the quoted trigger exemplar itself ("when it's a feature request that turns out to be 'document/capture X as a reference corpus,' it routes HERE FIRST"); `make-reference` gained a reciprocal NOT-for-intake fence folded into its existing make-pack fence to stay within the 3-fence/700-char diet. Both `evals.json` updated: `file-feature`'s t07 prompt left byte-identical (the fix is proven against the same flagship phrasing that stole it, never dodged by re-keying the case); `make-reference` gained n14 (the reverse control, t07's exact prompt, expect no-trigger) and t13 (the SAME topic with the feature-framing stripped, expect trigger — proves the fence discriminates on request framing, not topic vocabulary). Two fresh-context `harness:skill-checker` passes per description: the first caught a real diet-budget overshoot (815/826 chars, 4 fences on make-reference) and an unqualified exemplar risking a reverse false-positive; both fixed and re-verified PASS. Scoped blind routing re-judge (docs plugin menu, 3 independent `harness:routing-judge` dispatches, no tools, prompts inlined): both directions 3-of-3 clean — the feature-framed prompt routes to `file-feature`, the plain non-intake phrasing routes to `make-reference` — no new stolen/leaked cases in either suite. `release_gate.py docs`: CLEAN.
v1.21.2 · 2026-08-19 · closes #733 (one of three pre-existing routing residuals from #720's full-suite re-judges, resumed post-#732-merge): `agent-harness-rules`' description-fixed — its 'judging a criterion agent-runnable vs human-only' trigger phrase was near-paraphrasing n01's REQUIREMENT question ("does this SPEC's Acceptance section require an Agent verification answer"), leaking 2-of-3. Reworded to 'judging HOW to make a criterion agent-runnable' and sharpened the NOT-fence to name the required-section wording verbatim; reciprocal positive case added to `doc-writing-rules` (its new t11). Rebased onto `origin/main` post-#732-merge (`--onto` past the now-redundant `bug/720` commits, whose own content is already the v1.21.1 line below); re-slotted 1.21.1 (this branch's original claim, taken before #732 merged) -> 1.21.2, the next-free slot off main's post-merge v1.21.1.
v1.21.1 · 2026-08-19 · closes #720 (routing fence + t07 ruling, sweep #676): `agent-harness-rules`' description gains trigger-voice phrasing "or asking what instruments we already have before building a new verification harness" (t05, stolen 2-of-3 by harness-facts over shared 'harness' vocabulary). Description-only, no eval change (this plugin's own case) — harness's `check-state` t07 eval expectation flipped separately (harness 3.16.2). Re-slotted twice post-merge: rebased onto #729's v1.20.2 first (taking 1.20.3), then a second VALUE-race rebase onto #734's v1.21.0 (research-leader/research-rules, closes #721) — taking 1.21.1 rather than re-shipping either prior number. Fresh-context `harness:wording-checker` pass per plugin-authoring.md's semantic-edit invariant.
v1.21.0 · 2026-08-19 · closes #721 (the `lld-0023-research-specialist-deliverable-plan` follow-up build, PR #712): new agent `docs:agents/research-leader.md` — a NEW sibling to `harness:fact-finder`, synthesis-permitted (best practices, case studies, practitioner conversations, unique insights), not a fact-finder contract upgrade (Resolution 1 stays untouched; a disjointness note was added to `fact-finder`'s own body, the R-1-named mitigation locus). Tools `Read, Grep, Glob, WebSearch, WebFetch, Write`, no `Edit`, `model: sonnet`/`effort: high`, preloads its own owning skill `docs:research-rules`. New owning skill `docs:research-rules` bundles `references/DELIVERABLE-SCHEMA.md` (the six-field-per-finding schema) and `references/rubric.md` (the four closed axes — knowledge, actionable, grounding, novelty-vs-known — with the LLD's 1/3/5 anchors, a stated ≥3-on-all-four accept gate, and a measurement-plan column added for `make-rubric`'s own D5), plus `evals/evals.json` (8 positive / 8 adversarial-negative cases fencing off fact-finder, research-methods/experiment-runner, make-rubric, check-doc/doc-checker). Checker path left `doc-checker` unedited (Resolution 3 — its existing "reference doc... against the owning skill's bundled rubric.md" charter already covers this class). **Home decision (docs, not harness), recorded per the ticket's own IDR-0007 job-evidence + checker-seat-consolidation instruction:** the doctrine this agent reconciles against (`research-methods`, `pack-writing-rules`, `make-rubric`) and its checker (`doc-checker`) are both docs-owned; consolidating the generator, its rubric, and its checker in one plugin is the job-evidence IDR-0007 asks for, and `fact-finder` living in harness does not pull its sibling there since Resolution 1 makes the two agents disjoint by design, not co-located by convention. **Model/effort deviation from the LLD's own Components sketch, disclosed:** the LLD sketched `model: fable / effort: high` (the `experiment-runner`/`chore-planner` measured-loop-and-queue-synthesis exception row); this build pins `sonnet + high` instead per this ticket's own explicit dispatch instruction ("gather+synthesis seat — sonnet-class, never fable"), treated as this seat's own charter (a T0/T1 directive, not incidental record text) and followed over the LLD's own non-locked (`status: draft`) Components sketch — recorded as the deviation, not silently reconciled. **Naming — two live-ruled corrections, both surfaced not papered over.** The LLD's own sketched name `research-specialist` failed G12 outright (neither `research` nor `specialist` resolved in any lexicon). Kim's first ruling registered `research` in `naming.manifest.json`'s `topic_lex` and renamed the sketch to `research-leader` — insufficient for the AGENT's own orchestrator `{scope}-{role}` production, which resolves scope against `ObjectVocab ∪ ProcessLex` only (ADR-0015 D2, deliberately excludes `TopicLex`); confirmed via `authorkit/skills/naming-audit/scripts/validate.py --scope grammar` before any further change was made, never assumed. Kim's second ruling moved the registration to `ObjectVocab` — clears the agent's own name (`research` ∈ `ObjectVocab`, `leader` ∈ `RoleLex`, the `*-leader` dispatched-seat production this agent's shape fits; `research-finder` was rejected as colliding semantically with `fact-finder`'s own no-synthesis contract) but left ONE residual finding: a skill's plain nominal production draws only from `ObjectVocab ∪ ProcessLex`, never `RoleLex`, so `research-leader` still failed as a SKILL name even once it passed as an agent name — caught and reported rather than declared fixed prematurely. Resolved by keeping the agent `research-leader` and renaming only the owning skill to `research-rules` (ADR-0014 D1's `-rules` reserved tail — a knowledge/doctrine skill, exactly what this one is — `research` ∈ `ObjectVocab` clears the tail's own topic-phrase check), matching this estate's existing pattern of a `*-leader` AGENT owned by a distinctly-named skill (`fleet-marshal` ↔ `fleet-rules`, `build-leader` ↔ `dispatch-ticket`). Both renames applied across the agent + skill directories, frontmatter `name:`/`skills:` preload lines, every internal cross-reference, `evals.json`'s `skill` field and prompt text, `fact-finder`'s disjointness note, and a dated amendment note on `lld-0023-research-specialist-deliverable-plan.md`'s own Components section (its `id`/filename stays unchanged — a spine id, never renamed post-hoc). **`release_gate.py docs`'s own G12 now PASSES** — `python3 authorkit/skills/naming-audit/scripts/validate.py --target docs --manifest naming.manifest.json --scope grammar` reports zero findings on either `research-leader` or `research-rules`. `harness:agent-checker` fresh-context review found 3 Major/3 Minor (persona register, a restated schema with no `skills:` preload, no input-quarantine line, thin failure branches, description carrying mechanism, naming/model disclosure) — all applied inline (agent now third-person, `skills: [research-rules]` preloaded, quarantine + failure-branch bullets added, mechanism sentence trimmed from the description), re-lint clean. One proving-run dispatch (a real canary-rollout-practices topic, 7 findings across 4 categories) self-scored K1=4/A1=5/G1=4/N1=4; independent `doc-checker` grading converged closely (K1=4/A1=5/G1=4/N1=4), gate PASS on all four, with real findings (a per-row `novelty` field was blanketed at file-end instead of per finding — fixed in the agent's own body instruction so the next real dispatch doesn't repeat it; two self-score miscounts) — the rubric discriminated correctly rather than rubber-stamping. Full `/check-routing docs` sweep (all 22 model-invocable suites, 423 cases, single-pass blind judges, no contested-case voting round run — scoped out as pre-existing-suite noise unrelated to this slice, named not fixed) plus a post-rename single-suite re-proof on `research-rules`: 16/16 baseline held both times (the first single-judge pass on the final `research-rules` menu flagged 2 contested cases — one stolen by `research-methods`, one skipped outright; both resolved correctly 3-of-3/3-of-4 on a contested-case vote round, exactly the single-judge-noise pattern this estate's own routing doctrine names, not a real defect) — zero stolen/leaked/dead survives the vote, routing surface (the description) unchanged in substance across both renames; the 10 pre-existing failures from the full sweep touch none of this slice's artifacts (`agent-harness-rules`/`check-doc`/`file-feature`/`file-leftovers`×5/`make-rubric`(1 skipped)/`research-methods` — a routing-report snapshot recorded at `.claude/ops/routing-report.json`, dead=1/stolen=2/leaked=6 aggregate for the docs plugin, for a later, separately-ticketed cleanup). Verdicts in gh#721's dated Findings. `release_gate.py docs`: CLEAN — G12 PASS, every other gate (lint, structure, selftests, evals, G14 monotonicity) clean.
v1.20.2 · 2026-08-19 · gh#711 (lld-0024), rebased onto #713/#725's v1.20.1: `file-feature`'s Phase 4 classify step gains a UI-shape detection paragraph — a component/layout/flow-shaped seed consults `screens:feature-intake-rules`, a token/palette/typography-shaped seed consults `design:token-feature-intake-rules`, both by soft named mention (degrades gracefully with the gap named in Scope/Open where either plugin isn't installed). Reciprocal NOT-fence + one no-trigger eval case added naming both new packs. Lands alongside, not in place of, #725's terse close-out rewrite below — both edits sit in disjoint sections of `file-feature/SKILL.md` and merged clean with no manual reconciliation beyond this version-slot rebump. `skill_lint`/`eval_check` clean; `/check-routing docs` re-proof at 16/18 baseline owed post-rebase.
v1.20.1 · 2026-08-19 · closes #713 (marshal feed noise reduction, lever 3): `file-bug`/
`file-feature`/`file-task`'s SKILL.md close-out contracts rewritten uniformly to ONE terse line —
`Filed: <id> · kind:<bug|feature|task> · owed-questions:<N>` — replacing a verbose form that
restated every unasked clarifying question's own text plus the resume command inline. The
questions' own full text still lands in the record itself (Classification for file-bug,
Scope/Open for file-feature/file-task) exactly as before; only the close-out REPORT got terser,
never the record. Each file's own companion Phase 2 sentence (describing what the close-out will
owe) reworded to match, so no file is left half-edited between what it promises and what it
delivers; an explicit exception-notes clause states the one line is the whole close-out in the
normal case, with pre-existing add-ons (a skipped Issue Type, a backend-create fallback, the
project-docs offer) appending as extra lines only when they occur. Two stale cross-references
(file-feature citing a "close-out's named question" it no longer names; file-task's Phase 1
claiming the close-out "suggests" a resume format string it no longer echoes) fixed in the same
change. Fresh-context `harness:skill-checker` pass across all three files: initial verdict FIX
FINDINGS FIRST (2 majors in file-feature — stale "batched list" wording at two sites, and the
one-line claim conflicting with the pre-existing project-docs-offer paragraph with no exception
carved out), both fixed same-change; re-lint clean on all three. `release_gate.py docs` clean.
v1.20.0 · 2026-08-19 · closes #649 (LLD lld-0013 v2, Resolutions 8-10; R11/R12 already shipped, not rebuilt): `make-artifact`/`artifact-rules` gain (1) a records-tree composition phase (Phase 1b) — a new `composition-model.md` reference maps Intent/User-Story/Concept/System record layers (brief/IDR → PRD/RDD → ADR/reference → SPEC/LLD, harvest output optional-where-present) to chapter/section patterns, degrading to the prose-draft path with the gap named in the provenance footer when the target's records tree isn't canonical; (2) a missing-design-system fallback (Phase 1) — `design` installed synthesizes one via make-palette/pick-fonts/make-design-system (provenance marked SYNTHESIZED), `design` absent emits a doctrine-neutral neutral-monochrome page on the artifact type doctrine's own faces/shell (gap named, never a fail/unstyled exit); (3) the emit-side type-doctrine binding — `css_build.py` now emits a fixed default-binding block (body/reading -> system-ui, interactive elements -> the mono stack, regardless of the source system's faces) plus an override-comment justification mechanism `design:artifact-styling-rules`' `artifact_check.py` already honors as its suppression path. Independent code-checker review caught a HIGH-severity bug before merge: the emitted doc comment's own prose originally embedded a literal `/* override: ... */` example, and CSS comments don't nest — the first such sequence silently terminated the whole comment early, corrupting every doctrine rule below it (verified: only the mono/interactive rule survived, the body binding silently dropped). Fixed by describing the override syntax in prose with no literal comment-close sequence inside the comment's own text; a new selftest guard asserts no stray close-sequence survives after stripping the block's own leading comment, closing the whole class (incident → infrastructure, same day). Selftest also gained: a doctrine-neutral-fallback reverse control (all 14 live roles bind clean from neutral grayscale input, Resolution 9.2) plus a grep-assert of the provenance gap line's exact required text; the override-path assertion now mirrors `artifact_check.py`'s REAL 120-char preceding-window algorithm (the prior version tested only a locally-built fixture against its own locally-built regex, exercising no production code) with its own reverse control (a comment outside the window must not suppress); the never-binds-a-brand-font negative control now asserts exact equality against the doctrine's own stack constants rather than a substring check a real brand name would have passed. `design:artifact-styling-rules`' `artifact_check.py`'s own `CSS_BUILD_OUTPUT_FIXTURE` cross-script regression fixture re-captured verbatim from the corrected build (byte-verified identical; finding count unchanged — the new rules resolve on-doctrine by construction). Both skills' descriptions and evals gain the new trigger vocabulary; blind routing simulation (6 suites: make-artifact, artifact-rules, make-doc, markdown-to-markup, project-facts, harness-facts) confirms zero leakage into or out of the changed family. `release_gate.py docs` clean.
v1.19.11 · 2026-08-18 · closes #683: `css_build.py`'s `ROLE_ALIASES` extended with a mechanically-derived `CONTAINER_GRAMMAR_ALIASES` (never a second hand-authored table) recognizing Material's `-container` naming for the same reduced-tier/soft-fill semantic the reference grammar spells `-soft` — a real (non-reference) design system export using that grammar no longer silently drops 8 of the doctrine's 14 live roles. Detection is generic (any source-role key containing `-container`, never one hardcoded literal); once detected, `check_doctrine_completeness` requires every one of the 14 doctrine roles to resolve under one grammar or the other, or the build fails loudly (exit 1, `NormalizeError` naming the unresolved role) — never a silent drop, and never tightened for a source that never signals the alternate grammar in the first place. `script-interface.md`'s exit-1 clause and output-contract bullet updated to match. New selftest coverage: a complete `-container`-grammar fixture (`FIXTURE_CONTAINER_GRAMMAR`) resolving all 14 doctrine roles, plus a negative control (a role dropped from both grammars still fails loudly, naming it). Cross-checked: the #679-landed cross-script regression fixture (css_build output → `artifact_check.py` clean) still passes unchanged. Rejected: doc-only reconciliation guidance with no mechanism change (would still require every non-reference consumer to hand-map their own tokens.json before every build — the exact manual tax the mechanism now closes); a second Material-only hardcoded table (rejected for the same reason `ROLE_ALIASES` itself avoids re-deriving anything — the mechanical derivation from the existing table covers every family with no new authored surface to drift). `release_gate.py docs` clean.
v1.19.10 · 2026-08-18 · closes #692 (docs slice, folded with teamwork/harness siblings in one PR): `recurrence-audit`'s first incident-class seeding round adds an incident-class citation tag (class `emitter-vs-checker-drift`, evidence #662, seeded 2026-08-18) to `artifact-rules/references/script-interface.md`'s existing "Cross-script regression fixture (#662)" section — additive citation only, no procedure change, no frontmatter/description touch, no `evals.json` update owed. `release_gate.py docs` clean.
v1.19.9 · 2026-08-18 · closes #670 (docs slice, folded with teamwork/harness siblings in one PR, precedent #639): `file-bug`'s fix-inline branch names the fresh-context checker dispatch UNNAMED and synchronous — a named dispatch strands the report at the root session instead of this seat (a fix fork's own checker dispatched `chk667` stranded its verdict this way, 2026-08-18 — the gh#154/#157 class, re-proven), citing harness's `agent-writing-rules` never-name rule as canon (soft mention, no restatement). Body-only edit, no frontmatter/description change — no `evals.json` update owed. `release_gate.py docs` clean.
v1.19.8 · 2026-08-18 · closes #662 (docs half): `css_build.py` emits the artifact page's own UNPREFIXED short role names (`--paper`, `--ink`, `--accent`, …) via a new `ROLE_ALIASES` mechanical lookup transcribing design's `token-architecture.md` 14-live-roles table — never `--c-<role>` (superseding lld-0013 Resolution 6's original emission, amended in place with a dated supersede note, never a silent edit). Two source roles aliasing to the same short property must resolve identically or the build fails naming the conflict; `--mono-bg` derives from `--chip` when the source carries no literal of its own. Mermaid re-theme block rebound to the new short names. `script-interface.md` rewritten to name the short-role output contract, the `ROLE_ALIASES` mapping, the `var(--font-*)`-is-on-doctrine note, and the new cross-script regression fixture (its own section). New selftest coverage: an alias-collision negative control, a `mono-bg` derivation check, a `--c-` absence guard, and a cross-script regression fixture proving verbatim output satisfies design's `artifact_check.py` ground checks (mirrored, not imported — plugin-authoring's hard cross-plugin-script-path boundary). Ships alongside design v1.1.2 in the same PR (one coherent contract fix, precedent PR #651). Rejected: leaving `css_build.py`'s emission as `--c-<role>` and pushing the rename onto every consuming page instead (the exact manual-rename tax #662 was filed to retire). `release_gate.py docs` clean.
v1.19.7 · 2026-08-18 · closes #654: `file-feature`'s SKILL.md gains a pre-fork grill step for big/open-decision-space seeds only (Kim's 2026-08-18 ruling) — since this skill is fixed at `context: fork` and its own Phase 2 already established the fork has no `AskUserQuestion` channel at all (gh#541), the interactive ask-sharpening round runs OUTSIDE the fork, in the caller's live session, via a soft cross-plugin mention of `teamwork:grill-the-ask` that degrades gracefully where teamwork isn't installed (never a preload or script-path cross-plugin reference). Fresh-context skill-checker's major finding (the fork can't verify the round ran before invocation) addressed with a fallback clause: an ungrilled big/open seed is captured anyway, "pre-fork grill skipped" named in Scope/Open and the close-out, never a mint-blocker. Body-only edit — no frontmatter/description change, so no `evals.json` update is owed under plugin-authoring's description-routing-surface rule. `file-bug`/`file-task` intake shapes are unchanged. `release_gate.py docs` clean.
v1.19.6 · 2026-08-18 · closes #657 (#655 wave 1, decisions 1/4/5): `doc-writing-rules` gains the full scope-grain table (BRIEF/IDR/ROADMAP=app · PRD/RDD=app+feature · SPEC/LLD=feature/component · ADR=decision-scoped any grain; TICKET/TASK/PLAN not grain-governed) plus the owed-chain canon text (ticket → +LLD → +ADR → +SPEC → +PRD entry → +RDD, mechanized in a later `teamwork:dispatch-ticket` wave). New `scope:` (per-type grain) and `audience:` (one-or-more of human/product-seat/planner/builder/reviewer/any-agent, no any-agent default) frontmatter fields, both required on NEW mints only — grandfather + ratchet (ADR-0011 D8 precedent): the existing corpus is exempt from presence, never from correctness when a value is present. Mechanical guard: `doc_lint.py` gains T12 (scope: grain FAIL) and T13 (audience: FAIL), both gated by the new git-HEAD-aware `is_new_mint()` helper (T4's `head_is_locked_ledger()` shape, inverted). All 11 templates carry an instructive `audience:` example; the 8 grain-governed templates also carry `scope:`. `make-doc`'s Phase 2 references both fields at capture time. Selftest fixtures prove new-mint FAIL, grandfathered non-FAIL, and out-of-vocabulary/out-of-grain FAIL regardless of grandfather status; a corpus-clean sweep proves all 66 existing `.claude/docs` files pass T12/T13 clean under their real `is_new_mint()` status. `release_gate.py docs` clean.
v1.19.5 · 2026-08-18 · closes #652: IDR's canon gains an explicit scope requirement (whole-app/product-thesis claims only — a feature/component/screen/endpoint claim is PRD/SPEC territory, never an IDR even if it passes the falsifiability test), PRD's ladder row states its own dual app+feature scope. `make-doc`'s IDR admission test gains the scope gate alongside falsifiability, redirecting a feature-grain claim rather than silently minting it. `product-lifecycle-rules`' `three-loops.md` states the North-star loop's IDR binding explicitly (a feature-grain hypothesis belongs to the Releases loop's PRD/SPEC, never promoted "up" as an IDR). Mechanical guard: `doc_lint.py` gains T11 (FAIL) — an IDR `## Claim` naming a feature/component/screen/endpoint grain (word-boundary heuristic, inline-code spans stripped so a skill-name mention like `` `file-feature` `` never false-positives) — verified clean against all 11 of this repo's own locked IDRs. `release_gate.py docs` clean.
v1.19.4 · 2026-08-18 · #650 (lld-0020): `artifact-rules` slimmed to procedure-side standards only (content classification, script interface, refresh procedure) — visual/styling doctrine (tokens, type, mermaid, shells) migrated wholesale to design's new `artifact-styling-rules` pack. `design-system-consumption.md`→`script-interface.md`, `shell-doctrine.md`→`content-structure.md` (both trimmed), `mermaid-style.md` retired. `make-artifact`'s Phase 3/4 citations updated to the new file names/pack. Both packs' evals re-scoped with reciprocal fences. `release_gate.py docs` clean (G14 only fail pre-fix; version bump resolves it).
v1.19.3 · 2026-08-18 · closes #634: `doc_lint.py`'s T5 REQ- check widens `\bREQ-\d+` to
`\bREQ-([A-Z]+-)?\d+` — a requirement ID may carry an uppercase infix for a distinguishable
per-doc pool (`REQ-P-001`, `REQ-R-001`), operator-ruled 2026-08-18 on cross-repo evidence
(gen-ui-kit#1625/#1627); selftest gains both forms plus a negative control (a lowercase infix
still warns). `doc-writing-rules`' ID-spine bullet (Standards item 3) documents the convention.
`release_gate.py docs` clean.
v1.19.2 · 2026-08-18 · closes #622 (docs slice, harness sibling same PR): the feedback-intake
door instrument for idr-0008 (LOCKED)/adr-0021 — no new door: `.github/ISSUE_TEMPLATE/
{bug,feature,task}.yml` already are it. `doc-writing-rules`' `references/backend-resolver.md`
gains a "Provenance tagging" section (the canonical `user-signal` label rule: git-native
record author's login vs. `friendlies.json`'s `policy.confirmed_by`, per idr-0008's clause-(a)
resolution — count what the spine can see, no adoption/install telemetry); `file-bug`/
`file-feature`/`file-task` each cite it at their Option-B record/resume phases. Full design in
`lld-0017`. Fresh-context `harness:wording-checker` pass over the semantic body edits. `release_gate.py docs` clean.
v1.19.1 · 2026-08-18 · closes #633 (docs slice, harness+teamwork siblings same PR): `doc_lint.py` gains T10 + a new `--spine [docs-root]` CLI mode — sweeps `.claude/docs/**` for two adr/idr/lld/rdd documents claiming the same (family, number), FAIL on collision, keyed on the number not the full `id:` string (a colliding pair plausibly differs only in its slug). Reproduces the 2026-08-18 incident (two parallel builds both minted `lld-0011`, caught only by a coordinator's manual pre-merge read) as a selftest fixture, plus two negative controls. Reaches release_gate.py's G10 through harness's `docs_check.py` (its own R7, a deliberate self-contained duplicate, not an import — the hard plugin-boundary rule forbids a harness script reaching into this plugin's `scripts/`) — no new G-check. `doc-writing-rules`' ID-spine section gains the companion dispatch-rule bullet (re-read the spine's highest id off `origin/main` immediately before numbering); `teamwork:dispatch-ticket` gains the mirrored line. `release_gate.py docs` clean.
v1.19.0 · 2026-08-17 · closes #619: new skills `make-artifact` (procedural; bundled `css_build.py` selftest-proven token→CSS build, `light-dark()` pairs, mandatory font fallbacks) + `artifact-rules` (4-axis references corpus: design-system consumption, shell doctrine, mermaid house style, refresh procedure — the 2026-07-16/2026-08-18 memory rulings now shipped rules). Full resolutions in `lld-0013` (renumbered twice: the ticket-keyed `lld-0619` draft -> `lld-0011` against the then-current 0001-0010 spine -> `lld-0013` once `origin/main` advanced mid-build and minted its own real `lld-0011`/`lld-0012` first). Reciprocal fences in `make-doc`/`markdown-to-markup` (+ design's `make-design-system`, shipped as design v1.0.9). New `object_vocab` entry `artifact` proposed but NOT registered this pass — `naming.manifest.json`'s confirm gate (manifest-authoring, #525) has no live human to confirm with in this build; flagged as an open follow-up, not silently substituted. `release_gate.py docs`: G12 red pending the `artifact` object_vocab confirm (#525); every other gate (lint, structure, selftests, evals, G14 monotonicity) clean.
v1.18.0 · 2026-08-17 · closes #611: RDD↔Issue binding ruled (prose citation in `## Sequencing`, never
frontmatter): `doc-writing-rules` RDD section + `rdd.md` template comment.
v1.17.0 · 2026-08-18 · closes #613 (2nd of the serial 2-ticket chain, #612→#613, shared docs slot): new skill `skills/harness-facts` harvests the same substance as `project-facts` (v1.16.0 below), reframed for a coding agent's harness — cites the shared `harvest-core.md` core, weighted Inside-Out-higher. New vs the sibling: an install-ready output contract (entry-file section, `.claude/rules/` files, pack seed candidates, dispatch-context digest, run manifest — staged, never installed in place) and a WITH-vs-WITHOUT delta eval. Full resolutions in `lld-0009` (doc-checker ratified). Named `harness-facts` not the ticket's `harvest-project-context` lean — same unattended G12 constraint as `project-facts`; already-registered vocabulary, rejecting a `project-`-prefixed name and a `*-rules` tail. Reciprocal fences both directions; sibling's stale `#613` placeholders corrected, name-only. `release_gate.py docs` clean.
v1.16.0 · 2026-08-18 · closes #612 (1st of a serial 2-ticket chain, #612→#613, shared docs version slot): new skill `skills/project-facts` harvests a project's domain-knowledge corpus — topic zones discovered per project (never fixed), scored two-axis (Outside-In x Inside-Out, weighted Outside-In-higher) against a new shared core (`references/harvest-core.md`) sibling #613 will cite, never duplicate. Full design resolutions + rejected alternatives in `lld-0008`. Named `project-facts` not the ticket's `harvest-domain-knowledge` lean — same unattended G12 naming-gate constraint as `agent-harness-rules` (v1.15.0 below); chose already-registered vocabulary instead (verified 0 new grammar errors). New eval-harness.md is a genuine new payload-layer harness per `agent-harness-rules`. Reciprocal fences in `make-reference`/`make-rubric` evals. Fresh-context `harness:skill-checker` pass; `release_gate.py docs` clean.
v1.15.3 · 2026-08-18 · closes #608 (docs slice, riding alongside teamwork's `dispatch-ticket` fix in one PR): `file-bug`'s Phase 5 gains a read-and-compare clause for the redirect marker's new `claim:<claim-comment-url>` token — live repro adiahealth/gen-ui-kit#1593 showed a pre-existing claim on a redirected ticket (a coordinator's own on-behalf-of claim, per teamwork `fleet-rules`' Section 2 amendment) being read as a competing seat and standing down. Now: the named claim comment still current on the record → proceed under it, never stand down; missing/superseded/mismatched → dedup preserved exactly as before, stand down and report the conflict. Rejected alternative (recorded inline): treating the bare marker as blanket authorization with no comparison — would erase dedup for every dispatch-ticket-sourced bug, including the genuine third-party case the acceptance criteria require preserved. No description change, no evals ride-along owed. Fresh skill-checker pass; `release_gate.py docs` clean.
v1.15.2 · 2026-08-17 · finishes #566's deferred item (#542, closed): `doc_lint` gains T9 (WARN) — a SPEC missing `## Agent verification` warns, matching the T6 orphan-ADR precedent (WARN because the 3 live draft SPECs — spec-naming-convention, spec-linear-adapter, spec-ticketing-watch-triage — predate the section and are retrofit debt, not because the requirement is soft). Verified: all 3 warn exactly, nothing else changes for them; the spec.md template (already carrying the section since v1.15.0) stays silent. Positive+negative selftest fixtures added. No description change, no evals ride-along owed. `release_gate.py docs` clean.
v1.15.1 · 2026-08-17 · closes #568 (Wave 2 of `prd-agent-testability.md`): `file-feature`'s Phase 2 extraction captures how a likely-Work idea's built result will be agent-verifiable (which assert layer, citing `docs:agent-harness-rules` rather than restating it) under the same capture-or-gap treatment as every other extraction question — no live `AskUserQuestion` round (a `context: fork` skill has no question channel, per #541); Phase 5's payload contract carries the answer into Acceptance, or "needs a harness first" into Scope/Open when unanswered. Scope held to file-feature only, per the ticket — no other intake skill touched this wave. No description change, so no evals ride-along owed. Fresh-context `harness:skill-checker` pass, findings fixed; `release_gate.py docs` clean.
v1.15.0 · 2026-08-17 · closes #542 (Wave 1 of `prd-agent-testability.md`, PR #559): `doc-writing-rules`' SPEC/PRD/LLD templates gain a `## Agent verification` section (template-carried, doc_lint T9 mechanization deliberately deferred); `check-doc` gains judgment criterion J7 (agent-testability); `make-doc` Phase 2 asks the question for SPEC/PRD/LLD. New knowledge pack `skills/agent-harness-rules` (5 axes, home ruled docs not harness) — named `agent-harness-rules` rather than the PRD's `agent-testability-rules` working title since G12's naming-grammar gate rejects the unregistered token `testability` and registering one needs a live confirm this unattended dispatch has no channel for. Wave 2 (file-feature intake) and T9 mechanization NOT built this pass. Reciprocal fences added to 3 sibling evals suites, verified via targeted blind routing-judge passes (all new cases route correctly). 4 fresh-context harness:skill-checker passes, all findings fixed; `release_gate.py docs` clean.
v1.14.0 · 2026-08-17 · ADR-0020 wave 5 (closes #523, docs slice — riding alongside teamwork's rather than a separate PR: the retiring `LEAD_HEAD_GRANDFATHER` constant grandfathers this skill too, so splitting the PR would leave a real gap between merges where estate-wide naming-audit fails): `skills/lead-intake/` → `skills/bind-intake/` (`name: bind-intake`, already skill-as-command shaped per #525 — no wrapper conversion needed, just the rename). Citers repointed: `agents/intake-leader.md`'s own NOT-fence, this README's live table row (row 18) and `agents/intake-leader`'s twin-reference row (row 28). Merged forward through #549's own `lead-intake` content fixes (ONE-batched-round host-mode delta, dropped stale reachability phrasing) — both changes coexist, this rename applied on top. Ledger/ADR/audit-report history of the old name left untouched.
v1.13.2 · 2026-08-17 · #548 docs batch: `lead-intake` restated its ONE-batched-round rule as its own host-mode delta (siblings no longer run a live round, gh#541), dropped the stale reachability phrasing. `file-bug` gained the missing clear-answered-gap clause its siblings had. `file-feature` fixed an orphaned "pointer line" disposition (now "offered and unanswered") and disambiguated its combined-resume trigger (`install-docs-index` fires only as the ENTIRE trailing text, else folds first). Fresh skill-checker passes ×3; no evals touched. Teamwork half of #548 stays open, blocked on #523.
v1.13.1 · 2026-08-17 · closes #541: `file-bug`/`file-feature`/`file-task` Phase 2 corrected — a `context: fork` dispatch has NO `AskUserQuestion` channel (falsifies the 2026-08-09 "forking doesn't remove the person" claim). Uniform degrade: capture-with-gaps names the unasked question(s), resume folds an answer in via Phase-1 (now unconditional/first). file-feature's Phase 6 offer fixed the same way. 3 fresh skill-checker passes; no evals touched. Details: `docs/skills/file-*/SKILL.md`.
v1.13.0 · 2026-08-17 · closes #514: `doc-writing-rules` gains a "Where documents live" host-override ladder (ADR-0019); `tidy-docs`/`check-stage` amended to consult it instead of hardcoding a root; `product-lifecycle-rules`/`make-doc` audited, no drift. Fresh skill-checker PASS ×2.
v1.12.1 · 2026-08-17 · closes #494: fixed `research-methods`' dead eval case ("stress-test this API before we ship", t12, PR #485's proof) — boundary-checked against `harness:experiment-runner` (its adversarial method already owns pre-ship stress tests, so this stays here) and added "stress-test this before we ship" to the description's Triggers. Fresh-context skill-checker PASS; blind routing re-proof 30/30, no new stolen/leaked.
v1.12.0 · 2026-08-17 · closes #477 (S8 lexicon amendment, ADR-0017/ADR-0018): `agents/intake-lead.md` → `agents/intake-leader.md` (+ intent twin), conforming via `{scope=intake}-{role=leader}` once `intake` registers in ObjectVocab. Full blast radius fixed: `lead-intake/SKILL.md`+`intent.md`, this README's live table rows, `teamwork/init-repo` SKILL.md+evals, `teamwork/README.md`, `doctrine.manifest.json`'s D05 path. Ledger/ADR/audit-report history of the old name left untouched. Two new ADRs (0017 RoleLex+10, 0018 make-/file- heads) + spec §14.6-§14.7 land alongside authorkit's manifest/validator change, same PR; both ADRs doc_lint clean + fresh doc-checker pass.
v1.11.3 · 2026-08-17 · closes #478 (harness companion, reciprocal side): `research-methods` gains a NOT-fence against `harness:thinking-depth-rules` — a recurring finding judged without a scorer is that skill's territory, not this one's baseline→change→remeasure loop — plus a reciprocal negative eval case (n16). Fence and case measured clean in a blind routing-judge proof (30/31 on the suite; the one miss is a pre-existing dead case unrelated to this change).

v1.11.2 · 2026-08-17 · plugin-shipped hook retired (#466, Kim's remove-all-hooks directive): `hooks/hooks.json` (the `doc_lint.py` PostToolUse wiring, T4's accepted-ADR append-only guard) deleted; `doc_lint.py` is unaffected and still runs via G10/CLI, T4 now enforced only at gate/CLI time, not per-write. No gate check asserted a hook must exist, so no gate amendment was needed
v1.11.1 · 2026-08-17 · issue #462 (overhaul #373 Wave-2 S5): `file-bug`/`file-feature`/`file-task` intake-template diet — backend-seam paragraph trimmed to a one-line pointer at backend-resolver.md; Failure-branches sections trimmed to only genuinely novel branches (no shared template file exists; edited directly). `skill-checker` PASS ×3; no description changes.
v1.11.0 · 2026-08-16 · issue #433: `product-authoring` skill full-moved to `teamwork/skills/lead-product` (Kim's ruling); `docs/agents/product-leader-agent.md` stays put, agent-file leg blocked on a grammar-ADR. `product-lifecycle-rules`/`check-stage`/`doc-writing-rules` stay canonical here, already soft-mentioned not preloaded, no boundary fix owed. teamwork bumps to 2.17.0 in step.
v1.10.4 · 2026-08-16 · IDR ratification round (#431): idr-0001..0005 locked, idr-0006 minted (success measure), brief Confirmed populated, Q5 deferred; new `provenance:` frontmatter key required on IDRs (doc_lint T8, FAIL-gated).
v1.10.3 · 2026-08-16 · file-leftovers boundary vs close-session sharpened structurally (#416, teamwork 2.16.1 companion): claims MANY-item/any-time sweep, disclaims a session-ending single verdict even phrased "capture"/"file". 3 mirror no-trigger cases added (n11/n12/n13). Routing proof 23/23, probes 3-of-3. `wording-checker` PASS.
v1.10.2 · 2026-08-16 · remove product-forge runtime mentions, supersedes #404 Addendum 2 (closes #411): `cold-start-adoption.md` step 4 rewritten native — `docs:doc-checker` fresh-context review replaces the `product-forge` soft mentions; flow is now orient→harvest→draft→review→ratify, no conditional plugin path; `product-leader-agent.md` updated to match. Per #404's same-day scope fence, the study-pass prior-art note stays minimal and a new soft "method-depth seam" defers JTBD/strategy/rubric method content to a future `product-management` plugin migration. Details in issue #411.
v1.10.1 · 2026-08-16 · product-authoring cold-start + schema follow-up, binding addenda landed after v1.10.0 merged (#404): first-class cold-start branch (orient→harvest→draft→council→ratify, `product-forge` soft mentions); new BRIEF doc type (north-star living index); explicit PRP→RDD+PLAN/ROADMAP/TICKET mapping. Details in issue #404.
v1.10.0 · 2026-08-16 · fleet bootstrap seat 4 (#404): new standing product seat — `agents/product-leader-agent.md` (WHY/WHAT + loop authority, spec-lock gate, IDR/PRP + living indexes, escalation-rides-citations; fable+high, dated tier-deviation line) plus its host-adoption pairing `skills/lead-product` (`lead-product`'s working name was unavailable under ADR-0011's new-name grammar). Details in issue #404.
v1.9.4 · 2026-08-16 · doctrine-audit D05 fix (closes #398, teamwork-side sibling): `agents/intake-lead.md`'s `disallowedTools` dropped the redundant, retired `Task` alias — `Agent` (already present) remains the sole block. A4 dispatch smoke test confirmed the wall still blocks `Agent` outright post-edit, recorded in issue #398. No behavior change.
v1.9.3 · 2026-08-16 · file-bug Phase 6 merged-state close criterion (#397): `done`/close now requires the fix ON MAIN — direct commit for a solo single-file fix, MERGED PR for campaign work, or an explicit deferral to the PR's own `Closes #` line; an unmerged fix leaves the record open with a dated Findings entry naming the PR. Closes the #380/#383 premature-close class (issue closed 13:41 on an unmerged branch, PR merged 15:01 — "landed in-tree" was ambiguous). Body-only edit, routing surface untouched, no evals.json change owed; fresh-context skill-checker pass per plugin-authoring.md.
v1.9.2 · 2026-08-16 · DE-standards adoption (#377): TICKET contract gains a required-at-close `## Rejected alternatives` section (ticket.md template + SKILL.md's type-contract note), same enforcement tier as the `## Findings` write-back convention — prior art PR #343's scope note, PR #347's no-split writeup. Two deliberate non-adoptions recorded (generator≠critic, ID-spine traceability — already stricter than the industry norm they'd import). No doc_lint.py T3 change — prose-tier, not a hard gate, matching Findings' own enforcement tier.
v1.9.1 · 2026-08-16 · checker-agent description diet (#357): doc-checker's description drops the shared fresh-isolated-context / maker-never-grades-own-document / gap-map boilerplate (collide.py's top cross-plugin *-checker collision baseline, 6 agents, 103.9-158.4); doctrine already lived in the body, strengthened with an explicit never-grades-own clause. collide.py re-run: ↔design-system-checker 158.4→78.3, ↔layout-checker 136.7→62.4, ↔flow-checker 92.7→58.9. Batched fresh-context critic pass (6 files) clean; no evals.json changes owed. Siblings in design/screens/teamwork trimmed same PR.
v1.9.0 · 2026-08-15 · overhaul W2-D (Issue #351): `file-bug`/`file-feature`/`file-task`'s restated ~330-char backend-seam paragraph (Jaccard 0.54–0.75, authorkit bloat-audit's own worked example) collapsed to a one-line pointer at `doc-writing-rules/references/backend-resolver.md`, already the canonical definition of the three options/ruling shape/fallback — behavior unchanged, only the retelling moved
v1.8.0 · 2026-08-16 · `check-stage` (Issue #336): read-only lifecycle-position report skill + `lifecycle_census.py` + evals; three `product-lifecycle-rules` forward-fences repointed (closes #321); `harness:check-state` gains the reciprocal disambiguation (harness 3.8.9)
v1.7.0 · 2026-08-16 · RDD doc type (Issue #332, `prd-rdd-framework.md`): doc_lint TYPES gains `rdd` (draft/locked/superseded, Scope/Acceptance/Sequencing/Completion), T4's ledger-lock guard generalized a third time (locked-RDD), new T7 FAIL (locked-or-beyond RDD missing `decision-refs:`/`dri:`), `references/templates/rdd.md`, doc-writing-rules/make-doc/project-docs gain the RDD row; `decision-watcher`'s RDD-escalation extension stays deferred until enough RDDs exist
v1.6.0 · 2026-08-16 · `product-lifecycle-rules` knowledge pack (Issue #320): realizes `product-lifecycle-bible.md` v1.1.0 as a portable, this-repo-agnostic pack (three loops, seven-stage build loop, IDR/ADR/RDD alignment doc grammar, knowledge-base habits, anti-patterns) — now this workspace's canonical operating source for that doctrine (the bible file demoted to a dated snapshot, v1.2.0); reciprocal routing fences added to `doc-writing-rules` (evals + description) and `project-docs` (SKILL.md consult table + description + its first-ever evals suite)
v1.5.0 · 2026-08-16 · IDR doc type (Issue #316, `prd-idr-framework.md`): doc_lint TYPES gains `idr` (draft/locked/superseded, Claim/Why/Proof), T4's ledger-lock guard generalized to cover both accepted-ADR and locked-IDR, new T6 WARN (orphan-ADR — no `intent-refs:` citation), `references/templates/idr.md`, doc-writing-rules' type/mutability tables gain the IDR row, make-doc routes `idr`; bootstrap auto-mint (cross-plugin, harness's `/make-plugin`) and the ADR 0001-0013 `intent-refs:` retrofit stay deferred follow-ups
v1.4.9 · 2026-08-16 · checker retier (Kim's ruling): 1 *-checker agent move effort high→medium, model fable unchanged — review quality held at medium across the 2026-08-15/16 rounds while inherited-xhigh runs added cost, not findings
v1.4.8 · 2026-08-15 · make-reference gains the save-lessons fence (NOT whether a fact EARNS an entry — save-lessons judges the bar, this skill writes the doc) + reciprocal eval n12, and a W8 description diet back under the 700-char budget (triggers preserved verbatim); twin found by authorkit attention-audit's first live run (PR #275)
v1.4.7 · 2026-08-13 · footer ledger regenerated to one line per version (issue #203) — governed by harness's plugin-writing-rules cap; no docs-side contract change
v1.4.6 · 2026-08-13 · `file-bug` Phase 5 gains a verified teardown clause (issue #190, tracked from gen-ui-kit#1151)
v1.4.5 · 2026-08-12 · `references/backend-resolver.md`'s `claim` operation row gains its first real caller (teamwork's `dispatch-ticket` Phase 3, issue #183/#184)
v1.4.4 · 2026-08-11 · two checker-prescribed fixes from the first /check-everything estate audit — intake-lead's team-lead-sender rule restored, file-bug's fix-inline branch gains the three-strikes checker-pass contract
v1.4.3 · 2026-08-10 · intake-lead's standing-mode contract written into the body (issue #167) — the description already advertised a long-lived sibling seat the body never defined; no behavior changed
v1.4.2 · 2026-08-10 · the intent record evicted from agents/ entirely
v1.4.1 · 2026-08-10 · intake-lead's forge intent record relocated to `agents/intents/`
v1.4.0 · 2026-08-10 · /lead-intake
v1.3.1 · 2026-08-10 · intake-lead A4 smoke findings applied — Skill tool now hard-blocked via disallowedTools, fork-from-agent confirmed to run in the BACKGROUND and route completion to the ROOT session
v1.3.0 · 2026-08-10 · new agent `intake-lead`
v1.2.1 · 2026-08-10 · ADR-0010 rename sweep
v1.2.0 · 2026-08-09 · file-bug/file-feature/file-task run forked (`context: fork`) by default
v1.1.0 · 2026-07-30 · file-leftovers
v1.0.9 · 2026-07-30 · ADR-0009 find-intent-rename sweep
v1.0.8 · 2026-07-25 · description diet (PR #92)
v1.0.7 · 2026-07-25 · retired the stale ADR-0006 transition-table section
v1.0.6 · 2026-07-22 · the #79 description diet
v1.0.5 · 2026-07-21 · #84 remainder fix
v1.0.4 · 2026-07-21 · ADR-0008 design-merge sweep
v1.0.3 · 2026-07-21 · ADR-0007 dir alignment
v1.0.2 · 2026-07-21 · make-llms-txt's best-practices.md symlink repointed (reference-forge → make-reference; the docs rename stranded it — symlink targets are invisible to text sweeps, macOS glob masked it locally, Linux CI crashed).
v1.0.1 · 2026-07-21 · ADR-0006 harness-rename sweep
v1.0.0 · 2026-07-21 · ADR-0006 rename PR 8/9
v0.22.4 · 2026-07-21 · ADR-0006 teamwork-rename sweep
v0.22.3 · 2026-07-21 · ADR-0006 screens-rename sweep
v0.22.2 · 2026-07-21 · ADR-0006 design-kits-rename sweep
v0.22.1 · 2026-07-21 · ADR-0006 color-rename sweep
v0.22.0 · 2026-07-19 · `knowledge-forge` retired
v0.21.0 · 2026-07-19 · ADR-0004 dual-write, corrected
v0.20.0 · 2026-07-19 · ADR-0005 ticket-claim protocol
v0.19.0 · 2026-07-19 · ADR-0004 dual-write, implemented (Issue #44)
v0.18.1 · 2026-07-18 · `research-methods` gains a reciprocal NOT-for fence against forge's `reasoning-orders`
v0.18.0 · 2026-07-18 · README artifact-table species sweep
v0.17.0 · 2026-07-18 · `doc-forge` converted from Command to Procedural species (`disable-model-invocation: true` → `false`), mirroring `skill-forge`'s own 2026-07-14 conversion
v0.16.0 · 2026-07-18 · the ADR-0003 Linear adapter (Issue #34)
v0.15.0 · 2026-07-17 · `.github/ISSUE_TEMPLATE/{task,bug,feature}.yml` (Issue #25)
v0.14.0 · 2026-07-16 · /issue
v0.13.2 · 2026-07-15 · /docs-alignment gains .gitignore discipline (ruled 2026-07-15)
v0.13.1 · 2026-07-15 · subfolder conformance (ruled 2026-07-15) — research-methods' examples/ folded into references/, consult-table row repaired
v0.13.0 · 2026-07-15 · the git-native work-item backend (closes the estate's Issue #1, per ADR-0002): /bug-report and /feature gain a Phase-0 backend seam
v0.12.0 · 2026-07-15 · doc_lint T4 made git-aware (ADR-0002's own authoring exposed the false-positive class same-day): the ledger protection now guards COMMITTED history
v0.11.1 · 2026-07-14 · displayName 'Scribe' added to the manifest
v0.11.0 · 2026-07-13 · doc-authoring-standards' location-and-naming rule gains repo-rootedness (ruled 2026-07-13)
v0.10.0 · 2026-07-12 · seat-ladder realignment (forge 1.22.0): doc-reviewer opus→fable+high, researcher opus→fable+high (review/hard-bug-analysis row — root-causing a resistant defect is the row's charter)
v0.9.0 · 2026-07-12 · BREAKING — docs-align renamed docs-alignment hours after 0.8.0, all seven references repaired same-change, reciprocal repo-alignment fence added
v0.8.0 · 2026-07-12 · /docs-align (renamed docs-alignment at 0.9.0)
v0.7.0 · 2026-07-12 · /feature Phase 6
v0.6.1 · 2026-07-10 · author attribution corrected to Kim G / NONOUN (was the Agentic Harness placeholder)
v0.6.0 · 2026-07-09 · /feature
v0.5.1 · 2026-07-09 · hygiene pass
v0.5.0 · 2026-07-09 · BREAKING (ADR-0001) — *-author renamed *-forge estate-wide (rubric/reference/knowledge/llms-txt/vision-memo), old handles fail visibly, no alias
v0.4.2 · 2026-07-09 · vision-memo-forge narrowed from 'evaluate and improve' to 'improve' with an explicit doc-review fence; doc-review's suite gains the reciprocal trigger
v0.4.1 · 2026-07-09 · agent fallback blocks 'Tests run'→'Tests/checks run' (harness-audit finding: the inline fallback failed forge handoff_check's H1 gate as written)
v0.4.0 · 2026-07-07 · bug-report's dispatch phase runs under orchestration's loop-design /goal recipe where installed (a dated Findings entry as the verifiable end-state, 5-try cap) instead of an open-ended fork
v0.3.0 · 2026-07-07 · measure/markdown-render/knowledge-docs folded in per a plugin-decompose partition; doc-reviewer agent closes scribe's generator≠critic gap
v0.2.0 · 2026-07-07 · bug-report orchestrator + TICKET `kind: bug` convention (Repro/Expected-vs-actual/Classification/Severity/Findings)
v0.1.0 · 2026-07-07 · initial: standards + forge + review + doc_lint + 8 templates + write hook
