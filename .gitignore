# macOS
.DS_Store

# Gate output — regenerate with `release_gate.py <plugin> --package`, never hand-edit or commit
dist/

# Personal/local Claude Code settings — not shared
harness-audit-*/

# Harness-managed git worktrees (EnterWorktree) — in-repo by design, never committed (ruled 2026-07-15)
.claude/worktrees/

# brand-design council instances' private critic attribution map — the real practitioner a
# persona is distilled from; never committed (make-critic's `.name-map.md` convention, S6 #829)
.name-map.md
