{
 "skill": "llm-gateway-facts",
 "note": "Routing corpus of record (minted 2026-07-13, not yet run; extended 2026-08-19 for issue #743). Positives: imperative / diagnostic / symptom / indirect phrasings of the gateway-pattern asks across all seven axes (adapter seam, registry + trust boundary, dev-proxy + bundler footguns, stateless session/turn model, single-flight 401 refresh, retry policy + streaming pass-through, live-gateway ops/model tiering — the pack's own 7-axis ceiling, pack-writing-rules). Negatives drawn adversarially from the dangerous near-neighbors: llm-streaming-facts (the sibling pack — SSE/JSONL wire-format concerns, a DIFFERENT layer), the agent-protocols plugin's a2ui-chat-agent-facts (this repo's OWN dated implementation, not the portable pattern) and a2ui-protocol-facts (the A2UI wire shape), plus a genuine build/implement ask (this pack answers, it does not build). Fixed 2026-08-19 (issue #715, check-routing sweep #676): n09-n11 (implement/build/write asks) were leaking to this pack — a passive 'ANSWERS; does not build' clause is not an explicit NOT fence for those verbs. Added an explicit 'NOT an implementation ask (implement/build/write the code)' fence; n09-n11 stay no-trigger.",
 "cases": [
  { "id": "t01", "prompt": "how do I support multiple LLM providers behind one interface", "expect": "trigger" },
  { "id": "t02", "prompt": "how do I keep an API key off the client and out of a static build", "expect": "trigger" },
  { "id": "t03", "prompt": "design a dev-time proxy for calling an LLM from a browser app", "expect": "trigger" },
  { "id": "t04", "prompt": "why does my API key end up in the built JS bundle", "expect": "trigger" },
  { "id": "t05", "prompt": "one config as both a provider picker and a server-side allowlist", "expect": "trigger" },
  { "id": "t06", "prompt": "how do I let a client pick provider and model safely", "expect": "trigger" },
  { "id": "t07", "prompt": "keep a multi-turn LLM conversation stateless server-side", "expect": "trigger" },
  { "id": "t08", "prompt": "why did my VITE_ prefixed env var leak into the production bundle", "expect": "trigger" },
  { "id": "t09", "prompt": "how do I add a new LLM vendor without touching the calling code", "expect": "trigger" },
  { "id": "t10", "prompt": "where should the API key live so it's never sent to the browser", "expect": "trigger" },
  { "id": "t11", "prompt": "the app says no API key found even though I set it in .env", "expect": "trigger" },
  { "id": "t12", "prompt": "should the server or the client hold the conversation history", "expect": "trigger" },
  { "id": "t13", "prompt": "how do I validate a client-chosen model against an allowlist before reading a secret", "expect": "trigger" },
  { "id": "t14", "prompt": "what's a clean way to inject a secret into an adapter without reading it at module scope", "expect": "trigger" },
  { "id": "t15", "prompt": "why does my picker UI offer a model the server then rejects", "expect": "trigger" },
  { "id": "t16", "prompt": "design the trust boundary for a browser app that calls a hosted LLM", "expect": "trigger" },
  { "id": "t17", "prompt": "concurrent requests all hit a 401 at once, how do I refresh the token exactly once", "expect": "trigger" },
  { "id": "t18", "prompt": "how do I replay a streaming request body after a token refresh", "expect": "trigger" },
  { "id": "t19", "prompt": "should this failed LLM gateway request retry, and with what backoff", "expect": "trigger" },
  { "id": "t20", "prompt": "should a failed POST request retry automatically, or does that risk a duplicate side effect", "expect": "trigger" },
  { "id": "t21", "prompt": "model X doesn't work in the chat, what should I check first", "expect": "trigger" },
  { "id": "t22", "prompt": "which model tier should planning vs execution work use", "expect": "trigger" },
  { "id": "n01", "prompt": "why is my SSE parser dropping text fragments", "expect": "no-trigger" },
  { "id": "n02", "prompt": "what's the exact event sequence Anthropic streams", "expect": "no-trigger" },
  { "id": "n03", "prompt": "how do I buffer a chunked SSE response correctly", "expect": "no-trigger" },
  { "id": "n04", "prompt": "how does validate-then-stream work for structured JSONL output", "expect": "no-trigger" },
  { "id": "n05", "prompt": "how does the A2UI live-agent's produce loop work in this repo", "expect": "no-trigger" },
  { "id": "n06", "prompt": "what is ADR-0088 proposing for conversational reasoning in @agent-ui/a2ui", "expect": "no-trigger" },
  { "id": "n07", "prompt": "why did this A2UI payload fail validation", "expect": "no-trigger" },
  { "id": "n08", "prompt": "what does a binding or a check look like on the A2UI wire", "expect": "no-trigger" },
  { "id": "n09", "prompt": "implement a new provider adapter in my codebase", "expect": "no-trigger" },
  { "id": "n10", "prompt": "build the dev-proxy plugin for my Vite app", "expect": "no-trigger" },
  { "id": "n11", "prompt": "write the code that reads process.env for my server", "expect": "no-trigger" },
  { "id": "n12", "prompt": "add a new axis to this knowledge pack", "expect": "no-trigger" }
 ]
}
