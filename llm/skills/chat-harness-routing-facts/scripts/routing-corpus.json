{
  "skill": "chat-harness-routing-facts",
  "_note": "Routing corpus of record (minted 2026-07-13; reciprocal negatives against tools-resources-and-services/knowledge-and-memory/observability added 2026-07-13 per independent audit, since those three siblings each explicitly name this skill as their own near-neighbor). Positives: imperative / diagnostic / symptom / indirect phrasings across the three axes (skill vs hardcoded feature, model-invoked vs user-invoked species, routing + adversarial evals). Negatives drawn adversarially from the dangerous near-neighbors: chat-harness-guardrail-facts (general instruction-layering/guardrails, not skill-authoring/routing mechanics), chat-harness-workflow-facts (multi-AGENT composition/hand-offs, distinct from single-skill routing), chat-harness-tool-facts (deferring a TOOL catalog's loading shares the 'load on demand' shape but a different mechanism than a skill's own routing), chat-harness-memory-facts (persisting a fact across sessions, not routing a live request to a capability), chat-harness-logging-facts (measuring routing accuracy AFTER the fact, not the routing mechanism itself), plus a genuine build/implement ask (this pack answers, it does not build).",
  "positives": [
    "should this capability be a skill or just hardcoded into the system prompt",
    "why does this always-on instruction cost context on every single turn",
    "when should I bake a feature directly into the harness instead of making it a skill",
    "does this capability need a slash command or should it just be a standing instruction",
    "should this go in CLAUDE.md or become its own skill",
    "should this skill auto-trigger or only run via a slash command",
    "what does disable-model-invocation actually do",
    "why does my skill only work when I type the slash command",
    "when should a skill be user-invoked instead of model-invoked",
    "can a skill be preloaded into a subagent if model invocation is disabled",
    "is user-invocable true the right setting for this workflow skill",
    "why did the wrong skill fire for this request",
    "how do I test that my skill's description actually routes correctly",
    "write an adversarial eval corpus for this skill's routing",
    "two of my skills keep getting confused for the same prompt",
    "how do I measure whether a skill's trigger phrasing actually works",
    "how does the assistant decide which skill or subagent to route a request to"
  ],
  "negatives": [
    "how should I layer instructions and safety guardrails across this whole harness",
    "write the standing safety rules that apply no matter what skill is active",
    "how do I structure CLAUDE.md's guardrail section",
    "what's the right way to enforce a hard constraint across every turn",
    "design a multi-agent workflow where three subagents hand off work in sequence",
    "how should the orchestrator coordinate a team of subagents on this task",
    "what's the right way to compose several agents into one pipeline",
    "how do I set up a review gate between a builder agent and a reviewer agent",
    "implement the routing logic for our custom chat harness in Python",
    "build a new slash command that runs our deploy script",
    "write the code that loads a skill's SKILL.md file at runtime",
    "add a new hook that fires before every tool call",
    "should this be a tool or a skill",
    "defer loading this large tool catalog's full schemas until needed",
    "remember this user's preference across future sessions",
    "persist this fact to memory so I don't have to re-derive it next time",
    "set up monitoring for how often the wrong skill fires",
    "measure our routing accuracy over the last month of real traffic"
  ]
}
