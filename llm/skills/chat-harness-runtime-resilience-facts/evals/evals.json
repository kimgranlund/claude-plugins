{
 "skill": "chat-harness-runtime-resilience-facts",
 "note": "Routing corpus of record (minted 2026-08-17 by plan-skill-split, issue #552 — the split of chat-harness-guardrail-facts's 8th-axis overage). Positives: imperative / diagnostic / symptom / indirect phrasings across the three axes (multi-turn validation and state-seeded gates — carried over verbatim from the parent pack's t22-t24; disclosure knobs and progress detail; failure surfacing in a deployed chat runtime). Negatives drawn adversarially from the dangerous near-neighbors: chat-harness-guardrail-facts (the sibling this pack split out of — instruction layering, injection defense, action risk tiers, none of which is this pack's producer-loop axis), chat-harness-workflow-facts (the self-correct feedback loop's own prompt DESIGN is that pack's; this pack only owns the harness-side halt-at-the-bound decision), llm-gateway-facts (the provider/secret trust-boundary pattern, a narrower different concern), llm-streaming-facts (the wire-format sibling), chat-harness-routing-facts (skill authoring/routing, not runtime resilience), agent-residency-facts (tier classification, not this pack's content), plus a genuine build/implement ask (this pack answers, it does not build).",
 "cases": [
  {
   "id": "t01",
   "prompt": "my per-turn output validator keeps rejecting the incremental update my renderer would accept",
   "expect": "trigger"
  },
  {
   "id": "t02",
   "prompt": "the model keeps re-sending the full structure it was explicitly told not to resend",
   "expect": "trigger"
  },
  {
   "id": "t03",
   "prompt": "should my producer loop validate each turn standalone or against the session's accumulated state",
   "expect": "trigger"
  },
  {
   "id": "t04",
   "prompt": "should turning on full reasoning detail automatically also turn on raw source payload detail",
   "expect": "trigger"
  },
  {
   "id": "t05",
   "prompt": "a progress disclosure knob is leaking more of the runtime's internal state than intended",
   "expect": "trigger"
  },
  {
   "id": "t06",
   "prompt": "the stream already sent a 200 but then failed silently with no error shown to the client",
   "expect": "trigger"
  },
  {
   "id": "t07",
   "prompt": "should I widen the retry bound when the model keeps failing the same self-correct round",
   "expect": "trigger"
  },
  {
   "id": "t08",
   "prompt": "an error message shown to the end user accidentally echoed raw text from the upstream provider",
   "expect": "trigger"
  },
  {
   "id": "t09",
   "prompt": "adding a new optional flag to my producer changed the request shape even for callers who never set it",
   "expect": "trigger"
  },
  {
   "id": "n01",
   "prompt": "why isn't my skill triggering when the user asks about this",
   "expect": "no-trigger"
  },
  {
   "id": "n02",
   "prompt": "how do I write a SKILL.md description so it routes correctly",
   "expect": "no-trigger"
  },
  {
   "id": "n03",
   "prompt": "layer a global CLAUDE.md, a project CLAUDE.md, and a session instruction so the more specific one wins",
   "expect": "no-trigger"
  },
  {
   "id": "n04",
   "prompt": "what's the defense against prompt injection through tool output",
   "expect": "no-trigger"
  },
  {
   "id": "n05",
   "prompt": "classify which actions need explicit confirmation versus which can just proceed",
   "expect": "no-trigger"
  },
  {
   "id": "n06",
   "prompt": "keep an API key off the client and out of a static build",
   "expect": "no-trigger"
  },
  {
   "id": "n07",
   "prompt": "design a dev-time proxy for calling an LLM from a browser app",
   "expect": "no-trigger"
  },
  {
   "id": "n08",
   "prompt": "why is my SSE parser dropping text fragments",
   "expect": "no-trigger"
  },
  {
   "id": "n09",
   "prompt": "the model keeps repeating the same mistake on retry, what should the feedback text actually say",
   "expect": "no-trigger"
  },
  {
   "id": "n10",
   "prompt": "decompose this task across a team of agents with a clear chain of command",
   "expect": "no-trigger"
  },
  {
   "id": "n11",
   "prompt": "is this a Resident Agent or an Ephemeral Agent",
   "expect": "no-trigger"
  },
  {
   "id": "n12",
   "prompt": "implement the retry logic for my chat app's producer loop",
   "expect": "no-trigger"
  }
 ]
}
