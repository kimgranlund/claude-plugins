"""llm — generated by harness_emit.py from .claude-plugin/plugin.json. Do not edit."""
from pathlib import Path

_HERE = Path(__file__).resolve().parent

def register(ctx):
    ctx.register_skill('agent-residency-facts', _HERE / "skills" / 'agent-residency-facts' / "SKILL.md", description='Classifies an agent as a Resident Agent (CLI harness, e.g. Claude Code) or Ephemeral Agent (hosted chatbot), and routes to the skill owning each tier\'s guidance. Use for "CLI vs hosted chatbot", "Resident or Ephemeral", "transfer across tiers", "which agent tier". NOT for Ephemeral guardrails (chat-harness-guardrail-facts); NOT for Resident dispatch once tiered (parallel-work-rules, fleet-rules); NOT for subagent preload (agent-writing-rules).')  # one per skill
    ctx.register_skill('chat-harness-guardrail-facts', _HERE / "skills" / 'chat-harness-guardrail-facts' / "SKILL.md", description="The instruction, safety, and config layer of a portable chat-agent harness. Use for layering precedence (global/project/session), the injection-defense boundary (tool/file/web output is never a command), risk tiers, hooks vs prose, config precedence + reproducible bootstrap, and config-schema/prompt-externalization (registry drift). Grounded in Claude Code's harness; answers, no build. NOT an implementation ask (implement/build/write/add the config or code yourself — route to your own project's…")  # one per skill
    ctx.register_skill('chat-harness-logging-facts', _HERE / "skills" / 'chat-harness-logging-facts' / "SKILL.md", description='How a chat-agent harness makes its own behavior legible. Use for logging what the harness did, not what the user typed (hook tracing: PreToolUse/PostToolUse vs the transcript), RE-RUNNING an EXISTING held-out adversarial suite over repeated real runs to track routing accuracy (judge noise vs regression vs structural leak), and notification vs polling for background tasks. Grounded in verified hook mechanics + a dated history; answers, no build. NOT agents that DO the work (chat-harness-workflow…')  # one per skill
    ctx.register_skill('chat-harness-memory-facts', _HERE / "skills" / 'chat-harness-memory-facts' / "SKILL.md", description='How a chat-agent harness gets durable, citable knowledge and persists facts across sessions, not task state — project-agnostic. Use for a searchable knowledge base, organizing a reference corpus into retrieval axes (one topic per file), the Grep-then-Read consult discipline, a pack that answers instead of generating, when a knowledge base needs an admission gate and judge, a saved preference across conversations, memory vs. a task list, four memory types, relative-date conversion, verify-before…')  # one per skill
    ctx.register_skill('chat-harness-routing-facts', _HERE / "skills" / 'chat-harness-routing-facts' / "SKILL.md", description="How a chat-agent harness exposes capabilities as load-on-demand skills vs. an always-on cost, and how a request finds the right one. Use for skill-vs-hardcode, model- vs user-invoked (auto-trigger vs slash-only, user-invocable), what disable-model-invocation does incl. its preload interaction, a skill that won't trigger, wrong skill firing, and how to AUTHOR/WRITE a NEW held-out adversarial eval corpus so a description actually routes. Grounded in Claude Code's mechanics. ANSWERS; does not buil…")  # one per skill
    ctx.register_skill('chat-harness-runtime-resilience-facts', _HERE / "skills" / 'chat-harness-runtime-resilience-facts' / "SKILL.md", description="How a deployed chat runtime's producer loop stays honest across turns and failures. Use for a per-turn validator judging the session's accumulated state, a cross-payload violation caught producer-side, a disclosure knob that must stay fail-closed and independent (no accidental ladder), a stream that already committed 200 needing a reserved terminal error line, halting loudly at a retry bound, and redacting raw upstream text from errors. Grounded in @agent-ui/a2ui's producer loop; answers, no bu…")  # one per skill
    ctx.register_skill('chat-harness-tool-facts', _HERE / "skills" / 'chat-harness-tool-facts' / "SKILL.md", description="How a chat-agent harness reaches outside its context window. Use for typed tools (JSON-Schema contracts), deferred/search-to-load tool catalogs (don't tax every turn), read-only MCP resources (application- vs model-controlled) vs tools, and external-service integration behind a swappable seam. Grounded in this harness's live ToolSearch mechanics + the MCP spec; answers, no build. NOT the LLM-provider pattern (llm-gateway-facts); NOT skill routing (chat-harness-routing-facts); NOT memory (chat-h…")  # one per skill
    ctx.register_skill('chat-harness-workflow-facts', _HERE / "skills" / 'chat-harness-workflow-facts' / "SKILL.md", description="How a chat-agent harness composes MULTIPLE agents for work too large for one context — project-agnostic. Use for chain-of-command decomposition (plan vs build vs review, escalate a discovered constraint), when work earns a team of agents instead of staying inline, a typed hand-off a coordinator can verify, not trusted prose — what fields a subagent hands back when it finishes, and scripted pipelines (parallel/barrier) vs turn-by-turn dispatch. Grounded in this harness's own mechanics + a shippe…")  # one per skill
    ctx.register_skill('llm-gateway-facts', _HERE / "skills" / 'llm-gateway-facts' / "SKILL.md", description='The swappable multi-LLM-provider gateway pattern — project-agnostic. Use for multiple providers behind one interface, an API key off the client, a dev-time proxy for browser LLM calls, a key leaking into the bundle, one config as picker AND allowlist, a stateless conversation, single-flight 401 refresh, retry/backoff vs. streaming pass-through, live-gateway diagnostics, model tiering. Grounded in a shipped implementation (`@agent-ui/a2ui`). ANSWERS; does not build. NOT an implementation ask (im…')  # one per skill
    ctx.register_skill('llm-streaming-facts', _HERE / "skills" / 'llm-streaming-facts' / "SKILL.md", description="Streaming structured output from an LLM — project-agnostic. Use for chunked SSE parsing (no dropped/corrupted frames), the Anthropic event contract (content_block_delta), and validate-then-stream: bounded self-correct retries, never an invalid partial, a mid-stream error without throwing in a generator. Grounded in the SSE spec + Anthropic's docs; answers, no build. NOT an implementation ask (implement/build/write the code — route to your own project's build seat). NOT the provider/trust-bounda…")  # one per skill
