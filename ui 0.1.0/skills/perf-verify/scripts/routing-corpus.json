{
  "skill": "perf-verify",
  "_note": "M2 corpus of record (excellence campaign batch 3, 2026-07-03; sibling re-aimed 2026-07-06 for the color-science-* split, corrected 2026-07-06 per host amendment A4 — the color-space/palette-math fence deflects color-science-spaces, not accessibility). Positives across imperative / diagnostic / symptom / indirect phrasings; negatives drawn adversarially from the -verify siblings (color-verify, focus-verify, i18n-verify, safety-verify), color-science-spaces, component-author, ui-audit, plus the out-of-corpus fence (wire latency / bundle profiling). Run: python3 ../../skill-author/scripts/routing_eval.py <description.txt> scripts/routing-corpus.json",
  "positives": [
    "verify the loading UX on the checkout page against our performance budget",
    "check that every async surface shows the right loading indicator",
    "run the performance budget card for /checkout and gate the CWV numbers",
    "the app feels slow even though the API responds in 150ms",
    "our CLS is 0.28 on the product page — audit layout stability",
    "the layout jumps around while images load and users lose trust",
    "content flashes and re-renders when the AI response finishes streaming",
    "a spinner flashes for a split second on every click",
    "should this dashboard fetch show a skeleton or a spinner",
    "users can't cancel a long-running export — review the loading feedback",
    "is optimistic UI safe for this mutation or should it be pending-then-commit",
    "audit the perceived latency of our page transitions"
  ],
  "negatives": [
    "check contrast ratios and WCAG pass-fail for this palette",
    "is this palette color-blind safe across themes",
    "verify the focus order and keyboard navigation in the modal",
    "the focus ring is invisible in dark mode",
    "audit the UI for RTL support and text-expansion headroom",
    "verify locale date and currency formatting with Intl",
    "review the destructive delete action — undo vs type-to-confirm",
    "check the audit-trail UX for the admin recall window",
    "convert this sRGB color to OKLCH and explain gamut mapping",
    "build a skeleton-loader web component with a shimmer animation",
    "audit the whole app's UX before launch",
    "profile our JS bundle size and cut back-end wire latency"
  ]
}