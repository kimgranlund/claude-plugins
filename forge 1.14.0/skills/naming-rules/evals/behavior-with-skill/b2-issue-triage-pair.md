# b2 — issue-triage pair (with skill)

- **Command:** `/sort-issues`
- **Agent:** `issue-sorter`

Reasoning: "triage" is a retired synonym — the registry verb for triaging into buckets is
`sort` (test 3). The runnable is verb-first (`sort-issues`, tests 1–2); the seat takes the
person-word shape (`issue-sorter`, test 2); the two are verb↔noun twins of the same words,
never one literal name shared across kinds.
