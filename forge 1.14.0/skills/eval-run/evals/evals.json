{
  "skill": "eval-run",
  "note": "Trigger-routing suite, minted 2026-07-20 when eval-run flipped command -> procedural (per explicit user direction, the 1.24.0 skill-forge precedent). expect=trigger: fires in a fresh session; expect=no-trigger: near-miss owned elsewhere (owner in comment). Known accepted-leak class: cases whose true owner is a user-only command (harness-audit, plugin-release) leak to model-only siblings on a model-only menu — benign, do not tune to chase.",
  "cases": [
    { "id": "t01", "prompt": "Run the trigger evals for this plugin.", "expect": "trigger" },
    { "id": "t02", "prompt": "Prove the routing still works after that description change.", "expect": "trigger" },
    { "id": "t03", "prompt": "Did my description edits break routing?", "expect": "trigger" },
    { "id": "t04", "prompt": "Run a routing simulation over the forge skills.", "expect": "trigger" },
    { "id": "t05", "prompt": "Check that the new skill's description routes correctly.", "expect": "trigger" },
    { "id": "t06", "prompt": "Rerun the eval suites and give me the routing matrix.", "expect": "trigger" },
    { "id": "t07", "prompt": "Do these prompts route to the right skills?", "expect": "trigger" },
    { "id": "t08", "prompt": "We batched several fence edits — run the wave-boundary routing check.", "expect": "trigger" },
    { "id": "t09", "prompt": "Blind-judge the routing for these suites.", "expect": "trigger" },
    { "id": "n01", "prompt": "Add a no-trigger case to this suite.", "expect": "no-trigger", "comment": "owner: skill-authoring-standards — suite conventions" },
    { "id": "n02", "prompt": "Why won't my skill trigger even though it's installed?", "expect": "no-trigger", "comment": "owner: skill-authoring-standards — live trigger debugging" },
    { "id": "n03", "prompt": "Run the release gate on docs.", "expect": "no-trigger", "comment": "owner: plugin-release (command, off-menu) / direct script run — accepted-leak class if a standards sibling wins" },
    { "id": "n04", "prompt": "Review this skill and score it.", "expect": "no-trigger", "comment": "owner: skill-review" },
    { "id": "n05", "prompt": "Write trigger evals for my new skill.", "expect": "no-trigger", "comment": "owner: skill-authoring-standards / skill-forge Phase 2" },
    { "id": "n06", "prompt": "Audit my whole harness.", "expect": "no-trigger", "comment": "owner: harness-audit (command, off-menu) — accepted-leak class" }
  ]
}
