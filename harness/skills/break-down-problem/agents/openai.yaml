interface:
  display_name: "Break Down Problem"
  short_description: "Decompose a problem along two crossing planes — OUTSIDE-IN (whole → parts) and INSIDE-OUT (actions → surfaces) — then cross-checked."
