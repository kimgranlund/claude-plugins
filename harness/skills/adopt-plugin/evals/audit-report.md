Skill: /Users/kimba/Projects/nonoun/plugins/forge 1.14.0/skills/plugin-onboard · Standards: skill-authoring-standards · Lint: clean
Verdict: PASS (no blocking finding)

| ID | Verdict | Severity | Evidence (file:line) | Fix |
|----|---------|----------|----------------------|-----|
| R1 Behavior delta | PASS | — | SKILL.md:52-55 ("There is no `\"source\": \"settings\"` inline-marketplace type…") directly negates the fabricated shape the live baseline produced (intent.md:69-76, transcript a2713708957032e3c); SKILL.md:57-58 ("Single-plugin repos → wrap, never reference directly") matches confirmed failure #1 (intent.md:21-23); SKILL.md:75-76 ("`<plugin-name>` is the `name` field… never guessed from the repo or URL") matches confirmed failure #2 (intent.md:24-25). All three fail the deletion test — remove any one and the corresponding documented failure mode recurs. | none |
| R2 Trigger fidelity | PASS (minor) | minor | SKILL.md:4-13 vs evals.json t01/t03/t06 — description phrasing is near-verbatim to eval prompts ("set up extraKnownMarketplaces", "asks why a declared plugin isn't showing up in /plugin"), and n01-n03 map cleanly onto the "permissions, env vars, model choice" fence. **But**: SKILL.md:9-10 fences "(update-config)" as the owner of non-plugin settings.json edits, and no `update-config` skill exists anywhere under `/Users/kimba/Projects/nonoun/plugins` (checked all 8 sibling plugins) or elsewhere on disk. intent.md:16 marks it "external skill" but it is not resolvable in this repo's corpus today. | Confirm `update-config` is a real, externally-installed skill the audience will have (name it as such, e.g. "(update-config, if installed)"), or point the fence at whatever config-editing skill/doc actually exists today; a phantom-owner fence correctly repels the query but leaves the user with no next stop. |
| R3 Species/dial agreement | PASS | — | `disable-model-invocation: false` / `user-invocable: true` (SKILL.md:14-15) match the Procedural row exactly; name `plugin-onboard` follows the family's `plugin-<verb>` head used by every sibling (plugin-decompose, plugin-forge, plugin-release). | none |
| R4 Register | PASS | — | 3 hard-gate lines total (SKILL.md:54, 57-58, 76 — "never"/"cannot"), at the ≤3 budget intent.md:60-63 claims (potency_lint NEVER 6→3). All three are declarative invariants ("cannot point at…", "never guessed from…"), not hedged description. | none |
| R5 No restatement | PASS | — | Step 4/5 reference `plugin.json`'s `name` field and `/reload-plugins` without restating `plugin-authoring-standards`' structure/reload-semantics rules (grep confirms zero shared prose between the two files beyond the field name itself); the settings.json-specific reload fact at SKILL.md:84-88 is a distinct claim (marketplace/plugin declarations), not a copy of that skill's hooks/agents/MCP reload section. | none |
| R6 Position | PASS | — | 134-line body, ~1.3k tokens total — Output contract (SKILL.md:93-98), Failure branches (100-107), and stopping predicate (109-110) all sit well inside the first-5,000-token compaction window; the worked Bad/Good example is the last section (112-133). | none |
| R7 Contracts | PASS | — | Output contract present (93-98: settings.json diff, wrapper contents, per-URL path+why, verification steps); 3 named failure branches (102-107: invalid source, unconfirmed plugin-name, unreachable repo); checkable stopping predicate (109-110: "every URL has a settings.json entry… and the verification steps are stated"). | none |
| R8 Quantities | PASS | — | No vague quantifiers on load-bearing dimensions; counts are concrete where they matter ("one `extraKnownMarketplaces` entry per repo", "one shared self-hosted catalog", "one line per URL"). | none |

**Fence non-overlap verified against real sibling content** (not just declared): read all 4 SKILL.md siblings in full.
- `plugin-authoring-standards` — only touches `marketplace.json` in passing (displayName field, line 31); zero procedural overlap with settings.json declaration.
- `plugin-forge` / `plugin-decompose` — greenfield family design / existing-surface partitioning, respectively; neither mentions `extraKnownMarketplaces`, `enabledPlugins`, or `settings.json` anywhere in body.
- `plugin-release` — releases the plugin author's *own* artifact (version bump, package to `dist/`); not fenced by plugin-onboard and correctly so — no real ambiguity exists between "declare someone else's plugin as a dependency" and "ship your own."
All four fences are real, not just declared.

**Process note (non-blocking):** intent.md:64 records `P5 validate: PENDING` — the skill's own authoring checklist (fence-closure / routing proof against `evals/evals.json`) has not yet been closed out, even though the suite is authored (evals.json, 14 cases, contested-boundary note). This floor review does not require running `routing_eval.py` (that's the deep-audit M2 gate), but the maker should close P5 with an actual eval-run before treating this skill as shipped, not just drafted.

Top 3:
1. R2/minor — `SKILL.md:9-10` fences "(update-config)" as the owner of general settings.json edits, but no such skill exists anywhere in the installed plugin corpus (checked). Fix: qualify it as external/not-yet-built, or repoint to whatever real resource currently owns that case.
2. Process/minor — `intent.md:64` P5 is PENDING; the suite exists (`evals/evals.json`) but hasn't been run. Fix: run the eval/routing check and flip P5 to PASS before calling the skill done.
3. nit — description sits at 886/1024 chars with 4 fences already spent; per the authoring standards' reciprocal-fence rule, any future fence added against a new sibling should be re-budgeted before, not after, it trips the 1,024 warn.
