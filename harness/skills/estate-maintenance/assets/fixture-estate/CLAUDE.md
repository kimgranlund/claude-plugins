# demo-estate — entry file (FIXTURE, synthetic content)

This file is `assets/fixture-estate/CLAUDE.md` — a synthetic entry file seeded on purpose
at 230 lines so `estate-maintenance`'s D4 census detector has a real positive control for
`skill_lint.py` C1's 200-line entry-file threshold. Nothing below describes a real project.

## Route the job before doing the job

| Job | Owner |
|---|---|
| Ship the demo widget | `/ship-widget` |
| Review the demo widget | `/review-widget` |
| Retire a stale demo fixture | `/retire-fixture` |

## Invariants

- Fixture invariant one: every synthetic entry names its own purpose inline.
- Fixture invariant two: nothing under `assets/fixture-estate/` is consumed by anything
  other than `estate-maintenance`'s own `collect.py`/`detect.py` selftests.

## Legacy routing entries (padding, D4 census only)

The numbered lines below exist ONLY to push this fixture entry file past the 200-line
census threshold so D4's `entry-file > 200 lines` finding has a real positive control to
detect deterministically — each line names a fictional legacy routing decision, never a
real one, and none of it is read by any real skill.

1. Legacy demo routing decision #001 — retired widget-1 routed to `/legacy-1` (kept only as census padding, never a real routing rule).
2. Legacy demo routing decision #002 — retired widget-2 routed to `/legacy-2` (kept only as census padding, never a real routing rule).
3. Legacy demo routing decision #003 — retired widget-3 routed to `/legacy-3` (kept only as census padding, never a real routing rule).
4. Legacy demo routing decision #004 — retired widget-4 routed to `/legacy-4` (kept only as census padding, never a real routing rule).
5. Legacy demo routing decision #005 — retired widget-5 routed to `/legacy-5` (kept only as census padding, never a real routing rule).
6. Legacy demo routing decision #006 — retired widget-6 routed to `/legacy-6` (kept only as census padding, never a real routing rule).
7. Legacy demo routing decision #007 — retired widget-7 routed to `/legacy-7` (kept only as census padding, never a real routing rule).
8. Legacy demo routing decision #008 — retired widget-8 routed to `/legacy-8` (kept only as census padding, never a real routing rule).
9. Legacy demo routing decision #009 — retired widget-9 routed to `/legacy-9` (kept only as census padding, never a real routing rule).
10. Legacy demo routing decision #010 — retired widget-10 routed to `/legacy-10` (kept only as census padding, never a real routing rule).
11. Legacy demo routing decision #011 — retired widget-11 routed to `/legacy-11` (kept only as census padding, never a real routing rule).
12. Legacy demo routing decision #012 — retired widget-12 routed to `/legacy-12` (kept only as census padding, never a real routing rule).
13. Legacy demo routing decision #013 — retired widget-13 routed to `/legacy-13` (kept only as census padding, never a real routing rule).
14. Legacy demo routing decision #014 — retired widget-14 routed to `/legacy-14` (kept only as census padding, never a real routing rule).
15. Legacy demo routing decision #015 — retired widget-15 routed to `/legacy-15` (kept only as census padding, never a real routing rule).
16. Legacy demo routing decision #016 — retired widget-16 routed to `/legacy-16` (kept only as census padding, never a real routing rule).
17. Legacy demo routing decision #017 — retired widget-17 routed to `/legacy-17` (kept only as census padding, never a real routing rule).
18. Legacy demo routing decision #018 — retired widget-18 routed to `/legacy-18` (kept only as census padding, never a real routing rule).
19. Legacy demo routing decision #019 — retired widget-19 routed to `/legacy-19` (kept only as census padding, never a real routing rule).
20. Legacy demo routing decision #020 — retired widget-20 routed to `/legacy-20` (kept only as census padding, never a real routing rule).
21. Legacy demo routing decision #021 — retired widget-21 routed to `/legacy-21` (kept only as census padding, never a real routing rule).
22. Legacy demo routing decision #022 — retired widget-22 routed to `/legacy-22` (kept only as census padding, never a real routing rule).
23. Legacy demo routing decision #023 — retired widget-23 routed to `/legacy-23` (kept only as census padding, never a real routing rule).
24. Legacy demo routing decision #024 — retired widget-24 routed to `/legacy-24` (kept only as census padding, never a real routing rule).
25. Legacy demo routing decision #025 — retired widget-25 routed to `/legacy-25` (kept only as census padding, never a real routing rule).
26. Legacy demo routing decision #026 — retired widget-26 routed to `/legacy-26` (kept only as census padding, never a real routing rule).
27. Legacy demo routing decision #027 — retired widget-27 routed to `/legacy-27` (kept only as census padding, never a real routing rule).
28. Legacy demo routing decision #028 — retired widget-28 routed to `/legacy-28` (kept only as census padding, never a real routing rule).
29. Legacy demo routing decision #029 — retired widget-29 routed to `/legacy-29` (kept only as census padding, never a real routing rule).
30. Legacy demo routing decision #030 — retired widget-30 routed to `/legacy-30` (kept only as census padding, never a real routing rule).
31. Legacy demo routing decision #031 — retired widget-31 routed to `/legacy-31` (kept only as census padding, never a real routing rule).
32. Legacy demo routing decision #032 — retired widget-32 routed to `/legacy-32` (kept only as census padding, never a real routing rule).
33. Legacy demo routing decision #033 — retired widget-33 routed to `/legacy-33` (kept only as census padding, never a real routing rule).
34. Legacy demo routing decision #034 — retired widget-34 routed to `/legacy-34` (kept only as census padding, never a real routing rule).
35. Legacy demo routing decision #035 — retired widget-35 routed to `/legacy-35` (kept only as census padding, never a real routing rule).
36. Legacy demo routing decision #036 — retired widget-36 routed to `/legacy-36` (kept only as census padding, never a real routing rule).
37. Legacy demo routing decision #037 — retired widget-37 routed to `/legacy-37` (kept only as census padding, never a real routing rule).
38. Legacy demo routing decision #038 — retired widget-38 routed to `/legacy-38` (kept only as census padding, never a real routing rule).
39. Legacy demo routing decision #039 — retired widget-39 routed to `/legacy-39` (kept only as census padding, never a real routing rule).
40. Legacy demo routing decision #040 — retired widget-40 routed to `/legacy-40` (kept only as census padding, never a real routing rule).
41. Legacy demo routing decision #041 — retired widget-41 routed to `/legacy-41` (kept only as census padding, never a real routing rule).
42. Legacy demo routing decision #042 — retired widget-42 routed to `/legacy-42` (kept only as census padding, never a real routing rule).
43. Legacy demo routing decision #043 — retired widget-43 routed to `/legacy-43` (kept only as census padding, never a real routing rule).
44. Legacy demo routing decision #044 — retired widget-44 routed to `/legacy-44` (kept only as census padding, never a real routing rule).
45. Legacy demo routing decision #045 — retired widget-45 routed to `/legacy-45` (kept only as census padding, never a real routing rule).
46. Legacy demo routing decision #046 — retired widget-46 routed to `/legacy-46` (kept only as census padding, never a real routing rule).
47. Legacy demo routing decision #047 — retired widget-47 routed to `/legacy-47` (kept only as census padding, never a real routing rule).
48. Legacy demo routing decision #048 — retired widget-48 routed to `/legacy-48` (kept only as census padding, never a real routing rule).
49. Legacy demo routing decision #049 — retired widget-49 routed to `/legacy-49` (kept only as census padding, never a real routing rule).
50. Legacy demo routing decision #050 — retired widget-50 routed to `/legacy-50` (kept only as census padding, never a real routing rule).
51. Legacy demo routing decision #051 — retired widget-51 routed to `/legacy-51` (kept only as census padding, never a real routing rule).
52. Legacy demo routing decision #052 — retired widget-52 routed to `/legacy-52` (kept only as census padding, never a real routing rule).
53. Legacy demo routing decision #053 — retired widget-53 routed to `/legacy-53` (kept only as census padding, never a real routing rule).
54. Legacy demo routing decision #054 — retired widget-54 routed to `/legacy-54` (kept only as census padding, never a real routing rule).
55. Legacy demo routing decision #055 — retired widget-55 routed to `/legacy-55` (kept only as census padding, never a real routing rule).
56. Legacy demo routing decision #056 — retired widget-56 routed to `/legacy-56` (kept only as census padding, never a real routing rule).
57. Legacy demo routing decision #057 — retired widget-57 routed to `/legacy-57` (kept only as census padding, never a real routing rule).
58. Legacy demo routing decision #058 — retired widget-58 routed to `/legacy-58` (kept only as census padding, never a real routing rule).
59. Legacy demo routing decision #059 — retired widget-59 routed to `/legacy-59` (kept only as census padding, never a real routing rule).
60. Legacy demo routing decision #060 — retired widget-60 routed to `/legacy-60` (kept only as census padding, never a real routing rule).
61. Legacy demo routing decision #061 — retired widget-61 routed to `/legacy-61` (kept only as census padding, never a real routing rule).
62. Legacy demo routing decision #062 — retired widget-62 routed to `/legacy-62` (kept only as census padding, never a real routing rule).
63. Legacy demo routing decision #063 — retired widget-63 routed to `/legacy-63` (kept only as census padding, never a real routing rule).
64. Legacy demo routing decision #064 — retired widget-64 routed to `/legacy-64` (kept only as census padding, never a real routing rule).
65. Legacy demo routing decision #065 — retired widget-65 routed to `/legacy-65` (kept only as census padding, never a real routing rule).
66. Legacy demo routing decision #066 — retired widget-66 routed to `/legacy-66` (kept only as census padding, never a real routing rule).
67. Legacy demo routing decision #067 — retired widget-67 routed to `/legacy-67` (kept only as census padding, never a real routing rule).
68. Legacy demo routing decision #068 — retired widget-68 routed to `/legacy-68` (kept only as census padding, never a real routing rule).
69. Legacy demo routing decision #069 — retired widget-69 routed to `/legacy-69` (kept only as census padding, never a real routing rule).
70. Legacy demo routing decision #070 — retired widget-70 routed to `/legacy-70` (kept only as census padding, never a real routing rule).
71. Legacy demo routing decision #071 — retired widget-71 routed to `/legacy-71` (kept only as census padding, never a real routing rule).
72. Legacy demo routing decision #072 — retired widget-72 routed to `/legacy-72` (kept only as census padding, never a real routing rule).
73. Legacy demo routing decision #073 — retired widget-73 routed to `/legacy-73` (kept only as census padding, never a real routing rule).
74. Legacy demo routing decision #074 — retired widget-74 routed to `/legacy-74` (kept only as census padding, never a real routing rule).
75. Legacy demo routing decision #075 — retired widget-75 routed to `/legacy-75` (kept only as census padding, never a real routing rule).
76. Legacy demo routing decision #076 — retired widget-76 routed to `/legacy-76` (kept only as census padding, never a real routing rule).
77. Legacy demo routing decision #077 — retired widget-77 routed to `/legacy-77` (kept only as census padding, never a real routing rule).
78. Legacy demo routing decision #078 — retired widget-78 routed to `/legacy-78` (kept only as census padding, never a real routing rule).
79. Legacy demo routing decision #079 — retired widget-79 routed to `/legacy-79` (kept only as census padding, never a real routing rule).
80. Legacy demo routing decision #080 — retired widget-80 routed to `/legacy-80` (kept only as census padding, never a real routing rule).
81. Legacy demo routing decision #081 — retired widget-81 routed to `/legacy-81` (kept only as census padding, never a real routing rule).
82. Legacy demo routing decision #082 — retired widget-82 routed to `/legacy-82` (kept only as census padding, never a real routing rule).
83. Legacy demo routing decision #083 — retired widget-83 routed to `/legacy-83` (kept only as census padding, never a real routing rule).
84. Legacy demo routing decision #084 — retired widget-84 routed to `/legacy-84` (kept only as census padding, never a real routing rule).
85. Legacy demo routing decision #085 — retired widget-85 routed to `/legacy-85` (kept only as census padding, never a real routing rule).
86. Legacy demo routing decision #086 — retired widget-86 routed to `/legacy-86` (kept only as census padding, never a real routing rule).
87. Legacy demo routing decision #087 — retired widget-87 routed to `/legacy-87` (kept only as census padding, never a real routing rule).
88. Legacy demo routing decision #088 — retired widget-88 routed to `/legacy-88` (kept only as census padding, never a real routing rule).
89. Legacy demo routing decision #089 — retired widget-89 routed to `/legacy-89` (kept only as census padding, never a real routing rule).
90. Legacy demo routing decision #090 — retired widget-90 routed to `/legacy-90` (kept only as census padding, never a real routing rule).
91. Legacy demo routing decision #091 — retired widget-91 routed to `/legacy-91` (kept only as census padding, never a real routing rule).
92. Legacy demo routing decision #092 — retired widget-92 routed to `/legacy-92` (kept only as census padding, never a real routing rule).
93. Legacy demo routing decision #093 — retired widget-93 routed to `/legacy-93` (kept only as census padding, never a real routing rule).
94. Legacy demo routing decision #094 — retired widget-94 routed to `/legacy-94` (kept only as census padding, never a real routing rule).
95. Legacy demo routing decision #095 — retired widget-95 routed to `/legacy-95` (kept only as census padding, never a real routing rule).
96. Legacy demo routing decision #096 — retired widget-96 routed to `/legacy-96` (kept only as census padding, never a real routing rule).
97. Legacy demo routing decision #097 — retired widget-97 routed to `/legacy-97` (kept only as census padding, never a real routing rule).
98. Legacy demo routing decision #098 — retired widget-98 routed to `/legacy-98` (kept only as census padding, never a real routing rule).
99. Legacy demo routing decision #099 — retired widget-99 routed to `/legacy-99` (kept only as census padding, never a real routing rule).
100. Legacy demo routing decision #100 — retired widget-100 routed to `/legacy-100` (kept only as census padding, never a real routing rule).
101. Legacy demo routing decision #101 — retired widget-101 routed to `/legacy-101` (kept only as census padding, never a real routing rule).
102. Legacy demo routing decision #102 — retired widget-102 routed to `/legacy-102` (kept only as census padding, never a real routing rule).
103. Legacy demo routing decision #103 — retired widget-103 routed to `/legacy-103` (kept only as census padding, never a real routing rule).
104. Legacy demo routing decision #104 — retired widget-104 routed to `/legacy-104` (kept only as census padding, never a real routing rule).
105. Legacy demo routing decision #105 — retired widget-105 routed to `/legacy-105` (kept only as census padding, never a real routing rule).
106. Legacy demo routing decision #106 — retired widget-106 routed to `/legacy-106` (kept only as census padding, never a real routing rule).
107. Legacy demo routing decision #107 — retired widget-107 routed to `/legacy-107` (kept only as census padding, never a real routing rule).
108. Legacy demo routing decision #108 — retired widget-108 routed to `/legacy-108` (kept only as census padding, never a real routing rule).
109. Legacy demo routing decision #109 — retired widget-109 routed to `/legacy-109` (kept only as census padding, never a real routing rule).
110. Legacy demo routing decision #110 — retired widget-110 routed to `/legacy-110` (kept only as census padding, never a real routing rule).
111. Legacy demo routing decision #111 — retired widget-111 routed to `/legacy-111` (kept only as census padding, never a real routing rule).
112. Legacy demo routing decision #112 — retired widget-112 routed to `/legacy-112` (kept only as census padding, never a real routing rule).
113. Legacy demo routing decision #113 — retired widget-113 routed to `/legacy-113` (kept only as census padding, never a real routing rule).
114. Legacy demo routing decision #114 — retired widget-114 routed to `/legacy-114` (kept only as census padding, never a real routing rule).
115. Legacy demo routing decision #115 — retired widget-115 routed to `/legacy-115` (kept only as census padding, never a real routing rule).
116. Legacy demo routing decision #116 — retired widget-116 routed to `/legacy-116` (kept only as census padding, never a real routing rule).
117. Legacy demo routing decision #117 — retired widget-117 routed to `/legacy-117` (kept only as census padding, never a real routing rule).
118. Legacy demo routing decision #118 — retired widget-118 routed to `/legacy-118` (kept only as census padding, never a real routing rule).
119. Legacy demo routing decision #119 — retired widget-119 routed to `/legacy-119` (kept only as census padding, never a real routing rule).
120. Legacy demo routing decision #120 — retired widget-120 routed to `/legacy-120` (kept only as census padding, never a real routing rule).
121. Legacy demo routing decision #121 — retired widget-121 routed to `/legacy-121` (kept only as census padding, never a real routing rule).
122. Legacy demo routing decision #122 — retired widget-122 routed to `/legacy-122` (kept only as census padding, never a real routing rule).
123. Legacy demo routing decision #123 — retired widget-123 routed to `/legacy-123` (kept only as census padding, never a real routing rule).
124. Legacy demo routing decision #124 — retired widget-124 routed to `/legacy-124` (kept only as census padding, never a real routing rule).
125. Legacy demo routing decision #125 — retired widget-125 routed to `/legacy-125` (kept only as census padding, never a real routing rule).
126. Legacy demo routing decision #126 — retired widget-126 routed to `/legacy-126` (kept only as census padding, never a real routing rule).
127. Legacy demo routing decision #127 — retired widget-127 routed to `/legacy-127` (kept only as census padding, never a real routing rule).
128. Legacy demo routing decision #128 — retired widget-128 routed to `/legacy-128` (kept only as census padding, never a real routing rule).
129. Legacy demo routing decision #129 — retired widget-129 routed to `/legacy-129` (kept only as census padding, never a real routing rule).
130. Legacy demo routing decision #130 — retired widget-130 routed to `/legacy-130` (kept only as census padding, never a real routing rule).
131. Legacy demo routing decision #131 — retired widget-131 routed to `/legacy-131` (kept only as census padding, never a real routing rule).
132. Legacy demo routing decision #132 — retired widget-132 routed to `/legacy-132` (kept only as census padding, never a real routing rule).
133. Legacy demo routing decision #133 — retired widget-133 routed to `/legacy-133` (kept only as census padding, never a real routing rule).
134. Legacy demo routing decision #134 — retired widget-134 routed to `/legacy-134` (kept only as census padding, never a real routing rule).
135. Legacy demo routing decision #135 — retired widget-135 routed to `/legacy-135` (kept only as census padding, never a real routing rule).
136. Legacy demo routing decision #136 — retired widget-136 routed to `/legacy-136` (kept only as census padding, never a real routing rule).
137. Legacy demo routing decision #137 — retired widget-137 routed to `/legacy-137` (kept only as census padding, never a real routing rule).
138. Legacy demo routing decision #138 — retired widget-138 routed to `/legacy-138` (kept only as census padding, never a real routing rule).
139. Legacy demo routing decision #139 — retired widget-139 routed to `/legacy-139` (kept only as census padding, never a real routing rule).
140. Legacy demo routing decision #140 — retired widget-140 routed to `/legacy-140` (kept only as census padding, never a real routing rule).
141. Legacy demo routing decision #141 — retired widget-141 routed to `/legacy-141` (kept only as census padding, never a real routing rule).
142. Legacy demo routing decision #142 — retired widget-142 routed to `/legacy-142` (kept only as census padding, never a real routing rule).
143. Legacy demo routing decision #143 — retired widget-143 routed to `/legacy-143` (kept only as census padding, never a real routing rule).
144. Legacy demo routing decision #144 — retired widget-144 routed to `/legacy-144` (kept only as census padding, never a real routing rule).
145. Legacy demo routing decision #145 — retired widget-145 routed to `/legacy-145` (kept only as census padding, never a real routing rule).
146. Legacy demo routing decision #146 — retired widget-146 routed to `/legacy-146` (kept only as census padding, never a real routing rule).
147. Legacy demo routing decision #147 — retired widget-147 routed to `/legacy-147` (kept only as census padding, never a real routing rule).
148. Legacy demo routing decision #148 — retired widget-148 routed to `/legacy-148` (kept only as census padding, never a real routing rule).
149. Legacy demo routing decision #149 — retired widget-149 routed to `/legacy-149` (kept only as census padding, never a real routing rule).
150. Legacy demo routing decision #150 — retired widget-150 routed to `/legacy-150` (kept only as census padding, never a real routing rule).
151. Legacy demo routing decision #151 — retired widget-151 routed to `/legacy-151` (kept only as census padding, never a real routing rule).
152. Legacy demo routing decision #152 — retired widget-152 routed to `/legacy-152` (kept only as census padding, never a real routing rule).
153. Legacy demo routing decision #153 — retired widget-153 routed to `/legacy-153` (kept only as census padding, never a real routing rule).
154. Legacy demo routing decision #154 — retired widget-154 routed to `/legacy-154` (kept only as census padding, never a real routing rule).
155. Legacy demo routing decision #155 — retired widget-155 routed to `/legacy-155` (kept only as census padding, never a real routing rule).
156. Legacy demo routing decision #156 — retired widget-156 routed to `/legacy-156` (kept only as census padding, never a real routing rule).
157. Legacy demo routing decision #157 — retired widget-157 routed to `/legacy-157` (kept only as census padding, never a real routing rule).
158. Legacy demo routing decision #158 — retired widget-158 routed to `/legacy-158` (kept only as census padding, never a real routing rule).
159. Legacy demo routing decision #159 — retired widget-159 routed to `/legacy-159` (kept only as census padding, never a real routing rule).
160. Legacy demo routing decision #160 — retired widget-160 routed to `/legacy-160` (kept only as census padding, never a real routing rule).
161. Legacy demo routing decision #161 — retired widget-161 routed to `/legacy-161` (kept only as census padding, never a real routing rule).
162. Legacy demo routing decision #162 — retired widget-162 routed to `/legacy-162` (kept only as census padding, never a real routing rule).
163. Legacy demo routing decision #163 — retired widget-163 routed to `/legacy-163` (kept only as census padding, never a real routing rule).
164. Legacy demo routing decision #164 — retired widget-164 routed to `/legacy-164` (kept only as census padding, never a real routing rule).
165. Legacy demo routing decision #165 — retired widget-165 routed to `/legacy-165` (kept only as census padding, never a real routing rule).
166. Legacy demo routing decision #166 — retired widget-166 routed to `/legacy-166` (kept only as census padding, never a real routing rule).
167. Legacy demo routing decision #167 — retired widget-167 routed to `/legacy-167` (kept only as census padding, never a real routing rule).
168. Legacy demo routing decision #168 — retired widget-168 routed to `/legacy-168` (kept only as census padding, never a real routing rule).
169. Legacy demo routing decision #169 — retired widget-169 routed to `/legacy-169` (kept only as census padding, never a real routing rule).
170. Legacy demo routing decision #170 — retired widget-170 routed to `/legacy-170` (kept only as census padding, never a real routing rule).
171. Legacy demo routing decision #171 — retired widget-171 routed to `/legacy-171` (kept only as census padding, never a real routing rule).
172. Legacy demo routing decision #172 — retired widget-172 routed to `/legacy-172` (kept only as census padding, never a real routing rule).
173. Legacy demo routing decision #173 — retired widget-173 routed to `/legacy-173` (kept only as census padding, never a real routing rule).
174. Legacy demo routing decision #174 — retired widget-174 routed to `/legacy-174` (kept only as census padding, never a real routing rule).
175. Legacy demo routing decision #175 — retired widget-175 routed to `/legacy-175` (kept only as census padding, never a real routing rule).
176. Legacy demo routing decision #176 — retired widget-176 routed to `/legacy-176` (kept only as census padding, never a real routing rule).
177. Legacy demo routing decision #177 — retired widget-177 routed to `/legacy-177` (kept only as census padding, never a real routing rule).
178. Legacy demo routing decision #178 — retired widget-178 routed to `/legacy-178` (kept only as census padding, never a real routing rule).
179. Legacy demo routing decision #179 — retired widget-179 routed to `/legacy-179` (kept only as census padding, never a real routing rule).
180. Legacy demo routing decision #180 — retired widget-180 routed to `/legacy-180` (kept only as census padding, never a real routing rule).
181. Legacy demo routing decision #181 — retired widget-181 routed to `/legacy-181` (kept only as census padding, never a real routing rule).
182. Legacy demo routing decision #182 — retired widget-182 routed to `/legacy-182` (kept only as census padding, never a real routing rule).
183. Legacy demo routing decision #183 — retired widget-183 routed to `/legacy-183` (kept only as census padding, never a real routing rule).
184. Legacy demo routing decision #184 — retired widget-184 routed to `/legacy-184` (kept only as census padding, never a real routing rule).
185. Legacy demo routing decision #185 — retired widget-185 routed to `/legacy-185` (kept only as census padding, never a real routing rule).
186. Legacy demo routing decision #186 — retired widget-186 routed to `/legacy-186` (kept only as census padding, never a real routing rule).
187. Legacy demo routing decision #187 — retired widget-187 routed to `/legacy-187` (kept only as census padding, never a real routing rule).
188. Legacy demo routing decision #188 — retired widget-188 routed to `/legacy-188` (kept only as census padding, never a real routing rule).
189. Legacy demo routing decision #189 — retired widget-189 routed to `/legacy-189` (kept only as census padding, never a real routing rule).
190. Legacy demo routing decision #190 — retired widget-190 routed to `/legacy-190` (kept only as census padding, never a real routing rule).
191. Legacy demo routing decision #191 — retired widget-191 routed to `/legacy-191` (kept only as census padding, never a real routing rule).
192. Legacy demo routing decision #192 — retired widget-192 routed to `/legacy-192` (kept only as census padding, never a real routing rule).
193. Legacy demo routing decision #193 — retired widget-193 routed to `/legacy-193` (kept only as census padding, never a real routing rule).
194. Legacy demo routing decision #194 — retired widget-194 routed to `/legacy-194` (kept only as census padding, never a real routing rule).
195. Legacy demo routing decision #195 — retired widget-195 routed to `/legacy-195` (kept only as census padding, never a real routing rule).
196. Legacy demo routing decision #196 — retired widget-196 routed to `/legacy-196` (kept only as census padding, never a real routing rule).
197. Legacy demo routing decision #197 — retired widget-197 routed to `/legacy-197` (kept only as census padding, never a real routing rule).
198. Legacy demo routing decision #198 — retired widget-198 routed to `/legacy-198` (kept only as census padding, never a real routing rule).

## End of fixture

Total line count is deliberately kept above 200 by the numbered block above; do not trim
it without re-checking `detect.py selftest`'s D4 positive control still fires.
