{
  "skill": "watch-tickets",
  "note": "New 2026-08-13 (issue #205) — issue-sorter's per-firing watch/triage/trust procedure, extracted out of the 235-line agent body into a preloaded skill. Negatives: the write-sandbox mechanism (ops-write-sandbox-rules), and actually running the seat (dispatch issue-sorter directly, or /sort-issues).",
  "cases": [
    { "id": "t01", "prompt": "what does issue-sorter do with a filed item from an author not on the friendlies list", "expect": "trigger" },
    { "id": "t02", "prompt": "how does the REQ-011 friendlies roster interview bootstrap on the first firing", "expect": "trigger" },
    { "id": "t03", "prompt": "what's the REQ-013 GitHub MCP offer and when does issue-sorter surface it", "expect": "trigger" },
    { "id": "t04", "prompt": "how does issue-sorter classify a filed item's shape as bug vs feature vs task", "expect": "trigger" },
    { "id": "t05", "prompt": "what happens when one of issue-sorter's discovery sources is unreachable this firing", "expect": "trigger" },
    { "id": "t06", "prompt": "walk me through issue-sorter's discover/classify/trust-check/mint procedure", "expect": "trigger" },
    { "id": "n01", "prompt": "why can't issue-sorter write friendlies.json directly", "expect": "no-trigger", "comment": "owner: ops-write-sandbox-rules" },
    { "id": "n02", "prompt": "run the issue-sorter intake sweep now", "expect": "no-trigger", "comment": "dispatches the agent directly, or /sort-issues, not a skill trigger" },
    { "id": "n03", "prompt": "how does decision-watcher tell a ratification from a supersession", "expect": "no-trigger", "comment": "owner: watch-adrs" },
    { "id": "n04", "prompt": "which repo-cleaner scripts are gated to run directly", "expect": "no-trigger", "comment": "owner: clean-git" }
  ]
}
