Skill: /Users/kimba/Projects/nonoun/plugins/forge 1.14.0/skills/knowledge-harvest · Standards: skill-authoring-standards · Lint: clean
Verdict: PASS (no blocking finding; 3 major findings to fix promptly)

## Mechanical checks (run for real, not re-derived by eye)

- `skill_lint.py` on `SKILL.md`: **clean** — `skill-postwrite-invocation-lint · clean · .../knowledge-harvest/SKILL.md`.
- `release_gate.py` on the whole `forge 1.14.0` plugin with this skill added: **CLEAN, 0 fail / 0 warn** — manifest forge v1.24.0, lint clean across 38 files, 15 bundled selftests green, no dangling `[[handles]]`, 17 eval suites valid/complete, docs match manifest, no stale sibling names.

## Dim table

| ID | Verdict | Severity | Evidence (file:line) | Fix |
|----|---------|----------|----------------------|-----|
| R1 | PASS | — | SKILL.md:33-35 (3rd-telling bar), SKILL.md:66-68 (never-silent-overwrite/versioning), SKILL.md:74-76 (recommended option IS the concrete plan, not a placeholder) — all 3 sampled lines survive the deletion test: cut any one and the resulting behavior (over-harvest on 2nd mention / silent in-place overwrite / vague AskUserQuestion) is measurably worse. | none |
| R2 | FAIL | minor | Positive match confirmed: 3 verbatim user phrasings ("third time I've explained X", "should this become a skill or reference", "check if our harvested knowledge is still accurate") all appear in description SKILL.md:8-11. Fence leakage measured with `routing_eval.py` against the skill's own `scripts/routing-corpus.json`: F1 0.759, but 2 of its own stated non-uses are wrongly grabbed — "write the SKILL.md and evals for this brand new corpus" (score 0.40, knowledge-forge's territory) and "decompose this system before we spec it" (score 0.50, system-decompose's territory). Root cause traced with the tool's own token diff: the shared tokens `skill`/`eval` (from description's own "SKILL.md + references + routing-corpus + evals" and "should this become a skill or reference", SKILL.md:6-9) and `spec` (from "ratified ADR/ticket/SPEC decision", SKILL.md:5) sit *before* the first `NOT for` marker, so `routing_eval.py`'s fence-split logic (fenced tokens only repel if absent from the positive zone) can never repel them — the NOT-for clause names the sibling skill but not this shared vocabulary. Proxy-only (routing_eval.py explicitly disclaims B4/semantic robustness), and real LLM routing likely disambiguates on "detect/propose" vs. "write the actual content" semantics the proxy can't see — but it is measured, evidenced, and worth the human read the tool itself asks for. | Reword the positive-zone artifact list to reduce raw "skill"/"eval" token reuse (e.g. "this workspace's knowledge-pack shape" instead of spelling out "SKILL.md ... evals" ahead of the fence), or accept as a documented proxy limit — do not weaken the truthful fence itself. |
| R3 | PASS | — | Both dials declared explicit (SKILL.md:15-16: `disable-model-invocation: false`, `user-invocable: true`); species = Procedural (workflow + contract) matches name grammar (`knowledge`-`harvest`, noun + zero-derivation verb, same pattern as `system-decompose`/`skill-forge`); not claimed preloadable, consistent with a Procedural skill that is itself user-dispatchable. | none |
| R4 | FAIL | major | The skill's own stated #1 hard requirement — "never authors silently" — is committed globally at SKILL.md:23-24 (identity) and at the Phase 3 gate SKILL.md:80 ("No write happens without a pass through this gate"), but **Phase 4 (SKILL.md:83-90, the write action itself) carries no local restatement of that precondition** — it opens with "Route the confirmed plan to `knowledge-forge`…" and relies entirely on the adjective "confirmed" plus phase-numbering to imply Phase 3 already ran. A partial or resumed read that lands on Phase 4 in isolation (a sub-dispatch, a compacted resume, an orchestrator invoking "just do the authoring step") has no in-place tripwire stopping it from writing on an unconfirmed candidate. | Open Phase 4 with an explicit local precondition, e.g. "Only reachable after Phase 3 returns confirmed-as-proposed or overridden — a skip/decline ends at Phase 3's own failure branch; never author because a candidate looks obviously right." |
| R5 | FAIL | major | SKILL.md:88-90 states citations are "trust-class labeled per **this workspace's own** citation convention (a 'Platform/vendor fact', a 'Worked instance', or an 'Observed harness behavior' — never left unlabeled)" as if this is a house-wide, workspace-level standard. Traced the taxonomy to its actual origin: `llm 0.1.0/skills/chat-harness-instructions-and-guardrails/references/sources.md:14` — a convention local to ONE knowledge pack in the `llm` plugin, not a cross-project standard. Verified against the fact this skill names two explicit target projects in its own reference file (`references/managed-docs-scan.md:10-11`: "agent-ui" and "nonoun-plugins"); grepped agent-ui's `.claude/docs/` tree for any of the three labels — zero hits (confirmed the repo uses an unrelated "trust class" concept, component-tier ADR-0112, nothing to do with citation labeling). Phase 4 as written would force-fit citations into a taxonomy that does not exist in one of the skill's own two named target projects, with no escape hatch stated. | Qualify SKILL.md:88-90: "trust-class labeled per **the receiving pack's own** citation convention where one exists (e.g. the llm-plugin's Platform/vendor fact / Worked instance / Observed harness behavior taxonomy — `llm/skills/chat-harness-instructions-and-guardrails/references/sources.md`); else state the claim's provenance plainly (file:line/quote/date) without forcing a label scheme the project doesn't use." |
| R6 | PASS | — | Whole body is 141 lines / ~2,148 tokens (well under the 500-line / 5,000-token budget, so the compaction-survival question is moot — everything is in the head). Output contract at SKILL.md:117-124, gates at SKILL.md:70-107, all inside the full body. References one level deep only (`references/managed-docs-scan.md`, `references/placement-heuristics.md`). | none |
| R7 | FAIL | major | Output contract present (SKILL.md:117-124) ✓; 5 named failure branches (SKILL.md:126-135) ✓; checkable Done/NOT-done stopping predicate (SKILL.md:137-140) ✓ — this is otherwise the skill's strongest dimension. But the Phase 5 structural-gate command is factually wrong: SKILL.md:96 gives `python3 "${CLAUDE_PLUGIN_ROOT}/../release_gate.py" <plugin-root>`. Checked against every other skill in the same plugin that invokes this exact script — `agent-forge/SKILL.md:83`, `plugin-release/SKILL.md:28,32`, `skill-forge/SKILL.md:90`, `skill-review/SKILL.md:19`, `harness-audit/SKILL.md:23`, etc. — all use `${CLAUDE_PLUGIN_ROOT}/scripts/release_gate.py` (flat, no `/../`). `${CLAUDE_PLUGIN_ROOT}` already resolves to the plugin root (`forge 1.14.0/`); `../release_gate.py` walks OUT of the plugin to its parent directory, where the script does not exist (confirmed: the real path is `forge 1.14.0/scripts/release_gate.py`, used to run the mechanical checks above). The line does carry a hedge — "(or the workspace's current path to release_gate.py)" — which keeps this from being a hard blocker, but the primary literal command as written will fail on first run. | Fix SKILL.md:96 to `python3 "${CLAUDE_PLUGIN_ROOT}/scripts/release_gate.py" <plugin-root>`, matching every sibling skill's convention; keep the parenthetical as a genuine cross-plugin fallback, not a cover for the wrong primary path. |
| R8 | PASS | — | Numeric anchors on every load-bearing dimension: "third" telling (SKILL.md:33), "Header ≤ 4 words" (SKILL.md:74), "same finding failing three times" (SKILL.md:107, 131). "High-impact" (description, SKILL.md:6) is the one un-anchored quantifier, but it names a judgment-based detector (Impact/blast-radius) elaborated with concrete non-numeric criteria at SKILL.md:36-37 — appropriate prose-intent register for a high-degrees-of-freedom heuristic, not a defect. | none |

## The three hard requirements, checked against the body (not the description's restatement of them)

1. **Never auto-authors/auto-edits silently** — genuinely delivered as a mechanism (Phase 3's AskUserQuestion carries the concrete plan, not a bare yes/no; SKILL.md:70-81), and the staleness loop correctly re-enters the *same* gate before any fix/retire edit (SKILL.md:113-115) rather than exempting itself. The one real gap is R4 above: the gate is asserted globally, not re-asserted locally at the write action.
2. **A real, checkable admission filter** — genuinely delivered, not "everything is knowledge": the frequency bar is numeric (3rd telling, not 1st/2nd — SKILL.md:33-35), the managed-docs allowlist is a closed, named list (ratified ADR Decision / closed-ticket Findings / ratified SPEC-PRD clause / cross-project gap-fill only — `references/managed-docs-scan.md:16-31`) that explicitly excludes draft/exploratory sections by name, and Phase 1's escape hatch (SKILL.md:46-49) gives a concrete rejection path for ephemeral facts. This is a real filter with teeth.
3. **Staleness/decay handling with a concrete invocation point** — genuinely delivered: Phase 6 (SKILL.md:109-115) fires only on named explicit asks ("check if our harvested knowledge is still accurate", "re-verify this skill's citations"), never assumed-background, and its remediation routes back through Phase 3's real gate rather than being a silent self-heal.

## Reference-file wiring (explicit check requested)

- Both `references/managed-docs-scan.md` and `references/placement-heuristics.md` are genuinely referenced from the body (SKILL.md:41, SKILL.md:65), not orphaned, and both carry a "What this file does NOT cover" closer that correctly hands off to each other and to `SKILL.md` (`managed-docs-scan.md:40-43`, `placement-heuristics.md:47-50`).
- One wiring nit (not scored, filed as a nit): SKILL.md:65 points to `placement-heuristics.md` only in the context of outcome 4 ("new skill"), but the file itself governs all four placement outcomes in order (`placement-heuristics.md:1-6`). A reader following Phase 2 linearly could miss that the file is the whole decision framework, not just the "new skill" escalation case. Low-cost fix: move or duplicate the pointer to the top of the Placement bullet (SKILL.md:60), not just the last outcome.

## Reciprocal fence closure — verified with tools, not trust (explicit check requested)

This was checked as **genuinely done**, not just claimed by knowledge-harvest's own corpus:

- `git diff` on the two sibling files confirms real, uncommitted edits made today, not prose-only claims: `scribe 0.1.0/skills/knowledge-forge/evals/evals.json` gained case `n13` ("this is the third time I've had to explain this convention, should it become a skill" → no-trigger) and its `scripts/routing-corpus.json` gained the matching negative string; `forge 1.14.0/skills/system-decompose/evals/evals.json` gained case `n07` ("is this fact worth turning into a knowledge-pack entry" → no-trigger) and its `scripts/routing-corpus.json` gained the matching negative plus `"knowledge-harvest"` in its `neighbors` list.
- Ran `routing_eval.py` for real against both siblings' current descriptions + corpora: knowledge-forge holds (precision 1.000, the new negative is a true negative, tn) and system-decompose holds (precision 0.812, the new negative is a true negative — its 3 precision holes are pre-existing PRD/SPEC/LLD-author overlap, unrelated to knowledge-harvest). The fence functions, not just documents itself as functioning.
- Neither sibling's frontmatter `description:` gained an explicit `NOT for … (knowledge-harvest)` prose clause — the reciprocal closure lives only in the routing-corpus/evals test fixtures, not in the router-facing description text. Filed as a nit, not a finding: the task's explicit ask ("a negative case naming this skill") is satisfied by the test-fixture evidence above, and the measured routing_eval run shows the existing descriptions already hold against the new adversarial phrasing without needing new fence prose — adding one anyway would be belt-and-suspenders, not a fix for a demonstrated gap.

## Top 3

1. **(major, R4)** Phase 4 (SKILL.md:83-90) never locally restates the Phase-3 confirmation precondition — the skill's own #1 hard requirement is asserted globally but not at the point of risk. Fix: open Phase 4 with an explicit local precondition line.
2. **(major, R7)** SKILL.md:96 gives the wrong `release_gate.py` path (`${CLAUDE_PLUGIN_ROOT}/../release_gate.py`), inconsistent with every other skill in the plugin (`${CLAUDE_PLUGIN_ROOT}/scripts/release_gate.py`) and will fail on first literal run. Fix: drop the `/..`.
3. **(major, R5)** SKILL.md:88-90 bakes in one knowledge pack's local citation-label taxonomy (Platform/vendor fact / Worked instance / Observed harness behavior, sourced from `llm/skills/chat-harness-instructions-and-guardrails/references/sources.md`) as if it were a universal "workspace convention" — verified absent from agent-ui, one of the skill's own two named target projects. Fix: qualify as the receiving pack's own convention where one exists, else state provenance plainly without forcing the label scheme.
