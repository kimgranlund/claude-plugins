# FLOOR audit — entry-file-rules · 2026-07-29

Scope: whole skill, attention on the new two-bullet block after the eviction routing table
("Mechanize-first ordering", "Eviction dials are pinned"). Auditor: skill-checker (fresh
context), against `skill-writing-rules`. Mechanics: `skill_lint.py` clean.

## Blocking

**B1 — "Eviction dials are pinned" contradicts the canonical species table for procedural
content.** The bullet pins *"Procedure and reference content"* to knowledge-species dials
(`user-invocable: false`, `disable-model-invocation: false`). skill-writing-rules' species
table — the canon this workspace routes species decisions to — gives the **Procedural**
species `user-invocable: true` (review/audit/migrate workflows are on the slash menu by
design). Two harness canon skills now give different dials for the same evicted procedure,
and the bullet frames only *command* species as the exception, silently overriding
procedural species entirely. Fix one of two ways: (a) narrow the bullet to *reference*
content (where it's already consistent — the routing table's own "Occasional reference →
model-only knowledge" row) and let procedures take their species from skill-writing-rules;
or (b) if the house ruling really is "evicted entry-file procedures default to model-only",
state that as an explicit, justified narrowing *of* the species table ("deviating from
skill-writing-rules' procedural dials because …"), not as a silent parallel rule.

**B2 — The same bullet restates substrate skill-writing-rules owns.** Slash-menu
visibility, model routing, zero-token-until-needed, agent preloadability, and the
command-species human-timing argument are all §The species mechanics in
skill-writing-rules — a section that already carries a documented correction history on
exactly these dials (the 2026-07 preload correction). Copying the mechanics here creates a
drift pair with a countdown (skill-writing-rules body rule 8; failure catalog "Restated
substrate"). Fix: keep only the routing verdict this skill actually owns and point —
e.g. "the landing skill's species and dials are `skill-writing-rules`' call" — plus at most
the one-line house default that survives B1's resolution.

## Minor

**M1 — "Mechanize-first ordering": the tie-break is the delta; the rationale tail is
restated substrate.** The genuinely new rule — content that is both procedural and
mechanically checkable routes to the mechanized tier before prose-as-skill — resolves an
ambiguity the routing table leaves open (row 1 vs row 2 overlap) and earns its place. But
the second sentence ("a skill body holding steps a script could run … kept its
hallucination surface") restates skill-writing-rules' solve-don't-punt / degrees-of-freedom
doctrine, and "selftest-bearing" restates script anatomy that `script-writing-rules` owns.
The `/make-script` pointer already carries that weight; trim the sentence to the pointer.

**M2 — Hook routing is now stated three times.** Routing table row 1 ("Mechanically
checkable rule → Hook"), bullet 1 ("a hook where an event should enforce it"), and the
failure catalog ("Checks in prose → Route to hooks"). The table and catalog serve different
lookups and predate the edit; bullet 1 should be phrased as an *ordering* over the table's
existing destinations rather than re-listing them, which also shortens it.

## Notes

**N1 — Register holds.** The new block stays in declarative knowledge register ("Two rules
bind the table's application"; rules stated as fact, no mid-catalog imperatives). Lock
styling is lowercase per skill-writing-rules rule 6.

**N2 — Whole-skill FLOOR sweep, no further findings.** Frontmatter: both dials explicit,
knowledge species consistent with the noun-head name. Description: trigger contract with
verbatim phrasings and an owner-fenced NOT. Body ~75 lines; contracts/physics in the head,
worked example in the tail; one labeled contrastive pair; no hard-gate inflation.
`skill_lint` C1/C2 citation verified live (`harness/scripts/skill_lint.py:408`, `:414`).
Provenance section present and dated.

**N3 — Tier owed.** This edit is a semantic body change (new binding rules), so it owes the
full loop: lint (done, clean), fresh-context critic (this audit), behavior check. No
description change, so no suite update or `/check-routing` run is owed.

Verdict: not shippable as written — B1 puts two canon skills in direct contradiction on
invocation dials; resolve it and fold B2/M1 into the same edit.
