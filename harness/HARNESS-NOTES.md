# harness 3.19.3 — harness overlay notes (generated by harness_emit.py; do not edit)

## Cross-harness token degradations
- `blocked-by-rules` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `check-all-agents` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `check-all-skills` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `check-entry-file` body — `$ARGUMENTS`: affects codex, hermes — argument token is Claude-only; non-Claude harnesses read it verbatim. (Pi excluded: its own `prompts/check-entry-file.md` substitutes this natively for this command-only skill.)
- `check-entry-file` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `check-everything` body — `$ARGUMENTS`: affects codex, hermes — argument token is Claude-only; non-Claude harnesses read it verbatim. (Pi excluded: its own `prompts/check-everything.md` substitutes this natively for this command-only skill.)
- `check-everything` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `check-reconstructibility` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `check-routing` body — `$ARGUMENTS`: affects codex, hermes, pi — argument token is Claude-only; non-Claude harnesses read it verbatim.
- `check-routing` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `check-skill` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `check-state` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `clean-git` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `estate-maintenance` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `hook-writing-rules` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `make-agent` body — `$ARGUMENTS`: affects codex, hermes — argument token is Claude-only; non-Claude harnesses read it verbatim. (Pi excluded: its own `prompts/make-agent.md` substitutes this natively for this command-only skill.)
- `make-agent` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `make-hook` body — `$ARGUMENTS`: affects codex, hermes — argument token is Claude-only; non-Claude harnesses read it verbatim. (Pi excluded: its own `prompts/make-hook.md` substitutes this natively for this command-only skill.)
- `make-hook` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `make-pack` body — `$ARGUMENTS`: affects codex, hermes — argument token is Claude-only; non-Claude harnesses read it verbatim. (Pi excluded: its own `prompts/make-pack.md` substitutes this natively for this command-only skill.)
- `make-pack` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `make-plugin` body — `$ARGUMENTS`: affects codex, hermes — argument token is Claude-only; non-Claude harnesses read it verbatim. (Pi excluded: its own `prompts/make-plugin.md` substitutes this natively for this command-only skill.)
- `make-script` body — `$ARGUMENTS`: affects codex, hermes, pi — argument token is Claude-only; non-Claude harnesses read it verbatim.
- `make-skill` body — `$ARGUMENTS`: affects codex, hermes, pi — argument token is Claude-only; non-Claude harnesses read it verbatim.
- `make-skill` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `plan-chores` body — `$ARGUMENTS`: affects codex, hermes — argument token is Claude-only; non-Claude harnesses read it verbatim. (Pi excluded: its own `prompts/plan-chores.md` substitutes this natively for this command-only skill.)
- `plan-plugin-split` body — `$ARGUMENTS`: affects codex, hermes, pi — argument token is Claude-only; non-Claude harnesses read it verbatim.
- `plan-plugin-split` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `plugin-writing-rules` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `reshape-skill` body — `$ARGUMENTS`: affects codex, hermes — argument token is Claude-only; non-Claude harnesses read it verbatim. (Pi excluded: its own `prompts/reshape-skill.md` substitutes this natively for this command-only skill.)
- `reshape-skill` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `save-lessons` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `script-writing-rules` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `ship-plugin` body — `$ARGUMENTS`: affects codex, hermes — argument token is Claude-only; non-Claude harnesses read it verbatim. (Pi excluded: its own `prompts/ship-plugin.md` substitutes this natively for this command-only skill.)
- `ship-plugin` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `skill-writing-rules` body — `$ARGUMENTS`: affects codex, hermes, pi — argument token is Claude-only; non-Claude harnesses read it verbatim.
- `skill-writing-rules` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `sort-issues` body — `$ARGUMENTS`: affects codex, hermes — argument token is Claude-only; non-Claude harnesses read it verbatim. (Pi excluded: its own `prompts/sort-issues.md` substitutes this natively for this command-only skill.)
- `sweep-chores` body — `$ARGUMENTS`: affects codex, hermes, pi — argument token is Claude-only; non-Claude harnesses read it verbatim.
- `sweep-chores` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `watch-adrs` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.
- `what-shipped` body — `${CLAUDE_PLUGIN_ROOT}`: affects codex, hermes, pi — path token is Claude-only; non-Claude harnesses read it verbatim.

## Codex
- Loads via `.codex-plugin/plugin.json`; enable with the `[marketplaces.nonoun-plugins]` + `[plugins."harness@nonoun-plugins"]` entries in `~/.codex/config.toml`.
- Dropped — agents: `agent-checker`, `chore-planner`, `decision-watcher`, `fact-finder`, `hook-checker`, `issue-sorter`, `plugin-checker`, `repo-cleaner`, `routing-judge`, `skill-checker`, `wording-checker`; hooks: none.
- Command-only skills — human-only timing preserved via each skill's own `agents/openai.yaml` `policy.allow_implicit_invocation: false` (#1008): `check-all-agents`, `check-all-skills`, `check-entry-file`, `check-everything`, `clean-repo`, `make-agent`, `make-hook`, `make-pack`, `make-plugin`, `plan-chores`, `reshape-skill`, `ship-plugin`, `sort-issues`.
- Fork skills (their `context` field drops; body runs inline): none.
- MCP: none shipped.

## Hermes
- Loads from a plugin directory carrying `plugin.yaml` + `__init__.py`. Two verified paths (hermes_cli/plugins.py): local dev — copy this directory to `~/.hermes/plugins/harness/` (user plugin) or `./.hermes/plugins/harness/` with `HERMES_ENABLE_PROJECT_PLUGINS=1` (project plugin, opt-in) — then `hermes plugins list` should show it; from a Git host — `hermes plugins install owner/repo`, then `hermes plugins enable harness` (or pass `--enable` to `install`) to activate it — install alone does not enable a plugin. Hermes has no marketplace file, so there is no registry listing step beyond the two paths above.
- Plugin skills are NOT surfaced in `<available_skills>` (hermes_cli/plugins.py:3334-3337) — they resolve only as an explicit `<plugin>:<skill>` load. Verify with a load, e.g. `load harness:adopt-plugin`, never with "list your skills".
- MCP: none shipped.
- Dropped — agents: `agent-checker`, `chore-planner`, `decision-watcher`, `fact-finder`, `hook-checker`, `issue-sorter`, `plugin-checker`, `repo-cleaner`, `routing-judge`, `skill-checker`, `wording-checker`; hooks: none.
- Command-only skills now model-routable: `check-all-agents`, `check-all-skills`, `check-entry-file`, `check-everything`, `clean-repo`, `make-agent`, `make-hook`, `make-pack`, `make-plugin`, `plan-chores`, `reshape-skill`, `ship-plugin`, `sort-issues` (R-5, #888: `ctx.register_command` carries no invocation-source metadata on Hermes — the plain-skill degradation is the CORRECT terminal fallback here, not a temporary gap).
- Fork skills (their `context` field drops; body runs inline): none.

## Pi
- Loads via `package.json`'s `"pi"` key (`"skills": ["./skills"]`); install with `pi install <path-to-harness> -l` from a checkout — confirm with `pi list`, which shows the install path under `Project packages:`. The `"pi-package"` keyword is added automatically (no extra step); once this package is published, that keyword is what the pi.dev gallery scrapes to list it — there is no separate marketplace file to maintain.
- Dropped — agents: `agent-checker`, `chore-planner`, `decision-watcher`, `fact-finder`, `hook-checker`, `issue-sorter`, `plugin-checker`, `repo-cleaner`, `routing-judge`, `skill-checker`, `wording-checker`; hooks: none.
- Command-only skills emitted as `prompts/{name}.md` templates (R-5 amendment, #888): `check-all-agents`, `check-all-skills`, `check-entry-file`, `check-everything`, `clean-repo`, `make-agent`, `make-hook`, `make-pack`, `make-plugin`, `plan-chores`, `reshape-skill`, `ship-plugin`, `sort-issues` — the human-only guard is preserved natively (a prompt template expands only from a human keystroke; the model has no channel to self-invoke it), unlike Codex/Hermes where these become plain, model-routable skills.
- Fork skills (their `context` field drops; body runs inline): none.
- MCP: none shipped.
