"""harness — generated by harness_emit.py from .claude-plugin/plugin.json. Do not edit."""
from pathlib import Path

_HERE = Path(__file__).resolve().parent

def register(ctx):
    ctx.register_skill('adopt-plugin', _HERE / "skills" / 'adopt-plugin' / "SKILL.md", description='Declares an external Claude Code plugin or marketplace in a repo\'s settings.json so contributors who trust the repo can install it. Use when the user asks to add a plugin repo, declare a marketplace, set up extraKnownMarketplaces, diagnose why enabledPlugins isn\'t showing in /plugin, or "this repo has no marketplace.json, how do I still make it installable". NOT for settings.json edits with no plugin object (update-config); NOT a NEW manifest (plugin-writing-rules); NOT a plugin\'s content (make…')  # one per skill
    ctx.register_skill('agent-writing-rules', _HERE / "skills" / 'agent-writing-rules' / "SKILL.md", description="Standards for writing Claude Code subagent files that stay thin and enforce structurally. Use when the user asks how to write, structure, review, or fix an agent; how the skills preload or tool allowlist works; why an agent is fat, drifts from a skill, returns unusable output, or fails to load; why a teammate seat's SendMessage report never arrived or landed in the wrong session, whether a team-lead-labeled message is a real coordinator, or whether to name a fanned-out dispatch; what belongs in…")  # one per skill
    ctx.register_skill('big-change-git-rules', _HERE / "skills" / 'big-change-git-rules' / "SKILL.md", description='This workspace\'s git campaign safety rules. Use for "gh reports a post-merge checkout error", "did the branch actually get deleted", "pull without clobbering a parallel session\'s work", "a git command said it worked but nothing changed", "solo commit or a full campaign", "resolve a stash-pop conflict safely", "were PRs on main ever required", "push or PR-create Blocked by classifier in a subagent", "execute this rename safely", "what lands with a rename", "stacked child PR auto-closed on parent…')  # one per skill
    ctx.register_skill('blocked-by-rules', _HERE / "skills" / 'blocked-by-rules' / "SKILL.md", description="How chore-planner reads a ticket's `Blocked-by: #NN` line and orders .claude/ops/plan.md so a blocked action queues behind its blocker, or names the dependency when the blocker isn't itself queued (issue #193). Use when asked how chore-planner orders blocked tickets, why one plan.md entry sits behind another, or how a `Blocked-by:` line affects queue ranking. NOT for the convention's own format or per-backend realization (teamwork's mobilize-chores, references/blocked-by-convention.md — the can…")  # one per skill
    ctx.register_skill('break-down-problem', _HERE / "skills" / 'break-down-problem' / "SKILL.md", description="Decompose a problem along two crossing planes — OUTSIDE-IN (whole → parts) and INSIDE-OUT (actions → surfaces) — then cross-checked. Use when breaking down a technical or UX architecture, goal, or system into parts, or when a breakdown feels lopsided — structure with no behavior — or acceptance criteria don't map to any task. NOT for authoring PRD/SPEC/LLD; NOT authoring skills (make-skill); NOT a split/merge decision (plan-skill-split); NOT a concrete UI layout — screens, regions, wireframes (…")  # one per skill
    ctx.register_skill('check-all-agents', _HERE / "skills" / 'check-all-agents' / "SKILL.md", description='Audit a whole agents/ team as ONE system — naming coherence, delegation front-matter, skill leverage: "review all my agents", "audit the agent team", "which agents are duplicates". Also runs the deep-review campaign against the standard of excellence (role-family templates, measured delegation, portfolio verdicts). NOT one agent\'s definition (agent-writing-rules/make-agent); NOT the skills corpus (check-all-skills); NOT wiring decisions (fleet-rules).')  # one per skill
    ctx.register_skill('check-all-skills', _HERE / "skills" / 'check-all-skills' / "SKILL.md", description="Audit a whole skills/ corpus as ONE system, not one skill. CORPUS pass: naming, language, routing, peer composition — are names consistent, do skills compose. CAMPAIGN: the deep-review loop against the standard of excellence — themed batches, portfolio verdicts. NOT for reviewing one skill's rubric (skill-writing-rules); NOT for the agents estate (check-all-agents); NOT for executing the restructures (reshape-skill).")  # one per skill
    ctx.register_skill('check-entry-file', _HERE / "skills" / 'check-entry-file' / "SKILL.md", description='Audit and slim CLAUDE.md (and .claude/rules/) by classifying every line against the residency test and migrating evictions to their proper tier — checks to hooks, procedures to skills, subtree truths to rules — with a landing artifact for every behavior removed. Run /check-entry-file [path, default ./CLAUDE.md]; add --strict for the bare-minimum pass (burden of proof inverts: every KEEP must justify itself). Human-timed; edits files on approval only.')  # one per skill
    ctx.register_skill('check-everything', _HERE / "skills" / 'check-everything' / "SKILL.md", description='Audit the whole harness surface — skills, agents, hooks, entry files, plugins — in one sweep: deterministic lint pass, then fan-out fresh-context reviews against the matching standards, then a triage table routing every finding to a fix, a decision, or permanent infrastructure. Run /check-everything [root, default .]. Read-heavy; writes only reports. The recurring outer loop.')  # one per skill
    ctx.register_skill('check-reconstructibility', _HERE / "skills" / 'check-reconstructibility' / "SKILL.md", description='ADR-0022\'s own instrument ("the repo is the backup"): a read-only sweep reporting what a fresh machine + clone of `origin/main` could NOT recover today, classified committed / enrolled-with-mitigation / defect / open (the four named exceptions: memory dir, plugin cache, credentials, user-scoped state). Use for "what would we lose from a fresh clone", "run the reconstructibility audit", "check the estate against ADR-0022". Report-only. NOT estate naming/ bloat/pattern conformance (authorkit\'s au…')  # one per skill
    ctx.register_skill('check-routing', _HERE / "skills" / 'check-routing' / "SKILL.md", description="Run a plugin's trigger-eval suites, or a project ESTATE's (no plugin manifest — a bare `.claude/skills/*/evals/` tree, auto-detected), as a blind routing simulation: a no-tools judge picks from the description menu, then a routing matrix with tuning targets (stolen/leaked/dead). Use to rerun the evals, or prove routing after a description edit, in either a plugin or a project estate. NOT for authoring a suite's cases or live trigger debugging (skill-writing-rules); NOT for judging a skill's con…")  # one per skill
    ctx.register_skill('check-skill', _HERE / "skills" / 'check-skill' / "SKILL.md", description='Audits a SKILL.md against skill-writing-rules and returns a findings report. Use when the user asks to review, audit, critique, score, or fix a skill, or when a skill never triggers, misfires, or restates what the model knows. Judgment tier; skill_lint.py checks mechanics first. NOT authoring a new skill (make-skill); NOT a bare "why does my skill never trigger" question with no review asked (skill-writing-rules); NOT judging a skill\'s length, verbosity, or ceremony disproportionate to task siz…')  # one per skill
    ctx.register_skill('check-state', _HERE / "skills" / 'check-state' / "SKILL.md", description='Read-only project work-state report: bundled collectors sweep git (branches, worktrees, stashes), tickets/PRs, and ROADMAP/PLAN/TICKET docs, cross-reference the layers, and report verdict-first — Blocked-on-you, Ready-to-close, Drift, Delta since last run — every finding naming its owning command. Use for "what\'s the state of the project", "where are we", "what\'s blocked on me", "give me a project state report", "what can be merged or deleted", "catch me up on this repo", or reviewing all roadm…')  # one per skill
    ctx.register_skill('checking-rules', _HERE / "skills" / 'checking-rules' / "SKILL.md", description='Discipline for how a review runs: evidentiary rigor for dismissals, runtime checks over claims not a changelog, and steelmanning before filing. Use when reviewing a diff for real bugs, a too-fast dismissal, confirming a claim, or grading a review pre-ship. NOT for the return-block (write-handoff); NOT for a bound rubric (reviewer/auditor); NOT for DEEP-tier campaign scoring (check-all-agents/check-all-skills).')  # one per skill
    ctx.register_skill('clean-git', _HERE / "skills" / 'clean-git' / "SKILL.md", description="The repo-cleaner agent's own per-firing procedure — inventory worktrees/branches/PRs/claimed tickets, classify each finding, and execute ONLY the three narrow actions already gated (campaign_close.py on a verified-merged PR, sync_main.py on an interactive dirty-main, a host repo's own gated branch-reap script), everything else proposed only. Use when asked what repo-cleaner executes directly vs proposes, what its three gated scripts verify, or its failure branches for a refused gate. NOT for th…")  # one per skill
    ctx.register_skill('clean-repo', _HERE / "skills" / 'clean-repo' / "SKILL.md", description='Run a structured alignment campaign on a repo whose agent harness and context records have drifted — duplicated instruction trees, bloated entry files, dead automation, orphaned files, unwritten conventions, and a stale living design-doc corpus (ADRs, PRDs, SPECs, LLDs, RFCs). Use when the user says "align/recalibrate this repo", "run a harness consolidation / hygiene campaign", "our CLAUDE.md is bloated and stale", "consolidate the .claude setup", "audit and clean up the agent harness", "our A…')  # one per skill
    ctx.register_skill('entry-file-rules', _HERE / "skills" / 'entry-file-rules' / "SKILL.md", description='Standards for CLAUDE.md and .claude/rules/ — the harness\'s most expensive real estate. Use when the user asks what belongs in CLAUDE.md, how to write, trim, restructure, or optimize it, why Claude ignores an instruction that\'s "right there in CLAUDE.md", how big it should be, or when to use path-scoped rules instead. NOT for running the /check-entry-file audit itself (this skill only carries the standard it applies).')  # one per skill
    ctx.register_skill('estate-maintenance', _HERE / "skills" / 'estate-maintenance' / "SKILL.md", description='Periodic self-improvement retrospective: mines memory, trend CSVs, decisions, and issues for negative patterns — repeated nudges, re-filed near-duplicate tickets, metric drift, context ceremony — proposing context fixes behind one confirm. Use for "what keeps going wrong", "why do I keep correcting you", "we keep re-filing the same ticket", "context keeps growing". NOT authorkit\'s attention-audit/bloat-audit/pattern-audit/doctrine-audit/repo-audit umbrella; NOT plugin lint health (check-everyth…')  # one per skill
    ctx.register_skill('find-intent', _HERE / "skills" / 'find-intent' / "SKILL.md", description='Extract the ROOT intent behind a task or brief before acting — including a bare goal with no stated scope or success criterion and no meta-language ("make our plugins easier to get started"), not only requests naming intent-extraction outright. Use when a request is vague or the wording and goal diverge — "what am I really asking for", "figure out what they actually want", "clarify this before we build it". Resolve gaps with multiple-choice questions. NOT for a request already stating its own s…')  # one per skill
    ctx.register_skill('find-open-questions', _HERE / "skills" / 'find-open-questions' / "SKILL.md", description='Clears a session\'s backlog of unresolved items — an open question, unconfirmed assumption, or undecided idea — into one batched AskUserQuestion round. Use for "anything still open", "still pending", "close this out", "loose ends", or "left hanging"; fires at a natural close, never unattended. NOT pre-task ambiguity (find-intent); NOT sweeping the session\'s dropped work into tickets (docs\' file-leftovers); NOT worktree state before session end (close-session).')  # one per skill
    ctx.register_skill('github-facts', _HERE / "skills" / 'github-facts' / "SKILL.md", description="Answers what GitHub's Issue, PR, Discussion, and Projects v2 primitives actually ARE (verified 2026-07). Use for issue vs. discussion, native issue types vs. labels, sub-issues vs. task-list checkboxes and nesting depth, closing keywords, squash-merge behavior, draft-PR/CODEOWNERS/ merge-queue, or Projects v2 GraphQL fields. NOT our own git mechanics (`big-change-git-rules`); NOT the TICKET/ADR/SPEC contract (`doc-writing-rules`); NOT filing a work item (`file-bug`/`file-feature`/`file-task`).")  # one per skill
    ctx.register_skill('hook-writing-rules', _HERE / "skills" / 'hook-writing-rules' / "SKILL.md", description='Standards for writing Claude Code hooks — the enforced, zero-token tier. Use when writing, structuring, or fixing a hook; picking an event or matcher; how exit codes work; why a hook fires never, twice, or silently fails to register; whether a rule belongs in a hook or a skill; or how its message should read. NOT for authoring the skill itself (make-skill); NOT for forging the hook end to end (make-hook, a command).')  # one per skill
    ctx.register_skill('make-agent', _HERE / "skills" / 'make-agent' / "SKILL.md", description='Forge a Claude Code subagent through five gated phases: fork-vs-agent route check, dispatch interview, thin-shell draft, cold-start language pass, lint + spawn smoke test. Run /make-agent [agent-name or one-line job]. Human-timed; writes files. NOT for skills (make-skill) or hooks (make-hook).')  # one per skill
    ctx.register_skill('make-hook', _HERE / "skills" / 'make-hook' / "SKILL.md", description='Forge a Claude Code hook through five gated phases: check-vs-judgment route, event/matcher interview, script + registration draft, repair-message language pass, simulated-event validation with a shipped selftest. Run /make-hook [hook-name or the rule to enforce]. Human-timed; writes files. NOT for skills (make-skill) or agents (make-agent).')  # one per skill
    ctx.register_skill('make-pack', _HERE / "skills" / 'make-pack' / "SKILL.md", description='Mint or grow a knowledge pack\'s reference corpus via question-led research waves: charter an axis, write the question set, gather dated sources, distill ask-shaped files, then register and validate. Run /make-pack [skill-dir or "new pack: domain"], one axis per wave. NOT the SKILL.md surface (make-skill); NOT a split/merge decision (plan-skill-split/plan-skill-merge); NOT the rules themselves (pack-writing-rules).')  # one per skill
    ctx.register_skill('make-plugin', _HERE / "skills" / 'make-plugin' / "SKILL.md", description='Forge a whole plugin from a domain intent — "create a plugin with all the skills related to X". Charter interview, family design via the four decomposition tests, a rejected-members ledger, scaffold, a make-skill pass per member, routing proof, ship. NOT for splitting a corpus (plan-skill-split, greenfield here); NOT for partitioning a surface (plan-plugin-split); NOT releasing a built plugin (ship-plugin).')  # one per skill
    ctx.register_skill('make-script', _HERE / "skills" / 'make-script' / "SKILL.md", description='AUTHOR a bundled scripts/taskname.py|mjs end to end, right now — qualify, plan, confirm, then BUILD it (selftest green, negative control bites, wired to its caller and the gate). An authoring-workflow ask, not a rules lookup. Use for "turn this checklist into a script", "mechanize this check", "build/add a selftest to this script", or "script this verification". NOT the standard itself, nor a bare "what should a selftest look like" / "is this mechanizable" rules question with nothing to build y…')  # one per skill
    ctx.register_skill('make-skill', _HERE / "skills" / 'make-skill' / "SKILL.md", description='Forge a Claude Code skill end-to-end: intake interview, evals-first draft, and audit. Use when the user wants to build, make, author, write, or scaffold a new skill — a reusable capability, turning a workflow into a skill, or adding something the model can invoke on its own. NOT for auditing an EXISTING skill (check-skill); NOT a split/merge decision (plan-skill-split).')  # one per skill
    ctx.register_skill('naming-rules', _HERE / "skills" / 'naming-rules' / "SKILL.md", description='Simple naming paradigm for plugins, skills, commands, agents. Use when naming something new ("what should we name this skill", "what should I call it"), simplifying a name ("too vague", "plain English", "five-year-old simple"), reviewing names, contrasting similar siblings, or a skill↔agent pair. NOT for legacy estate grammar (agent-writing-rules); NOT for executing renames (big-change-git-rules); NOT for plugin partitioning (plan-plugin-split); NOT for judging a name against the estate\'s ADR-0…')  # one per skill
    ctx.register_skill('ops-write-sandbox-rules', _HERE / "skills" / 'ops-write-sandbox-rules' / "SKILL.md", description='The .claude/ops/ write-sandbox contract (issue #125): why decision-watcher, issue-sorter, repo-cleaner, and chore-planner carry no Write tool, what a fenced target-pathed payload block is, and how chore-lead applies their computed state instead. Use when asked why an ops-family seat can\'t write its own state file, how a new ops seat should hand its output back, what "narrated-but-absent" means, or why a scheduled ops firing commits only its state files. NOT for one seat\'s own per-firing procedu…')  # one per skill
    ctx.register_skill('pack-writing-rules', _HERE / "skills" / 'pack-writing-rules' / "SKILL.md", description="Standards for authoring a knowledge skill's references/ corpus. Use when structuring a corpus or INDEX, sizing axes, citing/grounding reference content, how to run a research wave, muddy retrieval, or keeping snapshots from going stale. NOT for the pack's SKILL.md surface (skill-writing-rules); NOT a split/merge decision (plan-skill-split); NOT for executing a research wave (make-pack).")  # one per skill
    ctx.register_skill('pattern-sweeping', _HERE / "skills" / 'pattern-sweeping' / "SKILL.md", description='Runs a repo- or corpus-wide sweep for a code/DOM pattern or criterion safely: census → sanity-check → classify → report or transform → verify → ratchet. Use for "sweep the repo for X", "find every page/file that still does Y", "audit all drawers/pages/tables for Z", "codemod this pattern away", "how many places still use…", "migrate every remaining call site". Audit-first; mutation is a separately gated phase. NOT for the ops-queue sweep (sweep-chores); NOT for naming/bloat audits of instructio…')  # one per skill
    ctx.register_skill('plan-chores', _HERE / "skills" / 'plan-chores' / "SKILL.md", description="Run the standing chore-planner seat (agents/chore-planner.md) on demand — standalone, it plans from durable .claude/ops state plus live gh evidence and rewrites the prioritized action queue at .claude/ops/plan.md; every entry names action, owner, evidence, size. States the seat's operating contract as a fixed banner before the first-ever plan. Run /plan-chores [blank, or a focus instruction].")  # one per skill
    ctx.register_skill('plan-plugin-split', _HERE / "skills" / 'plan-plugin-split' / "SKILL.md", description="Analyze an existing harness surface — skills/agents/hooks/scripts, one oversized plugin, or scattered ones. Use for deciding how it partitions into 1–5 portable, composable plugins, or a gap analysis on what's missing. Direction-agnostic: fewer plugins is a merge verdict, more is a split, same tests either way. NOT one corpus splitting into packs (plan-skill-split); NOT greenfield family design (make-plugin); NOT releasing a single plugin (ship-plugin/plugin-writing-rules).")  # one per skill
    ctx.register_skill('plan-skill-merge', _HERE / "skills" / 'plan-skill-merge' / "SKILL.md", description="Decide whether several thin, overlapping, or porous knowledge skills should merge into one context-efficient pack — packs that step on each other's routing, or scattered siblings with gaps. Runs the inverse test battery into a consolidation manifest. NOT for authoring a single skill (make-skill); NOT for executing an already-decided merge (reshape-skill); NOT for splitting a corpus into a family (plan-skill-split).")  # one per skill
    ctx.register_skill('plan-skill-split', _HERE / "skills" / 'plan-skill-split' / "SKILL.md", description='Decide whether a sprawling knowledge corpus splits into a family of knowledge skills, and into which ones; the merge direction is plan-skill-merge. Use for "this skill has gotten huge, break it up", "one entry surface enough for these reference files", or "split this pack, too much context for one question". Tests sizing, co-occurrence, vocabulary, cost, then a file-mapping manifest, or a no-split verdict. NOT authoring a skill (make-skill); NOT executing a decided split (reshape-skill); NOT a…')  # one per skill
    ctx.register_skill('plugin-install-facts', _HERE / "skills" / 'plugin-install-facts' / "SKILL.md", description='Verified per-channel install instructions for Claude Code plugins — /plugin marketplace add + /plugin install (github shorthand, git https/SSH URLs, local paths), npm-sourced plugins, dev checkouts, CI forms, trust/scope/update facts. Use for "how do I install this plugin", "install instructions", "plugin install command", "install via npx or npm", "install from a local path", "which install method", "my installed plugin never got the update", or writing a README\'s install section. NOT for decl…')  # one per skill
    ctx.register_skill('plugin-writing-rules', _HERE / "skills" / 'plugin-writing-rules' / "SKILL.md", description="Standards for authoring and releasing Claude Code plugins. Use when asking how to structure a plugin, what goes in plugin.json, why a plugin fails to load, why the update you SHIPPED isn't picked up by users, where plugin state lives, or how namespacing works. NOT for RUNNING a release (ship-plugin); NOT for partitioning skills and hooks into plugins (plan-plugin-split); NOT for declaring a plugin/marketplace in settings.json (adopt-plugin).")  # one per skill
    ctx.register_skill('prompt-wording-rules', _HERE / "skills" / 'prompt-wording-rules' / "SKILL.md", description='Sharpen the language of a prompt-carrying artifact so it *instantiates* target behavior, not merely describes it — a skill, agent, or tool prompt: the model ignores an instruction, a prompt isn\'t effective, harden a description, rewrite so it works. NOT for sharpening WHAT is asked (find-intent); NOT improving a BRIEF\'s ask/content, even when it\'s called a "prompt brief" (find-intent); NOT for structure or why a skill isn\'t triggering (skill-writing-rules) — only the language inside it.')  # one per skill
    ctx.register_skill('repo-sync', _HERE / "skills" / 'repo-sync' / "SKILL.md", description='Safe fast-forward-only main sync, feature-branch rebase, PR-merge verification, and post-pull dev-freshness (build-tool cache / restart) for a shared git checkout. Use for "pull/rebase/merge", "sync to current dev", "sync main", "catch up on merges", stale code after a pull, or "is it safe to merge this PR". NOT for creating a PR (the host repo\'s own finish-work/PR skill); NOT a host repo\'s own dev-server pathology reference, where one exists; NOT a standing fleet-wide merge watcher (`repo-clea…')  # one per skill
    ctx.register_skill('reshape-skill', _HERE / "skills" / 'reshape-skill' / "SKILL.md", description="Execute an already-decided skill-corpus split, merge, or rename from its validated manifest. Moves files, retires old surfaces to a timestamped attic, rewrites every referrer, and proves the sweep clean. Run /reshape-skill [manifest.json]. Writes on approval only. NOT for deciding whether to split (plan-skill-split) or merge (plan-skill-merge); NOT for authoring the resulting packs' content (make-skill).")  # one per skill
    ctx.register_skill('runtime-state', _HERE / "skills" / 'runtime-state' / "SKILL.md", description='Locate — never export — session .jsonl transcripts: the CURRENT session\'s own transcript path plus its recent subagent transcripts (agent-*.jsonl, project/worktree-scoped dir), or a PAST session\'s by repo path or slug. Prints or clipboard-copies the path(s); zero new data written. Use for "where is my transcript", "find this session\'s jsonl", "path to grab for a bug report", "locate the subagent logs from this run", "transcript path for that old session". NOT the maximal debug-export bundle (fe…')  # one per skill
    ctx.register_skill('save-lessons', _HERE / "skills" / 'save-lessons' / "SKILL.md", description='Detects when a fact crosses the bar for durable project knowledge — a correction restated a third time, a ratified decision, a convention worth keeping — into a knowledge-pack entry. Use for "skill or stay in the conversation", "knowledge or noise", "scan our docs for uncaptured knowledge", "did this citation go stale", "already a reference covering this", "propose a plan for capturing this decision before we write anything". NOT authoring (make-pack); NOT authoring or reviewing the reference d…')  # one per skill
    ctx.register_skill('script-writing-rules', _HERE / "skills" / 'script-writing-rules' / "SKILL.md", description='Standards for bundled scripts — code instead of prose, shipped as scripts/taskname.py|mjs with a selftest that proves it. Covers structure, skill- vs plugin-level scripts/, python vs js, negative controls, the exit-code contract (0/1/2), and whether something is mechanizable. NOT for the authoring workflow (make-script); NOT for hooks.json (hook-writing-rules); NOT for the calling SKILL.md (skill-writing-rules).')  # one per skill
    ctx.register_skill('ship-plugin', _HERE / "skills" / 'ship-plugin' / "SKILL.md", description='Release a Claude Code plugin through the full gate: preflight (version bump, changelog), release_gate.py (structure, manifest, full lint, bundled selftests, phantom sweep), evals check, package to dist/. Run /ship-plugin [plugin-root, default .]. Human-timed; edits the manifest on approval and writes the .plugin artifact.')  # one per skill
    ctx.register_skill('skill-writing-rules', _HERE / "skills" / 'skill-writing-rules' / "SKILL.md", description='Standards for writing Claude Code skills that change behavior instead of documenting it. Use when the user asks how to write, structure, review, or fix a SKILL.md; choosing invocation flags (disable-model-invocation, user-invocable) or skill species (procedural, knowledge, command); why a skill never triggers, misfires, or stops influencing behavior; body length; or what belongs in description vs body vs references/.')  # one per skill
    ctx.register_skill('sort-issues', _HERE / "skills" / 'sort-issues' / "SKILL.md", description="Runs the standing issue-sorter agent on demand against this repo — a plain intake sweep, or a specific instruction (approve/deny a held item, answer the first-run roster or GitHub-MCP-offer interview). Shows the agent's operating-contract banner before the first confirmed dispatch and runs the REQ-011/REQ-013 interview here when the agent's report surfaces one pending. Run /sort-issues [blank, or an instruction].")  # one per skill
    ctx.register_skill('sweep-chores', _HERE / "skills" / 'sweep-chores' / "SKILL.md", description='Run one ops-family sweep on demand against this repo — fans out decision-watcher + issue-sorter + repo-cleaner in parallel and returns chore-planner\'s single prioritized queue (.claude/ops/plan.md). Reachable as /sweep-chores and via the Skill tool by name. Use for "run the ops sweep", "sweep the ops queue", "fan out the standing seats", or a scoped ask like "repo hygiene sweep only". NOT for one seat\'s own single-item job — triage one item (issue-sorter), clean one thing (repo-cleaner), check…')  # one per skill
    ctx.register_skill('thinking-depth-rules', _HERE / "skills" / 'thinking-depth-rules' / "SKILL.md", description='The n-order reasoning spectrum for matching judgment depth to a decision, and for naming when a rule itself — not just the latest attempt — is wrong. Use for "push the reasoning further", "question the rules themselves", "the same finding keeps recurring despite fixes", "are we at a local maximum", or "is this improvement real or just relabeling". NOT the wording layer (prompt-wording-rules); NOT a structural breakdown (break-down-problem); NOT extracting the ask (find-intent); NOT a scored bas…')  # one per skill
    ctx.register_skill('watch-adrs', _HERE / "skills" / 'watch-adrs' / "SKILL.md", description="decision-watcher's per-firing procedure — classify the ADR corpus vs a content-hash checkpoint, judge new/amended/superseded Decisions, queue candidates, advance the checkpoint; plus its Revalidation mode — sampled round-robin RE-TEST of accepted-ADR / locked-IDR / locked-RDD claims into a confirmed/falsified/untestable verdict (idr-0009). Use for how the review works, ADR dialects, ratification-vs-supersession, failure branches, or revalidation sampling/verdict routing. NOT write-sandbox bound…")  # one per skill
    ctx.register_skill('watch-tickets', _HERE / "skills" / 'watch-tickets' / "SKILL.md", description="The issue-sorter agent's own per-firing procedure — discover items since the last checkpoint, classify each one's shape, trust-check the filing author against the friendlies allow-list, mint or hold, and run the first-firing REQ-011 roster and REQ-013 GitHub-MCP-offer interviews. Use when asked how issue-sorter's watch/triage/trust flow actually works, what a held item is, how the friendlies allow-list bootstraps, or what its failure branches are for an unreachable source or an ambiguous shape.…")  # one per skill
    ctx.register_skill('what-shipped', _HERE / "skills" / 'what-shipped' / "SKILL.md", description='Summarizes what shipped in a date window — PRs merged and opened, GitHub issues, and ticket-backend records (local ticket files, GitHub Issues, or a connected adapter such as Linear) — defaulting to today, with release-bot noise counted but excluded and real work grouped by workstream. Use when the user asks "what shipped today", "summarize PRs merged today", "what did we ship this week", "what landed in the last 24 hours", "what changed yesterday", "give me a standup summary", or names a date…')  # one per skill
    ctx.register_skill('write-handoff', _HERE / "skills" / 'write-handoff' / "SKILL.md", description='Compose or review the standard handoff block a team agent returns when it hands work back to its coordinator or the host — Status · Summary · Files changed · Tests/checks run · Evidence · Risks · Open questions · Recommended next action. Use whenever a subagent finishes a unit of work and must report, when a coordinator rolls several reports into one, or when judging whether a handoff is verifiable and routable: "how do I hand this back", "report my results", "write the handback", "is this hand…')  # one per skill
