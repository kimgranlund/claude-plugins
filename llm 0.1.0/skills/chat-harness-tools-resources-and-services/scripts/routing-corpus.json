{
  "skill": "chat-harness-tools-resources-and-services",
  "_note": "Routing corpus of record (minted 2026-07-13; reciprocal negatives against instructions-and-guardrails/observability/orchestration-and-workflows added 2026-07-13 per independent audit). Positives: imperative / diagnostic / symptom / indirect phrasings across the four axes (typed tool schemas, deferred tool-catalog loading, read-only resources, general external-service integration). Negatives drawn adversarially from the dangerous near-neighbors: llm-provider-gateway (the sibling pack that owns the FULL LLM-provider adapter/registry/dev-proxy pattern this pack's a18 only routes to — the single most important negative, since a18 is directly adjacent), chat-harness-knowledge-and-memory (persisting facts/memory, not calling tools/services), chat-harness-skills-and-routing (a skill library's OWN routing, the SKILL-loading analogue of a16, not this pack's TOOL-loading axis), chat-harness-instructions-and-guardrails (which action-risk tier a tool call falls into, not the tool/resource/service distinction itself), chat-harness-observability (logging that a tool fired, not what makes something a tool in the first place), chat-harness-orchestration-and-workflows (an AGENT calling another agent is a composition concern, not this pack's tool/resource/service axis), plus a genuine build/implement ask (this pack answers, it does not build).",
  "positives": [
    "what's the actual difference between a tool, a skill, and a resource in an agent harness",
    "design a typed JSON-Schema contract for a new tool an agent can call",
    "why does calling this tool fail with a validation error before I've searched for it",
    "how do I defer loading a huge tool catalog's schemas so it doesn't tax every single turn",
    "what is search-to-load for tools",
    "how should a large tool catalog amortize its schema cost across a session",
    "expose a document as read-only context an agent can see without it taking an action",
    "what's the MCP distinction between a resource and a tool",
    "is a resource model-controlled or application-controlled in MCP",
    "how do I grant an agent read-only access to a file without turning it into a callable action",
    "what does a tool's inputSchema actually need to contain",
    "should this capability be a tool or a resource",
    "how do I integrate an external network service behind a swappable seam in general",
    "what does any external API integration need regardless of which vendor it is",
    "why is my tool call invalid even though the tool's name is clearly listed",
    "batch a ToolSearch call instead of fetching one tool schema at a time"
  ],
  "negatives": [
    "how do I support multiple LLM providers behind one interface",
    "design a dev-time proxy for calling an LLM from a browser app",
    "why does my Anthropic API key end up in the built JS bundle",
    "one config as both a provider picker and a server-side allowlist",
    "how do I validate a client-chosen model against an allowlist before reading a secret",
    "keep a multi-turn LLM conversation stateless server-side",
    "how do I persist facts about the user across sessions",
    "where should long-term memory live for a chat agent",
    "how does a skill library decide which skill to load into context",
    "why didn't this skill trigger even though it matches the request",
    "implement a new callable tool with a JSON schema in my codebase",
    "build the ToolSearch-style deferred loading mechanism for my own agent framework",
    "write the code that reads a resource file server-side for my MCP server",
    "add a new axis to this knowledge pack",
    "which action-risk tier does a destructive tool call fall into",
    "should this tool call require explicit confirmation before it runs",
    "log every tool call this session made, with timestamps",
    "why isn't a tool-call failure showing up in our trace",
    "have this agent dispatch a subagent to handle part of the task",
    "compose two agents so one calls the other's output as its own input"
  ]
}
