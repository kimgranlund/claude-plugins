{
  "skill": "llm-gateway-facts",
  "_note": "Routing corpus of record (minted 2026-07-13). Positives: imperative / diagnostic / symptom / indirect phrasings of the gateway-pattern asks across the four axes (adapter seam, registry + trust boundary, dev-proxy + bundler footguns, stateless session/turn model). Negatives drawn adversarially from the dangerous near-neighbors: llm-streaming-facts (the sibling pack — SSE/JSONL wire-format concerns, a DIFFERENT layer), the agentic-ui plugin's a2ui-conversational-agent (this repo's OWN dated implementation, not the portable pattern) and a2ui-protocol (the A2UI wire shape), plus a genuine build/implement ask (this pack answers, it does not build).",
  "positives": [
    "how do I support multiple LLM providers behind one interface",
    "how do I keep an API key off the client and out of a static build",
    "design a dev-time proxy for calling an LLM from a browser app",
    "why does my API key end up in the built JS bundle",
    "one config as both a provider picker and a server-side allowlist",
    "how do I let a client pick provider and model safely",
    "keep a multi-turn LLM conversation stateless server-side",
    "why did my VITE_ prefixed env var leak into the production bundle",
    "how do I add a new LLM vendor without touching the calling code",
    "where should the API key live so it's never sent to the browser",
    "the app says no API key found even though I set it in .env",
    "should the server or the client hold the conversation history",
    "how do I validate a client-chosen model against an allowlist before reading a secret",
    "what's a clean way to inject a secret into an adapter without reading it at module scope",
    "why does my picker UI offer a model the server then rejects",
    "design the trust boundary for a browser app that calls a hosted LLM"
  ],
  "negatives": [
    "why is my SSE parser dropping text fragments",
    "what's the exact event sequence Anthropic streams",
    "how do I buffer a chunked SSE response correctly",
    "how does validate-then-stream work for structured JSONL output",
    "how does the A2UI live-agent's produce loop work in this repo",
    "what is ADR-0088 proposing for conversational reasoning in @agent-ui/a2ui",
    "why did this A2UI payload fail validation",
    "what does a binding or a check look like on the A2UI wire",
    "implement a new provider adapter in my codebase",
    "build the dev-proxy plugin for my Vite app",
    "write the code that reads process.env for my server",
    "add a new axis to this knowledge pack"
  ]
}
