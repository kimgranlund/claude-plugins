{
  "skill": "chat-harness-workflow-facts",
  "_note": "Routing corpus of record (minted 2026-07-13; reciprocal negatives against chat-harness-memory-facts/chat-harness-guardrail-facts/chat-harness-tool-facts/llm-gateway-facts/llm-streaming-facts added 2026-07-13 per independent audit). Positives: imperative / diagnostic / symptom / indirect phrasings across the three axes (a10 chain-of-command decomposition, a11 typed hand-off contracts, a12 deterministic pipelines vs ad hoc dispatch). Negatives drawn adversarially from the dangerous near-neighbors: chat-harness-routing-facts (a sibling — ONE skill's own description-routing, not composing multiple AGENTS), chat-harness-logging-facts (another sibling — logging/measuring what a run did, not structuring how the agents that did it were composed), chat-harness-memory-facts (persisting a fact across sessions, not composing agents that act), chat-harness-guardrail-facts (standing instruction layering/enforcement, not multi-agent chain of command), chat-harness-tool-facts (the tool/resource/service distinction, not agent-to-agent composition), llm-gateway-facts (a single LLM-provider adapter seam, not multi-agent orchestration), llm-streaming-facts (parsing one model's own SSE wire format, unrelated to composing agents), plus a genuine build/implement ask (this pack answers, it does not build).",
  "positives": [
    "how do I decompose a task across a team of agents with a clear chain of command",
    "who should plan vs who should build vs who should review in a multi-agent setup",
    "the builder found the plan doesn't match reality, what should it do",
    "should a coordinator ever grade its own team's work",
    "when does a task actually earn a multi-agent team instead of just doing it inline",
    "how should a subagent escalate a discovered constraint instead of re-trying blindly",
    "what fields should a subagent hand back when it finishes a unit of work",
    "how do I make an agent's report verifiable instead of just trusted prose",
    "what does a typed hand-off block look like between agents",
    "the coordinator can't tell if the build actually passed from the report it got",
    "should a hand-off be editable after it's sent, or resent from scratch",
    "how do I run a deterministic scripted pipeline across many agents instead of dispatching them one at a time",
    "when should I use parallel versus pipeline for a multi-agent fan-out",
    "why would I script a multi-agent workflow instead of letting an agent decide dispatch turn by turn",
    "how do I fan out N review agents and then merge all their results together",
    "what's the difference between a barrier fan-out and a streaming per-item pipeline",
    "design a chain of command where a builder never certifies its own change"
  ],
  "negatives": [
    "why isn't this skill's description triggering when I ask about X",
    "how do I write a precise trigger phrase for a single skill",
    "should this be a skill or should I just answer inline",
    "why does my skill's routing collide with a sibling skill's description",
    "how do I log which tools an agent run actually called",
    "how do I measure the token spend and latency of a multi-agent run",
    "why did this agent run take so long, show me the trace",
    "what dashboard should I use to observe agent behavior over time",
    "implement the coordinator agent for my project",
    "write the workflow script that runs my review pipeline",
    "build a hand-off block generator for my team",
    "add a new field to our report format in code",
    "how do I persist a fact about the user across sessions",
    "how should I layer standing instructions and guardrails across this harness",
    "what's the difference between a tool, a resource, and a service in an agent harness",
    "how do I support multiple LLM providers behind one interface",
    "why is my SSE parser dropping text fragments"
  ]
}
