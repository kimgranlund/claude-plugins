"""authorkit — generated by harness_emit.py from .claude-plugin/plugin.json. Do not edit."""
from pathlib import Path

_HERE = Path(__file__).resolve().parent

def register(ctx):
    ctx.register_skill('attention-audit', _HERE / "skills" / 'attention-audit' / "SKILL.md", description='Audit an estate\'s attention economy — the always-on menu rent skill and agent descriptions charge every turn, ranked description-collision detection across plugin boundaries, real-usage cross-reference, and a per-release trend series. Use for "which skills tax Claude\'s performance the most", "optimize for smart attention and efficient routing", "audit our menu cost", "which skills never actually get used", "will these descriptions collide in routing", "retire-or-merge candidates". Reports only,…')  # one per skill
    ctx.register_skill('bloat-audit', _HERE / "skills" / 'bloat-audit' / "SKILL.md", description='Run the busy-work / over-specification audit over a skill, agent, command, or any corpus of instruction-carrying markdown — ceremony disproportionate to task size, prose that spends words without buying instruction- following value, and content restated across files instead of cited once. Use when asked to audit or review a skill/agent/plugin for verbosity, bloat, heavy-handedness, "does this need to be this long", restated/duplicated content across files, or busy-work root causes. Read-only: r…')  # one per skill
    ctx.register_skill('doctrine-audit', _HERE / "skills" / 'doctrine-audit' / "SKILL.md", description='Sweep an estate for doctrine drift: a canon rule a dependent violates, omits, or paraphrases away from. Reads typed edges (verbatim-line | ledger-sync | vocab-term | judgment) from doctrine.manifest.json. Use when the user asks to audit doctrine drift, check a forge template against the standard it invokes, verify a ledger row matches its file, or find bodies (agent/skill) using a stale tool name/term instead of canon (the vocab-term edge). Read-only. NOT naming (naming-audit), NOT busy-work (b…')  # one per skill
    ctx.register_skill('estate-audit', _HERE / "skills" / 'estate-audit' / "SKILL.md", description="The estate-audit family index — naming-audit, bloat-audit, attention-audit, pattern-audit, doctrine-audit, and orchestration-audit are six read-only diagnostic instruments sharing one batch-sweep mechanic, consolidated behind one parameterized agent (estate-audit-agent, issue #293/#272). Use when the user asks which audit instrument fits an ask, or how to name the batch agent's `instrument` parameter. NOT for running any one sweep directly — reach the owning instrument skill (naming-audit/ bloa…")  # one per skill
    ctx.register_skill('exemption-retire', _HERE / "skills" / 'exemption-retire' / "SKILL.md", description='Retire one exemption opportunistically — verify it via naming-audit, chain rename-planning for the plan, then rename-execute (whose own confirmation gate and atomic apply include the manifest exemption shrink) — for an artifact that is already being touched anyway. Use when asked to retire, burn down, or clear one named exemption, or "since we\'re touching this anyway, fix its name too." NOT for a bulk/estate-wide exemption burn-down across many members in one pass (overhaul-execute\'s waved camp…')  # one per skill
    ctx.register_skill('fix-old-names', _HERE / "skills" / 'fix-old-names' / "SKILL.md", description='Sweep a repo for references to RETIRED plugin, skill, agent, or command names from a rename wave and rewrite the live ones. Use for "my agent dispatch says no such agent", "these skill names look old", "did the rename break this repo", "migrate this repo to the new names", or "a CI check that fails when our config references a renamed agent" — that gate ships with it. Reads a derived manifest, never guesses. NOT for choosing a brand-new name (naming-rules); NOT for planning or executing a renam…')  # one per skill
    ctx.register_skill('host-audit', _HERE / "skills" / 'host-audit' / "SKILL.md", description='Run the local dev-HOST performance audit — measure what degrades the machine while agent fleets, parallel test suites, and worktree churn run, and emit a warned, discretionary fix checklist. Use for "why is my machine slow", "load average is huge", "laptop on fire while agents run", "audit my host/machine performance", "everything times out under parallel agents", "is Spotlight or Time Machine eating my CPU". Read-only: the user runs every fix. NOT whether a red TEST RUN is trustworthy (the hos…')  # one per skill
    ctx.register_skill('manifest-authoring', _HERE / "skills" / 'manifest-authoring' / "SKILL.md", description="Seed or edit a target estate's naming.manifest.json — lexicon membership proposals, ObjectVocab registrations with the anti-ambiguity gate, AuthorRegistry, and exemptions enumeration. Use when an estate has no manifest, when a new vocabulary token is needed, when the exemptions array must be enumerated, or when an exemption entry must be removed because its rename has already landed elsewhere. NOT for chaining verify -> plan -> execute to retire an exemption whose rename hasn't happened yet (ex…")  # one per skill
    ctx.register_skill('naming-audit', _HERE / "skills" / 'naming-audit' / "SKILL.md", description='Run the naming-convention conformance audit over a harness estate (a .claude/ folder or a plugin root). Use when asked to audit, validate, or check naming, review an estate or plugin for conformance, report the exemption burn-down, or verify after a rename. Read-only: reports, never renames. NOT for sweeping a repo for stale references to RETIRED names and rewriting them (fix-old-names) — that checks whether OLD handles are still referenced, not whether current names conform to the grammar.')  # one per skill
    ctx.register_skill('naming-conventions', _HERE / "skills" / 'naming-conventions' / "SKILL.md", description="The harness artifact naming convention — grammar, frontmatter schema (incl. the allowed-tools/tool-grant syntax), folder layout, and migration rules for .claude/ estates. Consult when parsing or judging any artifact name, authoring frontmatter or its tool-grant syntax, deciding where content lives in a skill folder, or resolving one artifact's rename/migration rule. NOT for planning or generating an estate-wide overhaul/reshape/merge/split campaign across many members (overhaul-planning); NOT f…")  # one per skill
    ctx.register_skill('orchestration-audit', _HERE / "skills" / 'orchestration-audit' / "SKILL.md", description='Sweep an estate for orchestration-archetype drift against fleet-rules\' eight per-archetype rubrics (A1..A8) — the mechanizable slice only: A3\'s durable-channel evidence reader, A7\'s workflows/*.js syntax lint tier, review-coverage existence, grant-literal and resume-path presence; every judgment criterion reports "queued, not built". Use when the user asks to audit orchestration archetypes, check workflows/*.js lint coverage, verify named-seat conduct evidence, or reproduce a review\'s verdicts…')  # one per skill
    ctx.register_skill('overhaul-execute', _HERE / "skills" / 'overhaul-execute' / "SKILL.md", description='Drive an approved estate overhaul end to end — scope confirm, measure, plan, then gated wave execution through rename-planning/rename-execute, reshape-skill, and build-leader. The execution counterpart of overhaul-planning: that skill generates the plan, this one drives it, through three confirm gates that each need a live user (no user -> stops SKIPPED, never self-approves). Use for driving or running an already-approved overhaul plan through its execution waves, or via /overhaul-execute direc…')  # one per skill
    ctx.register_skill('overhaul-planning', _HERE / "skills" / 'overhaul-planning' / "SKILL.md", description='Generate a phased estate-overhaul plan for a target (estate, plugin set, member list): measure first, a per-member kill-switch design doc, merge/split nominations, procedure-vs-knowledge tiers, then waved ticket seeds with Blocked-by edges (can return "no move"). Use for a rename/reshape/merge/split campaign across many members. Plan-only: writes the doc and seeds, never executes. NOT for one artifact rename (rename-planning); NOT for a plain audit (naming-audit, bloat-audit); NOT a single-pair…')  # one per skill
    ctx.register_skill('pattern-audit', _HERE / "skills" / 'pattern-audit' / "SKILL.md", description="Sweep a repo/corpus for a caller-supplied pattern or natural-language instruction and emit a structured match dataset (id/file/line/col/match/context/kind; kinds instruction-labeled) for a downstream step: review, bulk edit, migration, or an audit's own Phase 0. Use when asked to sweep, scan, or collect matches for a pattern/regex/instruction. NOT for naming (naming-audit), NOT for busy-work or restated content across files (bloat-audit), NOT for RETIRED-name sweeps (fix-old-names), NOT for par…")  # one per skill
    ctx.register_skill('recurrence-audit', _HERE / "skills" / 'recurrence-audit' / "SKILL.md", description='Instrument IDR-0006\'s estate success measures — incident-recurrence rate (primary) and the /check-routing pass-rate trend (secondary). Walks the incident ledger (gate checks, doctrine bullets, fixtures) for the seeded `LEDGER-CLASS:` convention and appends a dated trend row. Use for "is our incident-recurrence rate being measured", "walk the ledger for classes that came back", "track the routing-eval pass-rate trend release over release". NOT menu-rent/collision (attention-audit); NOT naming co…')  # one per skill
    ctx.register_skill('rename-execute', _HERE / "skills" / 'rename-execute' / "SKILL.md", description="Atomically apply one rename plan produced by rename-planning — folder, frontmatter, every enumerated invocation string, relation edges, exemption retirement — then verify via the validator, reverting the whole rename on any new error. Use when a rename plan already exists and needs applying, or when asked to execute, apply, or land a rename/move. The estate's single mutation point. NOT for producing the plan itself (rename-planning, always this skill's own precondition); NOT for a phased campai…")  # one per skill
    ctx.register_skill('rename-planning', _HERE / "skills" / 'rename-planning' / "SKILL.md", description='Plan a single artifact rename: propose the conforming target name and enumerate the full blast radius — every invocation string, relation edge, wrapper, hook, and workflow config the rename touches. Use when a naming violation or exemption needs a fix, before any rename is executed. Produces a typed plan; never executes.')  # one per skill
    ctx.register_skill('repo-audit', _HERE / "skills" / 'repo-audit' / "SKILL.md", description='One umbrella sweep across the full audit battery — Phase-0-style discover/scope-confirm, then fans out all six authorkit instruments plus, where installed, harness\'s cross-plugin axes — into ONE verdict-first 🟢/🟡/🔴 roll-up per estate per axis. Read-only: reports, never mutates. Use for "audit this repo", "run the full audit battery", "give me one verdict across naming, bloat, attention, and routing". Ships with an identical-name command wrapper (/repo-audit). NOT one instrument alone (naming/bl…')  # one per skill
    ctx.register_skill('spend-audit', _HERE / "skills" / 'spend-audit' / "SKILL.md", description='Instrument idr-0010\'s estate economy claim — a per-firing, per-archetype token-spend ledger at `.claude/ops/spend-ledger.csv` (attention-trend.csv-shaped, append-only), plus a merged-PR label-cohort split (`--label`) for lane-vs-rest comparison. Use for "price a sweep/build firing", "append a spend-ledger row", "is this sweep worth firing / worth its tokens", "validate the spend ledger", "backfill missing firing rows", "per-archetype cost gradient", "compare lane PRs vs the rest". NOT menu rent…')  # one per skill
