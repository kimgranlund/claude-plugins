interface:
  display_name: "Host Audit"
  short_description: "Run the local dev-HOST performance audit — measure what degrades the machine while agent fleets, parallel test suites, and worktree churn run, and emit a warned, discretionary fix checklist."
