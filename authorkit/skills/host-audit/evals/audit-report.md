Skill: /Users/kimba/Projects/nonoun/plugins/authorkit/skills/host-audit · Standards: skill-writing-rules · Lint: clean
Verdict: PASS — SHIP (no blocking findings; 4 minors + 2 nits worth a follow-up commit)

Depth: FLOOR (check-skill procedure). Lint run for real:
`skill-postwrite-invocation-lint · clean · .../host-audit/SKILL.md`
Selftest run for real (twice, incl. after the browser-pattern edit):
`node scripts/host_probe.mjs selftest` → `{"selftest":"ok","keys":8,"negativeControl":"exit-2 verified","tmpUnchangedHint":true}`, EXIT=0.

| ID | Verdict | Severity | Evidence (file:line) | Fix |
|----|---------|----------|----------------------|-----|
| R1 behavior delta | PASS | — | SKILL.md:50-53 (corroborate-before-accusing changes what gets reported about peer pids), :54-56 (anti-padding all-clear form changes healthy-host output), :46-48 (grep-only-matched-entries changes catalog load behavior) — each fails the deletion test in the right direction | none |
| R2 trigger fidelity | PASS | — | SKILL.md:7-9 carries the verbatim phrasings ("why is my machine slow", "load average is huge", "is Spotlight or Time Machine eating my CPU"); evals t01/t06/t09 match them; three fences in parseable `NOT … (owner)` form at :10-11 repel t13/t14/t15. Description 598 chars — inside the estate's ≤700 W8 diet and the 1,024 portability cap (measured, not eyeballed) | none |
| R3 species/dials | PASS | — | Procedural species; both dials explicit at SKILL.md:15-16 (`disable-model-invocation: false`, `user-invocable: true`); name `host-audit` = zero-derivation verb head; intent.md Slots section tells the same story | none |
| R4 register | PASS | — | Standing spec-present instructions throughout (:40-56); exactly 1 uppercase hard gate, spent on the catastrophic invariant (SKILL.md:78 "NEVER execute a remedy command") — well under the ≤3 cap | none |
| R5 no restatement | MINOR | minor | Drift pair: the portable-class list F3/F5/F6/F8/F9 is enumerated in BOTH SKILL.md:48 and references/remedies.md:82-84. If a class gains a portable probe, one copy goes stale silently | Body says "the portable classes the catalog's Non-macOS section names" without enumerating; the catalog stays the single home |
| R6 position | PASS | — | Output contract at SKILL.md:58-66 (head, well inside 5,000 tokens); labeled good/bad pair at :72-76 in the tail; references one level deep (references/remedies.md only) | none |
| R7 contracts | PASS | — | Output contract :58-66; 3 named failure branches :82-88 (probe exit 1, attribution-to-another-owner, user asks to fix); stopping predicate :90-91 is checkable world-state ("report printed… zero mutations beyond the report") | none |
| R8 quantities | PASS | — | Thresholds are numeric in the catalog (remedies.md:9 `≥ 50`, :27 `≥ 1.5`, :54 `> 3`, :69 `< 15`, :75 `≤ 256`); report contract demands measured numbers per row and the bad-row example punishes their absence (SKILL.md:63,75-76) | none |
| Script: read-only claim | PASS | — | Read the whole script: every shell call is a read (`ps`, `find` without -exec/-delete, `test -f`, `wc`, `git rev-parse`/`worktree list`, `ulimit -n`, `df`, `tmutil isexcluded`, `sysctl -n`, `vm_stat`, `ls /tmp`) — host_probe.mjs:14,29-37,57-58,65-74,80. No write verb anywhere. Selftest exit 0, negative control verified live | none |
| Script: selftest robustness | MINOR | minor | host_probe.mjs:87 — `execSync(`${process.execPath} ${process.argv[1]} bogus`)` leaves both paths unquoted; a spaced install path breaks the negative control with a confusing failure (the estate's known ESM spaced-path trap class) | Quote both: `execSync(`"${process.execPath}" "${process.argv[1]}" bogus`)` |
| Script: self-count skew | NIT | nit | host_probe.mjs:55 `nodeTotal: cpuByPattern('node')` always counts the probe's own node process (and any comm containing "node") — a permanent +1 that mildly inflates the census | Subtract 1 in-code or note it in the JSON key name (`nodeTotalInclSelf`) |
| remedies.md grounding | PASS | — | Header grounds all entries in the named incident (remedies.md:3-4); F1 cites measured 127%+87%+70% (:11), F3 cites 10.8-18× (:27), F5 "the incident ran seven" (:46). Every threshold key the catalog references exists in the probe JSON — walked all nine: F1 `macos.indexers.cpuPct`/`spotlightMarker`, F2 `macos.tmExcluded.*`, F3 `load.perCore1m`, F4 `devProcs.*.oldestEtime`, F5 `ownNodeModules`, F6 `gitWorktrees`/`parked`, F7 `memory.freeMB`/`macos.pageouts`/`swapUsed`, F8 `diskFreePct`, F9 `fdSoftLimit` — all real keys (host_probe.mjs:44-58,64-74). Body↔catalog F1-F9 and Non-macOS section both exist and agree | none |
| evals.json shape | PASS | — | Exactly 20 cases (verified via json.load, not eyeballed): 12 trigger / 8 no-trigger; every negative names its owner in a note (evals.json:16-23) and maps to a description fence or a genuinely out-of-scope class (prod app perf, cloud CI). Expectations honest — no gimme negatives | none |
| assertions↔contract mismatch | NIT | nit | evals/assertions.md:5 says "ALL FIVE fields" while SKILL.md:68 says "all six columns" (the `#` column is the sixth). Cosmetic, but a future eval grader keying on the number will wobble | Align assertions.md to "six columns (five content fields + #)" |
| Design risk: allowed-tools vs zero-mutation | ADEQUATE, one dead grant | minor | Per skill-writing-rules ("allowed-tools grants; it does not restrict"), tightening allowed-tools CANNOT make the fence structural — a wall would need `disallowed-tools`. The actual structure is: (a) the probe script is genuinely read-only (verified above), (b) every listed grant is a read-only command (SKILL.md:17-25 — ps, uptime, tmutil isexcluded, git worktree list, the probe), so no mutating command skips a prompt, and (c) any remedy command (touch/sudo tmutil/kill/rm) is unlisted and still hits the normal permission prompt — plus the behavioral NEVER at :78 and the stopping predicate at :91. That layering is proportionate for a user-invoked, user-session audit; do NOT add disallowed-tools (it would also block the user asking the same session to run a fix AFTER the audit, and clears on the next message anyway). One real defect in the set: SKILL.md:21 `Bash(node */scripts/host_probe.mjs*)` uses a mid-string wildcard — Claude Code's documented Bash permission patterns are exact or prefix (`trailing *`), so this grant likely never matches and the probe run prompts anyway (dead configuration, not a safety hole) | Replace :21 with a form the matcher supports, e.g. `Bash(node *)` is too broad — prefer dropping the grant (one prompt per audit is acceptable) or use the plugin-root-anchored literal path if authorkit renders one |

DM gate: not in scope — no fork/subagent/dispatch mention in body or frontmatter (checked SKILL.md:1-91).

Steelman pass (checking-rules): the allowed-tools finding's strongest rebuttal — "newer Claude Code versions accept glob anywhere" — would only downgrade the fix's urgency, not the recommendation (a grant you can't confirm matches is still worth replacing with a confirmable one); finding survives, filed as minor. The nodeTotal self-count finding's rebuttal — "the +1 is noise at the scales F4 triggers on" — survives too, which is why it files as nit not minor. Dismissal check: the "repoRoot shell-interpolation injection" candidate (host_probe.mjs:30,69) was walked and DISMISSED — repoRoot comes from `git rev-parse` on the user's own machine in a self-run diagnostic; there is no adversarial input path, and a `"` in one's own repo path yields a null probe field, not a mutation.

Top 3:
1) (minor) Dead permission grant SKILL.md:21 — mid-string wildcard likely never matches; fix or drop it.
2) (minor) Portable-class list duplicated (SKILL.md:48 vs remedies.md:82-84) — de-enumerate the body copy.
3) (minor) Quote the selftest's negative-control paths (host_probe.mjs:87) against spaced install dirs.

Verdict: SHIP.
