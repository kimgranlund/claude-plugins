# Overhaul plan — llm — 2026-08-14

**Dogfood run, issue #229 acceptance evidence.** Real measurements against the `llm` plugin (9
members, all knowledge-shaped) in this worktree, run through `overhaul-planning`'s Phase
0/1/2 by hand (the skill itself is model-invoked; this is a faithful manual trace of its
procedure using the actual instrument outputs below — no numbers invented). **Evidence only —
no ticket seed below is minted; nothing here is executed.**

## Phase 0 — Measurements

| Instrument | Available? | Finding summary |
|---|---|---|
| naming-audit | yes, ran | `llm/` carries no `naming.manifest.json` → "estate is ungoverned. Seed one via manifest-authoring." No baseline measured; every Phase-1 "where it lives" row below cites job-evidence, not a naming-audit finding. |
| bloat-audit | yes, ran | 9 files scanned, 6 flagged `long-body` (>6000 chars), 0 `duplicate_pairs`, 0 chars estimated recoverable. Real per-file numbers cited in Phase 1 below. |
| check-routing (harness) | **no, deferred this pass** | harness is installed, but a full 9-suite routing-judge sweep wasn't run for a dogfood pass — stated as unavailable rather than guessed; every Phase-1 "NO" merge/split verdict below that would have cited a steal/leak number instead says "no check-routing evidence this pass." |
| plan-plugin-split surface_map (harness) | yes, ran (`gaps llm`) | 2 dangling references (`chat-agent-facts` ← llm-gateway-facts, llm-streaming-facts; `protocol-facts` ← llm-streaming-facts — phantom siblings named in prose, no matching skill); family matrix: `agent` (1 member), `chat` (6 members, shared `chat-harness-*-facts` prefix), `llm` (2 members). |

## Phase 1 — Per-member kill-switch

| Member | Where it lives | Species | Blast radius (summary) | Merge/Split candidate? | Knowledge tier | Verdict |
|---|---|---|---|---|---|---|
| `agent-residency-facts` | llm plugin owns generalized LLM/agent-harness knowledge (CLAUDE.md routing table) — job evidence present, no absence/surplus defect | KNOWLEDGE (informs, "answers... no build" framing; no procedure) | invocation: skill name only; relations: none found; family_matrix: sole member of the `agent` family, no siblings | NO — sole member of its family, no thinness/duplicate/steal evidence (check-routing not run this pass, named above) | KNOWLEDGE · **keep-inline** — body 5708 chars, no `long-body`/`dense-description` flag (bloat-audit) | NO MOVE (correctly homed, no evidence to move it) |
| `chat-harness-guardrail-facts` | llm plugin, same job evidence | KNOWLEDGE | invocation: skill name; relations: none dangling; part of the 6-member `chat` family (surface_map) | NO for this member alone — 7 reference axes (at, not past, pack-writing-rules' 7-axis ceiling), no duplicate-pair hit; the 6-member `chat` family sharing a naming prefix is itself plan-skill-merge's own test-2 signal (shared-audience vocabulary) — worth a `harness:plan-skill-merge` look at the family level, but no per-member evidence names *this* member as the candidate, so no nomination from this row | KNOWLEDGE · **move-to-references** — body 8318 chars trips `long-body` (>6000); `done_section_chars`=4597 is the likely overflow source, cited as the specific number | NO MOVE |
| `chat-harness-logging-facts` | llm plugin, same job evidence | KNOWLEDGE | invocation: skill name; relations: none dangling; 3 reference axes | NO — 3 axes sits at, not below, pack-writing-rules' floor; no duplicate-pair hit | KNOWLEDGE · **move-to-references** — body 6543 chars trips `long-body` (>6000) | NO MOVE |
| `chat-harness-memory-facts` | llm plugin, same job evidence | KNOWLEDGE | invocation: skill name; relations: none dangling; 2 reference axes (`durable-memory-vs-ephemeral-task-state.md`, `knowledge-packs-and-cited-retrieval.md`) | **MERGE** — 2 reference axes is below pack-writing-rules' "below 3 axes is usually one axis padded" floor (real citation, not inferred); candidate set: this member + the 6-member `chat-harness-*-facts` family (surface_map's `chat` family_matrix, shared naming prefix = shared-audience evidence per plan-skill-merge's own test 2) → seed for `harness:plan-skill-merge`, executed later only via `/reshape-skill`. This skill nominates the candidate set and cites the evidence; it does not run plan-skill-merge's own redundancy/overlap or porosity tests itself — that pairing decision is plan-skill-merge's job. | KNOWLEDGE · **move-to-references** — body 6642 chars trips `long-body` (>6000); note the merge nomination and the knowledge-tier verdict are independent answers on the same row — a member can be both a merge candidate and separately overflowing its own body | NO MOVE (species/placement unaffected by the merge nomination) |
| `chat-harness-routing-facts` | llm plugin, same job evidence | KNOWLEDGE | invocation: skill name; relations: none dangling; 3 reference axes | NO — at floor, no duplicate-pair hit | KNOWLEDGE · **move-to-references** — body 7691 chars trips `long-body` (>6000) | NO MOVE |
| `chat-harness-tool-facts` | llm plugin, same job evidence | KNOWLEDGE | invocation: skill name; relations: none dangling; 4 reference axes | NO — above floor, no duplicate-pair hit | KNOWLEDGE · **move-to-references** — body 7179 chars trips `long-body` (>6000) | NO MOVE |
| `chat-harness-workflow-facts` | llm plugin, same job evidence | KNOWLEDGE | invocation: skill name; relations: none dangling; 3 reference axes | NO — at floor, no duplicate-pair hit | KNOWLEDGE · **move-to-references** — body 6666 chars trips `long-body` (>6000) | NO MOVE |
| `llm-gateway-facts` | llm plugin, same job evidence | KNOWLEDGE | invocation: skill name; relations: **dangling** — prose names a `chat-agent-facts` sibling (surface_map `gaps`) that matches no installed skill, same finding class as the G8 warning this very ticket's own gate run produced against `overhaul-planning`'s own prose | NO — 4 reference axes, above floor; no duplicate-pair hit; the dangling reference is a naming-drift/blast-radius finding, not a thinness/duplication merge signal | KNOWLEDGE · **keep-inline** — body 5767 chars, no flag | NO MOVE, but the dangling `chat-agent-facts` mention is a named finding for `llm`'s own maintainer to fix or allowlist (out of this ticket's scope — `llm/` is untouched by #229) |
| `llm-streaming-facts` | llm plugin, same job evidence | KNOWLEDGE | invocation: skill name; relations: **dangling** — prose names `chat-agent-facts` and `protocol-facts`, neither matches an installed skill | NO — 3 reference axes, at floor; no duplicate-pair hit | KNOWLEDGE · **keep-inline** — body 5676 chars, no flag | NO MOVE, same dangling-reference finding as above |

Every "NO" merge/split verdict above states its evidence (family size, axis count against the
3-axis floor, or "check-routing not run this pass") — no kill asserted without a cause, per
the kill-switch discipline this shares with the "no move" verdicts.

## Phase 2 — Waved ticket seeds (not yet minted — evidence only, none of these mint anything)

### Wave 0 — merge/split nominations
- [ ] MERGE: `chat-harness-memory-facts` (2 axes, below pack-writing-rules' floor) + the
  `chat-harness-*-facts` family → seed for `harness:plan-skill-merge` (Blocked-by: none)

### Wave 1 — mechanically-clean moves
- (none — every member's "where it lives" verdict is NO MOVE)

### Wave 2 — species changes
- (none — every member is already correctly species-classified as a knowledge skill)

### Wave 3 — contested
- [ ] `llm-gateway-facts` / `llm-streaming-facts` — open question: is the dangling
  `chat-agent-facts` mention a stale rename residue or an intended-but-never-built sibling?
  (Blocked-by: none — `llm/` is out of #229's scope; this row is evidence only, cited so the
  four-answer-type demonstration is honest, not to trigger llm/ work from this ticket)

### Grandfathered
- (none — no member failed to move cleanly in a wave it was nominated into; most simply have
  no move nominated at all)

## Phase 3 — Execution contract (per ticket, once approved)

Verbatim per `overhaul-planning/references/PLAN-TEMPLATE.md`'s Phase 3 — not run here.

## Phase 4 — Closeout (per wave, once tickets land)

Verbatim per `overhaul-planning/references/PLAN-TEMPLATE.md`'s Phase 4 — not run here.

## The five respect invariants

Verbatim per `overhaul-planning/references/PLAN-TEMPLATE.md` — evidence can veto the plan
(demonstrated repeatedly above: 8 of 9 members killed on both the move and the merge/split
axes); history preserved; consumers degrade gracefully (check-routing's absence this pass was
stated, not guessed); the old design's intent was read (each member's own description framing
grounded its species answer); nothing semantic rides hidden in a move (none proposed).

## Next step

This is a dogfood evidence artifact for issue #229's acceptance item — it demonstrates all
four Phase-1 answer types (where-it-lives, species, merge/split-candidate, knowledge-tier) in
one emitted plan against a real plugin. **No ticket seed above is minted, and `llm/` is
untouched by this ticket.** A human deciding to act on the Wave 0 or Wave 3 rows would mint
them through `file-feature`/`file-task` exactly like any other captured idea, same as a real
overhaul-planning run.
