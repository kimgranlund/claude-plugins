"""brand-design — generated by harness_emit.py from .claude-plugin/plugin.json. Do not edit."""
from pathlib import Path

_HERE = Path(__file__).resolve().parent

def register(ctx):
    ctx.register_skill('brand-advertising-facts', _HERE / "skills" / 'brand-advertising-facts' / "SKILL.md", description='What a brand-ADVERTISING/creative critic knows and judges: the Big Idea and word/image collision, inventive-vs-expressive creativity and whether a creative system scales, and psycho-logic / cheap alchemy over spurious quantification and safe bets. Grounded in the advertising-creative personas (George L., Nick L., Rory S.). Use for "what does an advertising/creative critic look for", "grounding for minting an advertising critic", "does this campaign have a real Big Idea". NOT for strategy substa…')  # one per skill
    ctx.register_skill('brand-corpus', _HERE / "skills" / 'brand-corpus' / "SKILL.md", description='Canonical structure for a brand\'s documentation — a retained 00-sources archive plus the layered 01-foundation … 08-evaluation corpus, two naming conventions, maturity stages 0–6, source ingestion, provenance, and the read-before-write discipline that keeps the corpus coherent. Use whenever organizing, naming, ingesting, attributing, or auditing brand files. Triggers on "brand corpus", "organize brand files", "set up brand docs", "where does this file go", "brand attribution", "what maturity st…')  # one per skill
    ctx.register_skill('brand-guidelines', _HERE / "skills" / 'brand-guidelines' / "SKILL.md", description='The methodology and ledger reference for building brand guidelines by GUIDED CHOICE — the six brand domains, the 2×2 quadrant mechanic, the append-only choice ledger, and how choices assemble into corpus docs. Use when the user asks about the mechanism itself — "what domains does the guidelines loop cover", "how does the choice ledger work", "how do choices assemble into the corpus". NOT for actually running the guided loop now — that\'s make-brand-guidelines, which owns the "build brand guideli…')  # one per skill
    ctx.register_skill('brand-identity-facts', _HERE / "skills" / 'brand-identity-facts' / "SKILL.md", description='What a brand-IDENTITY critic knows and judges: the specific "weird" over a generic brief, human signature vs. AI-generatable sameness, three-level (semantic/syntactic/pragmatic) coherence, editorial typography as recurring grammar, and liquid identity with editorial refusal. Grounded in the identity-family personas (Jessica W., Massimo V., Matt W., Paula S.). Use for "what does an identity critic look for", "grounding for minting an identity critic", "is this logo system liquid enough". NOT for…')  # one per skill
    ctx.register_skill('brand-methodology-rules', _HERE / "skills" / 'brand-methodology-rules' / "SKILL.md", description='Senior-practitioner methodology REFERENCE for building a brand from cultural authority, not aesthetics: research → strategy → expression → stewardship, archaeology, positioning, the Brand Foundation Canon, an expression system, staying coherent over time. Use when the user asks about the methodology itself — "what are the six foundation components", "what does the Canon reject", "how do the three seats work", "how do you stress-test a positioning territory", "cultural archaeology vs. market res…')  # one per skill
    ctx.register_skill('brand-rubrics', _HERE / "skills" / 'brand-rubrics' / "SKILL.md", description='The rubric library and evaluation-methodology reference — the four rubric families, the format-fitness caveat, the trust boundary, and the cold-examiner posture every review skill applies. Use when the user asks about the rubric mechanics themselves — "which rubric family covers this", "explain the format-fitness caveat", "how is severity classified", "what\'s the trust boundary for brand material". NOT for actually scoring work now — that\'s check-brand-rubric or check-brand-council, both of whi…')  # one per skill
    ctx.register_skill('brand-strategy-facts', _HERE / "skills" / 'brand-strategy-facts' / "SKILL.md", description='What a brand-STRATEGY critic knows and judges: transformation/becoming, zag-vs-zig differentiation, primary-source provenance vs. "brand landscape," and the strategy bow tie (pattern-insight-action, brand-type honesty). Grounded in the strategy-family personas (Brian C., John H., Luke S., Mark P.). Use for "what does a strategy critic look for", "grounding for minting a strategy critic", "zig vs zag", "the bow-tie test". NOT for visual identity (`brand-identity-facts`), copy/voice (`brand-voice…')  # one per skill
    ctx.register_skill('brand-voice-facts', _HERE / "skills" / 'brand-voice-facts' / "SKILL.md", description='What a brand-VOICE critic knows and judges: respect for the reader (honesty over hype- adjectives, earned vs. manufactured feeling), the query-not-assertion discipline for house style, and the premium brief (durable, clever-AND-relevant copy for a skeptical reader). Grounded in the voice-family personas (David A., Mary N., Tim D.). Use for "what does a voice/copy critic look for", "grounding for minting a voice critic", "is this copy condescending or honest". NOT for strategy substance (`brand-…')  # one per skill
    ctx.register_skill('check-brand-council', _HERE / "skills" / 'check-brand-council' / "SKILL.md", description='Convene the brand council — adversarial critique from named practitioners, fanned out by sub-council (strategy/design/voice/full/advisory) or a roster group (e.g. `leads`), severity-classified findings, cross-critic synthesis to one verdict, plus an optional chair-moderated deliberation round. Use for "convene the brand council", "get the critics on this", "run the design/voice sub-council", "what would Luke S. say about this", "have the critics deliberate on this". NOT rubric scoring (`check-b…')  # one per skill
    ctx.register_skill('check-brand-orientation', _HERE / "skills" / 'check-brand-orientation' / "SKILL.md", description='Gets your bearings in a brand before doing any work — inventories the corpus, reads it against the methodology as working / drifting / missing, and proposes next steps by pipeline stage. Use when the user has just been handed a brand and doesn\'t know what exists, or asks "where does this brand stand", "what\'s in this brand corpus", "orient me on this brand", "what should we do next with this brand". NOT for making new work (make-brand), scoring existing work (check-brand-rubric), or the one-pag…')  # one per skill
    ctx.register_skill('check-brand-rubric', _HERE / "skills" / 'check-brand-rubric' / "SKILL.md", description='Adversarially scores existing brand work against the rubric library — positioning, the Foundation Canon, an expression system, voice/tone, a brief, a full identity. Cold-examiner posture: names failures with evidence and the test that reveals them, never flatters. Use when the user wants a rubric-scored VERDICT on real, provided work — "score this brand work", "evaluate this brief", "is this on brand", "audit this positioning", "grade this identity". NOT making the work (`make-brand`). NOT the…')  # one per skill
    ctx.register_skill('council-rules', _HERE / "skills" / 'council-rules' / "SKILL.md", description='What a council IS and how it operates, domain-neutral: roster/persona contract, sub-councils, blind fan-out, severity taxonomy + 2-of-3 voting, five synthesis shapes, calibration discipline, the two-phase model (blind, then chair-moderated deliberation), and the role-agent contract (one addressable agent per role, its scoped convene semantics, the reserved-name rule). Use for the MECHANISM itself — "what makes something a council instead of a single reviewer", "why does blind fan-out run before…')  # one per skill
    ctx.register_skill('file-brand', _HERE / "skills" / 'file-brand' / "SKILL.md", description='Stamps a finished (or partial) brand corpus into a distributable — an installable plugin, a Claude-chat cloud skill, a standalone MCP, or the corpus repo itself made into its own Claude-ready seat — and runs the brand-lint structural check before ratifying. Always asks which form; never defaults. Use when the user wants to package, ship, stamp, or ratify a brand as a plugin/skill/MCP/project — "stamp this brand", "package the brand as a plugin", "ship this as a skill", "make this brand corpus d…')  # one per skill
    ctx.register_skill('file-brand-corpus', _HERE / "skills" / 'file-brand-corpus' / "SKILL.md", description='Exports a brand engagement\'s deliverables as a navigable Markdown corpus plus a self-contained site/ viewer (sticky nav, per-page ToC, GFM tables, mermaid, DOMPurify-sanitized). Use when the user wants to export, publish, or turn brand work into a shareable corpus or site — "export the brand corpus", "make this brand a shareable site", "turn the brand docs into a navigable folder", "build the corpus site". Requires existing brand deliverables (run make-brand first if none exist yet). NOT for pa…')  # one per skill
    ctx.register_skill('make-brand', _HERE / "skills" / 'make-brand' / "SKILL.md", description='Runs a collaborative Build-mode brand engagement across research, strategy, identity, and voice — partner posture, not vendor. Locates the real pipeline stage, blocks expression work (color, type, logo, naming) before strategy is grounded, and names an aspiration before the work converges, then hands the method to brand-methodology-rules. Use when the user wants to build, develop, or work on a brand\'s strategy, positioning, identity, or voice — "let\'s build this brand", "work on the brand strat…')  # one per skill
    ctx.register_skill('make-brand-guidelines', _HERE / "skills" / 'make-brand-guidelines' / "SKILL.md", description='Builds a brand-guidelines section by guided 2x2 choice — presents four concrete options for a domain (mark, voice, color, type, expression, governance), the designer picks a quadrant + comments, and the choice accumulates into a provenance-traced corpus. Use when the user wants to build, elicit, or structure brand guidelines interactively — "build brand guidelines", "guide me through the brand design system", "give me a 2x2 for the brand voice", "elicit the color/type system", "help me decide t…')  # one per skill
    ctx.register_skill('make-brand-muse', _HERE / "skills" / 'make-brand-muse' / "SKILL.md", description='Convenes the Muse — the aspirational seat that names the ideal, provocation, or guiding concept a brand\'s work should be pulled toward. Sets direction; never makes finished work, never judges. Use when the user wants an aspiration, a provocation, a north star, or a creative direction named before converging — "convene the muse", "what should this brand aspire to", "give me a provocation for this brand", "what\'s the pull here", "set the aspiration before we build". NOT for making the work itself…')  # one per skill
    ctx.register_skill('make-brand-stack', _HERE / "skills" / 'make-brand-stack' / "SKILL.md", description='Produces the one-page reading of a brand — six load-bearing tiers (Root, Position, Point of View, Expression, Product, Stewardship), each condensed to a thesis sentence plus a short elaboration from a corpus layer. Editor posture: condense ruthlessly, cite the corpus, never invent. Use when the user wants a one-pager, an executive summary, or a condensed reading of a brand — "give me the brand stack", "one-page summary of this brand", "condense this brand corpus", "executive summary of the bran…')  # one per skill
    ctx.register_skill('make-council', _HERE / "skills" / 'make-council' / "SKILL.md", description='Stand up a NEW council instance for a domain with none yet: domain intake, roster home + its role→agent mapping, sub-council groupings, chair wiring (reuse `council-chair-agent`; a critic shell off `brand-judge`; one role agent per sub-council off `strategy-convener`), a calibration seed, both run modes. Cites `council-rules`; never restates it. Use for "stand up a new council for X", "make a council that reviews Y", "I need a council like check-brand-council but for a different domain". NOT co…')  # one per skill
    ctx.register_skill('make-critic', _HERE / "skills" / 'make-critic' / "SKILL.md", description='Mint a NEW critic persona for a council: draft the persona file, apply the `.name-map.md` attribution discipline, register it in the roster + sub-councils, seed a calibration fixture, checker-pass it. Use for "mint a critic", "add a new critic persona", "create a critic for X lens", "write a new brand-council persona". NOT convening an existing council (`check-brand-council`). NOT the council mechanism (`council-rules`). NOT a whole new council (`make-council`). NOT a generic subagent (`harness…')  # one per skill
