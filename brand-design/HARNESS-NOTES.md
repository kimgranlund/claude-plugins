# brand-design 0.11.3 — harness overlay notes (generated by harness_emit.py; do not edit)

## Codex
- Loads via `.codex-plugin/plugin.json`; enable with the `[marketplaces.nonoun-plugins]` + `[plugins."brand-design@nonoun-plugins"]` entries in `~/.codex/config.toml`.
- MCP `[needs-substitution]`: `brand-corpus.args[0]`, `brand-corpus.env.BRAND_CORPUS_DIR` use Claude-runtime tokens (`${CLAUDE_PLUGIN_ROOT}`, `${user_config.*}`) that Codex cannot resolve — replace each with a literal value in `.mcp.json` before enabling this plugin in Codex (the tokens are flagged here, not rewritten in `.mcp.json` itself).
- Dropped — agents: `brand-judge`, `brand-writer`, `council-chair-agent`, `creative-convener`, `design-convener`, `muse-agent`, `strategy-convener`, `voice-convener`; hooks: none.
- Command-only skills now model-routable: none.
- Fork skills (their `context` field drops; body runs inline): none.
- MCP: `./.mcp.json` pointed at (2 value(s) flagged above).

## Hermes
- This backend isn't built yet (tracked as T-5; no install path exists for Hermes from this repo today) — no action needed here. What WOULD drop under this plugin's current Claude Code source, computed the same way Codex's row above is (kept for a preview, not yet verified against Hermes's own contract): agents — `brand-judge`, `brand-writer`, `council-chair-agent`, `creative-convener`, `design-convener`, `muse-agent`, `strategy-convener`, `voice-convener`; hooks: none.

## Pi
- This backend isn't built yet (tracked as T-6; no install path exists for Pi from this repo today) — no action needed here. What WOULD drop under this plugin's current Claude Code source, computed the same way Codex's row above is (kept for a preview, not yet verified against Pi's own contract): agents — `brand-judge`, `brand-writer`, `council-chair-agent`, `creative-convener`, `design-convener`, `muse-agent`, `strategy-convener`, `voice-convener`; hooks: none.
