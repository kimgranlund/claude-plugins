{
  "skill": "feature",
  "note": "Routing corpus of record (minted 2026-07-17, when feature converted from command to procedural species so it and its two siblings — issue, file-bug — can invoke each other directly on a mis-routed ask instead of just pointing the user at a command). Positives: natural capability-proposal phrasings plus id-resume syntax, plus (t07) the knowledge-shape claim the description makes but the original suite never tested, per a fresh-context review. t03 was swapped from an ambiguous 'I want a calculator with a tarzan theme' (plausibly a /build-feature ask, not an intake ask) to an unambiguous proposal phrasing. Negatives drawn adversarially from the two siblings (bug-shaped and chore-shaped asks must NOT trigger feature), check-doc, make-doc (authoring a doc directly rather than intake), intent-extract, and a build request (feature never builds). Open finding (2026-07-19, a full docs /eval-run alongside the knowledge-forge retirement, kimgranlund/claude-plugins#58): t07 ('this feature request is really asking us to document our color contrast standards as a reference corpus') was blind-judge-grabbed by make-reference instead of feature. No baseline run exists to confirm this predates the retirement campaign — make-reference's own NOT-for fence was edited in that same change (owner-name swap only, `knowledge-forge`→`pack-forge`) so campaign-independence is plausible, not proven. Tracked in the issue above; do not weaken feature's or make-reference's description to chase this without re-measuring first.",
  "cases": [
    {"id": "t01", "prompt": "can we add a dark mode", "expect": "trigger"},
    {"id": "t02", "prompt": "what if we supported CSV export", "expect": "trigger"},
    {"id": "t03", "prompt": "here's an idea: a calculator with a tarzan theme, worth capturing", "expect": "trigger"},
    {"id": "t04", "prompt": "pitch: a plugin marketplace browser", "expect": "trigger"},
    {"id": "t07", "prompt": "this 'feature' request is really asking us to document our color contrast standards as a reference corpus", "expect": "trigger"},
    {"id": "t05", "prompt": "/file-feature #17", "expect": "trigger"},
    {"id": "t06", "prompt": "/file-feature tkt-0022 also needs SSO login", "expect": "trigger"},
    {"id": "n01", "prompt": "the export button does nothing on Safari", "expect": "no-trigger"},
    {"id": "n02", "prompt": "note this down as a follow-up", "expect": "no-trigger"},
    {"id": "n03", "prompt": "/file-bug #14", "expect": "no-trigger"},
    {"id": "n04", "prompt": "/file-task #19 done", "expect": "no-trigger"},
    {"id": "n05", "prompt": "review this PRD against its own rubric", "expect": "no-trigger"},
    {"id": "n06", "prompt": "author a new SPEC for this system directly", "expect": "no-trigger"},
    {"id": "n07", "prompt": "build this feature now that it's sized", "expect": "no-trigger"},
    {"id": "n08", "prompt": "extract the root intent behind this idea", "expect": "no-trigger"},
    {"id": "n09", "prompt": "Before we wrap up, is there anything still open?", "expect": "no-trigger", "comment": "owner: forge's open-questions-sweep — closes a session's already-raised loose ends, not a new feature proposal"}
  ]
}
