# Changelog — knowledge-author

## 2026-07-03 — excellence-campaign batch 2 fixes
Deep-review fixes against `skills-audit/references/standard-of-excellence.md` v2 (ledger: `skills-audit/campaign/batch-2/knowledge-author.findings.jsonl`). Escalated per the template rule — this factory stamps packs, so taught-shape defects multiply:
- **S1** step 5's taught pack shape re-synced to the standard's pack row: **answer contract** typed (base form: claim + cited file + failure-mode/caveat; pattern-packs extend with name/anatomy/when-it-fits), **deviation doctrine**, **factory route**, and **corpus-of-record rule** (packs check in `scripts/routing-corpus.json`) added; an **Update** organ added (re-run the research wave for the affected axis when canon moves; re-date; re-verify counts).
- **A3** the INDEX.md mandate made scale-conditional (research-wave scale ships INDEX; a consult table suffices hand-authored) — matching the v2 pack row and the ui-patterns reference instance.
- **A4** the no-bundle choice argued in one line: cross-file pointer/count integrity is owned by [[skills-audit]]'s corpus-level integrity check, so this factory ships no checker of its own.
- **S3** "the maker never grades their own pack" replaced with the canonical handle: generator ≠ critic.
- **N3** the broad `knowledge` stem now carries its breadth citation (the factory's territory is the species itself).
- **M2** routing corpus of record checked in (10 pos / 12 neg) — F1 0.947, zero grabs, the one miss dispositioned (paraphrase-class proxy artifact).
- **L** one lowercase "never" demoted to affirmative; SKILL.md closes on the done/NOT-done predicate.
