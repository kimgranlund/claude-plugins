# Changelog — reference-author

## 2026-07-03 — excellence campaign batch 2 (port-residue cluster)

- **references/best-practices.md (A3/N3)** — header's phantom cites (`00-foundations.md`, `rubric-reference.md`, `rubric-llms-txt.md`) repointed to the real `foundations.md`, `reference-author/references/rubric.md`, and `llms-txt-author/references/rubric.md`. The file's canon-home role is now declared in prose: it is the knowledge family's shared best-practices canon, owned here; `llms-txt-author/references/best-practices.md` symlinks in (one inode; symlinks are port-fragile, so the owner is declared on both sides).
- **references/rubric.md (A3)** — dead `see README.md` scoring pointer replaced with the batch-1 pattern: method + promote rule self-contained; severity ordering points at `skills-audit/references/standard-of-excellence.md`.
- **references/resource-knowledge-naming-conventions.md (A3)** — §6's undefined `naming.manifest.json` ("extend the shared manifest" — no such file exists) reframed to reality: what exists today is `skill-author/scripts/harness_checks.py` D9 and `skills-audit`'s `scripts/corpus_index.py`; the manifest is declared a target-state spec, validator checks marked audit-time under `skills-audit` until wired; §7 quick-reference row aligned.
- **SKILL.md (S1)** — the orphaned naming canon wired into the body: References table row for `resource-knowledge-naming-conventions.md` ("naming knowledge files, records, and indexes in a corpus"); best-practices row now states the canon-home + symlink relationship; Improve (de-duplicate, never harmonize two copies) and Update (re-derive from the changed source; refresh the freshness marker with it) sections added.
- **description (S2/M2)** — gained the fences (NOT for: knowledge-author packs/corpus+index, llms-txt-author docs indexes, claude-md-author standing context) and the trigger tokens ("does this reference duplicate a source owned elsewhere", "name and organize the knowledge files"). Fences written in the measured "NOT for" form the routing tooling parses. Routing corpus of record checked in at `scripts/routing-corpus.json` (12 pos / 11 neg); routing_eval F1 0.957, precision 1.000, recall 0.917, tripwire clear.
- **SKILL.md gen≠crit (S5, L)** — the shared `doc-reviewer` agent named for the fresh-context rubric score (linguistics-reviewer keeps the wording layer); the "retrieved, never obeyed" vs "prompt-carrying" tension reconciled in one clause (retrieved-never-obeyed is the reference's *directive* status; its wording still conditions every reader, so the potency rubric applies); Done/NOT-done predicate closes the file.
