# Changelog — llms-txt-author

## 2026-07-03 — excellence campaign batch 2 (port-residue cluster)

- **SKILL.md:14 (A3)** — the operating-model depth pointer mis-aimed at `references/foundations.md` (the shared mental-models file); the llms.txt depth — standard shape, curation, placement — lives in `references/best-practices.md`. Repointed.
- **references/rubric.md (A3)** — dead `see README.md` scoring pointer replaced with the batch-1 pattern: method + promote rule self-contained; severity ordering points at `skills-audit/references/standard-of-excellence.md`.
- **description (M2/S2)** — gained the discovery vocabulary ("agent discovery of docs", "agents can't discover where our documentation lives", "discoverable"), the top-failure symptom trigger ("an llms.txt that is a list of concepts with no links"), and the fence NOT for the docs themselves (reference-author). Routing corpus of record checked in at `scripts/routing-corpus.json` (10 pos / 11 neg); routing_eval F1 0.947, precision 1.000, recall 0.900, tripwire clear.
- **SKILL.md organs (S1, S5, L)** — maker species completed with Improve (a link fix is a curation decision: resolve, replace, or cut) and Update (an llms.txt is a derived view — when the docs corpus moves, re-derive the index and rebuild llms-full.txt; never hand-patch entries) sections; gen≠crit now names the shared `doc-reviewer` agent for the fresh-context rubric score (linguistics-reviewer keeps the wording layer); Done/NOT-done predicate closes the file.
- **references table (A3)** — best-practices row now declares the shared canon relationship: canon home reference-author; this bundle symlinks in.
